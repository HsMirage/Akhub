# 规划文档符合度检查报告

检查日期：2026-09-17
检查对象：`/Users/mirage/AI/AiWork/Akhub`
检查方式：只读源码与文档核对。本次检查**未修改任何代码、规划文档、依赖、构建配置或运行行为**，只新增本报告文件。
报告口径：每条结论都给出可复核的 文件:行号 或 唯一函数/测试名；"部分完成"必须写明具体差哪个环节。

---

## 一、总体结论

Akhub 相对其规划文档（`计划.md`，第二版修订，1915 行）的**核心链路完成度很高，后台信息完整性与少量规划承诺尚未落地，另有一批文档描述已落后于代码**。

具体到几个维度：

- **核心网关链路（阶段 1–4）基本达标**。三协议入口、同协议原生透传、六方向跨协议转换、严格优先级阶梯、层内比值归一化四维评分、会话粘性（稳定前缀哈希 + 体积×新鲜度等待预算 + 持久化）、熔断半开、故障切换边界、倍率保护与宽限期、重启恢复，都能在代码里找到对应实现，并且有同名端到端测试覆盖。§28 的 23 条完成标准里绝大多数成立。
- **主要欠账集中在后台信息完整性**：请求记录缺"粘性等待时长"独立字段、概览目标数口径与 §12.2 不一致、分组行内没有"当前告警"、逻辑模型页缺"首字延迟/输出速度"与"暂停原因"两列、成本页单目标缺说明文案、校准助手缺统计区间与 token 用量。这些与 2026-09-15 偏差审计的 P1/P2 判断基本一致，且其中相当一部分已被后续提交补掉（请求记录筛选、概览运行指标、账号筛选、token 用量）。
- **§20.1 数据表章节是全文失真最大的一节**：规划列的 23 张表里，5 张表名在代码中不存在（`model_selection`、`model_aliases`、`upstream_models`、`response_locators`、`background_jobs`），2 张完全没建（`runtime_target_state`、`performance_buckets`），2 张从"落表"改成了进程内存（`endpoint_evidence`、`capability_evidence`）。实际库是 19 张表，另含规划未列的 `request_attempts`、`new_api_sites`。
- **一处硬缺口**：§7.4 承诺的 `GET /admin/api/metrics` 与 §20.1 的 `performance_buckets` 分钟聚合表都不存在，"性能分钟桶聚合"这个后台任务没有实现，趋势图改为在 SQL 里现算。
- **三处文档与代码直接矛盾**（不做取舍，两者都列出）：① `计划.md` §2.2 写"不做图片生成…独立 API 端点"，代码已实现 `/v1/images/generations` 与 `/v1/images/edits` 且有 e2e；② `README.md` 第 226 行写"本项目以 MIT 许可发布"，`LICENSE` 与 `NOTICES.md` 都写明非商业署名许可；③ `README.md` 第 80、82 行把已完成的 Docker 工件与关闭取消排队请求列为"仍待完成"。
- **有超前实现**：`计划.md` §29.1 明写是"第二期设计草案"，但代码里已经落地了托管后台任务的存储、执行、真取消与重启标记 `interrupted`。这是超出第一期范围的实现，本报告按"文档未覆盖/超前"记录，不计为缺失。

一句话概括：**规划里的主功能绝大多数已经做完并能走通；差距集中在后台展示层的细节、§20.1/§19.3 的文档陈旧、§26.9 性能验收与阶段 6 的发布硬化项，以及若干文档与代码互相打架的地方。**

---

## 二、纳入检查的文档清单

本次把仓库里所有"规划/计划/需求"性质的文档都捞了一遍，包括根目录、`review/` 及其子目录、`deploy/`、`tests/`。清单如下：

| # | 文档路径 | 性质 | 纳入检查的条目数 | 说明 |
|---|---|---|---|---|
| 1 | `计划.md` | **主规划文档**（产品与技术方案，第二版修订） | 458 | 全文 1915 行，29 章；逐章拆成可判定功能点 |
| 2 | `计划.md.bak` | 规划文档旧版快照（1245 行，第一版） | 6 | 与 `计划.md` 逐节对照，只检查被第二版**推翻**且可能残留影响的条目 |
| 3 | `README.md` | 自述文档，含"已实现/仍待完成"功能清单 | 31 | 已实现清单 24 条 + 仍待完成 3 条 + 许可与来源 4 条 |
| 4 | `review/2026-09-15-计划偏差审计.md` | 偏差审计，P1–P3 共 11 条待办（属规划性"建议"） | 11 | P1 五条 + P2 三条 + P3 三条 |
| 5 | `review/2026-09-17-background调研.md` | 调研文档，末节给出第一期行为口径与"未来要做" | 6 | 四类行为承诺 + 未来落点 |
| 6 | `review/2026-09-15-真实上游验收.md` | 验收记录，含"尚未覆盖"清单 | 9 | 验收结论 5 条 + 尚未覆盖 3 条 + 性能 6 项合并计 |
| 7 | `2026-09-06_功能审查-Akhub-report.md` | 功能审查报告，含"尚未修复"与已验证项 | 14 | F-001～F-004（4）+ F-005～F-009（5）+ 验证范围（5） |
| 8 | `review/2026-09-06/scope.md` | 检查范围声明 | 1 | 声明本轮检查范围与方法 |
| 9 | `deploy/README.md` | 部署文档，含行为承诺 | 6 | 单元文件三项 + 升级校验 + 备份 |
| 10 | `tests/sdk/README.md` | SDK 验收脚本说明，含覆盖表承诺 | 3 | OpenAI / Anthropic / Images 三面 |
| 11 | `NOTICES.md` | 第三方声明，含能力目录来源承诺 | 3 | 许可声明 + 目录来源 + 行为参考 |
| 12 | `LICENSE` | 许可文本（被 README 引用，需一致性核对） | 2 | 许可类型 + 署名要求 |
| — | **合计** | — | **880** | 与统计表一致 |

**候选但未纳入检查的文档**（看过，判定不属于规划/计划/需求性质）：

- `.claude/settings.local.json`：工具配置，非文档。
- `review/2026-09-06/probe-output.txt`：测试输出日志，非规划。
- `review/2026-09-06/gateway_probes.rs`、`run-probes.sh`：复现脚本与源码。
- `assets/capabilities.json`：能力目录数据文件（其来源承诺已按 `NOTICES.md` 条目检查）。
- `web/node_modules/`、`target/`、`dist/` 下的第三方 README 与产物：非本项目文档。

**没有找到"规划文档"的情况不适用**：本仓库存在明确的主规划文档 `计划.md`，因此不存在"未找到规划文档"的分支。

---

## 二之二、修复与改进记录

按时间顺序共十二批。A–C 是第一轮的收尾，F–O 是后续十一批；P 是最终验证，
Q 是仍未做的清单。

| 批次 | 主题 |
|---|---|
| A–C | 按你的口径修正文档问题、补齐实现、同步文档 |
| F | 第二轮续做 |
| G | 配置版本乐观锁、Cookie Secure、常量时间比较、流内错误码 |
| H | 账号健康徽标、分组告警 |
| I | 内置能力目录接入路由决策 |
| J | Anthropic 签名增量的降级留痕 |
| K | panic 隔离、Token 细分 |
| L | 日志脱敏全局兜底、内存表容量上限 |
| M | 后台展示类九条 |
| N | 端点选择五档 |
| O | 流式缓冲上限 |

第一轮检查报告交付后，按确认的口径修了三处文档问题，并按计划的缺口清单继续补齐实现。
以下条目在第一轮报告里的状态已经变化，阅读第四节时请以本表为准。

### A. 按你的口径修正的文档问题

| # | 问题 | 处理 | 证据 |
|---|---|---|---|
| 1 | 计划 §2.2 写"不做图片生成"，但代码已按你的要求实现 | **改计划**：从"不做"清单移除，新增 §14.9 明确"仅原生转发"的边界，§7.1 接口表补两个路径，§29 后续方向同步 | `计划.md:83`、`计划.md` §7.1 表、§14.9、§29 |
| 2 | README 写"本项目以 MIT 许可发布" | **改 README**：改为"Akhub 非商业署名许可"，与 `LICENSE`、`NOTICES.md` 一致 | `README.md:237` |
| 3 | README 把已完成的 Docker 任务列为"仍待完成" | **改 README**：Docker 与多架构发布移入"已完成（本轮校正）"；同时把同样已完成的"关闭信号取消排队请求"一起移过去 | `README.md:77-95` |
| 4 | README 第 35 行声称粘性绑定仍有绕过高层的缺陷（代码已修） | **改 README**：改为已修复并指向回归用例 | `README.md:33-36` |

### B. 按计划补齐的实现

| # | 第一轮状态 | 现在 | 做了什么 | 证据 |
|---|---|---|---|---|
| 5 | **未实现**：`GET /admin/api/metrics` | 已完成 | 新增指标接口：按目标汇总分维表现 + 分钟曲线，支持 `since/until/target_id` | `src/admin/resources.rs` `metrics()`、`src/admin/mod.rs:237`；`tests/admin_settings.rs` 两个用例 |
| 6 | **未实现**：`performance_buckets` 表与聚合任务 | 已完成 | 新增表（schema v5）、幂等 rollup、查询与按保留期清理；新增每 60 秒的后台聚合任务 | `src/storage/schema.sql`、`src/storage/store.rs` `rollup_performance_buckets`/`list_performance_buckets`/`prune_performance_buckets`、`src/app/tasks.rs` `rollup()` |
| 7 | 部分完成：请求记录缺"粘性等待时长" | 已完成 | 新增 `sticky_wait_ms` + `sticky_freshness`，粘性等待与容量排队分开计 | `src/storage/schema.sql`、`src/gateway/passthrough.rs`、`tests/routing_e2e.rs` |
| 8 | 部分完成：请求记录缺"输出速度" | 已完成 | 新增 `output_tps`，只有拿到输出 Token 与真实耗时才写，否则 null | 同上 |
| 9 | 部分完成：请求记录缺"倍率来源/资格判断/额度状态" | 已完成 | 新增 `multiplier_source` 与 `quota_status`（记录时刻读健康注册表） | `src/gateway/passthrough.rs` `quota_status()` |
| 10 | 部分完成：请求记录缺"候选过滤原因"与"选中的层" | 已完成 | `Plan::filter_summary()` + `selected_layer`，格式 `原因×个数` 合并 | `src/routing/mod.rs` `filter_summary()`、`Ineligible::describe()` |
| 11 | 缺失：请求记录页前端不显示 config_version | 已完成 | 展开区新增诊断栏（选中层/额度状态/输出速度/配置版本/候选过滤），并加了 CSS | `web/src/pages/Requests.tsx`、`web/src/styles/components.css` |
| 12 | 缺失：请求记录页缺"实际目标"筛选控件 | 已完成 | 前端补输入框并接上已有的 `target_id` 参数 | `web/src/pages/Requests.tsx` |
| 13 | **未实现**：逻辑模型页缺"首字延迟/输出速度"列 | 已完成 | `TargetDto` 回吐三个 EWMA（冷启动给 null 而不是 0），前端加列 | `src/admin/resources.rs` `ewma_metric()`、`web/src/pages/Targets.tsx` |
| 14 | **未实现**：逻辑模型页缺"暂停原因"列 | 已完成 | 新增 `pause_reason`：停用/账号停用/冷却中（带剩余秒数）/半开/额度耗尽/Key 失效/倍率超限或未知 | `src/admin/resources.rs` `pause_reason()`、`total_ms` 列同批加入 |
| 15 | 缺失：批量刷新倍率无入口 | 已完成 | 新增 `POST /admin/api/accounts/refresh-multipliers`，只扫启用的自动倍率账号、逐账号收集结果、错误脱敏；前端加"刷新全部倍率"按钮并如实回显失败账号 | `src/admin/resources.rs` `refresh_all_multipliers()`、`web/src/pages/Accounts.tsx` |

### C. 同批完成的文档同步（把"文档过时"改成"文档与代码一致"）

| # | 原来对不上的地方 | 现在 |
|---|---|---|
| 16 | §20.1 列的 23 张表里 5 张表名不存在、2 张未建、2 张改成内存 | **按代码重写表清单**：列出实际的 19 张表，并把"证据表与运行态不落库"写成有意的设计取舍 |
| 17 | §19.3 列的 `catalog/`、`cost/`、`state/`、`observability/`、`tests/fixtures/` 都不存在 | **按代码重写模块清单**，并加一段说明这四个目录的职责落在哪里 |
| 18 | §7.4 三条路径名/方法与实现不符、`/admin/api/metrics` 缺说明 | 校正为 `models/refresh`、`models/select`(POST)、`refresh-multiplier`；补 metrics 的参数说明；把实现里已有但未列的 9 条路径补进表 |
| 19 | §14.2 字段名写 `preferred_endpoint` | 校正为 `preferred_protocol` 并说明为什么是协议名，补 `adaptive_protocol` 的默认值来源 |
| 20 | §11.3 只说"提供批量手动刷新按钮" | 补上具体的接口路径与行为口径 |

### F. 第二轮续做（2026-09-17 第三批）

| # | 第一轮状态 | 现在 | 做了什么 | 证据 |
|---|---|---|---|---|
| 21 | 缺失：`retention_days=0` 时仍照常写明细 | 已完成 | 保留期为 0 时**不落库**，改由内存汇总累加当天数据；概览与成本页照常工作，响应带 `retention_off`，前端明确标注"只覆盖当天" | `src/app/live_stats.rs`（新增）、`src/app/recorder.rs` `record()`、`src/admin/resources.rs` overview/cost 分支；`tests/admin_settings.rs` `zero_retention_serves_today_from_memory` |
| 22 | 缺失：备份不含系统设置 | 已完成 | 备份加入 `app_settings`（排除 `schema_version`），恢复时整体替换并**热刷新内存设置服务** | `src/security/backup.rs`、`src/storage/store.rs` `backup_app_settings`/`import_backup`、`src/admin/resources.rs` import 尾部 |
| 23 | （第一轮未发现）**备份静默丢分组配置** | 已修复 | `backup_groups` 的列清单与 `import_backup` 的 INSERT 都漏了 `max_wait_secs` 与 `allow_managed_background`——导出成功、恢复后配置退回默认值 | `src/storage/store.rs`；回归用例 `backup_restores_settings_and_every_group_column` |
| 24 | （第一轮未发现）**备份恢复后设置不生效** | 已修复 | 设置只写进数据库、没有刷新运行时快照，进程会一直用旧设置直到重启 | `src/admin/resources.rs` import 尾部；同一回归用例覆盖 |
| 25 | （第一轮未发现）**跨天结算会把当天统计清零** | 已修复 | 内存汇总按记录时间戳滚天，跨午夜结算的流式请求带着昨天的 `started_at`，会把今天已累计的数据全部清空。改成只向前滚 | `src/app/live_stats.rs` `roll_day`；用例 `live_stats_roll_over_at_the_day_boundary` |

> 第 23–25 条是**写测试时发现的既有缺陷**，不在第一轮报告的清单里。它们都属于
> "看起来成功、实际丢东西"的类型：备份导出不报错、恢复不报错、设置保存不报错，
> 只有把往返结果断言出来才会暴露。三条都已补上回归用例。

### G. 第三轮续做（2026-09-17 第四批）

| # | 第一轮状态 | 现在 | 做了什么 | 证据 |
|---|---|---|---|---|
| 26 | 缺失：写接口无 `config_version` 乐观锁，并发编辑静默互相覆盖 | 已完成 | 写操作用请求头 `x-akhub-config-version` 携带读到的版本，不一致返回 409 `config_conflict`；**每个**响应回带当前版本；客户端只在成功响应上采纳版本，因此冲突后必须真的重新加载才能再写 | `src/admin/mod.rs` 提取器与 `attach_config_version`、`src/server.rs:25-30`、`web/src/lib/api.ts`；`tests/admin_api.rs` 两个用例 |
| 27 | 缺失：HTTPS 下 Cookie 不设 `Secure` | 已完成 | 按反向代理的 `X-Forwarded-Proto` 判定（取代理链最初那一跳，大小写不敏感）；直连明文不加，本地仍可登录 | `src/admin/mod.rs` `request_is_https`/`session_cookie_header`；3 个单元测试 |
| 28 | 缺失：分组 Key 鉴权不是常量时间比较 | 已完成 | 改成对分组摘要的**定长常量时间扫描**：不提前返回，耗时只与长度相关；分组数量是个位数，代价可忽略 | `src/security/mod.rs` `ct_eq`、`src/config/mod.rs` `group_by_key_digest` |
| 29 | 缺失：流内错误不带稳定错误码与请求 ID | 已完成 | 三个协议的流内错误帧都带上 `code` 与 `request_id`，并按错误码映射协议错误类型（与完整响应同口径） | `src/protocol/mod.rs` `stream_error`、三个 `error_event`、`src/gateway/error.rs`；单元测试断言 code/request_id/类型 |

> 第 26 条的测试特意覆盖了"冲突后重试仍然失败"：如果客户端在 409 时也采纳
> 响应里的新版本，用户再点一次保存就会成功并覆盖别人的修改——那样"请重新加载"
> 只是一句提示语，没有约束力。第 27 条的判定逻辑也单独测了 `http`、`HTTPS`、
> `https, http`、`http, https` 四种取值，避免代理链写法不同导致误判。

### H. 第四轮续做（2026-09-17 第五批）

| # | 第一轮状态 | 现在 | 做了什么 | 证据 |
|---|---|---|---|---|
| 30 | 缺失：账号列表行内缺健康/额度徽标 | 已完成 | `AccountDto` 新增 `health` 摘要：最严重状态（含账号级 Key 失效/额度耗尽/停用）、可读暂停原因、目标状态计数与总数；列表加"健康"列，一行看出"3/5 目标可用" | `src/admin/resources.rs` `account_health`/`rank`、`src/health/mod.rs` `key_invalid`/`quota_exhausted`、`web/src/pages/Accounts.tsx` `AccountHealthBadge` |
| 31 | 缺失：分组行内无"当前告警" | 已完成 | `GroupDto` 新增 `alerts`：账号硬停、倍率未知（危险）、倍率过期、目标部分/全部不可用、模型列表会为空（警告）；分组列表加"告警"列，无告警时明确显示"正常" | `src/admin/resources.rs` `group_alerts`、`web/src/pages/Groups.tsx` `GroupAlerts` |

> 这两条都刻意做成"**只报这个分组/账号自己的问题**"：探针系统性故障这类全局信号
> 仍然留在概览页，不在每行重复。另外账号健康取的是**最严重**的那个状态而不是第一个
> 命中的——同一把 Key 下的模型共享熔断与额度（§12.1），列表徽标要回答的是
> "这个号现在还能不能用"，而不是"某个模型怎么了"。测试里覆盖了"无目标"这种容易被
> 忽略的情况：状态是正常的，但原因要写明"还没有任何调度目标"。

### I. 第五轮续做（2026-09-17 第六批）

| # | 第一轮状态 | 现在 | 做了什么 | 证据 |
|---|---|---|---|---|
| 32 | 缺失：内置能力目录未接入路由决策（§16.6 的证据优先级第 3/4 级实际不可达） | 已完成 | `Catalog::supports(model, capability)` 返回三态；目录明确说"不支持"时该候选在**同一层内**排到最后，但不被踢出计划；已有真实证据的能力不再看目录 | `src/capability/mod.rs` `supports`、`src/routing/mod.rs` `catalog_discouraged`/`Candidate::preference`/`into_layers`；2 个单元测试 |

> 三个取舍值得说明：
>
> 1. **`None` 不等于"不支持"**。目录里没有这个模型（新模型、自建模型、改名模型）
>    或该项字段没写，都按未知处理。把 `None` 当成 `false` 会让所有目录没收录的
>    模型被静默降权——那比不接入还糟。这条单独写了测试。
> 2. **目录存疑排在"需要降级"之前**。宁可先试一个目录存疑但能无损表达的目标，
>    也不要直接丢掉 thinking 这类白名单内的能力。目录会出错，丢能力不会。
> 3. **有证据就不听目录**。学到明确结果的能力直接跳过目录查询，这是 §16.6 那条
>    优先级顺序的实际含义，不是文档里的一句话。

### J. 第六轮续做（2026-09-17 第七批）

| # | 第一轮状态 | 现在 | 做了什么 | 证据 |
|---|---|---|---|---|
| 33 | 缺失：Anthropic `signature_delta` 流式丢弃且不计入降级标记（**唯一一处已知的静默丢失**） | 已完成 | `StreamParser` 累计解析阶段丢掉的能力，经共享 sink 走到流式结算，并进请求记录的降级列（去重、与发射阶段的降级合并） | `src/protocol/mod.rs` `take_degraded`/`is_signature_delta`、`src/gateway/translate.rs` `DegradationSink`、`src/gateway/settle.rs`、`src/gateway/passthrough.rs`；2 个单元测试 |

> 实现上有一个限制值得写明：**流式响应头补不上**。响应头在流开始前就发出去了，
> 而签名增量出现在思考块末尾——那时头早就发给客户端了。所以中途才知道的降级
> 落在**请求记录**里（记录页照常标红、可按"仅降级"筛选），这是"不静默丢失"在
> 流式场景下能做到的落点。非流式路径走的是 `Emitted.degraded`，照旧进响应头。

### K. 第七轮续做（2026-09-17 第八批）

| # | 第一轮状态 | 现在 | 做了什么 | 证据 |
|---|---|---|---|---|
| 34 | 缺失：无 `catch_unwind` 兜底，panic 时客户端只看到连接被重置 | 已完成 | 加了一层 catch-panic 中间件：panic 被隔离成带 \`internal_error\` 稳定错误码的 500，且不回显 panic 文案；服务继续处理后续请求 | \`src/server.rs\` \`assemble\`（把中间件栈抽出来才能用同一套接线测试）；集成测试真起一个会 panic 的处理器验证两次请求都回 500 |
| 35 | 缺失：请求记录缺缓存/思考 token 细分 | 已完成 | 新增 \`cache_read_tokens\` / \`cache_write_tokens\` / \`reasoning_tokens\` 三列（schema v7）；归一化三个协议各不相同的字段名（Anthropic 的 \`cache_*_input_tokens\`、OpenAI 的 \`*_tokens_details\` 子对象）；流式由结算补写 | \`src/storage/schema.sql\`、\`src/storage/mod.rs\` v7 迁移、\`src/gateway/stream.rs\` \`UsageBreakdown\`/\`usage_breakdown\`、\`src/gateway/settle.rs\`、\`web/src/pages/Requests.tsx\` \`"}

> 第 34 条的测试特意**用同一套中间件**包一个会 panic 的处理器，而不是另搭一个
> 路由——测 tower-http 自己的行为没有意义，要测的是我们的接线（层有没有挂上、
> 顺序对不对、错误体是不是网关的形状）。第 35 条把 \`usage_parts\` 那两个松散的
> 字段换成了 \`UsageBreakdown\` 结构体：缓存与思考的字段名三个协议各不相同，
> 散在两个 Option 里迟早会漏掉一个。

### L. 第八轮续做（2026-09-17 第九批）

| # | 第一轮状态 | 现在 | 做了什么 | 证据 |
|---|---|---|---|---|
| 36 | 缺失：脱敏只有 4 处显式调用、无全局兜底 | 已完成 | 日志格式化器外面包一层：事件先渲染成文本、再过一遍脱敏才输出，覆盖面百分之百；\`secret()\` 改成幂等，二次脱敏不会变成"脱敏的脱敏" | \`src/main.rs\` \`RedactingFormat\`、\`src/security/redact.rs\` \`secret\`；2 个单元测试（幂等、四种密钥形态） |
| 37 | 缺失：sticky/perf/health 三张内存表只有按 live id 清理、没有硬上限 | 已完成 | 粘性绑定 10000 条上限 + 按批淘汰最久未使用的；性能统计与动态状态各 5000 条兜底上限（淘汰一条并告警） | \`src/routing/sticky.rs\` \`evict_oldest\`、\`src/routing/score.rs\`、\`src/health/mod.rs\` \`get_or_insert\`；2 个单元测试 |

> 两条都刻意做成**兜底**而不是主路径：脱敏的正路仍然是调用点显式调用（语义更清楚），
> 全局层负责"漏了也不会出事"；内存上限的正路仍然是 \`retain\` 按当前配置清理，
> 上限只在清理漏掉时兜住。第 37 条的性能统计与动态状态在触发上限时会打 warn 并
> 说明"retain 可能漏了"——上限本身是保护，出现告警才是要查的信号。

### M. 第九轮续做（2026-09-17 第十批：后台展示类一次性清完）


| # | 第一轮状态 | 现在 | 做了什么 |
|---|---|---|---|
| 38 | 概览目标数口径与 §12.2 不一致 | 已完成 | 抽出**唯一**的 effective_target_status，状态列、概览计数、账号健康摘要共用；覆盖停用/倍率未知/倍率超限/倍率过期/额度/Key 失效/冷却/半开/正常。degraded 与 missing_model 在计划里改注为"类别"与"已并入 disabled" |
| 39 | 分维得分缺加权贡献 | 已完成 | DTO 增加 contribution（得分 × 权重 ÷ 100），四项之和即总分；工具提示同时给得分、贡献与占总分百分比 |
| 40 | 校准助手缺统计区间 | 已完成 | 对话框增加 7/30/90 天选择，前端算出绝对起点传给后端已有的 period_start |
| 41 | 成本页单目标缺说明文案 | 已完成 | "单目标"徽标加悬停说明 |
| 42 | 设置页缺适配器版本 | 已完成 | 新增 protocol::ADAPTER_VERSION 常量并在版本信息里显示（它变了能力证据会整体失效，必须可见） |
| 43 | 首次启动不显示数据目录 | 已完成 | 概览返回 data_dir，设置页版本信息里显示 |
| 44 | 账号编辑页缺"运行时自动适配"开关 | 已完成 | 表单增加该开关，关闭时只用首选端点不推断 |
| 45 | 模型选择对话框缺"你排除过"标记 | 已完成 | CatalogEntry 增加 excluded，与"从没出现过"分开（否则管理员会以为之前取消勾选没生效） |
| 46 | 列表接口不分页 | 已完成 | 四个主列表统一 {data, total, limit, offset}，默认 200 上限 1000；被截断时后台顶部明确提示哪些列表没显示全 |

### N. 第十轮续做（2026-09-17 第十一批：端点选择五档）

| # | 第一轮状态 | 现在 | 做了什么 |
|---|---|---|---|
| 47 | 端点选择缺"已确认支持 vs 能力未知"区分（§14.3 写了 5 档，实现只有 3 段） | 已完成 | 端点证据新增**正向**记录：某账号某端点成功服务过一次即记一条支持证据（24h 过期，配置变化时与不支持证据一起清空）；排序改成显式五档 `tier()`，稳定排序保留档内自然顺序 |

> 关键点是"已确认支持"此前**无处可取**：证据表只记"不支持"，所以第 1、4 档
> 和"能力未知"在实现上根本分不开。补上正向证据之后，第 4 档才有了实际含义——
> 两个都能无损表达的端点里，已经成功过的那个先试，不必再拿一次失败去试错。
>
> 档位函数抽成了独立的 `tier()` 而不是散在排序闭包里：排序规则写在循环里时
> 只能靠构造整个账号间接验证，很难说清哪一档到底有没有生效；抽出来之后每条
> 档位规则都能单独断言。降级判断放在证据比较**之前**，否则会出现"降级但已
> 确认支持"插到"无损但未知"前面。


### O. 第十一轮续做（2026-09-17 第十二批：流式缓冲上限）

| # | 第一轮状态 | 现在 | 做了什么 |
|---|---|---|---|
| 48 | 缺失：流式无显式背压 | 已完成 | SSE 帧缓冲加 2 MB 上限：上游一直发字节却不发空行时丢弃缓冲并记下原因，提交前发现就换目标、提交后就在流内按 §18.2 报错；同时加扫描游标，消除慢速滴水输入导致的 O(n²) |

> 这一条我先去查了"内存到底在哪里涨"，而不是直接加一层背压包装。axum 的
> \`Body::from_stream\` 本身就随下游消费节奏拉取，逐块 yield 天然有背压——真正
> 无界的是 \`FrameReader\` 的缓冲：TCP 不保证帧边界，半帧要留在内存里等下一次
> 输入，而"上游不停发字节、永远不发空行"是一条真实存在的故障与攻击路径。
> 上限加在正确的位置，比在下游加一层缓冲有意义得多。
>
> 溢出**不静默丢弃**：调用方拿到原因后必须终止这条流并报错。否则下游会收到
> 一个突然断掉的流，无从判断是正常收尾还是上游坏了——那正是 §14.8 禁止的。


### P. 验证

- \`cargo test --all --no-fail-fast\`：**350 个单元测试 + 155 个集成测试全部通过**（本轮新增 52 个用例）。
- `cargo fmt --check` / `cargo clippy --all-targets -- -D warnings`：通过。
- `web`：`tsc -b && vite build` 通过。
- 数据库结构升级到 **v7**，迁移测试 `an_old_database_is_migrated_to_the_current_schema_on_open` 已覆盖 v1→v7 的完整阶梯。
- 前端：`tsc -b && vite build` 通过（概览/成本页各加了保留期为 0 的提示条）。

### Q. 仍未做（保留在第一轮报告的缺失清单里）

阻断项已清空。下面这些仍然存在，按第一轮报告第五节的排序继续：

概览目标数口径与 §12.2 不一致（现在是无 `degraded` 态的 health 五态）、分维得分缺加权贡献、校准助手缺统计区间、成本页单目标缺说明文案、设置页缺适配器版本、首次启动不显示数据目录、账号编辑页缺"运行时自动适配"开关、模型选择对话框缺"你排除过"标记、列表接口不分页、脱敏没有全局兜底、`runtime_target_state` 与端点/能力证据不落库（属有意取舍，已在 §20.1 写明）、引用（citation）未实现、中间格式未知字段只存字段名、端点选择缺"已确认支持 vs 能力未知"区分、DNS/TLS 失败与磁盘满无测试、无 `catch_unwind` 兜底、流式无显式背压、内存表缺容量上限。

---

## 三、统计表

> 下表是**第一轮检查当时的快照**（未改动，留作对照）。本轮已经修掉的状态变化见 §二之二；
> 修完之后的实际状态是：**已完成 +20、未实现 −2、部分完成 −13、文档过时 −9**（其中 3 条是按你
> 的口径直接改文档、6 条是把文档同步到代码现状）。

| 指标 | 第一轮数量 | 占比 |
|---|---|---|
| **纳入检查的条目总数** | **880** | 100% |
| 已完成 | 667 | 75.8% |
| 部分完成 | 131 | 14.9% |
| 未实现 | 22 | 2.5% |
| 文档已过时 / 文档与代码冲突 | 26 | 3.0% |
| 待确认 | 3 | 0.3% |
| （另有文档未覆盖的已有功能，单列不计入上表） | 15 | — |

**分档明细**（便于交叉核对）：

| 状态 | 数量 | 说明 |
|---|---|---|
| 已完成 | 667 | 含 §4.14 测试策略里"已完成"的测试项 |
| 部分完成 | 131 | 逐条在"缺口说明"列写清了差哪个环节 |
| 未实现 | 22 | 列在第五节的"阻断"与"缺失"两档 |
| 文档过时 / 冲突 | 26 | 含 1 条"自相矛盾"（README 许可） |
| 待确认 | 3 | 规划本身或现状证据不足，见第五节"待确认"档 |

**统计口径说明**：

- 一条 = 一个可独立判定的功能、界面元素、接口、配置项或交互行为。
- 上表按第四节 4.1–4.17 的表格数据行统计（`grep`/awk 计数，非目测）：正文表格共 872 行，加上文档清单表里被逐条核对的条目，去重后计 880 条。
- 严重程度列里的"不一致"归入"文档已过时/冲突"档；"缺失"归入"部分完成"或"未实现"档，不重复计数。
- 4.17"文档未覆盖的已有功能"15 条按要求**不计入**完成度分母。

---

## 四、逐项核对表

### 4.1 核心概念与产品决策（`计划.md` §2–§5）

| 规划条目 | 出处 | 当前状态 | 证据（代码路径 + 行号 / 函数名） | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| 下游只用逻辑模型名，不感知上游 | 计划.md:56 | 已完成 | `src/gateway/models.rs:84 listable_models`、`:99 owned_by=akhub`；`tests/gateway_e2e.rs:425` | — | — |
| 同逻辑模型可绑多个"账号+模型"并组内调度 | 计划.md:57 | 已完成 | `src/config/mod.rs` TargetView 多目标；`tests/discovery_selection.rs:137` | — | — |
| 人工优先级是硬排序，高优先级层有合格目标就不用低层 | 计划.md:58 | 已完成 | `src/routing/mod.rs:350 into_layers`、`:634 the_ladder_is_strict...`；`tests/routing_e2e.rs:54` | — | — |
| 同一会话粘同一账号 | 计划.md:59 | 已完成 | `src/routing/sticky.rs:66 derive`；`tests/routing_e2e.rs:313` | — | — |
| 慢请求不并行复制 | 计划.md:60 | 已完成 | `tests/routing_e2e.rs:189 a_slow_upstream_is_neither_retried_nor_tripped` | — | — |
| 三协议尽可能完整转换 | 计划.md:61 | 已完成 | `src/protocol/*`、`src/protocol/canonical.rs`；`tests/protocol_e2e.rs:96` 六方向 | — | — |
| 文本/图片/工具/结构化输出不静默删除 | 计划.md:62 | 已完成 | `src/protocol/degrade.rs:5-12 DEGRADABLE`；`tests/protocol_e2e.rs:469` | — | — |
| thinking 仅故障切换时降级并标记 | 计划.md:62 | 已完成 | `src/routing/mod.rs:365-370` 层内无损优先；`tests/protocol_e2e.rs:302` | — | — |
| 自动获取 New API / Sub2API 倍率 | 计划.md:63 | 已完成 | `src/multiplier/probe.rs:289 new_api_groups`、`:173`；`tests/routing_e2e.rs:757,831` | — | — |
| 倍率未知按风险余量宽限 | 计划.md:64 | 已完成 | `src/multiplier/mod.rs:374-414`；`tests/routing_e2e.rs:663` | — | — |
| 刷新模型后自动归并同名模型 | 计划.md:65 | 已完成 | `src/discovery/mod.rs:406-442 reconcile_row`；`tests/discovery_selection.rs:137` | — | — |
| 热路径不等待 SQLite 写入 | 计划.md:66 | 已完成 | `src/app/recorder.rs:3,42 try_send`；`tests/gateway_e2e.rs:544` | — | — |
| 重启后粘性与性能数据可恢复 | 计划.md:67 | 已完成 | `src/app/mod.rs:409-431 restore`；`tests/routing_e2e.rs:942` | — | — |
| §2.2 不做下游计费/账单/多租户/RBAC | 计划.md:79-80 | 已完成 | 全仓无 billing/tenant/rbac 模块；`src/admin/mod.rs:81 Admin` 单管理员 | — | — |
| §2.2 不跨组调度 | 计划.md:81 | 已完成 | `src/config/mod.rs:144,319`；`tests/admin_api.rs:336` | — | — |
| §2.2 不做 Responses WebSocket | 计划.md:82 | 已完成 | `src/gateway/mod.rs:27-51` 无 ws 路由 | — | — |
| **§2.2 不做图片生成等独立 API 端点** | 计划.md:83 | **文档与代码冲突** | 代码已实现：`src/gateway/mod.rs:33-34`、`src/gateway/mod.rs:69-77`、`tests/images_e2e.rs`（7 用例）；`README.md:30` 也声称已实现 | 计划说"不做"，代码与 README 都做了。三方口径不一致，不做取舍，两个都列出 | 不一致 |
| §2.2 不做多实例集群/Redis | 计划.md:84 | 已完成 | `Cargo.toml` 无 redis；单进程 `src/server.rs:16` | — | — |
| §2.2 不做 Windows 发行版 | 计划.md:85 | 已完成 | `.github/workflows/release.yml:23-25` 仅两个 Linux target | — | — |
| §2.2 不做运行时脚本插件 | 计划.md:86 | 已完成 | `src/lib.rs:1-22` 编译期模块 | — | — |
| §2.2 不维护模型价格表 | 计划.md:88 | 已完成 | `src/capability/mod.rs:26-46` 无价格字段；`src/capability/mod.rs:237` 测试 | — | — |
| §3 Rust + 单实例 + SQLite | 计划.md:94-95 | 已完成 | `Cargo.toml:1-4`；`src/storage/mod.rs:29-54` | — | — |
| §3 单管理员首次必须设密码 | 计划.md:96 | 已完成 | `src/admin/mod.rs:282-309 setup`；`tests/admin_api.rs:51` | — | — |
| §3 每分组一个 Key，同时接受两种鉴权头 | 计划.md:98 | 已完成 | `src/auth/mod.rs:26-68`；`tests/gateway_e2e.rs:503` | — | — |
| §3 账号一键复制且配置独立 | 计划.md:101 | 已完成 | `src/admin/resources.rs:1801-1923 copy_account`；`tests/admin_cost.rs:338` | — | — |
| §3 调度权重 40/25/20/15 分组级可调 | 计划.md:104 | 已完成 | `src/domain/mod.rs:199-208`；`src/domain/mod.rs:194 is_valid` | — | — |
| §3 校准系数账号级默认 1.0 | 计划.md:106 | 已完成 | `src/domain/mod.rs:268`；`src/admin/resources.rs:715` | — | — |
| §3 尝试预算按成本+时间，硬上限 8 目标 | 计划.md:110 | 已完成 | `src/routing/mod.rs:25 MAX_TARGET_ATTEMPTS=8`；`src/gateway/passthrough.rs:720-721 cheap`；`tests/routing_e2e.rs:266` | 与 §13.1 的三条硬约束一致 | — |
| §3 每目标独立队列 + 分组总容量默认 100 | 计划.md:111 | 已完成 | `src/routing/queue.rs:33 GroupQueues`、`:103 wait_for_any`；`src/routing/mod.rs:538` | — | — |
| §3 请求体 8 MB 内存 / 64 MB 上限 | 计划.md:112 | 已完成 | `src/gateway/passthrough.rs:2122 IN_MEMORY_BODY_LIMIT`、`:2126 MAX_UPSTREAM_BODY_BYTES`、`:2165 落盘` | — | — |
| §3 模型拉取手动勾选 + 选择集持久 | 计划.md:113 | 已完成 | `src/discovery/mod.rs:275-321`；`web/src/components/ModelSelectionDialog.tsx` | — | — |
| §3 空模型列表不改变现有列表 | 计划.md:115 | 已完成 | `src/discovery/mod.rs:66-78,160-163`；`tests/discovery_selection.rs:850` | — | — |
| §3 测试按钮发真实 hi，不污染统计 | 计划.md:116 | 已完成 | `src/admin/resources.rs:1935-1980 test_account`；`tests/admin_cost.rs:473` | — | — |
| §3 普通请求不保存正文 | 计划.md:117 | 已完成 | `src/storage/schema.sql:142-195` 无正文列 | — | — |
| §3 元数据默认保留 30 天可调 | 计划.md:118 | 已完成 | `src/main.rs:32`；`src/admin/resources.rs:191` | — | — |
| §3 元数据设为 0 时只保留实时聚合 | 计划.md:118 | 部分完成 | 清理跳过 `src/app/tasks.rs:236-245`（仅 `retention_days>0` 才删） | **仍会照常写入 `request_records`**：`src/gateway/passthrough.rs:1068`、`src/gateway/settle.rs:171` 写记录时没有 retention 门；"只保留内存汇总"未成立 | 缺失 |
| §3 Responses 状态仅保存必要加密状态 | 计划.md:119 | 已完成 | `src/gateway/responses.rs:42-47 should_store`；`src/storage/schema.sql:201-216` | — | — |
| §3 配置不可变快照原子切换 | 计划.md:121 | 已完成 | `src/config/mod.rs:123-133 reload`；`src/gateway/mod.rs:117 current()` | — | — |

### 4.2 后台页面与操作流程（`计划.md` §6）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| §6.1 设置管理员密码界面 | 计划.md:252 | 已完成 | `web/src/pages/Gate.tsx:45-90`；`src/admin/mod.rs:282-309` | — | — |
| §6.1 显示数据目录 | 计划.md:253 | 未实现 | `src/app/mod.rs:385` 持有 `data_dir`，但 `src/admin/resources.rs:127-175 overview` 与 `src/admin/mod.rs:269-279 setup_status` 都不返回它 | 后端从不把数据目录路径回吐给后台，界面上看不到它 | 不一致 |
| §6.1 显示主密钥状态 | 计划.md:253 | 已完成 | `web/src/pages/Gate.tsx:93-99`；`src/admin/mod.rs:136-140`；`web/src/pages/SettingsPage.tsx:500-508` | — | — |
| §6.1 设置后进入空的概览页 | 计划.md:254 | 已完成 | `web/src/App.tsx:78-81,210`；`web/src/pages/Overview.tsx:166-200` | — | — |
| §6.1 不强迫创建分组或账号 | 计划.md:256 | 已完成 | `web/src/pages/Overview.tsx:42-67` 被动引导 | — | — |
| §6.2 请求量/成功率/平均延迟/高位延迟 | 计划.md:262 | 已完成 | `src/admin/resources.rs:145-149`；`web/src/pages/Overview.tsx:114-136` | 窗口固定 24 小时（`resources.rs:89`），文档未写窗口 | — |
| §6.2 在途请求数/排队请求数/队列超时数 | 计划.md:263 | 已完成 | `resources.rs:150-152`；`src/app/mod.rs:254`；`src/routing/queue.rs:65 waiting` | — | — |
| §6.2 可用/降级/暂停/倍率过期/倍率未知目标数 | 计划.md:264 | 部分完成 | `resources.rs:72-85,133 target_status`；`:134-135` multiplier_stale/unknown | ①状态集是 `src/health/mod.rs:148-166` 五态（active/cooldown/half_open/quota_exhausted/key_invalid），不是"可用/降级/暂停"；②全仓无 `degraded` 状态；③倍率过期/未知返回的是**账号数组**而非目标数 | 不一致 |
| §6.2 最近错误 | 计划.md:265 | 已完成 | `resources.rs:102-106,153-160`；`src/storage/store.rs:1087` | — | — |
| §6.2 最近配置变化 | 计划.md:265 | 已完成 | `resources.rs:107-111,161-167`；`store.rs:1114` | — | — |
| §6.2 倍率探针系统性故障红色告警 | 计划.md:266 | 已完成 | `src/multiplier/refresh.rs:132,181`；`resources.rs:136`；`web/src/pages/Overview.tsx:372-378 danger` | — | — |
| §6.3 名称与 Key 前缀 | 计划.md:274 | 已完成 | `web/src/pages/Groups.tsx:66-67,80-81` | — | — |
| §6.3 分组倍率上限 | 计划.md:275 | 已完成 | `web/src/pages/Groups.tsx:68,82` | — | — |
| §6.3 四项权重可编辑、总和必须 100 | 计划.md:276 | 已完成 | `web/src/pages/Groups.tsx:190-202,331-362`；`src/admin/resources.rs:2694-2702 validate_weights` | — | — |
| §6.3 队列总容量 | 计划.md:277 | 已完成 | `web/src/pages/Groups.tsx:70,87`；`src/domain/mod.rs:221` | — | — |
| §6.3 最长等待时间 | 计划.md:277 | 已完成 | `web/src/pages/Groups.tsx:71,88-90`；`src/domain/mod.rs:223 max_wait_secs`；`src/routing/mod.rs:265` | — | — |
| §6.3 故障切换允许降级开关默认开启 | 计划.md:278 | 已完成 | `web/src/pages/Groups.tsx:72,91-95`；`src/admin/resources.rs:401` | — | — |
| §6.3 逻辑模型数与可用目标数 | 计划.md:279 | 已完成 | `web/src/pages/Groups.tsx:73,96-100`；`resources.rs:336-337` | — | — |
| §6.3 分组行内"当前告警" | 计划.md:279 | 未实现 | `GroupDto` `src/admin/resources.rs:322-338` 无告警字段；告警只在 `resources.rs:44-70,127-136` 的全局概览 | 分组列表页看不到该分组自己的告警，必须回概览页 | 缺失 |
| §6.3 不再有"最大尝试目标数"设置项 | 计划.md:281 | 已完成 | 前端 grep 零命中；`GroupDto` 无该字段 | — | — |
| §6.3 Key 只完整显示一次、可重新生成且旧的立即失效 | 计划.md:283 | 已完成 | `web/src/pages/Groups.tsx:421-457 KeyRevealModal`；`src/admin/resources.rs:481-503 regenerate_key`；`tests/admin_api.rs:487` | — | — |
| §6.4 账号列表按分组/状态/上游类型筛选 | 计划.md:287 | 已完成 | `web/src/pages/Accounts.tsx:88-110,232-283` | — | — |
| §6.4 行内：编辑 | 计划.md:289 | 已完成 | `web/src/pages/Accounts.tsx:397-399`；AccountDrawer `:548-1065` | — | — |
| §6.4 行内：一键复制 | 计划.md:290 | 已完成 | `web/src/pages/Accounts.tsx:400-407`；`src/admin/resources.rs:1801` | — | — |
| §6.4 行内：测试连接 | 计划.md:291 | 已完成 | `web/src/pages/Accounts.tsx:408-415`；`src/admin/resources.rs:1935` | — | — |
| §6.4 行内：获取模型 | 计划.md:292 | 已完成 | `web/src/pages/Accounts.tsx:387-389`；`ModelSelectionDialog.tsx:235`；`resources.rs:1603` | — | — |
| §6.4 行内：手动刷新倍率 | 计划.md:293 | 已完成 | `web/src/pages/Accounts.tsx:517-533`；`resources.rs:861` | — | — |
| §6.4 行内：启用/停用 | 计划.md:294 | 已完成 | `web/src/pages/Accounts.tsx:364-372`；`resources.rs:797-799` | — | — |
| §6.4 复制创建停用状态"名称 - 副本" | 计划.md:296 | 已完成 | `src/admin/resources.rs:1822,1836`；`tests/admin_cost.rs:409` | — | — |
| §6.4 复制静态配置 + 选择集 + 别名 | 计划.md:296 | 已完成 | `src/admin/resources.rs:1819-1840,1872-1895`；`tests/admin_cost.rs:448` | — | — |
| §6.4 复制时重新加密 Key | 计划.md:296 | 已完成 | `src/admin/resources.rs:1844-1862` cipher.open→seal；`tests/admin_cost.rs:422` | — | — |
| §6.4 复制重置健康/粘性/性能/倍率状态 | 计划.md:296 | 已完成 | `src/admin/resources.rs:1820,1903-1907` 新 ID；`src/health/mod.rs:170-198` 按 ID 索引 | — | — |
| §6.4 复制后打开编辑页 | 计划.md:296 | 部分完成 | `web/src/pages/Accounts.tsx:166-177` 只 toast + refresh | 未调用 `setEditing(copy)`，复制后停留在列表，要再点一次"编辑" | 不一致 |
| §6.4 改分组后清除原分组逻辑模型绑定 | 计划.md:296 | 文档过时 | `AccountPatch` `src/admin/resources.rs:533-552` 无 `group_id`；`web/src/pages/Accounts.tsx:743-760` 明确"归属分组后不可迁移" | 实现改成**禁止改分组**（比文档更强），不存在"改分组后清理绑定"的路径 | 不一致 |
| §6.4 编辑页：名称/所属分组/上游类型/BaseURL/APIKey | 计划.md:300-305 | 部分完成 | `Accounts.tsx:762-772`（名称）、`:810-823`（BaseURL）、`:825-840`（APIKey）、`:743-760`（分组，仅新建）、`:775-790`（上游类型） | ①编辑态没有"所属分组"控件；②编辑态"上游类型"可改并随 PATCH 提交，但 `AccountPatch` 无该字段，serde 静默丢弃——改了不生效且无提示 | 缺失 |
| §6.4 首选协议 + 允许运行时自动适配 | 计划.md:307-308 | 部分完成 | 首选协议 `Accounts.tsx:792-807`；`adaptive_protocol` 字段 `src/admin/resources.rs:516,539` | **前端没有"允许运行时自动适配"控件**：`web/src/lib/types.ts:64` 与 `api.ts:110` 有字段但 `Accounts.tsx` 从不渲染，管理员无法开关（新建时后端默认 true） | 缺失 |
| §6.4 默认人工优先级 0-100 | 计划.md:310 | 已完成 | `Accounts.tsx:842-858,631-635`；`resources.rs:2704-2709 validate_priority` | — | — |
| §6.4 倍率模式自动/手动 | 计划.md:312 | 已完成 | `Accounts.tsx:860-884`；`src/domain/mod.rs:243-246` | — | — |
| §6.4 NewAPI 访问令牌 + 用户 ID（仅自动+NewAPI 显示） | 计划.md:313-317 | 已完成 | `Accounts.tsx:886-971`（条件渲染）；`resources.rs:521-524,688-704` | — | — |
| §6.4 校准系数默认 1.0 | 计划.md:318 | 已完成 | `Accounts.tsx:601`；`resources.rs:715` | — | — |
| §6.4 自动同步上游模型开关 | 计划.md:320 | 已完成 | `Accounts.tsx:1048-1057`；`resources.rs:802-805,846-850` | — | — |
| §6.4 模型别名表 | 计划.md:322-324 | 已完成 | `ModelSelectionDialog.tsx:360-418`；`resources.rs:1759-1794` | 位置偏差：别名在"模型"对话框里，不在账号编辑页（信息可达） | 不一致 |
| §6.4 RPM / TPM / 最大并发 | 计划.md:326 | 已完成 | `Accounts.tsx:1008-1039`；`resources.rs:2714-2727 validate_limits` | — | — |
| §6.4 不出现上下文/多模态/工具/思考能力输入框 | 计划.md:331 | 已完成 | `Accounts.tsx` 表单 742-1063 无这些输入；`web/src/pages/Models.tsx` 亦无 | — | — |
| §6.5 模型与目标默认自动生成 | 计划.md:335-339 | 已完成 | `src/discovery/mod.rs:275-321 apply_selection`→`:406-442 reconcile_row`；`tests/discovery_selection.rs:485` | — | — |
| §6.5 目标行"目标"列 | 计划.md:351 | 已完成 | `web/src/pages/Targets.tsx:323,339-353` | — | — |
| §6.5 "优先级"列 | 计划.md:352 | 已完成 | `Targets.tsx:299,348-352`；`resources.rs:1270-1272` | 呈现偏差：优先级显示在层标题上而非每行（信息等价） | — |
| §6.5 "有效倍率"列 | 计划.md:353 | 已完成 | `Targets.tsx:324,354`；`resources.rs:568,633` | — | — |
| §6.5 "综合评分"列 | 计划.md:354 | 已完成 | `Targets.tsx:325,355-361`；`resources.rs:1336-1420 score_dto` | — | — |
| §6.5 "分维得分"列（含加权贡献） | 计划.md:355 | 部分完成 | `web/src/components/ui.tsx:418-440 ScoreMeter` 四维条形；`resources.rs:1288-1297` | **缺"加权贡献"**：只画归一化得分，不显示"得分 × 分组权重"的贡献值，权重还需去分组页对照 | 不一致 |
| §6.5 "状态"列 | 计划.md:356 | 已完成 | `Targets.tsx:328,366-368,221-263`；`resources.rs:1278` | — | — |
| §6.5 "首字延迟 / 输出速度"列（EWMA） | 计划.md:357 | 未实现 | EWMA 在后端 `src/routing/score.rs:49-99`（`first_token_ms`/`output_tps`），但 `TargetDto` `resources.rs:1265-1284` 不含这两个值；`Targets.tsx:321-331` 无此列 | 后端没把 EWMA 回吐给后台，前端无从显示 | 缺失 |
| §6.5 "暂停原因"列 | 计划.md:358 | 未实现 | 全仓 grep `pause_reason`/"暂停原因" 零命中；`TargetDto` 无该字段 | 只有状态徽标与冷却秒数，没有"倍率超限/额度耗尽/Key 失效"这类结构化暂停原因文本 | 缺失 |
| §6.5 可覆盖某目标优先级 | 计划.md:343 | 已完成 | `Targets.tsx:582-599`；`TargetPatch.priority_override` `resources.rs:1259,1510-1515` | — | — |
| §6.5 可排除某个自动生成目标 | 计划.md:344 | 部分完成 | 取消勾选即移除：`src/discovery/mod.rs:416-420`、`:453-469 cleanup_auto_model`；`ModelSelectionDialog.tsx:216-237` | 没有独立"排除"开关，也没有 `deselected` 状态；排除后目标直接消失，逻辑模型页看不到"已排除" | 缺失 |
| §6.5 可手工创建自定义对外名的逻辑模型 | 计划.md:345 | 已完成 | `web/src/pages/Models.tsx:189-367`；`resources.rs:1155-1192 origin=Manual` | — | — |
| §6.6 按时间筛选 | 计划.md:366 | 已完成 | `Requests.tsx:42-46,205-221`；`resources.rs:2560-2563` | 只有 1h/24h/7d/全部 预设，无任意区间选择器 | — |
| §6.6 按请求 ID 筛选 | 计划.md:366 | 已完成 | `Requests.tsx:283-293`；`resources.rs:2564,2588` | — | — |
| §6.6 按分组筛选 | 计划.md:366 | 已完成 | `Requests.tsx:222-238`；`resources.rs:2565` | — | — |
| §6.6 按逻辑模型筛选 | 计划.md:366 | 已完成 | `Requests.tsx:239-255`；`resources.rs:2566` | — | — |
| §6.6 按实际目标筛选 | 计划.md:366 | 部分完成 | 后端支持 `target_id`/`account_id`：`resources.rs:2567-2568,2591-2592`；`store.rs:2022-2029` | **前端没有这个筛选控件**：`Requests.tsx:24-31` 的表单状态与 `:204-294` 的输入项都没有 target/account，用户不可达 | 缺失 |
| §6.6 按状态筛选 | 计划.md:366 | 已完成 | `Requests.tsx:256-271`；`resources.rs:2569-2570` | — | — |
| §6.6 按错误码筛选 | 计划.md:366 | 已完成 | `Requests.tsx:272-282`；`resources.rs:2571` | — | — |
| §6.6 记录：请求 ID / 开始时间 / 协议 / 是否流式 | 计划.md:368 | 已完成 | `resources.rs:2610-2614`；`Requests.tsx:322,351-396` | — | — |
| §6.6 记录：分组和逻辑模型 | 计划.md:369 | 部分完成 | 逻辑模型 `resources.rs:2617`；分组 ID `:2615` | 表格里没有分组列（`Requests.tsx:322-331`），单行记录看不到所属分组，只能靠筛选 | 不一致 |
| §6.6 记录：每次尝试的目标/端点/耗时/失败原因/是否计入预算 | 计划.md:370 | 已完成 | `resources.rs:2638-2649`；`Requests.tsx:468-520`；`schema.sql:181-195` | 未渲染该次尝试的开始时间（后端 `resources.rs:2644` 已返回） | — |
| §6.6 记录：首字/总延迟/Token/切换次数/配置版本 | 计划.md:371 | 部分完成 | first_token `resources.rs:2633`→`Requests.tsx:424`；duration `:2612`→`:425`；Token `:2634-2635`→`:423`；attempts `:2629`→`:430`；config_version `:2636` | **config_version 前端不显示**（`Requests.tsx:322-457` 无此列），换个页面再回来就看不到 | 不一致 |
| §6.6 记录：粘性键命中与否 / 粘性等待时长 | 计划.md:372 | 部分完成 | sticky_hit `resources.rs:2631`→`Requests.tsx:428`；`queued_ms` `:2630`→`:433` | **没有独立的粘性等待时长**：`schema.sql:166-168` 只有 `queued_ms`，粘性等待被累加进同一个计数（`passthrough.rs:563,951`），二者不可区分 | 缺失 |
| §6.6 记录：能力降级标红并注明降级能力 | 计划.md:373 | 已完成 | `resources.rs:2625`→`Requests.tsx:340 row-degraded`+`:391-395` | — | — |
| §6.6 记录：最终网关错误码与 HTTP 状态码 | 计划.md:374 | 部分完成 | `resources.rs:2621-2623`；`Requests.tsx:448-457` | `upstream_status` 未展示；表头未标注这是网关返回码 | 不一致 |
| §6.6 默认不显示也不保存正文 | 计划.md:376 | 已完成 | `schema.sql:142-174,181-195` 无正文列；`Requests.tsx:194-196` | — | — |
| §6.7 自动倍率刷新间隔 | 计划.md:382 | 已完成 | `SettingsPage.tsx:19,33`；`resources.rs:194,205` | — | — |
| §6.7 自动模型同步间隔 | 计划.md:383 | 已完成 | `SettingsPage.tsx:20,33`；`resources.rs:195,206` | — | — |
| §6.7 请求元数据保留天数 | 计划.md:384 | 已完成 | `SettingsPage.tsx:16,29`；`resources.rs:191,202` | — | — |
| §6.7 Responses 可重放状态保留天数 | 计划.md:385 | 已完成 | `SettingsPage.tsx:17,30`；`resources.rs:192,203` | — | — |
| §6.7 请求总超时 | 计划.md:386 | 已完成 | `SettingsPage.tsx:14,27`；`resources.rs:189,200` | — | — |
| §6.7 请求体上限 | 计划.md:386 | 已完成 | `SettingsPage.tsx:15,28`；`resources.rs:190,201` | — | — |
| §6.7 配置备份与恢复 | 计划.md:387 | 已完成 | `SettingsPage.tsx:529-607`；`resources.rs:2410-2438,2451-2538` | — | — |
| §6.7 当前版本 | 计划.md:388 | 已完成 | `resources.rs:209`；`SettingsPage.tsx:492-495` | — | — |
| §6.7 模型目录版本 | 计划.md:388 | 部分完成 | `resources.rs:208 capability_catalog_revision`；`SettingsPage.tsx:496-499` | 展示的是**内置能力目录** revision，不是账号级"上游模型目录"；后者没有版本号/时间戳可查，命名与实际对不上 | 不一致 |
| §6.7 适配器版本 | 计划.md:388 | 未实现 | 全仓 grep 只命中文档；`resources.rs:198-214 settings_json` 无该字段 | 协议适配层没有可展示的版本标识 | 缺失 |
| §6.7 高级算法参数不进入后台 | 计划.md:390 | 已完成 | `SettingsPage.tsx:9-21` 无权重；权重只在 `Groups.tsx:190-202` | — | — |
| §6.8 成本页以逻辑模型为单位组织 | 计划.md:394 | 已完成 | `resources.rs:2049-2204` 按 (group,model) 分块；`Cost.tsx:79-88` | — | — |
| §6.8 每个模型显示 token 用量 | 计划.md:397 | 部分完成 | `resources.rs:2183`；`Cost.tsx:150` | 上游不报 usage 时恒为 0（`store.rs:1218-1220` 注释），没有"未知"态；只有一句"上游未上报 Token，占比按请求数"（`Cost.tsx:176-178`） | 不一致 |
| §6.8 每个模型显示请求次数 | 计划.md:397 | 已完成 | `resources.rs:2182`；`Cost.tsx:149` | — | — |
| §6.8 各账号有效倍率 | 计划.md:398 | 已完成 | `resources.rs:2112-2124,2193`；`Cost.tsx:214-216` | — | — |
| §6.8 各账号流量占比 | 计划.md:398 | 已完成 | `resources.rs:2186-2192`；`Cost.tsx:207-226` | — | — |
| §6.8 加权均倍率 | 计划.md:401 | 已完成 | `resources.rs:2137-2147,2167-2171`；`Cost.tsx:157` | — | — |
| §6.8 最便宜目标 | 计划.md:402 | 部分完成 | `resources.rs:2145,2197`；`Cost.tsx:158-161` | 与最贵合并成"最便宜 → 最贵"一格，缺文档示例里"只用它会更省，但没有备份"的说明 | 不一致 |
| §6.8 最贵目标 | 计划.md:403 | 部分完成 | `resources.rs:2146,2198`；`Cost.tsx:158-161` | 同上 | 不一致 |
| §6.8 相对最贵节省百分比 | 计划.md:404 | 已完成 | `resources.rs:2172-2178 saving_vs_cheapest`；`Cost.tsx:140-141,162` | 口径为"全走最便宜目标可再省"，与文档语义一致 | — |
| §6.8 单目标时显示"无调度空间"提示 | 计划.md:409 | 部分完成 | `resources.rs:2201 single_target`；`Cost.tsx:151` | 只有一个"单目标"徽标，没有文档写的那句说明 | 不一致 |
| §6.8 全局区 token 总用量与请求总数 | 计划.md:418 | 已完成 | `resources.rs:2207-2208,2243-2244`；`Cost.tsx:99-109` | — | — |
| §6.8 全局区各账号请求数与 token 占比 | 计划.md:419 | 部分完成 | `resources.rs:2210-2238`（含 token 绝对值）；`Cost.tsx:111-126` | 只显示占比与请求次数，**不显示该账号 token 绝对值**（后端已经返回） | 不一致 |
| §6.8 不出现跨模型"总成本/总节省" | 计划.md:420 | 已完成 | `resources.rs:2206-2208` 只有 total_requests/total_tokens；`tests/admin_cost.rs:92` | — | — |
| §6.8 校准助手：选模型 | 计划.md:425 | 已完成 | `web/src/components/CalibrationDialog.tsx:102-117` | — | — |
| §6.8 校准助手：统计区间 | 计划.md:426 | 未实现 | 前端不传 `period_start`（`CalibrationDialog.tsx:57`），后端固定最近 30 天（`resources.rs:2273`） | 界面上没有区间选择器，"本月"这个选项不存在 | 缺失 |
| §6.8 校准助手：显示本账号该模型用量 | 计划.md:427 | 部分完成 | 结果显示请求次数 `CalibrationDialog.tsx:141-147` | 不显示"该模型 token 用量"（`resources.rs:2300` 有该数据但未回吐展示） | 不一致 |
| §6.8 校准助手：站点扣费输入 + 反算系数 | 计划.md:428-429 | 已完成 | `CalibrationDialog.tsx:118-131`；`resources.rs:2265-2372` | — | — |
| §6.8 校准必须按单模型对账 | 计划.md:432 | 已完成 | `resources.rs:2291-2307`；`tests/admin_cost.rs:242` | 多账号共享时用全组均倍率近似并显式提示（`resources.rs:2366-2371`） | — |
| §6.9 账号列表行内徽标显示健康/额度/倍率 | 计划.md:434-436 | 部分完成 | 倍率状态 `Accounts.tsx:500-510,517-533`；启用状态 `:364-372` | **健康与额度徽标缺失**：`AccountDto` `resources.rs:554-589` 无这两个字段，账号级 `key_invalid`/`quota_exhausted` 只能从目标状态间接看出来 | 缺失 |
| §6.9 不单独设账号级状态页面 | 计划.md:436 | 已完成 | `web/src/routes.ts:2-11` 无该路由 | — | — |
| §6.9 详细信息在逻辑模型页目标行展开 | 计划.md:436 | 已完成 | `Targets.tsx:221-263,339-383`；`resources.rs:1277-1330` | — | — |

### 4.3 下游公开接口（`计划.md` §7）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| POST /v1/chat/completions | 计划.md:444 | 已完成 | `src/gateway/mod.rs:29`；`tests/gateway_e2e.rs:217,261` | — | — |
| POST /v1/messages | 计划.md:445 | 已完成 | `src/gateway/mod.rs:30`；`tests/gateway_e2e.rs:217` | — | — |
| POST /v1/messages/count_tokens | 计划.md:446 | 已完成 | `src/gateway/mod.rs:31`；`tests/gateway_e2e.rs:291` | 只能原生转发，见 §15.5 相关行 | — |
| POST /v1/responses | 计划.md:447 | 已完成 | `src/gateway/mod.rs:32`；`tests/responses_chain.rs:68` | — | — |
| GET /v1/responses/{id} | 计划.md:448 | 已完成 | `src/gateway/mod.rs:36-39`；`src/gateway/responses.rs:394 retrieve`；`tests/responses_lifecycle.rs:139` | — | — |
| DELETE /v1/responses/{id} | 计划.md:449 | 已完成 | `src/gateway/mod.rs:38`；`src/gateway/responses.rs:490 destroy`；`tests/responses_chain.rs:438` | — | — |
| POST /v1/responses/{id}/cancel | 计划.md:450 | 已完成 | `src/gateway/mod.rs:40`；`responses.rs:535 cancel`；`tests/responses_lifecycle.rs:173` | — | — |
| GET /v1/responses/{id}/input_items | 计划.md:451 | 已完成 | `src/gateway/mod.rs:41-44`；`responses.rs:460 input_items`；`tests/responses_lifecycle.rs:206` | 跨协议/store:false 时明确返回不支持 | — |
| POST /v1/responses/compact | 计划.md:452 | 已完成 | `src/gateway/mod.rs:47`；`responses_compact` 同文件:80；`tests/responses_lifecycle.rs:278,327` | — | — |
| POST /v1/responses/input_tokens | 计划.md:453 | 已完成 | `src/gateway/mod.rs:48`；`responses_input_tokens` 同文件:85 | — | — |
| GET /v1/models | 计划.md:454 | 已完成 | `src/gateway/mod.rs:49`；`src/gateway/models.rs:16 list`；`tests/gateway_e2e.rs:425` | — | — |
| GET /v1/models/{model} | 计划.md:455 | 已完成 | `src/gateway/mod.rs:50`；`src/gateway/models.rs:34 get` | 只有单元测试（`models.rs:248-275`），没有 e2e | — |
| 不增加历史别名或兼容路径 | 计划.md:457 | 已完成 | `src/gateway/mod.rs:27-51` 仅标准路径 + 2 条图片路由 | — | — |
| §7.2 所有 /v1 用分组 Key 鉴权 | 计划.md:461 | 已完成 | `src/gateway/mod.rs:106-126`；`src/gateway/models.rs:69 resolve` | — | — |
| §7.2 同时支持 Bearer 与 x-api-key | 计划.md:462 | 已完成 | `src/auth/mod.rs:26-43`；`tests/protocol_e2e.rs:684` | — | — |
| §7.2 两个头同时存在且值不同 → 鉴权错误 | 计划.md:463 | 已完成 | `src/auth/mod.rs:45-49`；`tests/gateway_e2e.rs:503-512` | — | — |
| §7.3 双形状按鉴权头判定 | 计划.md:468-475 | 已完成 | `src/gateway/models.rs:25-29,60-64,103,120`；`tests/gateway_e2e.rs:425` | — | — |
| §7.3 两者都有且值相同 → 按 x-api-key 判 Anthropic | 计划.md:475 | 已完成 | `src/auth/mod.rs:50-54`；单测 `:129` | — | — |
| §7.3 只返回已启用且有调度目标的逻辑模型 | 计划.md:479 | 已完成 | `src/config/mod.rs:44 is_listable`；`src/gateway/models.rs:84` | — | — |
| §7.3 目标全部不健康时模型仍在列表 | 计划.md:480 | 已完成 | `is_listable` 不查健康；`src/gateway/models.rs:80-83` 注释 | 没有直接断言这一点的测试 | — |
| §7.3 零目标：自动创建清理 / 手工创建保留但移出列表 | 计划.md:481 | 部分完成 | 移出列表 `src/config/mod.rs:44`；清理 `src/discovery/mod.rs:454 cleanup_auto_model`（`:455` 跳过手工）；`tests/discovery_selection.rs:524,935` | 清理只在**模型同步路径**触发（`src/discovery/mod.rs:203,418`）；管理员在"目标"页删掉最后一个目标时是否触发清理，代码里找不到路径 | 待确认 |
| §7.3 owned_by 固定 akhub | 计划.md:482 | 已完成 | `src/gateway/models.rs:99`；`tests/gateway_e2e.rs:463` | — | — |
| §7.3 display_name 用逻辑模型名 | 计划.md:482 | 已完成 | `src/gateway/models.rs:115`；`tests/gateway_e2e.rs:449` | — | — |
| §7.3 未知模型 model_not_found，不跨组 | 计划.md:483 | 已完成 | `src/gateway/models.rs:44-52`；`src/routing/mod.rs:231-237`；`tests/gateway_e2e.rs:516` | — | — |

### 4.4 后台 REST API（`计划.md` §7.4）

> 说明：规划表实际是 **25 行**（`计划.md:491-515`），不是任务书里提到的 28 个路径。

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| GET /admin/api/setup/status | 计划.md:491 | 已完成 | `src/admin/mod.rs:147`→`:269-279 setup_status` | — | — |
| POST /admin/api/setup | 计划.md:492 | 已完成 | `src/admin/mod.rs:148`→`:282-309`；`tests/admin_api.rs:51` | — | — |
| POST /admin/api/auth/login | 计划.md:493 | 已完成 | `src/admin/mod.rs:150`→`:312-340` | — | — |
| POST /admin/api/auth/logout | 计划.md:494 | 已完成 | `src/admin/mod.rs:151`→`:343-352`；`tests/admin_api.rs:183` | — | — |
| GET /admin/api/overview | 计划.md:495 | 已完成 | `src/admin/mod.rs:152`→`resources.rs:27`；`tests/admin_settings.rs:419` | — | — |
| GET/POST /admin/api/groups | 计划.md:496 | 已完成 | `src/admin/mod.rs:153-156`；`tests/admin_api.rs:115,163` | — | — |
| GET/PATCH/DELETE /admin/api/groups/{id} | 计划.md:497 | 已完成 | `src/admin/mod.rs:157-162`；`resources.rs:372,423,462` | — | — |
| POST /admin/api/groups/{id}/regenerate-key | 计划.md:498 | 已完成 | `src/admin/mod.rs:163-166`→`resources.rs:481`；`tests/admin_api.rs:509` | — | — |
| GET/POST /admin/api/accounts | 计划.md:499 | 已完成 | `src/admin/mod.rs:171-174`→`resources.rs:652,674` | — | — |
| GET/PATCH/DELETE /admin/api/accounts/{id} | 计划.md:500 | 部分完成 | `src/admin/mod.rs:175-178`（仅 PATCH/DELETE） | **缺 GET /accounts/{id}**：没有单账号读取 handler，前端也只能整表拉取 | 不一致 |
| POST /admin/api/accounts/{id}/copy | 计划.md:501 | 已完成 | `src/admin/mod.rs:187`→`resources.rs:1801`；`tests/admin_cost.rs:401` | — | — |
| POST /admin/api/accounts/{id}/test | 计划.md:502 | 已完成 | `src/admin/mod.rs:188`→`resources.rs:1935`；`tests/admin_cost.rs:495` | — | — |
| POST /admin/api/accounts/{id}/models/fetch | 计划.md:503 | 文档过时 | 实际是 POST `/accounts/{id}/models/refresh`（`src/admin/mod.rs:204-207`→`resources.rs:1603`） | 路径名与文档不符（fetch → refresh） | 不一致 |
| PUT /admin/api/accounts/{id}/models/selection | 计划.md:504 | 文档过时 | 实际是 POST `/accounts/{id}/models/select`（`src/admin/mod.rs:208-211`→`resources.rs:1631`） | 路径名不符，方法也从 PUT 改成 POST | 不一致 |
| POST /admin/api/accounts/{id}/multiplier/refresh | 计划.md:505 | 文档过时 | 实际是 POST `/accounts/{id}/refresh-multiplier`（`src/admin/mod.rs:179-182`→`resources.rs:861`） | 路径名与文档不符 | 不一致 |
| POST /admin/api/accounts/{id}/calibrate | 计划.md:506 | 已完成 | `src/admin/mod.rs:189-192`→`resources.rs:2265`；`tests/admin_cost.rs:290` | — | — |
| GET/POST /admin/api/logical-models | 计划.md:507 | 已完成 | `src/admin/mod.rs:220-223`→`resources.rs:1124,1155` | — | — |
| GET/PATCH/DELETE /admin/api/logical-models/{id} | 计划.md:508 | 部分完成 | `src/admin/mod.rs:224-227`（update `resources.rs:1194`、delete `:1225`） | **缺 GET /logical-models/{id}** | 不一致 |
| GET/POST/PATCH/DELETE /admin/api/targets | 计划.md:509 | 部分完成 | 集合 GET/POST `src/admin/mod.rs:228-231`；`/targets/{id}` PATCH/DELETE `:232-235`（`resources.rs:1489,1533`） | 集合路径上没有 PATCH/DELETE；能力齐备但与文档"同一路径四方法"不符 | 不一致 |
| GET /admin/api/cost | 计划.md:510 | 已完成 | `src/admin/mod.rs:197`→`resources.rs:2049`；`tests/admin_cost.rs:134,212` | — | — |
| GET/PATCH /admin/api/settings | 计划.md:511 | 已完成 | `src/admin/mod.rs:237-240`→`resources.rs:217,225`；`tests/admin_settings.rs:33` | — | — |
| GET /admin/api/requests | 计划.md:512 | 已完成 | `src/admin/mod.rs:236`→`resources.rs:2580`；`tests/admin_settings.rs:335` | — | — |
| **GET /admin/api/metrics** | 计划.md:513 | **未实现** | 全仓 grep `/admin/api/metrics` 零命中；`src/admin/mod.rs:143-254` 无该路由；`src/storage/schema.sql` 无分钟聚合表 | 规划承诺的"查询分钟聚合和目标性能"接口不存在；目标性能只能从 `/targets` 的 score 字段间接看到 | 缺失 |
| POST /admin/api/backup/export | 计划.md:514 | 已完成 | `src/admin/mod.rs:198`→`resources.rs:2410`；`tests/admin_cost.rs:637` | — | — |
| POST /admin/api/backup/import | 计划.md:515 | 已完成 | `src/admin/mod.rs:199`→`resources.rs:2451`；`tests/admin_cost.rs:575,668` | — | — |
| §7.4 所有列表接口都分页并限制上限 | 计划.md:517 | 部分完成 | 只有 `/requests` 分页：`resources.rs:2580-2604` + `store.rs:993 limit.clamp(1,500)` | 其余列表全部一次性全量返回：`list_groups` `resources.rs:360-370`、`list_accounts` `:652-672`、`list_logical_models` `:1124-1153`、`list_targets` `:1426-1438`、`list_account_models` `:1575-1597`、`list_calibrations` `:2376-2399`、`list_new_api_sites` `:968-986` | 缺失 |
| §7.4 返回对象 Key 只有前缀和状态 | 计划.md:517 | 部分完成 | `GroupDto.key_prefix` `resources.rs:326-327`；`AccountDto` 只回 `has_new_api_token` `:578-579` | **旁路**：`/backup/export` 会明文导出上游 Key（`src/security/backup.rs:39`、`src/storage/store.rs:1375-1386`），管理员可借此拿到完整 Key | 缺失 |
| §7.4 写接口带 config_version，冲突返回 409 config_conflict | 计划.md:517 | **未实现** | `config_version` 只出现在请求记录与概览（`resources.rs:128`、`store.rs:1916,2376`、`schema.sql:173`）；`AdminError::conflict` 只用于唯一约束（`resources.rs:2786-2793`） | 所有写 DTO（`resources.rs:298-320,509-552,1245-1262`）都没有 `config_version` 字段，也没有 409 `config_conflict` 分支；两个浏览器同时编辑会静默互相覆盖 | 缺失 |
| §7.4 刷新/测试/复制返回结构化结果并写审计日志 | 计划.md:517 | 已完成 | refresh `resources.rs:885-891`；test `:2001-2033`；copy `:1919-1922`；`audit()` `:2803-2811` | 审计 `result` 恒为 "ok"，失败路径不写审计 | — |

### 4.5 请求处理流程、调度、粘性、队列（`计划.md` §8–§13）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| §8 请求 ID 生成 | 计划.md:521 | 已完成 | `src/gateway/mod.rs:103`；`src/gateway/passthrough.rs:1856 x-akhub-request-id` | — | — |
| §8 校验请求体大小与 JSON 格式并记录字节数 | 计划.md:522 | 已完成 | `src/gateway/passthrough.rs:2134-2170 read_body`、`:2162 上限`；字节数 `:275` | — | — |
| §8 校验分组 Key、取不可变配置快照 | 计划.md:523 | 已完成 | `src/gateway/mod.rs:106-126`；`src/config/mod.rs:123-133` | — | — |
| §8 按路径识别协议、解析逻辑模型与所需能力 | 计划.md:524 | 已完成 | `src/gateway/mod.rs:104 endpoint.protocol()`；`src/protocol/mod.rs` | — | — |
| §8 构建可重放请求体（8 MB 内存 / 临时文件） | 计划.md:525 | 已完成 | `src/gateway/passthrough.rs:2165 落盘`；`tests/routing_e2e.rs` 大请求用例 | — | — |
| §8 找目标并按优先级分层 | 计划.md:526 | 已完成 | `src/routing/mod.rs:225 plan`、`:350 into_layers` | — | — |
| §8 硬性资格过滤，从最高层找第一个合格层 | 计划.md:527 | 已完成 | `src/routing/mod.rs:158 check_eligibility`；`src/gateway/passthrough.rs:346 逐层游走` | — | — |
| §8 计算粘性键并查绑定，命中且合格优先使用 | 计划.md:528 | 已完成 | `src/routing/sticky.rs:66 derive`；`src/gateway/passthrough.rs:299-321` | — | — |
| §8 未命中粘性时层内按评分加权随机选择 | 计划.md:529 | 已完成 | `src/routing/score.rs:349 weighted_order`；`src/routing/mod.rs:375 shuffle` | — | — |
| §8 获取并发槽位（粘性忙进目标队列 / 无粘性有空位直走 / 全满进任意队列） | 计划.md:530-533 | 已完成 | `src/routing/queue.rs:103 wait_for_any`；`src/gateway/passthrough.rs:324-343,373-413` | — | — |
| §8 发出上游请求前读最新倍率与动态状态终检 | 计划.md:534 | 已完成 | `src/gateway/passthrough.rs:1011-1030 recheck_multiplier`、`:966-998` | — | — |
| §8 选择原生端点或适配器，判定是否降级 | 计划.md:535 | 已完成 | `src/upstream/endpoints.rs:46 choices`；`src/protocol/degrade.rs` | — | — |
| §8 转发非流式或按状态机逐事件转发 SSE | 计划.md:536 | 已完成 | `src/gateway/passthrough.rs:1579-1603`；`src/gateway/stream.rs` | — | — |
| §8 更新真实流量样本、健康、额度、能力证据 | 计划.md:537 | 已完成 | `src/routing/score.rs:136 observe`；`src/health/mod.rs`；`src/gateway/passthrough.rs:1787 learn_capability_limitation` | — | — |
| §8 异步写请求元数据、释放槽位、唤醒队列 | 计划.md:538 | 已完成 | `src/app/recorder.rs:42 record`；`src/gateway/settle.rs:171` | — | — |
| §8 SQLite 写与后台刷新不阻塞第 12–13 步 | 计划.md:540 | 已完成 | `src/app/recorder.rs:3` 热路径只 try_send | — | — |
| §9.1 硬性过滤：不属于当前分组 | 计划.md:548 | 已完成 | `src/routing/mod.rs:232 group.models.get`；`src/config/mod.rs:144,319` | — | — |
| §9.1 账号/逻辑模型/目标被停用 | 计划.md:549 | 已完成 | `src/routing/mod.rs:163-165`；`src/routing/mod.rs:720 disabled_accounts_are_skipped...` | — | — |
| §9.1 上游模型消失或被移出选择集 | 计划.md:550 | 已完成 | `src/discovery/mod.rs:233-265 sync_missing_targets`；`tests/discovery_selection.rs:318` | 状态名在代码里是 `account_models.missing` 整数列，不是文档写的 `missing_model` 字符串 | 不一致 |
| §9.1 自动倍率未知且超宽限 → multiplier_unknown | 计划.md:551 | 已完成 | `src/routing/mod.rs:172-175`；`src/routing/mod.rs:984 an_unknown_multiplier_hard_stops_the_target` | — | — |
| §9.1 有效倍率高于分组上限 → multiplier_exceeded | 计划.md:552 | 已完成 | `src/routing/mod.rs:176-179`；`tests/routing_e2e.rs:789` | — | — |
| §9.1 Key 失效/账号封禁/总额度耗尽 | 计划.md:553 | 已完成 | `src/health/mod.rs:258,310 Unavailable::KeyInvalid/QuotaExhausted`；`tests/routing_e2e.rs:534` | — | — |
| §9.1 冷却中且没拿到半开名额 | 计划.md:554 | 已完成 | `src/health/mod.rs:968 cooldown_opens_exactly_one_trial_slot...`；`src/routing/mod.rs:1075 a_cooling_target_leaves_the_plan...` | — | — |
| §9.1 请求需要的能力证实不支持且不在降级白名单 | 计划.md:555 | 已完成 | `src/routing/mod.rs:191-206 `；`src/capability/mod.rs:226-230 DEGRADABLE` | — | — |
| §9.1 RPM/TPM/最大并发达硬限制 | 计划.md:556 | 已完成 | `src/health/mod.rs:359-378 check`；`src/health/mod.rs:1247,1269,1173` | — | — |
| §9.1 粘性请求"并发满"不构成不合格 | 计划.md:556 | 已完成 | `src/health/mod.rs:112 is_queueable`；`src/routing/mod.rs:440 sticky_still_valid` | — | — |
| §9.1 能力"未知"按保守中性分 | 计划.md:558 | 已完成 | `src/routing/score.rs:22 NEUTRAL=0.6`、`:321-323` | — | — |
| §9.1 取消"不能无损转换即不合格" | 计划.md:560 | 已完成 | `src/routing/mod.rs:155-157` 注释与实现 | — | — |
| §9.2 默认优先级 50，范围 0-100 | 计划.md:564 | 已完成 | `src/domain/mod.rs`；`src/admin/resources.rs:2704-2709 validate_priority` | — | — |
| §9.2 目标可覆盖账号默认值 | 计划.md:565 | 已完成 | `src/admin/resources.rs:1259 priority_override`；`tests/admin_api.rs:780` | — | — |
| §9.2 同值同层，层是硬边界 | 计划.md:566-567 | 已完成 | `src/routing/mod.rs:350-372 into_layers`；`:607,634` 测试 | — | — |
| §9.2 同 Key 多模型分别用各自有效优先级 | 计划.md:568-569 | 已完成 | `src/routing/mod.rs:124 find`；`tests/routing_e2e.rs:534` | — | — |
| §9.3 只用真实请求，测试与后台任务不进样本 | 计划.md:577 | 已完成 | `src/routing/score.rs:34-37` 注释；`src/admin/resources.rs:1933-1934`；`tests/admin_cost.rs:473` | — | — |
| §9.3 按"目标 + 协议 + 是否流式"维护统计 | 计划.md:577 | 已完成 | `src/routing/score.rs:115-119 Dimension` | — | — |
| §9.3 成功率与连续错误 | 计划.md:579 | 部分完成 | `src/routing/score.rs:80 success_rate`；连续错误在 `src/health/mod.rs:701-714,785` | 连续错误只在 `health` 熔断器里，没有作为评分信号；EWMA 里没有单独的"连续错误"维度 | 不一致 |
| §9.3 首字/首个语义事件延迟 | 计划.md:580 | 已完成 | `src/routing/score.rs:42,53 first_token_ms` | — | — |
| §9.3 非流式总延迟 | 计划.md:581 | 已完成 | `src/routing/score.rs:44 total`、`:54 total_ms`（记录为 `duration_ms`） | 该值参与 EWMA，但不参与四维评分 | — |
| §9.3 每秒输出 Token | 计划.md:582 | 已完成 | `src/routing/score.rs:45-46,56 output_tps` | — | — |
| §9.3 429/5xx/损坏响应比例 | 计划.md:583 | 未实现 | `src/routing/score.rs` 无 429/5xx/corrupt 相关字段（grep 零命中） | 评分里只有合并后的 `success_rate`，不区分 429、5xx 与损坏响应；这些只在熔断器 `src/health/mod.rs` 里按 Outcome 分类 | 缺失 |
| §9.3 当前在途数与最大并发占比 | 计划.md:584 | 未实现 | `src/routing/score.rs` 无 inflight/并发占比字段（grep 零命中） | 并发状态在 `src/health/mod.rs`，没有进入评分；文档承诺"在途数占比"作为信号未落地 | 缺失 |
| §9.3 有效倍率 | 计划.md:585 | 已完成 | `src/routing/score.rs:245+ multiplier` | — | — |
| §9.3 EWMA + 最小样本数 + 滞后防抖 | 计划.md:587 | 已完成 | `src/routing/score.rs:15 ALPHA`、`:17 MIN_SAMPLES=20`；`tests/routing_e2e.rs:942` | — | — |
| §9.3 每 60 秒快照到 SQLite 并重启加载 | 计划.md:587 | 已完成 | `src/app/tasks.rs:18-19 SNAPSHOT_INTERVAL`、`:68-92`；`tests/routing_e2e.rs:942` | — | — |
| §9.4 比值归一化，非 min-max 非硬编码刻度 | 计划.md:593 | 已完成 | `src/routing/score.rs:3,296-320 score_one`；`:419 ratio_normalisation...` | — | — |
| §9.4 倍率得分 = 参照系最低 / 自己 | 计划.md:596 | 已完成 | `src/routing/score.rs:297-304` | — | — |
| §9.4 可靠性得分 = 自己成功率 | 计划.md:597 | 已完成 | `src/routing/score.rs:308` | — | — |
| §9.4 首字得分 = 最低 / 自己 | 计划.md:598 | 已完成 | `src/routing/score.rs:309-314` | — | — |
| §9.4 速度得分 = 自己 / 最高 | 计划.md:599 | 已完成 | `src/routing/score.rs:315-319` | — | — |
| §9.4 归一化参照系 = 整个分组 | 计划.md:611 | **部分完成 / 不一致** | 倍率维做到了：`src/routing/mod.rs:283-298 cheapest_in_group`、`src/routing/score.rs:248-259` | 计划写的是"分组"，但 **性能三维（首字/速度）的参照系只用了本次计划里的候选**（`src/routing/score.rs:263-268 warm() 遍历 candidates`），不是整个分组。同一目标在不同逻辑模型下的首字/速度得分可能不一致，与 §9.4"每个目标只有一个分数、跨层可比"的承诺有出入。倍率维因账号级属性而正确用了全组。 | 不一致 |
| §9.4 选择作用域 = 当前层 | 计划.md:611 | 已完成 | `src/gateway/passthrough.rs:346 逐层`；`src/routing/mod.rs:365-370` | — | — |
| §9.4 每个目标只有一个跨层可比分数 | 计划.md:613 | 已完成 | `src/routing/mod.rs:323 score_all`；`tests/admin_api.rs:780` | — | — |
| §9.4 权重默认 40/25/20/15，分组级可调总和 100 | 计划.md:625-632 | 已完成 | `src/domain/mod.rs:188 TOTAL=100`、`:194 is_valid`、`:199-208` | — | — |
| §9.4 冷启动样本 < 20 取中性分，倍率维正常 | 计划.md:636 | 已完成 | `src/routing/score.rs:17,307,321-323`；`:464 cold_targets_use_a_neutral...` | — | — |
| §9.5 按 score^k 加权随机，非赢家通吃 | 计划.md:640 | 已完成 | `src/routing/score.rs:30 POWER=8`、`:349 weighted_order` | — | — |
| §9.5 k 为内置常数不暴露后台 | 计划.md:642 | 已完成 | `src/routing/score.rs:30` 私有 const；设置页无此字段 | — | — |
| §9.5 分差小时接近均分，分差大时留保命口粮 | 计划.md:644-648 | 已完成 | `src/routing/score.rs:556,566,578 the_documented_example_reproduces` | — | — |
| §9.5 粘性命中不参与抽签 | 计划.md:655 | 已完成 | `src/gateway/passthrough.rs:298-343` | — | — |
| §10.1 粘性键：Responses conversation/previous_response_id/网关响应 ID | 计划.md:663 | 已完成 | `src/routing/sticky.rs:442 the_derivation_order_follows_the_specification`；`tests/responses_chain.rs:146` | — | — |
| §10.1 显式会话头 | 计划.md:664 | 已完成 | `src/routing/sticky.rs:23 SESSION_HEADERS` | — | — |
| §10.1 prompt_cache_key 或稳定用户字段 | 计划.md:665 | 已完成 | `src/routing/sticky.rs:66-112 derive` | — | — |
| §10.1 稳定前缀摘要 = hash(system + 工具定义 + 分组 + 模型名) | 计划.md:669 | 已完成 | `src/routing/sticky.rs:123-159 stable_prefix`、`:182-190 key_with` | — | — |
| §10.1 第 4 级不含任何用户消息 | 计划.md:672 | 已完成 | `src/routing/sticky.rs:129-159`；`:402 injected_timestamps...` | — | — |
| §10.1 只保存哈希 | 计划.md:681 | 已完成 | `src/routing/sticky.rs:195 key_with`（HMAC→hex）；`:495 the_key_never_contains_the_prompt_itself` | — | — |
| §10.2 命中且合格直接用 | 计划.md:685 | 已完成 | `src/gateway/passthrough.rs:306-343` | — | — |
| §10.2 6 种情况清除绑定 | 计划.md:686 | 已完成 | `src/routing/mod.rs:440-445 sticky_still_valid`；`src/gateway/passthrough.rs:316-321` | — | — |
| §10.2 "忙"不清除而是等待 | 计划.md:687 | 已完成 | `src/health/mod.rs:112 is_queueable`；`src/routing/mod.rs:436-444` | — | — |
| §10.2 普通粘性 1 小时滑动过期且持久化 | 计划.md:688 | 已完成 | `src/routing/sticky.rs:20 TTL=3600`；`tests/routing_e2e.rs:942` | — | — |
| §10.2 Responses 状态链独立持久化映射 | 计划.md:689 | 已完成 | `src/gateway/responses.rs`；`src/storage/schema.sql:201` | — | — |
| §10.3 体积 8 档等待预算 | 计划.md:707-716 | 已完成 | `src/routing/sticky.rs:327-337 SIZE_BUDGET`、`:356 wait_budget`；`:535 the_wait_budget_follows_size_and_freshness` | — | — |
| §10.3 新鲜度 3 档系数 | 计划.md:720-724 | 已完成 | `src/routing/sticky.rs:344 freshness`；`:569 a_cold_cache_makes_waiting_nearly_worthless` | — | — |
| §10.3 预算 = 档位 × 系数 | 计划.md:703 | 已完成 | `src/routing/sticky.rs:356-368` | — | — |
| §10.3 429 带 Retry-After ≤ 剩余预算则等待 | 计划.md:733 | 已完成 | `src/gateway/passthrough.rs:935 retry_after_within`；`tests/routing_e2e.rs:435` | — | — |
| §10.3 预算耗尽降级为无粘性并重绑 | 计划.md:735 | 已完成 | `src/gateway/passthrough.rs:338-343`；`:618-626 bind` | — | — |

### 4.6 倍率与成本（`计划.md` §11）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| §11.1 倍率是账号级，不下沉到模型 | 计划.md:741 | 已完成 | `src/domain/mod.rs:242 calibration`、`:246 manual_multiplier`；`src/storage/schema.sql:37-62` | — | — |
| §11.1 有效倍率 = 上游倍率 × 校准系数 | 计划.md:744 | 已完成 | `src/domain/fixed.rs:94 mul_ceil`；`src/multiplier/mod.rs:157`；`src/domain/mod.rs:268` | — | — |
| §11.1 校准系数账号级、手填、默认 1.0 | 计划.md:748 | 已完成 | `src/admin/resources.rs:715 unwrap_or(Multiplier::ONE)`；`tests/admin_cost.rs:242` | — | — |
| §11.1 分组倍率上限是绝对红线 | 计划.md:749 | 已完成 | `src/routing/mod.rs:176-179`；`tests/routing_e2e.rs:789` | — | — |
| §11.1 有效倍率 ≤ 上限才能发新请求 | 计划.md:750 | 已完成 | `src/routing/mod.rs:176-179`；`src/routing/mod.rs:674` | — | — |
| §11.1 倍率比较用定点整数不用浮点 | 计划.md:751 | 已完成 | `src/domain/fixed.rs:13 SCALE`；`src/routing/mod.rs:177`；`tests/admin_cost.rs:290` | — | — |
| §11.2 每账号只能选一种来源 | 计划.md:758 | 已完成 | `src/domain/mod.rs:243 multiplier_mode`；`src/admin/resources.rs:519` | — | — |
| §11.2 自动-Sub2API 调 Key 级 `/v1/sub2api/billing` | 计划.md:762 | 已完成 | `src/multiplier/probe.rs`（sub2api 分支）；`tests/routing_e2e.rs:757` | 现场字段形状已按 `review/2026-09-15-真实上游验收.md:36-39` 兼容两代 | — |
| §11.2 Sub2API 严格校验 object/version/scope/有效倍率/观察时间/峰值 | 计划.md:762 | 已完成 | `src/multiplier/probe.rs:131-133 RawBilling`、`:214`；`:449,491,515` 测试 | — | — |
| §11.2 自动-New API 探针必须是 `GET /api/user/self/groups` | 计划.md:766 | 已完成 | `src/multiplier/probe.rs:289 new_api_groups`；`tests/routing_e2e.rs:831` | — | — |
| §11.2 携带 Authorization 访问令牌 + New-Api-User 用户 ID 头 | 计划.md:769-771 | 已完成 | `src/multiplier/probe.rs`（两个头）；`tests/routing_e2e.rs:831`；`src/domain/mod.rs:247-250` | — | — |
| §11.2 不能用 `/api/pricing` 或 `/api/ratio_config` | 计划.md:773 | 已完成 | 全仓 grep 这两个路径零命中 | — | — |
| §11.2 手动倍率状态始终已知，不受刷新失败影响 | 计划.md:785 | 已完成 | `src/multiplier/mod.rs:363 manual_multipliers_ignore_refresh_failures_entirely` | — | — |
| §11.3 默认每 5 分钟刷新，可调 | 计划.md:789 | 已完成 | `src/main.rs:31`（默认 1800s 对应模型同步，倍率 300s）；`src/admin/resources.rs:194` | — | — |
| §11.3 各账号加随机抖动 | 计划.md:790 | 已完成 | `src/multiplier/refresh.rs:31 JITTER_RATIO=0.25` | — | — |
| §11.3 账号级手动刷新按钮 | 计划.md:791 | 已完成 | `web/src/pages/Accounts.tsx:517-533`；`src/admin/resources.rs:861` | — | — |
| §11.3 **批量**手动刷新按钮 | 计划.md:791 | 未实现 | 无批量刷新接口（`src/admin/mod.rs:143-254` 无该路由）；`web/src` 无批量刷新控件 | 只有单账号刷新，管理员无法一次刷新全部账号 | 缺失 |
| §11.3 Sub2API 峰值时段本地算当前有效倍率 | 计划.md:792 | 已完成 | `src/multiplier/probe.rs:28-42,78`、`:532,549 a_reading_switches_to_the_peak_multiplier_locally` | — | — |
| §11.3 失败退避 30s/1m/2m/5m/10m/30m + 抖动 | 计划.md:793 | 已完成 | `src/multiplier/refresh.rs:22-29 BACKOFF` | — | — |
| §11.4 失败进入 multiplier_stale 并保留最后已知值 | 计划.md:803 | 已完成 | `src/multiplier/mod.rs:374-414`；`tests/routing_e2e.rs:663` | — | — |
| §11.4 宽限期 ≤60%→60min / 60-90%→15min / >90%→0 | 计划.md:805-809 | 已完成 | `src/multiplier/mod.rs:372 the_grace_period_shrinks_as_the_risk_margin_thins` | — | — |
| §11.4 宽限期内仍参与调度并层内降权 | 计划.md:811-814 | 已完成 | `src/routing/score.rs:24 STALE_PENALTY`、`:330-334`；`src/routing/score.rs:501` | — | — |
| §11.4 目标行显示"倍率已过期 N 分钟" | 计划.md:815 | 已完成 | `web/src/pages/Overview.tsx:386-393`；`src/admin/resources.rs:58-70 stale_for` | 展示在概览页告警条与账号列表徽标，不是"目标行" | 不一致 |
| §11.4 概览页黄色告警 | 计划.md:816 | 已完成 | `web/src/pages/Overview.tsx:386-393 tone=warn` | — | — |
| §11.4 宽限期结束转 multiplier_unknown、硬停、清粘性、按退避继续刷新 | 计划.md:818 | 已完成 | `src/routing/mod.rs:172-175`；`src/multiplier/mod.rs` 状态机；`src/multiplier/refresh.rs` | — | — |
| §11.4 恢复成功后自动解除 | 计划.md:818 | 已完成 | `tests/routing_e2e.rs:725-733` | — | — |
| §11.4 系统性故障：过半账号同轮失败 → 宽限期统一延长 60 分钟 + 红色告警 | 计划.md:820-822 | 已完成 | `src/multiplier/refresh.rs:132,173,181 SYSTEMIC_WINDOW`；`:464`；`web/src/pages/Overview.tsx:372-378 danger` | — | — |
| §11.5 刷新结果通过动态状态原子替换 | 计划.md:826 | 已完成 | `src/multiplier/mod.rs:218-239`；`src/app/mod.rs:183-198` | — | — |
| §11.5 排队中或未发出的请求重新校验 | 计划.md:827 | 部分完成 | `src/gateway/passthrough.rs:1011-1030 recheck_multiplier`、`:601 调用点`、`:573` | 实现存在，但**没有"排队等待期间倍率升高 → 终检拒绝"的测试用例** | 缺失 |
| §11.5 超过上限立即 multiplier_exceeded 硬停 | 计划.md:828 | 已完成 | `src/routing/mod.rs:176-179` | — | — |
| §11.5 已发出的调用不取消 | 计划.md:829 | 待确认 | 终检只在发送前执行（`src/gateway/passthrough.rs:601`），逻辑上已发出后不再检查 | 有实现依据但没有"请求已发往上游后改倍率仍完成"的测试；需补一个用例才能算已证实 | 待确认 |
| §11.5 倍率恢复后自动恢复 | 计划.md:830 | 已完成 | `tests/routing_e2e.rs:796-799` | — | — |
| §11.6 记录保存逻辑模型/实际目标/有效倍率 | 计划.md:836 | 已完成 | `src/storage/schema.sql:160-164`；`src/gateway/passthrough.rs:1106` | — | — |
| §11.6 记录保存输入/输出/缓存 token 细分 | 计划.md:837 | 部分完成 | `schema.sql:170-172` 只有 `input_tokens`/`output_tokens` | **没有缓存 token 细分列**：`canonical.rs:348-379 Usage` 里有 `cache_read`/`cache_write`/`reasoning`，但记录表不存这三项 | 缺失 |
| §11.6 记录保存当时最贵/最便宜合格目标的有效倍率 | 计划.md:838 | 已完成 | `schema.sql:163-164`；`src/routing/mod.rs:336-337`；`src/gateway/passthrough.rs:1107-1108` | — | — |
| §11.6 成本聚合只在同一逻辑模型内 | 计划.md:840 | 已完成 | `src/admin/resources.rs:2103 BTreeMap<(group,model)>`；`tests/admin_cost.rs:92` | — | — |
| §11.6 加权均倍率 = Σ(倍率×token)/Σ(token) | 计划.md:843 | 部分完成 | `resources.rs:2167-2171` | 实际按**请求次数**加权（`weighted_count += sample.requests`，`:2141-2143`），不是按 token 加权；当各次请求 token 量差异大时与文档口径有偏差 | 不一致 |
| §11.6 相对最贵节省 = 1 - 加权均/最贵 | 计划.md:844 | 部分完成 | `resources.rs:2172-2178` | 实现的是"相对**最便宜**可再省"（`1 - cheapest/avg`），与文档写的"相对最贵节省"（`1 - avg/dearest`）不是一个口径 | 不一致 |

### 4.7 健康、熔断、超时与队列（`计划.md` §12–§13）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| §12.1 Key 失效/封禁/额度耗尽/倍率问题影响整个账号 | 计划.md:857 | 已完成 | `src/health/mod.rs:1049 an_invalid_key_pauses_every_model_on_the_account`；`tests/routing_e2e.rs:534` | — | — |
| §12.1 模型不存在/限流/能力不支持影响账号+模型 | 计划.md:858 | 已完成 | `tests/routing_e2e.rs:534`（429 只停账号+模型） | — | — |
| §12.1 单端点 404/405 影响账号+协议端点 | 计划.md:859 | 已完成 | `src/upstream/evidence.rs:32 note_unsupported`；`tests/protocol_e2e.rs:566` | — | — |
| §12.1 单次网络错误/5xx 影响当前尝试，达阈值影响目标 | 计划.md:860 | 已完成 | `src/health/mod.rs:918 isolated_faults_only_switch...`；`tests/routing_e2e.rs:606` | — | — |
| §12.1 单纯变慢只降评分，不熔断 | 计划.md:861 | 已完成 | `src/routing/score.rs:44,54` 延迟进 EWMA；`tests/routing_e2e.rs:189 a_slow_upstream_is_neither_retried_nor_tripped` | — | — |
| §12.2 active | 计划.md:871 | 已完成 | `src/health/mod.rs:148-154 TargetStatus` | — | — |
| §12.2 degraded | 计划.md:872 | 未实现 | `src/health/mod.rs:148-154` 无 `Degraded` 变体；全仓 grep `degraded` 只命中协议降级语义 | 规划要求"仍可调用但层内评分降低"的独立状态，代码里没有；降权由 `STALE_PENALTY` 等隐式表达，后台也没有这个状态可显示 | 缺失 |
| §12.2 cooldown / half_open | 计划.md:873-874 | 已完成 | `src/health/mod.rs:150-151`；`tests/routing_e2e.rs:606` | — | — |
| §12.2 multiplier_stale / multiplier_unknown / multiplier_exceeded | 计划.md:875-877 | 已完成 | `src/multiplier/mod.rs`；`tests/routing_e2e.rs:663,789` | — | — |
| §12.2 quota_exhausted | 计划.md:878 | 已完成 | `src/health/mod.rs:152`、`:1068 quota_exhaustion_honours_the_upstream_recovery_time` | — | — |
| §12.2 missing_model | 计划.md:879 | 部分完成 | 用 `account_models.missing` 整数列（`schema.sql:231`）+ `sync_missing_targets`（`src/discovery/mod.rs:233-265`） | **状态名与文档不符**：代码里没有 `missing_model` 这个状态字符串，后台展示为"上游已消失"徽标；`src/health/mod.rs` 的 TargetStatus 里也没有它 | 不一致 |
| §12.2 deselected | 计划.md:880 | 未实现 | `src/health/mod.rs:148-154` 无该变体；`AccountModelRow.selected=0` 只表示未勾选 | 被移出选择集的目标直接消失，没有"已排除"状态可展示（与 §6.5 那条是同一个缺口） | 缺失 |
| §12.2 disabled | 计划.md:881 | 已完成 | `src/domain/mod.rs:254 enabled`；`src/routing/mod.rs:163-165` | — | — |
| §12.3 401/403 立即暂停账号 | 计划.md:885 | 已完成 | `src/health/mod.rs:483-488`；`tests/routing_e2e.rs:534` | — | — |
| §12.3 额度不足带恢复时间 → 按恢复时间半开 | 计划.md:886 | 已完成 | `src/health/mod.rs:533`、`:1068` | — | — |
| §12.3 无恢复时间 → 指数冷却后只放行一个真实请求 | 计划.md:887 | 已完成 | `src/health/mod.rs:30 COOLDOWN_BASE`、`:968 cooldown_opens_exactly_one_trial_slot...` | — | — |
| §12.3 429 影响账号+模型并尊重 Retry-After；粘性不直接换号 | 计划.md:888 | 已完成 | `src/health/mod.rs:136-139`、`:1139 a_429_with_retry_after_cools_for_exactly_that_long`；`src/gateway/passthrough.rs:935` | — | — |
| §12.3 60 秒内连续或高比例错误进冷却；孤立错误只切换 | 计划.md:889 | 已完成 | `src/health/mod.rs:20-22 RATE/FAULT_WINDOW`、`:918,940` | — | — |
| §12.3 半开成功恢复、失败延长冷却 | 计划.md:890 | 已完成 | `src/health/mod.rs:968,998` | — | — |
| §12.3 不发后台 hi 探测恢复 | 计划.md:891 | 已完成 | `src/app/tasks.rs:1-7` 注释；后台任务只打模型列表与计费接口 | 管理员手动"测试连接"会发 hi（`src/admin/resources.rs:1982`），属人工触发而非自动探测 | — |
| §13.1 尝试预算按成本和时间，不按次数 | 计划.md:897 | 已完成 | `src/routing/mod.rs:25`（硬上限 8）；`src/gateway/passthrough.rs:720-721`（廉价失败标记） | 与 §13.1 的三条硬约束一致 | — |
| §13.1 廉价失败不计入任何上限 | 计划.md:907 | 已完成 | `src/gateway/passthrough.rs:720`；`tests/routing_e2e.rs:266 cheap_failures_do_not_count_against_any_attempt_limit` | — | — |
| §13.1 昂贵失败禁止重试 | 计划.md:914 | 已完成 | `src/gateway/stream.rs:519 the_first_text_delta_closes_the_switching_window`；`tests/routing_e2e.rs:488` | — | — |
| §13.1 硬约束：每目标最多一次 | 计划.md:918 | 已完成 | `src/gateway/passthrough.rs:430 already_tried` | — | — |
| §13.1 硬约束：请求总超时默认 10 分钟可调 | 计划.md:919 | 已完成 | `src/main.rs:29`（默认 600s）；`src/admin/resources.rs:189` | — | — |
| §13.1 硬约束：硬上限 8 个目标 | 计划.md:920 | 已完成 | `src/routing/mod.rs:25 MAX_TARGET_ATTEMPTS=8`；`src/routing/mod.rs:1039` | — | — |
| §13.1 层内先耗尽再降层 | 计划.md:921 | 已完成 | `src/gateway/passthrough.rs:346-358`；`src/routing/mod.rs:375 weighted_order`（不放回） | — | — |
| §13.1 每次新尝试前重新终检 | 计划.md:929 | 已完成 | `src/gateway/passthrough.rs:601` | — | — |
| §13.2 可切换的 8 种情况 | 计划.md:935-942 | 已完成 | `src/gateway/passthrough.rs:1826 is_switchable_status`；`tests/routing_e2e.rs:534,606`；`tests/gateway_e2e.rs:323` | — | — |
| §13.3 不切换的 9 种情况 | 计划.md:946-953 | 已完成 | `src/gateway/passthrough.rs:2191-2195`（JSON 非法直接失败）；`src/routing/mod.rs:231-243`（模型不存在）；`tests/routing_e2e.rs:189,488` | — | — |
| §13.4 只收到头/空白/注释/ping 时仍可切换 | 计划.md:959 | 已完成 | `src/gateway/translate.rs:216-235`；`tests/routing_e2e.rs:488 an_error_event_before_content_switches_but_a_delta_does_not` | — | — |
| §13.4 一旦发送语义增量禁止拼接第二个上游 | 计划.md:960 | 已完成 | `src/gateway/stream.rs:519-545`；`tests/protocol_e2e.rs:699` | — | — |
| §13.4 后续错误发终止错误事件并关闭 | 计划.md:961 | 部分完成 | `src/protocol/mod.rs:304 stream_error`；`src/protocol/openai_chat.rs:1075`、`anthropic.rs:834`、`openai_responses.rs:1190` | 事件帧只有 `message`，**不带稳定网关错误码与请求 ID**（见 §18.2 那条） | 缺失 |
| §13.4 不伪造正常完成事件 | 计划.md:962 | 已完成 | `src/gateway/translate.rs:228`；`src/gateway/settle.rs:96 Ending::Failed` | — | — |
| §13.5 默认总超时 10 分钟可调 | 计划.md:966 | 已完成 | `src/main.rs:29`；`src/admin/resources.rs:189` | — | — |
| §13.5 超时后取消上游并返回超时，不再重放 | 计划.md:967 | 已完成 | `src/gateway/passthrough.rs:433-437` | — | — |
| §13.5 客户端断开立即取消并释放资源 | 计划.md:968 | 部分完成 | 并发名额绑到响应体生命周期（`src/gateway/settle.rs:150-199`）；`src/routing/queue.rs:92` 析构归还 | 断开路径靠 Drop 隐式取消，**没有 e2e 级"客户端断开后名额归还/上游请求取消"用例**（只有单元层 `src/routing/queue.rs:250 a_dropped_ticket_leaves_the_queue`） | 缺失 |
| §13.6 每调度目标独立队列，非分组单队列 | 计划.md:973 | 已完成 | `src/routing/queue.rs:1-7` 注释；`:103 wait_for_any` | — | — |
| §13.6 粘性命中有空位直接走 | 计划.md:988 | 已完成 | `src/gateway/passthrough.rs:324-343` | — | — |
| §13.6 粘性忙进该目标队列，预算耗尽降级重选并重绑 | 计划.md:990 | 已完成 | `src/gateway/passthrough.rs:327-343`；`tests/routing_e2e.rs:356` | — | — |
| §13.6 无粘性有空位立即使用，根本不排队 | 计划.md:991 | 已完成 | `src/gateway/passthrough.rs:486-516` | — | — |
| §13.6 无粘性全满进"任意目标"队列 | 计划.md:992 | 已完成 | `src/routing/queue.rs:119-121 select_all`；`tests/routing_e2e.rs:54` | — | — |
| §13.6 分组总容量上限默认 100，可改可设 0，超出 queue_full | 计划.md:994 | 已完成 | `src/routing/queue.rs:53-62`；`tests/routing_e2e.rs:95` | — | — |
| §13.6 只有临时容量不足/冷却/可恢复限流才排队 | 计划.md:998 | 已完成 | `src/health/mod.rs:112 is_queueable` | — | — |
| §13.6 multiplier_unknown/exceeded/鉴权失败/未知模型/不支持能力绝不排队 | 计划.md:999 | 已完成 | `src/routing/mod.rs:54-56 is_queueable`（只有 `Unavailable` 里两种可排队） | — | — |
| §13.6 等待期间配置或倍率改变重新选择 | 计划.md:1000 | 已完成 | `src/gateway/passthrough.rs:573` 注释、`:601` | — | — |
| §13.6 客户端断开立即移出队列 | 计划.md:1001 | 已完成 | `src/routing/queue.rs:92-95 QueueTicket` 析构归还；`:250` | — | — |
| §13.6 等待超时返回 queue_timeout 并给 Retry-After | 计划.md:1002 | 已完成 | `src/gateway/passthrough.rs:354`；`src/gateway/error.rs:57,205`；`tests/routing_e2e.rs:123` | — | — |

### 4.8 协议网关（`计划.md` §14）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| §14.1 同协议尽量原样透传，只改鉴权/BaseURL/模型名 | 计划.md:1010 | 已完成 | `src/gateway/passthrough.rs:1579-1603`；`:1881 rewrite_model`；`tests/protocol_e2e.rs:778 a_native_target_never_pays_for_conversion` | Responses 入口同协议时为换 ID 会重解析并重序列化 JSON（`passthrough.rs:1582-1600`），非严格字节级透传 | — |
| §14.1 上游有匹配原生端点优先 | 计划.md:1011 | 已完成 | `src/upstream/endpoints.rs:126-132` | — | — |
| §14.1 没有原生端点才跨协议转换 | 计划.md:1012 | 已完成 | `src/protocol/translate.rs:60-71`；`src/gateway/passthrough.rs:904 translation.emit` | — | — |
| §14.1 未知字段同协议原样保留 | 计划.md:1013 | 已完成 | `src/protocol/translate.rs:60-63` 整包原体；`tests/protocol_e2e.rs:506` | 保留靠"整包不解析"，中间格式本身不保存原值 | — |
| §14.1 白名单内可降级+标记，白名单外报错 | 计划.md:1014 | 已完成 | `src/protocol/degrade.rs:5-12`；`src/upstream/endpoints.rs:133-138`；`tests/protocol_e2e.rs:469` | — | — |
| §14.2 保存 preferred_endpoint | 计划.md:1022 | 文档过时 | 实际字段名是 `preferred_protocol`（协议名而非端点名）：`src/domain/mod.rs:238`、`src/storage/schema.sql:43` | 名称与语义都漂移了。且 `schema.sql:44 adaptive_protocol` **没有 DB 默认值**，只有创建路径给 true（`src/admin/resources.rs:713`） | 不一致 |
| §14.2 保存 adaptive_protocol，默认开启 | 计划.md:1023 | 部分完成 | `src/domain/mod.rs:239`；`src/admin/resources.rs:713`；`src/config/mod.rs:249` | 字段存在、后端默认 true，但**前端没有开关控件**（见 §6.4 那条） | 缺失 |
| §14.2 不用后台模拟对话探测端点 | 计划.md:1027 | 已完成 | `src/app/tasks.rs:55-63` 只 4 类任务；`src/upstream/evidence.rs:32` 证据来自真实 404 | 管理员手动"测试连接"会发真实 hi，属人工触发 | — |
| §14.3 端点选择 5 级顺序 | 计划.md:1031-1037 | 部分完成 | `src/upstream/endpoints.rs:102-149`：native → preferred → 其余 INFERENCE → 降级最后 | ①**没有区分"已确认支持"与"能力未知"**（第 1、2 档被合并）；②没有按"转换保真度相同"分组（第 4 档）；③`MAX_ENDPOINTS_PER_TARGET=2`（`endpoints.rs:41`）截断，第 3–5 档基本用不上 | 不一致 |
| §14.3 明确不支持的端点不重复尝试 | 计划.md:1039 | 已完成 | `src/upstream/endpoints.rs:120-122`；`src/upstream/evidence.rs:17-45`；`tests/protocol_e2e.rs:566` | 证据不持久化，重启后重试一次（`evidence.rs:7-9` 明确说明） | — |
| §14.4 中间格式：逻辑模型与真实模型 | 计划.md:1045 | 已完成 | `src/protocol/canonical.rs:210 model`；`src/gateway/passthrough.rs:916 rewrite_model` | — | — |
| §14.4 中间格式：角色语义 | 计划.md:1046 | 已完成 | `src/protocol/canonical.rs:17-28 Role`；`tests/protocol_e2e.rs:96` | 工具结果不是独立角色（`canonical.rs:14-16`）；developer 在 Anthropic 并入 system（`src/protocol/anthropic.rs:364-370`） | — |
| §14.4 中间格式：有顺序索引的内容块 | 计划.md:1047 | 部分完成 | `canonical.rs:76-100 Part`（Text/Image/Document/ToolCall/ToolResult/Thinking/Refusal） | **缺引用（citation）内容块**：`grep citation` 在 `src/` 零命中 | 缺失 |
| §14.4 中间格式：工具定义/选择/并行/参数增量/结果 | 计划.md:1048 | 已完成 | `canonical.rs:108-122`；`tests/protocol_e2e.rs:96,254` | — | — |
| §14.4 中间格式：JSON Schema 结构化输出 | 计划.md:1049 | 已完成 | `canonical.rs:195-204 OutputFormat`；`src/protocol/anthropic.rs:342-348` 不可表达时 Unsupported | — | — |
| §14.4 中间格式：思考强度/摘要/加密块/签名/供应商扩展 | 计划.md:1050 | 部分完成 | `canonical.rs:61-72 ThinkingBlock`、`:174-193 ThinkingConfig`；签名/加密 `src/protocol/anthropic.rs:529-539` | **"供应商原始扩展"没有保留区**：vendor 工具直接拒绝（`src/protocol/anthropic.rs:114`、`src/protocol/openai_responses.rs:108`） | 不一致 |
| §14.4 中间格式：stop/温度/top-p/最大输出 | 计划.md:1051 | 已完成 | `canonical.rs:220-223`；发射 `src/protocol/openai_chat.rs:497-508` 等 | — | — |
| §14.4 中间格式：usage 细分 | 计划.md:1052 | 已完成 | `canonical.rs:348-379 Usage`（input/output/cache_read/cache_write/reasoning）；`canonical.rs:484 usage_merge_never_fabricates` | — | — |
| §14.4 中间格式：未识别字段原始扩展区 | 计划.md:1053 | 部分完成 | `canonical.rs:231-232 unknown: Vec<String>` 只存字段名 | **不是"原始扩展区"**：不保存原值，跨协议一律拒绝（`canonical.rs:260-265`） | 不一致 |
| §14.5 流事件：响应开始与 ID | 计划.md:1061 | 已完成 | `canonical.rs:414-418 Event::Start` | — | — |
| §14.5 流事件：内容项开始与结束 | 计划.md:1062 | 已完成 | `canonical.rs:419 ItemStart`、`:435 ItemEnd` | — | — |
| §14.5 流事件：文本增量 | 计划.md:1063 | 已完成 | `canonical.rs:423 TextDelta`；`tests/protocol_e2e.rs:189` | — | — |
| §14.5 流事件：思考或思考摘要增量 | 计划.md:1064 | 部分完成 | `canonical.rs:427 ThinkingDelta`；`src/protocol/openai_responses.rs:1022` | **Anthropic `signature_delta` 被显式丢弃**（`src/protocol/anthropic.rs:658-659`）且不计入 degraded 标记 | 缺失 |
| §14.5 流事件：工具调用开始/参数增量/结束 | 计划.md:1065 | 已完成 | `canonical.rs:405,431`；`tests/protocol_e2e.rs:254` | — | — |
| §14.5 流事件：引用/拒绝/其他内容项 | 计划.md:1066 | 部分完成 | 拒绝：`canonical.rs:99,406`、`src/protocol/openai_chat.rs:186,273` | **引用完全未实现**：Responses 发射时硬编码 `"annotations": []`（`src/protocol/openai_responses.rs:1156`） | 缺失 |
| §14.5 流事件：usage 更新 | 计划.md:1067 | 已完成 | `canonical.rs:438 Event::Usage`；`src/gateway/stream.rs:315-370 StreamAccounting` | — | — |
| §14.5 流事件：正常/未完成/错误 | 计划.md:1068 | 已完成 | `canonical.rs:439-445`；`src/protocol/mod.rs:304 stream_error` | — | — |
| §14.5 适配器维护事件顺序/内容索引/工具 ID/stop/usage | 计划.md:1070 | 部分完成 | `src/protocol/mod.rs:124-215 StreamParser`；`canonical.rs:409-445` | 状态机在管道层硬编码 enum 分派，没有可插拔适配器抽象；"内容索引"只由 Responses/Anthropic 发射器维护 | 不一致 |
| §14.6 文本消息保留角色与顺序 | 计划.md:1076 | 已完成 | `canonical.rs:77 Part::Text`；`tests/protocol_e2e.rs:96` | — | — |
| §14.6 图片输入 Base64/URL，不为转换下载外部 URL，不可降级 | 计划.md:1077 | 已完成 | `canonical.rs:30-59 MediaSource`、`:78-81 Image`；`tests/protocol_e2e.rs:141` | 无"不下载外部 URL"的行为测试 | — |
| §14.6 标准函数工具，不可降级 | 计划.md:1078 | 已完成 | `canonical.rs:108-114`；`tests/protocol_e2e.rs:96,254` | — | — |
| §14.6 供应商专有工具只有能完整表达才允许 | 计划.md:1079 | 已完成 | `src/protocol/anthropic.rs:114,339`；`src/protocol/openai_responses.rs:108,449` | — | — |
| §14.6 思考可降级 | 计划.md:1080 | 部分完成 | `src/protocol/degrade.rs:11`；`src/protocol/anthropic.rs:529-541` | Anthropic `signature_delta` 流式丢弃未标记为降级（同 §14.5 那条） | 缺失 |
| §14.6 结构化输出不可降级 | 计划.md:1081 | 已完成 | `src/protocol/anthropic.rs:342-348` 返回 Unsupported；`src/protocol/mod.rs:515` | — | — |
| §14.6 stop reason 映射并保留原始原因 | 计划.md:1082 | 部分完成 | `canonical.rs:335-344 StopReason`；`src/protocol/mod.rs:181-214` 缺省回退 | **没有保留内部原始原因**：`grep raw_stop` 零命中；`RequestRecord` 也没有对应字段（`src/gateway/passthrough.rs:1088-1117`） | 不一致 |
| §14.6 usage 目标协议无字段时放入扩展区，不伪造数字 | 计划.md:1083 | 部分完成 | `canonical.rs:357-365 Usage::merge` 不伪造；`src/protocol/openai_responses.rs:792 emit_usage` | "放入允许的扩展区"未实现，直接省略字段 | 不一致 |
| §14.6 未知参数跨协议返回 unsupported_parameter | 计划.md:1084 | 部分完成 | `canonical.rs:260-272`；`src/gateway/passthrough.rs:904-912`；`tests/protocol_e2e.rs:506` | **资格过滤阶段混合原因时返回 no_eligible_target**（`src/gateway/passthrough.rs:360-365`、`src/routing/mod.rs:400-411`），只有全部目标都是 Unsupported 才映射成 400 | 不一致 |
| §14.7 下游鉴权头绝不转发 | 计划.md:1088 | 已完成 | `src/upstream/mod.rs:316-364 build_headers`（新 HeaderMap）；单测 `:422` | — | — |
| §14.7 上游正确 Authorization/x-api-key/anthropic-version | 计划.md:1089 | 已完成 | `src/upstream/mod.rs:342-361`；单测 `:438` | — | — |
| §14.7 anthropic-beta/openai-beta 按白名单转发 | 计划.md:1090 | 已完成 | `src/upstream/mod.rs:18-20,353,359 copy_allowed`；单测 `:452` | — | — |
| §14.7 禁止跨域带 Key 重定向，探针禁重定向 | 计划.md:1091 | 已完成 | `src/upstream/mod.rs:84 redirect(Policy::none())` | 无专门断言重定向不跟随的测试 | — |
| §14.7 返回安全请求 ID/限流/Retry-After，不泄漏 URL/Key | 计划.md:1092 | 已完成 | `src/gateway/passthrough.rs:1856`；`src/gateway/error.rs:199`；`src/security/redact.rs:48` | — | — |
| §14.7 降级返回 `X-Akhub-Degraded` 头 | 计划.md:1093 | 已完成 | `src/gateway/passthrough.rs:1848-1853`；`src/protocol/degrade.rs:87`；`tests/protocol_e2e.rs:351` | — | — |
| §14.8 白名单仅 thinking 与协议独有采样参数 | 计划.md:1099-1106 | 文档过时 | `src/protocol/degrade.rs:5-12`；`src/protocol/mod.rs:321-334 sampling_key` | **代码把 Responses 的 stop 当采样参数丢弃**（`src/protocol/openai_responses.rs:557-559`），而 §14.8 的表里没有 stop——超出了文档承诺的白名单 | 不一致 |
| §14.8 白名单外不可降级 | 计划.md:1102-1104 | 已完成 | `src/upstream/endpoints.rs:133-138`；`tests/protocol_e2e.rs:469` | — | — |
| §14.8 降级只在故障切换生效，首次选择降权 | 计划.md:1108-1110 | 已完成 | `src/upstream/endpoints.rs:141-148`；`src/gateway/passthrough.rs:378-412 walk_layer`；`tests/protocol_e2e.rs:302,375` | "评分降权"用分区排序实现而非改分数，效果等价 | — |
| §14.8 降级时响应头 + 记录标红 + 不改响应体 | 计划.md:1113-1117 | 已完成 | `passthrough.rs:1848`；`passthrough.rs:1104-1105`；`web/src/pages/Requests.tsx:340,391-393` | — | — |

### 4.9 Responses 全套能力（`计划.md` §15）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| §15.1 对外生成 `resp_akh_*` ID | 计划.md:1125 | 已完成 | `src/gateway/responses.rs:24 GATEWAY_PREFIX`、`:50`；`src/gateway/translate.rs:495` | — | — |
| §15.1 保存到上游 ID/账号/模型/分组映射 | 计划.md:1125 | 已完成 | `src/gateway/responses.rs:788-831 record_state`；`src/storage/schema.sql:201-216` | — | — |
| §15.1 不暴露上游身份 | 计划.md:1125 | 已完成 | `src/gateway/translate.rs:287-373 ResponseIdRewriter`；`tests/responses_chain.rs:79` | — | — |
| §15.1 不能跨分组访问 | 计划.md:1125 | 已完成 | `src/gateway/responses.rs:64-80 lookup(group_id)`；`tests/responses_chain.rs:285` | — | — |
| §15.2 store 未设或 true 时加密保存输入/输出/工具/思考项 | 计划.md:1133 | 已完成 | `src/gateway/responses.rs:42-47 should_store`、`:808-812 cipher.seal` | 流式路径保存的是入口请求体 + 最终输出项（`src/gateway/settle.rs:176-202`），不是逐项增量 | — |
| §15.2 store:false 只保存最小定位 | 计划.md:1134 | 已完成 | `responses.rs:42-47,809`；`tests/responses_chain.rs:182` | — | — |
| §15.2 默认保留 30 天可调 | 计划.md:1135 | 已完成 | `src/main.rs:30`；`src/app/mod.rs:35,50`；`src/admin/resources.rs:192,203` | — | — |
| §15.2 保留 0 天时不保存可重放正文 | 计划.md:1136 | 已完成 | `responses.rs:43-45`；单测 `:856` | — | — |
| §15.2 过期返回 response_state_expired | 计划.md:1137 | 已完成 | `responses.rs:55-80`；`tests/responses_chain.rs:266,326` | — | — |
| §15.2 删除时删本地正文/索引/上游映射，上游支持则转发 | 计划.md:1138 | 已完成 | `src/gateway/responses.rs:490-528 destroy`；`src/storage/store.rs:562 delete_response_state` | — | — |
| §15.3 原生 background:true 代理创建/查询/取消并保持目标粘性 | 计划.md:1142 | 部分完成 | 创建走原生转发；查询 `responses.rs:394-454`；取消 `:535-581`；粘性 `src/gateway/passthrough.rs:1658-1704` | 无真实原生后台上游的 e2e（假上游）；"目标粘性"无针对性断言 | 缺失 |
| §15.3 上游不支持时不伪装成后台任务 | 计划.md:1143 | 已完成 | `src/gateway/background.rs:511-531 maybe_start_managed`；`tests/managed_background_e2e.rs:42` | 分组显式开开关时 Akhub 自行托管（见 §29.1） | — |
| §15.3 只有适配器实现并通过生命周期测试才允许网关托管 | 计划.md:1144 | 已完成 | `src/gateway/background.rs:170-401 TaskRunner`；`tests/managed_background.rs`、`tests/managed_background_e2e.rs:68,120,193,304,361` | "通过生命周期测试"是流程要求，代码里没有门禁机制 | — |
| §15.3 拿到上游 in_progress ID 就固定目标，查询错误不转移上游 | 计划.md:1145 | 部分完成 | 原生路径记录 account_id/upstream_id（`responses.rs:788-827`、`:250-274 native_target`） | **托管执行器不按登记目标复用**：`src/gateway/background.rs:259-275` 实际重新走调度（`forward_no_managed`），登记字段在 commit 中不被读取。"固定目标"只对原生路径成立 | 缺失 |
| §15.3 倍率在任务开始后变化，已接受任务允许完成 | 计划.md:1146 | 已完成 | `background.rs:533-553` 创建即登记；执行复用登记时的 group/body（`:245-296`） | 无专门断言倍率变化不影响在途任务的测试 | — |
| §15.4 compact 只用原生或等价适配器 | 计划.md:1150 | 已完成 | `src/upstream/endpoints.rs:54-99` native-only 分支；`tests/responses_lifecycle.rs:278,327` | — | — |
| §15.4 input_tokens 优先上游原生计数 | 计划.md:1151 | 已完成 | `src/upstream/mod.rs:131`；`endpoints.rs:69-73`；`tests/responses_lifecycle.rs:310-322` | — | — |
| §15.4 无法准确计算返回不支持，不返回估算值 | 计划.md:1152 | 已完成 | `endpoints.rs:57-99`；`tests/protocol_e2e.rs:664` | `estimate_tokens`（`passthrough.rs:1890`）只用于准入余量，未用于对外计数 | — |
| §15.5 count_tokens 优先原生转发 | 计划.md:1156 | 已完成 | `src/upstream/mod.rs:183`；`endpoints.rs:57-63,93-99`；`tests/gateway_e2e.rs:291` | — | — |
| §15.5 count_tokens 其次本地精确计算 | 计划.md:1156 | 未实现 | 全仓无 tokenizer：`grep tokenizer|tiktoken` 只命中注释 | 没有本地精确计数能力，实现退化为"仅原生转发"；按文档"都不可行返回不支持"仍自洽，但这一档本身没有实现 | 待确认 |
| §15.5 count_tokens 都不可行返回不支持 | 计划.md:1156 | 已完成 | `src/upstream/endpoints.rs:62`；`tests/protocol_e2e.rs:664,690` | 真实上游 newapi 未实现该端点，实测返回 400（`review/2026-09-15-真实上游验收.md:24`） | — |

### 4.10 模型发现与选择（`计划.md` §16）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| §16.1 按账号协议调上游模型列表接口 | 计划.md:1166 | 已完成 | `src/upstream/mod.rs:216-223 models_url`、`:232-263 fetch_model_list`；`src/discovery/mod.rs:66` | — | — |
| §16.1 限制响应体大小并校验状态/JSON/模型 ID | 计划.md:1167 | 已完成 | `src/upstream/mod.rs:226 MODELS_MAX_BYTES=8MiB`、`:246-249`、`:267-283` | — | — |
| §16.1 去空去重标准化但保留大小写 | 计划.md:1168 | 已完成 | `src/upstream/mod.rs:274-289`；`src/discovery/mod.rs:535-547 validate_model_id` | — | — |
| §16.1 应用账号的模型别名表 | 计划.md:1169 | 已完成 | `src/discovery/mod.rs:80-87,116` | — | — |
| §16.1 弹出勾选对话框 | 计划.md:1170 | 已完成 | `web/src/components/ModelSelectionDialog.tsx:11-459`；`src/admin/resources.rs:1603-1619` | — | — |
| §16.1 报错/格式异常/0 模型时保留原列表，只记错误不弹框 | 计划.md:1172 | 已完成 | `src/discovery/mod.rs:66-78,160-163`；`src/admin/resources.rs:1609-1616`；`tests/discovery_selection.rs:274,850` | — | — |
| §16.2 每账号维护持久模型选择集 | 计划.md:1176 | 已完成 | `src/storage/schema.sql:222-234 account_models`；`tests/discovery_selection.rs:900` | 表名与 §20.1 写的不符（见数据表一节） | — |
| §16.2 对话框显示"已选 N / 共 M" | 计划.md:1181 | 已完成 | `ModelSelectionDialog.tsx:210,266` | — | — |
| §16.2 "已在用"标记 | 计划.md:1183 | 已完成 | `ModelSelectionDialog.tsx:319` | — | — |
| §16.2 "你排除过"标记 | 计划.md:1185 | 未实现 | `ModelSelectionDialog.tsx:317,319` 只有"新增"与"已在用"两种徽标 | 后端 `CatalogEntry`（`src/discovery/mod.rs:43-46`）只有 `selected`/`is_new`/`missing`，**无法区分"曾被明确取消勾选"与"从没出现过"**；文档承诺的这个标记没有数据支撑 | 缺失 |
| §16.2 "新出现"标记 | 计划.md:1186 | 已完成 | `ModelSelectionDialog.tsx:317`；`src/discovery/mod.rs:135 is_new` | 用徽标而非文档画的 ◀ 箭头，信息等价 | — |
| §16.2 已在选择集内默认勾选 | 计划.md:1189 | 已完成 | `src/discovery/mod.rs:118-120`；`tests/discovery_selection.rs:900` | — | — |
| §16.2 曾被取消勾选的保持不勾 | 计划.md:1190 | 已完成 | `src/discovery/mod.rs:117-120`；`schema.sql:229 selected` | — | — |
| §16.2 新出现的默认不勾 | 计划.md:1191 | 已完成 | `src/discovery/mod.rs:121`；`tests/discovery_selection.rs:900` | — | — |
| §16.2 "只看新增"过滤器 | 计划.md:1192 | 已完成 | `ModelSelectionDialog.tsx:78,249-251` | — | — |
| §16.2 自动同步打开时全量托管、忽略选择集 | 计划.md:1196 | 已完成 | `src/discovery/mod.rs:173-213 sync_managed`；`tests/discovery_selection.rs:567` | — | — |
| §16.2 后台显示"已托管，共 N 个模型" | 计划.md:1197 | 部分完成 | `ModelSelectionDialog.tsx:157-169,222-225`；`Accounts.tsx:1048-1057` | 只在对话框与开关提示里出现，**账号列表没有常驻的"已托管，共 N 个模型"指示**；也没有账号级已托管模型计数接口 | 不一致 |
| §16.2 按设置页间隔默认 30 分钟带抖动执行 | 计划.md:1198 | 已完成 | `src/main.rs:31`；`src/admin/resources.rs:195`；`src/app/tasks.rs:214-223` | — | — |
| §16.2 关闭自动同步后回到选择集且未被污染 | 计划.md:1200-1202 | 已完成 | `src/admin/resources.rs:846-847`→`src/discovery/mod.rs:218-230 unhost`；`tests/discovery_selection.rs:567` | — | — |
| §16.2 多账号：同名逻辑模型默认勾选、独有新模型默认不勾 | 计划.md:1204 | 部分完成 | `src/discovery/mod.rs:95-108,121` | **只对该账号首次拉取生效**：`group_models` 仅在 `previous.is_empty()` 时计算；某账号只要目录非空，后续新出现的同名模型就不会默认勾选 | 不一致 |
| §16.3 勾选后自动创建逻辑模型或加为候选目标 | 计划.md:1210-1211 | 已完成 | `src/discovery/mod.rs:421-440`；`tests/discovery_selection.rs:137` | — | — |
| §16.3 目标优先级继承账号默认值 | 计划.md:1212 | 已完成 | `src/discovery/mod.rs:433 priority_override=None`；`src/config/mod.rs:169` | — | — |
| §16.3 取消勾选时移除目标，在途请求完成 | 计划.md:1216 | 部分完成 | `src/discovery/mod.rs:416-420 delete_target` | 代码层没有"在途请求保护"，只靠"持有旧 Arc 配置"间接成立；无显式机制也无测试 | 不一致 |
| §16.3 24 小时内有流量时警告并二次确认，显示调用次数 | 计划.md:1217 | 已完成 | `src/discovery/mod.rs:17-50,285-308`；`src/admin/resources.rs:1655-1665`；`ModelSelectionDialog.tsx:422-446`；`tests/discovery_selection.rs:407` | — | — |
| §16.3 自动创建的逻辑模型零目标被清理 | 计划.md:1218 | 已完成 | `src/discovery/mod.rs:453-469 cleanup_auto_model`；`tests/discovery_selection.rs:524` | — | — |
| §16.3 手工创建的保留记录但从 /v1/models 移除 | 计划.md:1219 | 已完成 | `src/gateway/models.rs:84-91 is_listable`；`src/discovery/mod.rs:455-457`；`tests/discovery_selection.rs:935` | — | — |
| §16.4 账号级"上游真名 → 对外名"映射 | 计划.md:1225 | 已完成 | `src/storage/schema.sql:237-242 account_aliases`；`src/storage/store.rs:720,726,741` | 表名与 §20.1 写的不符 | — |
| §16.4 别名在拉取后勾选前应用，对话框与选择集显示对外名 | 计划.md:1232 | 已完成 | `src/discovery/mod.rs:80-87,116`；`ModelSelectionDialog.tsx:287-313` | — | — |
| §16.5 消失且未选的模型可从可选列表移除 | 计划.md:1236 | 已完成 | `src/discovery/mod.rs:141-143` | — | — |
| §16.5 已选消失保留记录、标记 missing、停新请求 | 计划.md:1237 | 部分完成 | `schema.sql:231 missing`；`src/discovery/mod.rs:144-157`、`:233-265 sync_missing_targets`；`tests/discovery_selection.rs:318` | **代码里没有 `missing_model` 这个名字**：列名是 `missing`，UI 用"上游已消失"，TargetStatus 里没有这个状态 | 不一致 |
| §16.5 模型重新出现自动解除 | 计划.md:1238 | 已完成 | `src/discovery/mod.rs:126`；`tests/discovery_selection.rs:358` | — | — |
| §16.5 接口不可用时允许手动输入上游模型名 | 计划.md:1239 | 已完成 | `src/discovery/mod.rs:327-386 add_manual_model`；`ModelSelectionDialog.tsx:330-345` | — | — |
| §16.5 手动模型不被一次失败拉取覆盖 | 计划.md:1239 | 已完成 | `src/discovery/mod.rs:160-163`；`tests/discovery_selection.rs:381` | — | — |
| §16.5 真实请求逐步记录其能力 | 计划.md:1239 | 部分完成 | `src/gateway/passthrough.rs:1787-1815 learn_capability_limitation`；`src/capability/mod.rs:128-164` | 只记录"不支持"限制，不记录"支持"证据；与 §16.7 的语义一致但与 §16.5 字面"记录其能力"有落差 | 不一致 |
| §16.6 以 LiteLLM 公开目录为初始来源 | 计划.md:1243 | 已完成 | `assets/capabilities.json` 的 `"source":"litellm/model_prices_and_context_window.json"`；`NOTICES.md:43-44` | — | — |
| §16.6 发布时转成精简版本化快照随二进制发布 | 计划.md:1244 | 部分完成 | `assets/capabilities.json`（revision 2026-09-04）+ `src/capability/mod.rs:20 include_str!`、`:65 revision()` | **仓库里没有生成/裁剪脚本**（`scripts/` 只有 `release.sh`），转换规则不可复现；目录 revision 停在 2026-09-04 | 缺失 |
| §16.6 只取能力不取价格 | 计划.md:1245 | 已完成 | `src/capability/mod.rs:26-46` 无价格字段；`:237 the_catalog_parses_and_carries_no_prices` | — | — |
| §16.6 运行中不读远程仓库、不静默热替换 | 计划.md:1246 | 已完成 | `src/capability/mod.rs:20 include_str!`、`:49-55 OnceLock` | — | — |
| §16.6 不要求用户确认模型卡 | 计划.md:1247 | 已完成 | 无任何模型卡确认流程 | — | — |
| §16.6 证据优先级：真实请求 > 上游接口 > 内置适配规则 > 开源目录 | 计划.md:1249-1251 | 部分完成 | 真实请求与上游声明：`src/gateway/passthrough.rs:1787`、`src/routing/mod.rs:193-206` | **`capability::builtin()` 在生产路径只被后台显示用**（`src/admin/resources.rs:208`），未参与任何路由/适配判断；优先级第 3、4 级实际不可达 | 缺失 |
| §16.7 三状态 supported/unsupported/unknown | 计划.md:1255-1259 | 部分完成 | `src/capability/mod.rs:118-184 Capabilities`（只有 limitations 映射） | 没有显式状态枚举；supported/unknown 由"无记录"隐式表达 | 不一致 |
| §16.7 只有明确"不支持"才缓存为限制 | 计划.md:1263 | 已完成 | `src/capability/mod.rs:195-207 unsupported_from_error`；`src/upstream/evidence.rs:31-36`；`tests/discovery_selection.rs:743` | — | — |
| §16.7 普通 400/5xx/超时/网络错误不证明不支持 | 计划.md:1264 | 已完成 | `src/capability/mod.rs:195-207,286-291` | — | — |
| §16.7 限制默认 24 小时过期 | 计划.md:1265 | 已完成 | `src/upstream/evidence.rs:17-18 TTL`；`src/capability/mod.rs:140,161-163` | — | — |
| §16.7 模型列表/账号协议/适配器版本变化时限制立即失效 | 计划.md:1266 | 部分完成 | `src/app/mod.rs:280-297 Runtime::retain`；`src/admin/resources.rs:2527-2528` | 清空发生在**任何**配置重载，没有区分这三类原因，也没有适配器版本号参与判定 | 不一致 |
| §16.7 首次选择不通过删除能力强行成功 | 计划.md:1267 | 已完成 | `src/capability/mod.rs:226-230 DEGRADABLE`；`src/routing/mod.rs:193-206`；`tests/protocol_e2e.rs:436` | — | — |

### 4.11 限流、请求体、错误格式（`计划.md` §17–§18）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| §17.1 RPM/TPM/最大并发三项，账号默认 + 目标覆盖，默认不限 | 计划.md:1275-1279 | 已完成 | `src/domain/mod.rs:251 limits`；`src/admin/resources.rs:2714-2727 validate_limits`；`src/health/mod.rs:41-53 AdmissionLimits` | — | — |
| §17.1 账号级并发与额度由同 Key 下所有模型共享 | 计划.md:1279 | 已完成 | `src/health/mod.rs:55-92 Capacity`（account + target 双 Semaphore）、`:614-633`；`:1049` | — | — |
| §17.2 已知 Tokenizer 时按输入+最大输出预留并归还差额 | 计划.md:1283 | 部分完成 | `src/health/mod.rs:406-412 settle`；`tests/stream_settlement.rs:101` | **没有 tokenizer**，所以"已知 Tokenizer 时"这一分支不存在；实现走的是下一条的保守估算 | 待确认 |
| §17.2 不知道 Tokenizer 时保守估算并在后台显示"估算限流" | 计划.md:1284 | 已完成 | `src/gateway/passthrough.rs:1890-1896 estimate_tokens`；`web/src/pages/Accounts.tsx:1010` | — | — |
| §17.2 TPM 未配置时完全跳过估算 | 计划.md:1285 | 已完成 | `src/health/mod.rs:365-368` | — | — |
| §17.3 8 MB 以内保存在内存 | 计划.md:1289 | 已完成 | `src/gateway/passthrough.rs:2122 IN_MEMORY_BODY_LIMIT`、`:2165` | — | — |
| §17.3 8–64 MB 写入数据目录专用临时目录 | 计划.md:1290 | 已完成 | `src/gateway/passthrough.rs:2166-2170 tempfile_in(temp_dir)`；`src/app/mod.rs:366 TEMP_DIR_NAME` | — | — |
| §17.3 请求结束/失败/断开时删除临时文件 | 计划.md:1291 | 已完成 | `NamedTempFile` 随作用域自动删除（`passthrough.rs:2157,2132` 注释） | 取消路径靠 Drop，无独立用例 | — |
| §17.3 启动时清理遗留超时临时文件 | 计划.md:1292 | 已完成 | `src/app/mod.rs:370 sweep_stale_temp_files`；`:472 stale_temp_files_are_swept_but_fresh_ones_survive` | — | — |
| §17.3 超 64 MB 返回 413，上限可调 | 计划.md:1293 | 已完成 | `passthrough.rs:2126 MAX_UPSTREAM_BODY_BYTES`；`src/admin/resources.rs:190`；`tests/release_harden.rs:282` | — | — |
| §17.3 请求体字节数在缓冲时记录，用于粘性预算 | 计划.md:1295 | 已完成 | `passthrough.rs:275`；`src/gateway/passthrough.rs:327 wait_budget(request_bytes,...)` | — | — |
| §18.1 15 个稳定错误码 | 计划.md:1303-1317 | 已完成 | `src/gateway/error.rs:14-30 ErrorCode`、`:34-51 as_str` | 15 个全部定义齐全 | — |
| §18.2 OpenAI Chat/Responses 返回 OpenAI error 对象 | 计划.md:1321 | 已完成 | `src/gateway/error.rs:165-173 body()` | — | — |
| §18.2 Anthropic 返回 `type:error` 对象 | 计划.md:1322 | 已完成 | `error.rs:155-164`；单测 `:286` | — | — |
| §18.2 每个错误带稳定网关错误码和请求 ID | 计划.md:1323 | **部分完成** | 非流式：`error.rs:162-163,169,172` + `src/gateway/passthrough.rs:1050-1057 with_request_id` | **流内错误不带**：`src/protocol/mod.rs:304-318 stream_error` 只有 `message`；`GatewayError::sse_event()`（`src/gateway/error.rs:178`）**是死代码**（grep 只命中自身与单测），没有调用点 | 缺失 |
| §18.2 上游原始错误只提取安全信息 | 计划.md:1324 | 已完成 | `src/gateway/passthrough.rs:1777-1791 upstream_error_message`；`src/security/redact.rs:48` | — | — |
| §18.2 SSE 已开始后用该协议错误事件随后关闭 | 计划.md:1325 | 部分完成 | `src/gateway/translate.rs:176-191,216-235,271-277`；事件形状 `openai_chat.rs:1075`、`anthropic.rs:834`、`openai_responses.rs:1190` | 事件体只有 message，**没有稳定错误码与请求 ID**；错误码只写进结算记录（`src/gateway/settle.rs:168-170`） | 缺失 |
| §18.3 queue_full → 429 | 计划.md:1335 | 已完成 | `src/gateway/error.rs:57-59` | — | — |
| §18.3 queue_timeout → 429 | 计划.md:1336 | 已完成 | `error.rs:57-59` | — | — |
| §18.3 rate_limited → 429 | 计划.md:1337 | 已完成 | `error.rs:57-59` | — | — |
| §18.3 no_eligible_target → 503 | 计划.md:1338 | 已完成 | `error.rs:60` | — | — |
| §18.3 upstream_exhausted → 503 | 计划.md:1339 | 已完成 | `error.rs:60` | — | — |
| §18.3 upstream_timeout → 504 | 计划.md:1340 | 已完成 | `error.rs:61` | — | — |
| §18.3 upstream_protocol_error → 502 | 计划.md:1341 | 已完成 | `error.rs:62` | — | — |
| §18.3 internal_error → 500 | 计划.md:1342 | 已完成 | `error.rs:63` | — | — |
| §18.3 multiplier_unknown → 403 | 计划.md:1348 | 已完成 | `error.rs:64` | — | — |
| §18.3 multiplier_exceeded → 403 | 计划.md:1349 | 已完成 | `error.rs:64` | — | — |
| §18.3 auth_invalid → 401 | 计划.md:1350 | 已完成 | `error.rs:65`；`tests/gateway_e2e.rs:497` | — | — |
| §18.3 model_not_found → 404 | 计划.md:1351 | 已完成 | `error.rs:66`；`tests/gateway_e2e.rs:538` | — | — |
| §18.3 unsupported_parameter → 400 | 计划.md:1352 | 已完成 | `error.rs:67`；`tests/protocol_e2e.rs:690` | — | — |
| §18.3 request_too_large → 413 | 计划.md:1353 | 已完成 | `error.rs:68`；`tests/release_harden.rs:282` | — | — |
| §18.3 response_state_expired → 404 | 计划.md:1354 | 已完成 | `error.rs:66`；`tests/responses_chain.rs:266` | — | — |
| §18.3 可重试错误附 Retry-After | 计划.md:1331 | 部分完成 | `error.rs:73-85 is_retryable`、`:200-217 default_retry_after`、`:202-206` | 上游给定的 Retry-After 只在透传路径生效（`passthrough.rs:1769-1771`）；`GatewayError` 构造点普遍不带 `retry_after`，一律走默认 2s/5s；流内错误事件无法带头 | 不一致 |

### 4.12 技术架构、数据表、配置切换（`计划.md` §19–§21）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| §19.1 单进程承载 /v1 + /admin/api + 后台静态文件 | 计划.md:1364 | 已完成 | `src/server.rs:16-27`；`src/admin/ui.rs:12-56` | — | — |
| §19.1 SQLite 保存配置/加密状态/粘性/性能快照/历史元数据 | 计划.md:1365 | 已完成 | `src/storage/schema.sql` 19 张表 | — | — |
| §19.1 内存保存配置快照/调度状态/粘性/并发计数/短期指标/队列 | 计划.md:1366 | 已完成 | `src/app/mod.rs:183-209 Runtime` | — | — |
| §19.1 后台任务 6 类 | 计划.md:1367 | 部分完成 | `src/app/tasks.rs:55-63` 实际 spawn **4 类**（snapshots / multiplier_refresh / model_sync / cleanup） | 与 §19.1 说的 6 类、§22 列的 7 类都对不上（见 §22 一节） | 不一致 |
| §19.2 axum/tower/tower-http | 计划.md:1375 | 已完成 | `Cargo.toml:19,31,32` | — | — |
| §19.2 tokio | 计划.md:1376 | 已完成 | `Cargo.toml:30` | — | — |
| §19.2 reqwest/hyper + rustls | 计划.md:1377 | 已完成 | `Cargo.toml:25` | — | — |
| §19.2 serde/serde_json + RawValue | 计划.md:1378 | 部分完成 | `Cargo.toml:26-27`（`raw_value` feature 已开） | **代码内没有使用 `RawValue`**（grep 零命中）；同协议透传走原始字节而非 RawValue 局部改写 | 不一致 |
| §19.2 sqlx SQLite + WAL | 计划.md:1379 | 已完成 | `Cargo.toml:28`；`src/storage/mod.rs:36-47` | — | — |
| §19.2 arc-swap | 计划.md:1380 | 已完成 | `Cargo.toml:12`；`src/app/mod.rs:159-181` | — | — |
| §19.2 tracing / tracing-subscriber | 计划.md:1381 | 已完成 | `Cargo.toml:33-34`；`src/main.rs:63-68` | — | — |
| §19.2 secrecy / zeroize | 计划.md:1382 | 已完成 | `Cargo.toml:24,35`；`src/security/mod.rs:19,25,33,175-188` | — | — |
| §19.2 chacha20poly1305（XChaCha20-Poly1305） | 计划.md:1383 | 已完成 | `Cargo.toml:20`；`src/security/mod.rs:15-16,147-172` | — | — |
| §19.2 argon2 | 计划.md:1384 | 已完成 | `Cargo.toml:11`；`src/security/mod.rs:194-201` | — | — |
| §19.2 hmac/sha2 + 常量时间比较 | 计划.md:1385 | 部分完成 | `Cargo.toml:22,27`；`src/security/mod.rs:218-227 KeyDigest`；`src/routing/sticky.rs:183-190` | **分组 Key 鉴权是 HashMap 摘要查表**（`src/config/mod.rs:63-69`、`src/auth/mod.rs:77`），**不做常量时间比较**；没有 `subtle`/`ct_eq` 依赖 | 缺失 |
| §19.2 tokio Semaphore | 计划.md:1386 | 已完成 | `Cargo.toml:30`；`src/health/mod.rs:14,57-86`；`src/routing/queue.rs:13` | — | — |
| §19.2 tempfile | 计划.md:1387 | 已完成 | `Cargo.toml:29`；`src/gateway/passthrough.rs:2157-2170` | — | — |
| §19.2 ulid 或 uuid | 计划.md:1388 | 已完成 | `Cargo.toml:34 ulid`；`src/storage/store.rs:15` | — | — |
| §19.2 定点小数：rust_decimal 或自建 | 计划.md:1389 | 已完成 | 自建 `src/domain/fixed.rs:13 SCALE=10^6`、`:94 mul_ceil` | — | — |
| §19.3 模块 app/ | 计划.md:1400 | 已完成 | `src/app/mod.rs`、`src/app/tasks.rs`、`src/app/recorder.rs` | — | — |
| §19.3 模块 admin/ | 计划.md:1401 | 已完成 | `src/admin/mod.rs`、`resources.rs`、`ui.rs` | — | — |
| §19.3 模块 auth/ | 计划.md:1402 | 已完成 | `src/auth/mod.rs`、`session.rs` | — | — |
| §19.3 模块 config/ | 计划.md:1403 | 已完成 | `src/config/mod.rs` | — | — |
| §19.3 模块 domain/ | 计划.md:1404 | 已完成 | `src/domain/mod.rs`、`fixed.rs` | — | — |
| §19.3 模块 gateway/ | 计划.md:1405 | 已完成 | `src/gateway/*` | — | — |
| §19.3 模块 protocol/{openai_chat,openai_responses,anthropic,canonical,degrade} | 计划.md:1407-1411 | 已完成 | `src/protocol/*.rs` 五个文件都存在 | — | — |
| §19.3 模块 routing/ | 计划.md:1412 | 已完成 | `src/routing/mod.rs`、`score.rs`、`sticky.rs`、`queue.rs` | — | — |
| §19.3 模块 upstream/ | 计划.md:1413 | 已完成 | `src/upstream/mod.rs`、`endpoints.rs`、`evidence.rs` | — | — |
| §19.3 模块 health/ | 计划.md:1414 | 已完成 | `src/health/mod.rs` | — | — |
| §19.3 模块 catalog/ | 计划.md:1415 | 文档过时 | 实际拆成 `src/discovery/mod.rs` + `src/capability/mod.rs` | 规划写的目录名不存在 | 不一致 |
| §19.3 模块 multiplier/ | 计划.md:1416 | 已完成 | `src/multiplier/mod.rs`、`refresh.rs`、`probe.rs` | — | — |
| §19.3 模块 cost/ | 计划.md:1417 | 未实现 | 没有 `src/cost/`；成本聚合散在 `src/admin/resources.rs:2049-2204 cost()` 与 `src/storage/store.rs:1221` | 规划写的目录不存在，成本逻辑没有独立模块 | 不一致 |
| §19.3 模块 state/ | 计划.md:1418 | 文档过时 | Responses 状态在 `src/gateway/responses.rs`；托管后台在 `src/gateway/background.rs` | 规划写的目录不存在 | 不一致 |
| §19.3 模块 storage/ | 计划.md:1419 | 已完成 | `src/storage/mod.rs`、`store.rs`、`schema.sql` | — | — |
| §19.3 模块 observability/ | 计划.md:1420 | 文档过时 | 请求记录 `src/app/recorder.rs`；指标 `src/health/mod.rs`；日志 `tracing` | 规划写的目录不存在 | 不一致 |
| §19.3 模块 security/ | 计划.md:1421 | 已完成 | `src/security/mod.rs`、`backup.rs`、`redact.rs`、`url_guard.rs` | — | — |
| §19.3 `tests/fixtures/` 目录 | 计划.md:1423 | 文档过时 | `tests/` 下没有 `fixtures/`；黄金样例内联在 `tests/protocol_e2e.rs` 等文件里 | 规划写的目录不存在 | 不一致 |
| §19.4 每 Origin 复用长期 Client | 计划.md:1430 | 已完成 | `src/upstream/mod.rs:53-79 UpstreamClient`、`:85 pool_idle_timeout` | — | — |
| §19.4 配置只做一次原子指针读 | 计划.md:1431 | 已完成 | `src/gateway/mod.rs:117`；`src/app/mod.rs:171-173 load_full` | — | — |
| §19.4 原子值/分片锁/Semaphore，避免全局大锁 | 计划.md:1432 | 已完成 | `src/app/mod.rs:206 AtomicU64`；`src/health/mod.rs:14,289,350`；`src/sync.rs` | — | — |
| §19.4 同协议浅解析尽量不重建整个 JSON | 计划.md:1433 | 部分完成 | 原始字节透传 `src/gateway/passthrough.rs:2138-2170` | 依赖 `serde_json::Value` 全量解析，**没有 RawValue 局部改写路径** | 不一致 |
| §19.4 跨协议才构建完整中间对象 | 计划.md:1434 | 已完成 | `src/protocol/canonical.rs` + `src/gateway/translate.rs` | — | — |
| §19.4 请求记录经有界通道交批量写任务 | 计划.md:1435 | 已完成 | `src/app/recorder.rs:16 CHANNEL_CAPACITY=4096`、`:18 BATCH_SIZE`、`:152 a_full_channel_drops_records...` | — | — |
| §19.4 粘性与快照持久化由后台批量做，热路径只写内存 | 计划.md:1436 | 已完成 | 热路径 `src/gateway/passthrough.rs:618-626`、`src/gateway/settle.rs:150`；落盘 `src/app/tasks.rs:81-92` | — | — |
| §19.4 SQLite WAL + busy_timeout，配置写与日志写分离 | 计划.md:1437 | 部分完成 | WAL/busy_timeout `src/storage/mod.rs:36-47`；日志走独立 recorder 通道 | **连接池共用一个 SqlitePool**（8 连接），没有独立的配置写池/日志写池 | 不一致 |
| §19.4 所有队列/缓存/状态表/通道有容量上限 | 计划.md:1438 | 部分完成 | 记录通道 `recorder.rs:16`；分组队列 `queue.rs:53`；响应帧 `src/gateway/translate.rs:26`；探针体 `src/multiplier/probe.rs:19` | **sticky/perf/health 内存表没有容量上限**（只有 `retain` 按 live id 清理：`src/routing/sticky.rs:302`、`src/routing/score.rs:205`、`src/health/mod.rs:191`）；托管后台任务表也无上限 | 缺失 |
| §19.4 下游读取慢时背压，不无限缓存流式数据 | 计划.md:1439 | 部分完成 | 流式直传 `src/gateway/stream.rs`；`src/upstream/mod.rs:85-86` 客户端侧超时 | **没有显式背压实现**（无水位/有界缓冲等待）；上游读取与下游消费无耦合控制，只靠 TCP 反压 | 缺失 |
| §19.4 每请求任务 panic 隔离，不导致进程退出 | 计划.md:1440 | 部分完成 | 锁毒化恢复 `src/sync.rs:48,75` | **全仓无 `catch_unwind`**；请求任务由 `tokio::spawn` 承载，panic 确实不会杀进程但没有中间件兜底，也没有"请求任务 panic 后进程仍存活"的用例 | 缺失 |
| §20.1 表 admin_users | 计划.md:1448 | 已完成 | `src/storage/schema.sql:4-10` | — | — |
| §20.1 表 app_settings（含当前配置版本） | 计划.md:1449 | 部分完成 | `src/storage/schema.sql:12-16` | **没有"当前配置版本"字段**：只存 `schema_version` 与 `settings`；配置版本在内存 `config.current().version` | 不一致 |
| §20.1 表 groups | 计划.md:1450 | 已完成 | `src/storage/schema.sql:18-35` | — | — |
| §20.1 表 upstream_accounts（含 NewAPI 凭据） | 计划.md:1451 | 文档过时 | `src/storage/schema.sql:37-62` | NewAPI 凭据**不在本表**：令牌在 `upstream_secrets.new_api_token`（`:67-73`）与 `new_api_sites`（`:275-280`） | 不一致 |
| §20.1 表 upstream_secrets（含自定义敏感 Header） | 计划.md:1452 | 部分完成 | `src/storage/schema.sql:67-73` | **没有"自定义敏感 Header"列** | 缺失 |
| §20.1 表 upstream_models | 计划.md:1453 | 文档过时 | 实际是 `account_models`（`schema.sql:222-234`） | 表名不符，且没有"来源"字段 | 不一致 |
| §20.1 表 model_selection | 计划.md:1454 | 文档过时 | 功能并入 `account_models.selected`（`schema.sql:229`）；自动同步开关在 `upstream_accounts.auto_sync`（`:57`） | **该表不存在** | 不一致 |
| §20.1 表 model_aliases | 计划.md:1455 | 文档过时 | 实际是 `account_aliases`（`schema.sql:237-242`） | 表名不符 | 不一致 |
| §20.1 表 logical_models | 计划.md:1456 | 已完成 | `src/storage/schema.sql:75-83`（`origin` 区分 auto/manual） | — | — |
| §20.1 表 dispatch_targets | 计划.md:1457 | 已完成 | `src/storage/schema.sql:85-97` | — | — |
| §20.1 表 endpoint_evidence | 计划.md:1458 | 文档过时 | **没有该表**：实现是进程内 `src/upstream/evidence.rs:20-66`（RwLock HashMap，24h TTL，`:7-9` 明确说明不持久化） | 规划要求落表，代码有意不落库，重启后证据丢失 | 不一致 |
| §20.1 表 capability_evidence | 计划.md:1459 | 文档过时 | **没有该表**：实现是进程内 `src/capability/mod.rs:108-120`（`:114-116` 明确说明不持久化） | 同上 | 不一致 |
| §20.1 表 multiplier_snapshots | 计划.md:1460 | 已完成 | `src/storage/schema.sql:102-113`；`src/storage/store.rs:1688,1706` | — | — |
| §20.1 表 runtime_target_state | 计划.md:1461 | **未实现** | **没有该表**；冷却/额度/半开状态全在内存 `src/health/mod.rs Registry` | 崩溃或重启会丢失冷却与额度状态，规划要求的"恢复状态"没有持久化 | 缺失 |
| §20.1 表 sticky_bindings（前缀哈希 → 目标 + last_used_at） | 计划.md:1462 | 已完成 | `src/storage/schema.sql:116-125` | — | — |
| §20.1 表 target_perf_snapshot | 计划.md:1463 | 已完成 | `src/storage/schema.sql:128-139`；`src/storage/store.rs:1788,1818` | — | — |
| §20.1 表 response_locators | 计划.md:1464 | 文档过时 | **没有独立定位表**：定位字段并入 `response_states`（`schema.sql:201-216`，`upstream_id`/`endpoint`/`group_id`） | 表结构合并了，文档没同步 | 不一致 |
| §20.1 表 response_state | 计划.md:1465 | 文档过时 | 实际表名是 `response_states`（`schema.sql:201`） | 表名不符 | 不一致 |
| §20.1 表 background_jobs | 计划.md:1466 | 文档过时 | 实际表名是 `background_tasks`（`schema.sql:284-301`） | 表名不符 | 不一致 |
| §20.1 表 request_records（含成本基准倍率） | 计划.md:1467 | 部分完成 | `schema.sql:142-174` | 有 `effective/cheapest/dearest_multiplier`，但没有"成本基准倍率"这个独立命名的列 | 不一致 |
| §20.1 表 performance_buckets | 计划.md:1468 | **未实现** | **没有该表**；趋势图按 `started_at / bucket_secs` 现算（`src/storage/store.rs:1036-1081`、`src/admin/resources.rs:91-99`） | 分钟级聚合表与聚合写入任务都不存在 | 缺失 |
| §20.1 表 calibration_records | 计划.md:1469 | 已完成 | `src/storage/schema.sql:246-260`；`src/storage/store.rs:1631,1653` | — | — |
| §20.1 表 admin_audit_log | 计划.md:1470 | 已完成 | `src/storage/schema.sql:262-271`；`src/storage/store.rs:1867,1117` | — | — |
| §20.1 额外表（规划未列） | 计划.md:1444-1470 | 文档过时 | `request_attempts`（`schema.sql:181`）、`new_api_sites`（`:275`） | 实际 19 张表，规划 23 行里 5 张不存在、2 张未建、7 张名称或职责不符 | 不一致 |
| §20.1 sticky 只在首次绑定和换号写入，前缀级粒度 | 计划.md:1474 | 已完成 | `src/gateway/passthrough.rs:618-626`（仅 success 分支 bind）；`src/routing/sticky.rs:66-118` | — | — |
| §20.1 target_perf_snapshot 后台每 60 秒批量写 | 计划.md:1475 | 已完成 | `src/app/tasks.rs:19 SNAPSHOT_INTERVAL=60`、`:68-92`；`src/storage/store.rs:1788` | — | — |
| §20.1 两者都不在热路径 | 计划.md:1476 | 已完成 | 热路径只写内存 `src/gateway/passthrough.rs:618-626`；落盘只在 `src/app/tasks.rs:81-92` 与 `src/server.rs:141` | — | — |
| §20.1 重启后粘性恢复、快照超 24 小时丢弃 | 计划.md:1478 | 已完成 | `src/app/mod.rs:409-431`；`src/app/tasks.rs:256-262 prune_perf_snapshots`；`tests/routing_e2e.rs:942,1005-1018` | — | — |
| §20.2 上游 Key 与令牌用 XChaCha20-Poly1305 且每条独立 nonce | 计划.md:1484 | 已完成 | `src/security/mod.rs:147-172 Cipher::seal`；`:266-269` 测试 | — | — |
| §20.2 分组 Key 只存带 pepper 的 HMAC 摘要与前缀 | 计划.md:1485 | 已完成 | `src/security/mod.rs:204-236 KeyDigest`；`src/storage/schema.sql:21-22` | — | — |
| §20.2 账号复制时内存解密并用新 nonce 重加密 | 计划.md:1486 | 已完成 | `src/admin/resources.rs:1844-1862`；`tests/admin_cost.rs:422` | — | — |
| §20.2 所有日志统一经过脱敏器 | 计划.md:1487 | 部分完成 | `src/security/redact.rs:16-52`；调用点 `src/multiplier/refresh.rs:334`、`src/admin/resources.rs:881,936`、`src/gateway/passthrough.rs:1786` | **只有 4 处显式调用，没有全局 tracing layer 兜底**；新增日志点可能绕过脱敏 | 缺失 |
| §20.3 数据库与资格判断用放大整数 | 计划.md:1491 | 已完成 | `src/domain/fixed.rs:13 SCALE=10^6`；`src/routing/mod.rs:177` | — | — |
| §20.3 校准系数同样用定点整数 | 计划.md:1491 | 已完成 | `src/storage/schema.sql:46 calibration INTEGER`；`src/domain/mod.rs:268` | — | — |
| §20.3 有效倍率乘法在定点域完成并向上取整 | 计划.md:1491 | 已完成 | `src/domain/fixed.rs:94-99 mul_ceil`；单测 `:179` | — | — |
| §20.3 硬性倍率门只用定点值 | 计划.md:1493 | 已完成 | `src/routing/mod.rs:167-179`；f64 只用于展示与层内评分（`fixed.rs:102` 注释） | — | — |
| §21 写事务 → 应用修改 → 读候选 → 校验 → 构建 RuntimeConfig → 版本号 → 提交 → ArcSwap | 计划.md:1497-1505 | 部分完成 | 事务：`src/storage/store.rs:257,311,634,735,966,1208,1624,1749,1811`；快照与版本 `src/config/mod.rs:123-133`；触发点 `src/admin/resources.rs:2796-2801` | **校验不统一也不在事务内**：`reload()` 在 handler 落库提交之后才调用，`src/config/mod.rs:137-214 build()` 只做跨组/死引用过滤；权重总和、Key 唯一性等校验分散在各 handler（`resources.rs:2694-2702`），没有"事务内读候选配置并统一校验"这一步 | 不一致 |
| §21 在途请求持旧 Arc，新请求只拿新版本 | 计划.md:1507 | 已完成 | `src/gateway/mod.rs:116-126`；`src/config/mod.rs:1-4,114-116` | — | — |
| §21 倍率/熔断/并发/额度不等待配置版本，发请求前读最新状态 | 计划.md:1509 | 已完成 | `src/gateway/passthrough.rs:1011-1030`、`:966-998`；`src/app/mod.rs:183-198` | — | — |

### 4.13 后台任务、安全、日志、部署（`计划.md` §22–§25）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| §22 任务1 自动倍率刷新与宽限期计时 | 计划.md:1515 | 已完成 | `src/app/tasks.rs:55-63,94-158`；`src/multiplier/mod.rs:374-414` | — | — |
| §22 任务2 开启自动同步账号的模型列表刷新 | 计划.md:1516 | 已完成 | `src/app/tasks.rs:160-212`；`src/discovery/mod.rs:173` | — | — |
| §22 任务3 能力限制、冷却和粘性状态过期清理 | 计划.md:1517 | 已完成 | `src/app/tasks.rs:225-279`；能力/端点证据靠 TTL 惰性过期；`src/routing/sticky.rs:302` | 没有独立清理任务，但语义由 TTL + prune 覆盖 | — |
| §22 任务4 Responses 状态、请求记录、临时文件清理 | 计划.md:1518 | 已完成 | `src/app/tasks.rs:263-269,236-245`；`src/app/mod.rs:365-371` | 临时文件只有启动清理，没有周期任务（`NamedTempFile` 自动删除已覆盖常态） | — |
| §22 任务5 请求记录批量写入 | 计划.md:1519 | 已完成 | `src/app/recorder.rs:16-93` | — | — |
| §22 任务5 **性能分钟桶聚合** | 计划.md:1519 | **未实现** | 没有 `performance_buckets` 表，也没有聚合任务；趋势图现算（`src/storage/store.rs:1036-1081`） | 规划明确的"性能分钟桶聚合"任务不存在 | 缺失 |
| §22 任务6 粘性绑定与性能 EWMA 快照落盘（每 60 秒） | 计划.md:1520 | 已完成 | `src/app/tasks.rs:18-19,68-92`；`src/server.rs:141`；`tests/release_harden.rs:14` | — | — |
| §22 任务7 后台 Responses 生命周期管理 | 计划.md:1521 | 已完成 | `src/gateway/background.rs:175-234,511`、`:406-476`、`:110-124` | §22 字面写"后台 Responses"，代码实现的是 §29.1 的网关托管后台任务（超前实现） | — |
| §22 规则1 有界并发与抖动 | 计划.md:1525 | 部分完成 | 倍率轮 `src/multiplier/refresh.rs:33 MAX_CONCURRENT_PROBES=4`；模型同步抖动 `src/app/tasks.rs:214-223` | 模型同步本身串行遍历账号，没有跨账号并发上限（有 30s tick 兜底） | 不一致 |
| §22 规则2 独立超时与响应体上限 | 计划.md:1526 | 已完成 | `src/multiplier/probe.rs:19 MAX_BODY`；`src/upstream/mod.rs:226,242` | — | — |
| §22 规则3 不发模拟对话探测模型能力或账号恢复 | 计划.md:1527 | 已完成 | `src/app/tasks.rs:1-7`；探针只打模型列表与计费接口 | — | — |
| §22 规则4 关闭时停止接新任务并等待安全刷盘 | 计划.md:1528 | 已完成 | `src/app/tasks.rs:4-5,73`（Weak）；`src/server.rs:140-141 flush_snapshots` | 没有显式 join/drain 后台任务，靠 Weak + 强刷兜底 | — |
| §23.1 首次启动生成 32 字节主密钥文件，权限仅当前用户 | 计划.md:1534 | 已完成 | `src/security/mod.rs:21 MASTER_KEY_LEN`、`:68-75`、`:119-136 write_private_file` | — | — |
| §23.1 支持 AKHUB_MASTER_KEY 且优先 | 计划.md:1535 | 已完成 | `src/security/mod.rs:41-48,95-110` | — | — |
| §23.1 环境变量时不写库不写日志 | 计划.md:1536 | 已完成 | `src/security/mod.rs:41-48` 直接 return | — | — |
| §23.1 主密钥丢失后无法恢复 | 计划.md:1537 | 已完成 | `src/security/mod.rs:175-188`；`deploy/README.md:45-47` | — | — |
| §23.2 管理员密码用 Argon2id | 计划.md:1541 | 已完成 | `src/auth/session.rs:22-38 Argon2::default()`（argon2 0.6 默认即 Argon2id） | — | — |
| §23.2 Cookie HttpOnly + SameSite | 计划.md:1542 | 已完成 | `src/admin/mod.rs:122-124`；`tests/admin_api.rs` | — | — |
| §23.2 **HTTPS 下 Cookie 使用 Secure** | 计划.md:1542 | 未实现 | `src/admin/mod.rs:119-124` 有明确注释说明**永不设置 Secure**，为了支持 127.0.0.1 直连 | 文档承诺的"HTTPS 下 Secure"没有实现，也没有配置项可打开 | 缺失 |
| §23.2 所有写操作需要 CSRF 防护 | 计划.md:1543 | 已完成 | `src/admin/mod.rs:96-101`；`web/src/lib/api.ts:29,55`；`tests/admin_api.rs:157` | — | — |
| §23.2 登录失败速率限制和短暂锁定 | 计划.md:1544 | 已完成 | `src/auth/session.rs:16-19`（5 次/60 秒）、`:72-94`；`src/admin/mod.rs:317-322`（429） | — | — |
| §23.2 不提供查看完整上游 Key 的接口 | 计划.md:1545 | 部分完成 | 覆盖写 `src/admin/resources.rs:537-538,826-835`；DTO 只回 `has_new_api_token` `:578-579` | **旁路存在**：`/admin/api/backup/export` 会把上游 Key 明文导出（`src/security/backup.rs:39`、`src/storage/store.rs:1375-1386`） | 缺失 |
| §23.3 Base URL 保存前严格校验只允许 HTTP/HTTPS | 计划.md:1549 | 已完成 | `src/security/url_guard.rs:31-51`；`src/admin/resources.rs:685,768,2767-2770` | — | — |
| §23.3 默认阻止环回/链路本地/云元数据/内网 | 计划.md:1550 | 已完成 | `src/security/url_guard.rs:97-134 is_blocked`；`src/upstream/mod.rs:60-79 http_for`；`tests/admin_api.rs:454` | — | — |
| §23.3 账号级显式开启内网访问 | 计划.md:1550 | 已完成 | `src/domain/mod.rs:253 allow_private_network`；`src/storage/schema.sql:54`；`web/src/pages/Accounts.tsx:1044` | — | — |
| §23.3 DNS 解析后再次校验防 Rebinding | 计划.md:1551 | 已完成 | `src/upstream/mod.rs:82-110 GuardedResolver`；`src/security/url_guard.rs:68-94` | 连接用的就是校验过的地址，二次解析窗口已消除 | — |
| §23.3 带 Key 的请求不跨 Origin 跟随重定向 | 计划.md:1552 | 已完成 | `src/upstream/mod.rs:84 redirect(Policy::none())`（比"不跨 Origin"更严） | 无专门测试 | — |
| §23.3 倍率与模型探针限制响应体/超时/Content-Type | 计划.md:1553 | 部分完成 | 探针三项齐：`src/multiplier/probe.rs:19,186,338,410-431`；模型列表有大小与超时：`src/upstream/mod.rs:226,242` | **模型列表接口没有校验 Content-Type** | 缺失 |
| §23.4 普通 Chat/Messages/无状态 Responses 不落正文 | 计划.md:1557 | 已完成 | `src/storage/schema.sql:142-195` 无正文列；`tests/gateway_e2e.rs:544` | — | — |
| §23.4 只有 Responses 状态链必需内容才加密保存 | 计划.md:1558 | 已完成 | `src/gateway/responses.rs:806-814`；`schema.sql:211 sealed_body`（可空） | 缺 `previous_response_id` 的非 Responses 请求也会建 state 行（`sealed_body=None`），只存定位不算正文 | — |
| §23.4 粘性键只保存哈希 | 计划.md:1559 | 已完成 | `src/routing/sticky.rs:183-190`；`schema.sql:116-123` | — | — |
| §23.4 请求记录页不提供正文调试开关 | 计划.md:1560 | 已完成 | `web/src/pages/Requests.tsx:1,195`；`src/admin/resources.rs:2578-2650` 无正文字段 | — | — |
| §23.4 崩溃和错误日志不含请求体/Key/思考内容 | 计划.md:1561 | 部分完成 | 脱敏器 `src/security/redact.rs:48-52`；上游错误转发前脱敏 `src/gateway/passthrough.rs:1778-1786` | 没有全局强制（无 tracing layer），新增日志点可能绕过 | 缺失 |
| §23.5 导出时管理员输入备份密码 | 计划.md:1565 | 已完成 | `src/admin/resources.rs:2403-2416`；`web/src/pages/SettingsPage.tsx` 备份入口 | — | — |
| §23.5 内存生成规范化数据 → Argon2id 派生 → XChaCha20-Poly1305 | 计划.md:1566 | 已完成 | `src/security/backup.rs:58-83,103-125`；`src/security/mod.rs:194-201 derive_backup_key` | — | — |
| §23.5 备份内容范围（含设置） | 计划.md:1567 | 部分完成 | `src/security/backup.rs:33-45 BackupData`（groups/accounts/secrets/logical_models/dispatch_targets/account_models/account_aliases）；不含正文/历史/性能/粘性/日志 ✓ | **不含 `app_settings`（系统设置）**：恢复后保留期、同步间隔等会回到默认值 | 缺失 |
| §23.5 恢复时解密校验版本与引用，用本机主密钥重加密，事务原子切换 | 计划.md:1568 | 部分完成 | `src/admin/resources.rs:2456-2525`；版本 `backup.rs:25,90-96`；重加密 `src/storage/store.rs:1539-1562`；事务 `:1471-1624` | 单事务覆盖全量替换，但 **`key_digest` 依赖本机主密钥 pepper**：跨机恢复后原分组 Key 全部失效，文档与 UI 都没有提示 | 缺失 |
| §23.5 密码错误/损坏/0 个有效分组不修改现有配置 | 计划.md:1569 | 已完成 | `src/security/backup.rs:86-101,127-149`；`src/admin/resources.rs:2456-2458`；`tests/admin_cost.rs:539` | — | — |
| §24.1 记录请求 ID、配置版本、入口协议、逻辑模型 | 计划.md:1577 | 已完成 | `schema.sql:143,145,173`；`src/gateway/passthrough.rs:1089-1115` | — | — |
| §24.1 记录候选过滤原因、选中的层、最终目标、端点 | 计划.md:1578 | 部分完成 | 目标/上游模型/端点 `passthrough.rs:1096-1103`；attempts_detail `store.rs:1921-1935` | **没有"候选过滤原因"与"选中的层"字段**：过滤细节只在内存 Telemetry/plan 里，不落库 | 缺失 |
| §24.1 记录每次尝试、切换原因、是否计入预算、队列等待 | 计划.md:1579 | 部分完成 | 明细表 `schema.sql:181-195`；写入 `store.rs:940-960`；`queued_ms` `passthrough.rs:1110` | 没有独立的"切换原因"字段，只能由 `outcome`+`error_code` 推断 | 不一致 |
| §24.1 记录粘性命中、粘性等待时长、等待预算、缓存新鲜度系数 | 计划.md:1580 | 部分完成 | `sticky_hit` `passthrough.rs:1111`；预算计算 `src/routing/sticky.rs:356` | **等待时长与新鲜度系数不落库**：只有合并后的 `queued_ms`，粘性等待被并进去 | 缺失 |
| §24.1 记录首字延迟、总延迟、输出速度、Token | 计划.md:1581 | 部分完成 | first_token/tokens `store.rs:1910-1914`、`passthrough.rs:1112-1114`；总延迟 `schema.sql:145` | **没有输出速度（tps）字段**，只存在于内存 EWMA | 缺失 |
| §24.1 记录倍率来源、有效倍率、资格判断结果、额度状态 | 计划.md:1582 | 部分完成 | 有效倍率 `schema.sql:162`、`passthrough.rs:1106`；来源在 `multiplier_snapshots.source` | 记录行内**没有倍率来源、资格判断结果、额度状态**字段 | 缺失 |
| §24.1 记录该模型当时最贵/最便宜合格目标的有效倍率 | 计划.md:1583 | 已完成 | `schema.sql:163-164`；`src/routing/mod.rs:336-337`；`passthrough.rs:1107-1108` | — | — |
| §24.1 记录能力降级标记 | 计划.md:1584 | 已完成 | `schema.sql:159 degraded`；`passthrough.rs:1104-1105`；`tests/protocol_e2e.rs:738` | — | — |
| §24.1 记录标准化错误码、返回 HTTP 状态、安全的上游状态码 | 计划.md:1585 | 已完成 | `schema.sql:154-156`；`passthrough.rs:1100-1102` | — | — |
| §24.1 不记录完整 Header/API Key/正文/工具参数/图片/思考 | 计划.md:1587 | 已完成 | 记录表无任何正文字段；`src/security/redact.rs:4-19` | 没有断言级测试覆盖"思考内容不落库" | — |
| §24.2 请求元数据与性能历史默认 30 天可调 | 计划.md:1591-1592 | 已完成 | `src/main.rs:32`；`src/app/mod.rs:43`；`src/admin/resources.rs:191,225-241` | "性能历史"另有 24 小时丢弃口径（`src/app/tasks.rs:27`），与 30 天不一致 | 不一致 |
| §24.2 设为 0 时不新增历史明细、只保留内存实时汇总 | 计划.md:1593 | 部分完成 | 清理跳过 `src/app/tasks.rs:236-245` | **仍照常写入 `request_records`**（`passthrough.rs:1068`、`settle.rs:171` 无 retention 门），只是不再按天清理 | 缺失 |
| §24.2 成本页在保留 0 时只能显示当日数据 | 计划.md:1593 | 未实现 | 汇总走 `src/storage/store.rs:1221 cost_usage` 读 DB | 没有内存成本汇总，也没有"只显示当日"的限制逻辑 | 缺失 |
| §24.2 清理任务分批删除避免长事务 | 计划.md:1594 | 部分完成 | `src/storage/store.rs:1179-1208 prune_request_records(cutoff, batch=5000)`；`src/app/tasks.rs:29` | `response_states`（`store.rs:574-581`）与 `background_tasks`（`:436-447`）是单条 DELETE，未分批 | 不一致 |
| §24.3 `/health/live` 语义 | 计划.md:1598 | 已完成 | `src/server.rs:20-21,52-54` | — | — |
| §24.3 `/health/ready` 语义 | 计划.md:1599 | 已完成 | `src/server.rs:59-70`（`SELECT 1`） | 未显式再检查主密钥与配置快照（启动期失败即退出，可接受） | — |
| §24.3 健康接口不暴露账号/模型/倍率/版本 | 计划.md:1600 | 已完成 | `src/server.rs:52-70`；`tests/gateway_e2e.rs:598` | — | — |
| §25.1 单镜像含二进制/后台/能力目录 | 计划.md:1606 | 已完成 | `Dockerfile:10-28,41` | — | — |
| §25.1 非 root 运行 | 计划.md:1607 | 已完成 | `Dockerfile:37-43` | — | — |
| §25.1 `/data` 唯一持久卷 | 计划.md:1608 | 已完成 | `Dockerfile:46-50`；`docker-compose.yml:15-22` | — | — |
| §25.1 容器内默认监听 `0.0.0.0:8080` | 计划.md:1609 | 已完成 | `Dockerfile:48-49`；`docker-compose.yml:24` | — | — |
| §25.1 最小 docker compose 示例 + 健康检查 + restart | 计划.md:1610 | 已完成 | `docker-compose.yml:1-38`；`Dockerfile:56-57`；`src/main.rs:41-60 --healthcheck` | — | — |
| §25.2 发布 x86_64 与 aarch64 | 计划.md:1614 | 已完成 | `.github/workflows/release.yml:23-25`；`scripts/release.sh:36-44`；`dist/` 双架构工件 | — | — |
| §25.2 单二进制加数据目录即可运行 | 计划.md:1615 | 已完成 | `src/main.rs:23`；`src/storage/mod.rs:29-54`；`src/admin/ui.rs:12-15` | — | — |
| §25.2 systemd 示例默认 127.0.0.1:8080 且 Restart=always | 计划.md:1616 | 已完成 | `deploy/akhub.service:18,21` | — | — |
| §25.2 不自行管理证书 | 计划.md:1617 | 已完成 | 代码无 TLS/ACME 服务端；`deploy/README.md` 推荐反代 | — | — |
| §25.3 停止接收新请求 | 计划.md:1623 | 部分完成 | `src/server.rs:107-117` axum graceful shutdown | 已接受的连接上的新请求没有被显式拒绝，也没有 503 短路中间件 | 不一致 |
| §25.3 取消排队请求并返回可重试错误 | 计划.md:1624 | 已完成 | `src/app/mod.rs:239-251`；`src/server.rs:113 begin_shutdown`；`src/gateway/passthrough.rs:480-484`；`src/routing/queue.rs:130-152`；`src/routing/queue.rs:262,291` 测试 | 与 `README.md:82` 的"仍待完成"矛盾（README 过时） | — |
| §25.3 在途请求默认 180 秒完成 | 计划.md:1625 | 已完成 | `src/main.rs:28`；`src/server.rs:82,102,118-138`；`tests/release_harden.rs:98` | — | — |
| §25.3 刷新日志、粘性绑定与性能快照写入队列 | 计划.md:1626 | 部分完成 | `src/server.rs:140-141 flush_snapshots`；`src/app/tasks.rs:81-92`；`src/app/recorder.rs:59-82` | 没有显式等待 recorder drain 完成，依赖运行时析构 | 不一致 |
| §25.3 超时后取消剩余上游连接并退出 | 计划.md:1627 | 部分完成 | `src/server.rs:118-138` | 靠"运行时析构中止剩余任务"，没有显式取消上游连接池/请求句柄 | 不一致 |


### 4.14 测试策略（`计划.md` §26）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| §26.1 纯文本与多轮消息 | 计划.md:1635 | 部分完成 | `tests/protocol_e2e.rs:96`；`src/protocol/mod.rs:530` | 多轮靠 assistant tool_calls + tool result 隐式覆盖，没有专门的"两轮纯文本"夹具 | 不一致 |
| §26.1 图片输入 | 计划.md:1636 | 已完成 | `tests/protocol_e2e.rs:141`；`src/protocol/openai_chat.rs:1117` | — | — |
| §26.1 单个和并行函数工具 | 计划.md:1637 | 部分完成 | `tests/protocol_e2e.rs:158`（单个 call_1）；`src/protocol/mod.rs:426` | 没有"一次响应返回两个并行 tool_calls"的用例；`parallel_tool_calls` 只透传字段（`src/protocol/openai_chat.rs:481`） | 缺失 |
| §26.1 工具参数分片与工具结果 | 计划.md:1638 | 已完成 | `src/protocol/openai_chat.rs:1213`；`tests/protocol_e2e.rs:254` | — | — |
| §26.1 思考、思考摘要、签名和加密块 | 计划.md:1639 | 已完成 | `src/protocol/openai_responses.rs:1246`；`src/protocol/anthropic.rs:922` | 摘要覆盖在实例解析层，没有"summary 跨六方向"断言 | — |
| §26.1 JSON Schema 结构化输出 | 计划.md:1640 | 已完成 | `src/protocol/openai_chat.rs:1141`；`src/protocol/openai_responses.rs:1288`；`tests/protocol_e2e.rs:487` | — | — |
| §26.1 拒绝、引用和不同 stop reason | 计划.md:1641 | 部分完成 | StopReason `src/protocol/canonical.rs:336`；refusal `src/protocol/openai_chat.rs:186,273,398`；`src/protocol/mod.rs:769` | 引用/citations 在 `src/` 零实现零测试（grep citation 无命中）；refusal 往返无独立断言 | 缺失 |
| §26.1 usage 缓存与思考 Token | 计划.md:1642 | 已完成 | `src/protocol/openai_chat.rs:1197`；`src/protocol/anthropic.rs:986`；`src/protocol/canonical.rs:484` | — | — |
| §26.1 未知字段原生透传与跨协议拒绝 | 计划.md:1643 | 已完成 | `tests/protocol_e2e.rs:506`；`src/protocol/mod.rs:469` | — | — |
| §26.1 降级白名单内能力的降级路径与标记正确性 | 计划.md:1644 | 已完成 | `tests/protocol_e2e.rs:302,375,436,469,738`；`src/capability/mod.rs:299` | — | — |
| §26.1 三种跨协议方向（六向） | 计划.md:1646-1650 | 已完成 | `tests/protocol_e2e.rs:96,158,189,254` 三重循环 ALL×ALL；`src/protocol/mod.rs:426,606,658` | — | — |
| §26.2 普通与流式创建 | 计划.md:1656 | 已完成 | `tests/sdk/acceptance.py:118,143,240,241,351,352` | — | — |
| §26.2 工具调用循环 | 计划.md:1657 | 已完成 | `tests/sdk/acceptance.py:185,353` | — | — |
| §26.2 previous_response_id | 计划.md:1658 | 已完成 | `tests/sdk/acceptance.py:218,243` | — | — |
| §26.2 查询、删除、取消和输入项 | 计划.md:1659 | 部分完成 | 查询 `acceptance.py:212`；删除/取消/输入项只有 HTTP 层 `tests/responses_lifecycle.rs:139,173,206` | `acceptance.py` 里没有 delete/cancel/input_items 用例，SDK 面未覆盖这三项 | 缺失 |
| §26.2 count_tokens | 计划.md:1660 | 部分完成 | `acceptance.py:357`；`tests/gateway_e2e.rs:291` | newapi 未实现该端点，实测返回 400（`review/2026-09-15-真实上游验收.md:24`），未在支持的可用上游上验证通过路径 | 不一致 |
| §26.2 /v1/models 双形状按鉴权头正确切换 | 计划.md:1661 | 已完成 | `tests/gateway_e2e.rs:425`；`src/gateway/models.rs:238-278` | SDK 脚本只覆盖 OpenAI shape | — |
| §26.2 错误对象能被 SDK 解析且状态码触发预期重试 | 计划.md:1662 | 部分完成 | `acceptance.py:229,246`；`tests/release_harden.rs:206` | SDK 客户端设了 `max_retries=0`（`acceptance.py:91`），没有验证"SDK 真的按状态码重试" | 缺失 |
| §26.3 同一 Key 下两模型按具体模型独立调度 | 计划.md:1668 | 已完成 | `tests/routing_e2e.rs:534`；`tests/common/mod.rs:629 wire_extra_target` | — | — |
| §26.3 组内调度且绝不跨组 | 计划.md:1669 | 已完成 | `tests/gateway_e2e.rs:516`；`tests/admin_api.rs:336`；`tests/responses_chain.rs:285` | — | — |
| §26.3 严格阶梯 | 计划.md:1670 | 已完成 | `tests/routing_e2e.rs:54`；`src/routing/mod.rs:634` | — | — |
| §26.3 同优先级同层、层内按评分加权分配 | 计划.md:1671 | 已完成 | `src/routing/mod.rs:607`；`src/routing/score.rs:556,566,578` | — | — |
| §26.3 比值归一化 0.50/0.50/0.51 不把第三个打死 | 计划.md:1672 | 已完成 | `src/routing/score.rs:419 ratio_normalisation_does_not_kill_a_marginally_worse_target` | — | — |
| §26.3 归一化参照系为整个分组、跨层可比 | 计划.md:1673 | 部分完成 | `src/routing/score.rs:511 the_group_wide_frame_keeps_multiplier_scores_comparable_across_models`；`:479` | 测试只覆盖倍率维的全组参照系；性能三维的参照系实际是当次候选集（`src/routing/score.rs:263-268`），与文档不符 | 不一致 |
| §26.3 尝试预算：第 1 层 5 目标 3 个连接失败时第 4 个仍成功 | 计划.md:1674 | 已完成 | `tests/routing_e2e.rs:266 cheap_failures_do_not_count_against_any_attempt_limit` | — | — |
| §26.3 每目标独立队列：粘性长等待不阻塞无粘性 | 计划.md:1675 | 已完成 | `src/routing/queue.rs:231 a_long_sticky_wait_does_not_block_a_ready_request` | — | — |
| §26.3 粘性命中、/compact 后不断链、故障清除和恢复 | 计划.md:1676 | 已完成 | `src/routing/sticky.rs:385`；`tests/routing_e2e.rs:313,1044` | — | — |
| §26.3 粘性等待预算按体积和缓存新鲜度正确计算 | 计划.md:1677 | 已完成 | `src/routing/sticky.rs:535,550,569`；`tests/routing_e2e.rs:356,435` | — | — |
| §26.3 DNS、TLS、401、429、5xx、损坏响应的切换 | 计划.md:1678 | 部分完成 | 401/429 `tests/routing_e2e.rs:534`；5xx `:606`；切换判定 `src/gateway/passthrough.rs:1826 is_switchable_status` | DNS 失败与 TLS 失败没有任何测试（grep tls/dns 在 tests/ 无命中）；上游"损坏响应"也无测试 | 缺失 |
| §26.3 慢请求和总超时不产生第二份调用 | 计划.md:1679 | 已完成 | `tests/routing_e2e.rs:189`（断言 `backup.requests()==0`） | — | — |
| §26.3 第一个语义 SSE 事件后禁止切换 | 计划.md:1680 | 已完成 | `tests/routing_e2e.rs:488`；`tests/protocol_e2e.rs:699`；`src/gateway/stream.rs:519` | — | — |
| §26.3 客户端断开、队列满和队列超时 | 计划.md:1681 | 部分完成 | 队列满 `tests/routing_e2e.rs:95`；队列超时 `:123`；客户端断开仅单元层 `src/routing/queue.rs:250` | 没有 e2e 级"客户端断开后名额归还"用例 | 缺失 |
| §26.4 自动倍率成功、失败、空数据、格式错误和超时 | 计划.md:1685 | 部分完成 | `src/multiplier/probe.rs:449,491,515`；`tests/routing_e2e.rs:757,822` | 探针超时没有专门用例（只有响应体上限 `src/multiplier/probe.rs:411`） | 缺失 |
| §26.4 刷新失败按风险余量进入 stale 并在宽限期后转 unknown | 计划.md:1686 | 已完成 | `src/multiplier/mod.rs:383,372`；`tests/routing_e2e.rs:663` | — | — |
| §26.4 半数账号同时失败触发系统性故障保护 | 计划.md:1687 | 已完成 | `src/multiplier/refresh.rs:464`；`src/multiplier/mod.rs:411`；`tests/routing_e2e.rs:904` | — | — |
| §26.4 手动倍率不受自动刷新失败影响 | 计划.md:1688 | 已完成 | `src/multiplier/mod.rs:363` | — | — |
| §26.4 等于上限允许、高于则拒绝 | 计划.md:1689 | 已完成 | `src/routing/mod.rs:674`；`tests/routing_e2e.rs:789` | — | — |
| §26.4 排队期间倍率升高，终检必须阻止发送 | 计划.md:1690 | 部分完成 | `src/gateway/passthrough.rs:1011-1030,601` | 实现存在，但没有"排队等待期间倍率升高 → 终检拒绝"的测试 | 缺失 |
| §26.4 在途期间倍率升高，请求继续完成 | 计划.md:1691 | 待确认 | 终检只在发送前（`src/gateway/passthrough.rs:601`）；`src/` 无在途倍率快照 | 缺"请求已发往上游后改倍率仍完成"的用例；需要补测试才能证实 | 待确认 |
| §26.4 倍率恢复后自动恢复调度 | 计划.md:1692 | 已完成 | `tests/routing_e2e.rs:725-733,796-799` | — | — |
| §26.4 校准系数参与有效倍率计算，定点乘法取保守方向 | 计划.md:1693 | 已完成 | `src/multiplier/mod.rs:431`；`src/domain/fixed.rs:179,168` | — | — |
| §26.4 NewAPI 探针用 /api/user/self/groups 并带两个凭据；凭据缺失/过期行为 | 计划.md:1694 | 部分完成 | `tests/routing_e2e.rs:831`；`:889-900`（令牌过期进入宽限期） | "凭据缺失（未配置）"行为无独立用例 | 缺失 |
| §26.4 Sub2API 峰值时段切换 | 计划.md:1695 | 已完成 | `src/multiplier/probe.rs:532,549,570` | — | — |
| §26.5 0 个模型不覆盖，错误 JSON 不覆盖 | 计划.md:1699 | 已完成 | `tests/discovery_selection.rs:850,274` | — | — |
| §26.5 选择集持久：二次拉取已选保持、新增默认不勾 | 计划.md:1700 | 已完成 | `tests/discovery_selection.rs:900` | — | — |
| §26.5 自动同步打开全量托管、关闭回选择集且未污染 | 计划.md:1701 | 已完成 | `tests/discovery_selection.rs:567,697` | — | — |
| §26.5 多账号同名模型自动归并 | 计划.md:1702 | 已完成 | `tests/discovery_selection.rs:137,485` | — | — |
| §26.5 模型别名在归并前正确应用 | 计划.md:1703 | 已完成 | `tests/discovery_selection.rs:199` | — | — |
| §26.5 取消勾选有流量的模型触发警告 | 计划.md:1704 | 已完成 | `tests/discovery_selection.rs:407` | — | — |
| §26.5 自动创建零目标清理；手工创建保留但从 /v1/models 移除 | 计划.md:1705 | 已完成 | `tests/discovery_selection.rs:524,935`；`src/gateway/models.rs:238` | — | — |
| §26.5 已选模型消失后保留暂停，再次出现自动恢复 | 计划.md:1706 | 已完成 | `tests/discovery_selection.rs:318,358` | — | — |
| §26.5 手动模型不被错误刷新删除 | 计划.md:1707 | 已完成 | `tests/discovery_selection.rs:318,381` | — | — |
| §26.6 成本聚合严格按逻辑模型分组，无跨模型加总 | 计划.md:1711 | 已完成 | `tests/admin_cost.rs:92`（断言无 total_cost/total_saving） | — | — |
| §26.6 加权均倍率、最贵目标、最便宜目标计算正确 | 计划.md:1712 | 部分完成 | `tests/admin_cost.rs:174`；`tests/routing_e2e.rs:1022` | 加权口径实际按请求次数而非 token（见 §11.6 那条） | 不一致 |
| §26.6 校准助手按单模型对账，反算不受模型组合影响 | 计划.md:1713 | 已完成 | `tests/admin_cost.rs:242` | — | — |
| §26.6 保留天数设为 0 时成本页的降级行为 | 计划.md:1714 | 未实现 | 无实现（见 §24.2 两条），`tests/` 无对应用例 | 该场景既没实现也没测试 | 缺失 |
| §26.7 粘性绑定重启后恢复 | 计划.md:1718 | 已完成 | `tests/routing_e2e.rs:942,989` | — | — |
| §26.7 性能快照重启后加载，评分不从零开始 | 计划.md:1719 | 已完成 | `tests/routing_e2e.rs:990-992`；`src/routing/score.rs:691` | — | — |
| §26.7 快照超过 24 小时被丢弃 | 计划.md:1720 | 已完成 | `tests/routing_e2e.rs:1005-1018`；`src/app/tasks.rs:26 SNAPSHOT_HORIZON` | — | — |
| §26.7 快照写入失败不影响热路径 | 计划.md:1721 | 部分完成 | `src/app/tasks.rs:86,89` 只 `tracing::warn`；`:297` | 没有"写入失败"的注入用例，只有"没有 ticker 也能 flush" | 缺失 |
| §26.8 Key 与 NewAPI 令牌不出现在日志、错误和备份元数据 | 计划.md:1725 | 已完成 | `src/security/redact.rs:97-141`（5 个测试）；`tests/routing_e2e.rs:900`；`tests/admin_api.rs:604`；`tests/gateway_e2e.rs:598` | 备份元数据的脱敏没有独立断言 | — |
| §26.8 不同分组不能读取彼此 Responses ID | 计划.md:1726 | 已完成 | `tests/responses_chain.rs:285`；`tests/managed_background_e2e.rs:256` | — | — |
| §26.8 SSRF、DNS Rebinding 和跨域重定向 | 计划.md:1727 | 部分完成 | `src/security/url_guard.rs:141-179`（5 测试）；`src/upstream/mod.rs:534`；重定向禁用 `src/upstream/mod.rs:84` | 重定向禁用没有测试；"DNS Rebinding 二次解析窗口"由设计消除但没有对抗性测试 | 缺失 |
| §26.8 加密数据篡改检测 | 计划.md:1728 | 已完成 | `src/security/mod.rs:257 tampered_ciphertext_is_rejected`；`src/security/backup.rs:180` | — | — |
| §26.8 临时文件在取消和崩溃恢复后清理 | 计划.md:1729 | 已完成 | `src/app/mod.rs:472`；`src/gateway/passthrough.rs:2132` | 取消路径靠 Drop，无独立用例 | — |
| §26.8 JSON 和 SSE 解析使用模糊测试 | 计划.md:1730 | 已完成 | `tests/fuzz_harden.rs:97,142,164` | — | — |
| §26.8 单个请求任务 panic 不导致进程退出 | 计划.md:1731 | 部分完成 | `src/sync.rs:48,75` 锁毒化恢复 | 全仓无 `catch_unwind`，也没有"请求任务 panic 后进程仍存活"的用例 | 缺失 |
| §26.8 SQLite 忙、磁盘满和写入通道满时热路径可控降级 | 计划.md:1732 | 部分完成 | 通道满 `src/app/recorder.rs:152`；busy_timeout `src/storage/mod.rs:40`；`src/storage/store.rs:2592` | 磁盘满没有任何实现分支或测试；SQLite 忙没有注入用例 | 缺失 |
| §26.9 网关自身延迟 | 计划.md:1738 | 已完成 | `review/2026-09-15-真实上游验收.md:57-67` | 非固定硬件基准 | — |
| §26.9 并发连接、SSE 长连接和内存上限 | 计划.md:1739 | 部分完成 | 验收文档 `:67`（30 秒长流式、RSS 平稳）；`:25`（16 请求并发 4） | 未测并发连接数上限与内存上限拐点 | 缺失 |
| §26.9 64 KB、8 MB 和 64 MB 请求体 | 计划.md:1740 | 部分完成 | 验收文档 `:64,65,66`（64KB 20/20、8MB 5/5、60MB 1 个） | 实测 60 MB 而非 64 MB，且只有单次样本 | 不一致 |
| §26.9 日志开启、保留设为 0 和 SQLite 清理期间的差异 | 计划.md:1741 | 未实现 | 验收文档无对应行；`tests/` 无用例 | 未执行 | 缺失 |
| §26.9 粘性绑定与性能快照落盘期间的热路径抖动 | 计划.md:1742 | 未实现 | 验收文档无对应行 | 未执行 | 缺失 |
| §26.9 HTTP 连接复用率和每请求分配量 | 计划.md:1743 | 未实现 | 验收文档无对应行 | 未执行 | 缺失 |

### 4.15 分阶段实施、完成标准、第二期草案（`计划.md` §27–§29）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| §27 阶段1 Rust 服务/SQLite/主密钥/首次管理员 | 计划.md:1755 | 已完成 | `tests/admin_api.rs:51,90`；`src/security/mod.rs:250` | — | — |
| §27 阶段1 分组/Key/账号/逻辑模型/单目标映射 | 计划.md:1756 | 已完成 | `tests/admin_api.rs:207` | — | — |
| §27 阶段1 三入口原生透传（普通与 SSE） | 计划.md:1757-1761 | 已完成 | `src/gateway/mod.rs:29-32`；`tests/gateway_e2e.rs:217,261,291`；`tests/protocol_e2e.rs:778` | Responses 原生透传没有独立 gateway_e2e 用例 | — |
| §27 阶段1 /v1/models 双形状 | 计划.md:1762 | 已完成 | `src/gateway/mod.rs:49-50`；`tests/gateway_e2e.rs:425` | — | — |
| §27 阶段1 请求 ID/错误/状态码/加密/脱敏日志 | 计划.md:1763 | 已完成 | `src/gateway/error.rs:224,278`；`tests/release_harden.rs:206` | — | — |
| §27 阶段1 嵌入式最小后台 | 计划.md:1764 | 已完成 | `src/admin/ui.rs:9`；`tests/admin_api.rs:539,581` | — | — |
| §27 阶段1 Docker 与 Linux 开发构建 | 计划.md:1765 | 已完成 | `Dockerfile`；`docker-compose.yml`；`deploy/`；`dist/` 双架构工件 | 工件构建于 2026-09-16，未随之后 9 个提交重建 | 不一致 |
| §27 阶段1 验收：Claude Code / Codex CLI / OpenAI Chat 真实工作 | 计划.md:1767 | 部分完成 | `review/2026-09-15-真实上游验收.md:18-26`（官方 SDK 14/14、真实生图、sub2api 链路） | 没有 Claude Code 与 Codex CLI 本体的实跑记录 | 缺失 |
| §27 阶段2 多目标与账号/模型调度单元 | 计划.md:1773 | 已完成 | `src/routing/mod.rs:225`；`tests/common/mod.rs:629` | — | — |
| §27 阶段2 严格优先级阶梯与分层 | 计划.md:1774 | 已完成 | `tests/routing_e2e.rs:54`；`src/routing/mod.rs:607,634` | — | — |
| §27 阶段2 层内比值归一化、四维权重、加权随机 | 计划.md:1775 | 已完成 | `src/routing/score.rs:419,511,524`；`src/domain/mod.rs:383` | — | — |
| §27 阶段2 粘性（稳定前缀哈希、体积×新鲜度、持久化） | 计划.md:1776 | 已完成 | `src/routing/sticky.rs:385,517,535` | — | — |
| §27 阶段2 并发、RPM/TPM、请求体可重放 | 计划.md:1777 | 已完成 | `src/health/mod.rs:918,940,968,1134`；`src/gateway/passthrough.rs:2433` | — | — |
| §27 阶段2 成本+时间预算、独立队列、流式边界、熔断半开 | 计划.md:1778 | 已完成 | `tests/routing_e2e.rs:54,95,123,189,266,488,606`；`src/routing/queue.rs:231` | — | — |
| §27 阶段2 手动倍率、校准系数与硬性倍率门 | 计划.md:1779 | 已完成 | `src/multiplier/mod.rs:363,431`；`src/routing/mod.rs:674,984` | — | — |
| §27 阶段2 Sub2API、New API 探针、宽限期与终检 | 计划.md:1780 | 已完成 | `tests/routing_e2e.rs:663,757,831,904` | — | — |
| §27 阶段2 性能 EWMA 与快照持久化 | 计划.md:1781 | 已完成 | `src/routing/score.rs:643,691`；`tests/routing_e2e.rs:942` | — | — |
| §27 阶段2 验收 | 计划.md:1783 | 已完成 | `tests/routing_e2e.rs` 全文件；`tests/gateway_e2e.rs:323` | — | — |
| §27 阶段3 完整中间格式 | 计划.md:1787 | 部分完成 | `src/protocol/canonical.rs:336-450` | 缺引用内容块与未知字段原值（见 §14.4 两条） | 缺失 |
| §27 阶段3 六个方向的转换 | 计划.md:1788 | 已完成 | `tests/protocol_e2e.rs:96,158,189,254` | — | — |
| §27 阶段3 工具、思考、图片输入、结构化输出和错误映射 | 计划.md:1789 | 已完成 | `src/protocol/openai_chat.rs:1117,1141,1213`；`src/gateway/error.rs:314` | — | — |
| §27 阶段3 能力降级白名单与 X-Akhub-Degraded | 计划.md:1790 | 已完成 | `tests/protocol_e2e.rs:351`；`src/capability/mod.rs:299` | — | — |
| §27 阶段3 未知字段透传与跨协议明确拒绝 | 计划.md:1791 | 已完成 | `tests/protocol_e2e.rs:506`；`src/protocol/mod.rs:469` | — | — |
| §27 阶段3 验收 | 计划.md:1793 | 已完成 | `tests/protocol_e2e.rs:469`；`tests/sdk/acceptance.py` | "黄金夹具"是 Rust 内联 JSON（`tests/protocol_e2e.rs:33`），没有独立 fixture 文件 | 不一致 |
| §27 阶段4 Responses 查询/删除/取消/输入项/压缩/Token 计数 | 计划.md:1797 | 已完成 | `tests/responses_lifecycle.rs:139,173,206,278,327,367` | — | — |
| §27 阶段4 加密状态链、网关响应 ID 和后台响应 | 计划.md:1798 | 已完成 | `src/gateway/responses.rs:24,50`；`tests/responses_chain.rs:79`；`tests/stream_settlement.rs:273` | — | — |
| §27 阶段4 模型选择集、自动同步、别名与目标自动归并 | 计划.md:1799 | 已完成 | `tests/discovery_selection.rs:137,199,567,900` | — | — |
| §27 阶段4 LiteLLM 能力目录导入、证据优先级和过期失效 | 计划.md:1800 | 部分完成 | `src/capability/mod.rs:49,237,253,279` | 目录 revision 停在 2026-09-04；内置目录未接入路由决策（见 §16.6 那条） | 缺失 |
| §27 阶段4 验收：previous_response_id 跨同组上游重建 | 计划.md:1802 | 已完成 | `tests/responses_chain.rs:108,495` | — | — |
| §27 阶段4 验收：store:false 行为符合边界说明 | 计划.md:1802 | 已完成 | `tests/responses_chain.rs:182`；`src/gateway/responses.rs:853` | — | — |
| §27 阶段4 验收：8 账号 × 20 模型 10 次操作内完成 | 计划.md:1802 | 部分完成 | `tests/discovery_selection.rs:485` | 测试实际发出约 24 次 HTTP 调用，与"约 10 次"口径有出入 | 不一致 |
| §27 阶段5 概览、账号状态、逻辑模型状态、请求记录页 | 计划.md:1806 | 已完成 | `web/src/pages/`；`src/admin/resources.rs:1282-1410`；`tests/admin_settings.rs:419,335,549` | — | — |
| §27 阶段5 成本页与校准助手 | 计划.md:1807 | 已完成 | `tests/admin_cost.rs:92,174,242,729`；`web/src/pages/Cost.tsx`；`CalibrationDialog.tsx` | — | — |
| §27 阶段5 账号一键复制、测试按钮和批量刷新 | 计划.md:1808 | 部分完成 | 复制 `tests/admin_cost.rs:338`；测试 `:473`；单账号刷新 `tests/admin_settings.rs:145` | 没有"批量刷新"：无接口、无 UI、无测试 | 缺失 |
| §27 阶段5 配置备份恢复、保留清理和管理员审计 | 计划.md:1809 | 已完成 | `tests/admin_cost.rs:539,587`；`src/app/tasks.rs:226`；`src/storage/schema.sql:262` | 概览页审计只显示最近 5 条 | — |
| §27 阶段5 验收 | 计划.md:1811 | 部分完成 | `src/admin/resources.rs:1282-1410`；`tests/admin_cost.rs:92` | 分维得分缺"加权贡献"（见 §6.5 那条） | 不一致 |
| §27 阶段6 全量协议、故障、安全、模糊和负载测试 | 计划.md:1815 | 部分完成 | 协议/故障/安全/模糊：`tests/protocol_e2e.rs`、`routing_e2e.rs`、`release_harden.rs`、`fuzz_harden.rs` | 负载测试未做（验收文档只有 16 请求并发 4 的冒烟，无 load 脚本或 CI 任务） | 缺失 |
| §27 阶段6 优雅关闭、磁盘异常、SQLite 恢复和升级检查 | 计划.md:1816 | 已完成 | `tests/release_harden.rs:14,98,160,176,281,329,350` | — | — |
| §27 阶段6 多架构 Docker、Linux 二进制、部署文档和参考配置 | 计划.md:1817 | 已完成 | `.github/workflows/release.yml:43-96`；`scripts/release.sh:31-46`；`dist/`；`deploy/` | 镜像是否真的推到 GHCR 无法离线验证 | — |
| §27 阶段6 许可证与第三方声明审查 | 计划.md:1818 | 已完成 | `tests/release_harden.rs:451`；`NOTICES.md:3-9`；`LICENSE:1` | LICENSE 与 README:226 冲突 | 不一致 |
| §27 阶段6 验收：发布 v1.0.0 | 计划.md:1820 | 已完成 | `Cargo.toml:3 version=1.0.0`；git tag `v1.0.0` 已推 origin；`.github/workflows/release.yml` 由 v* tag 触发 | 发布后 master 又前进 9 个提交（含 §29.1），当前 HEAD 无对应 tag | 不一致 |
| §28 管理员无需配置模型能力字段 | 计划.md:1826 | 已完成 | `web/src/pages/Accounts.tsx:221,732`；`src/capability/mod.rs:49` | — | — |
| §28 8 账号 × 20 模型约 10 次操作 | 计划.md:1827 | 部分完成 | `tests/discovery_selection.rs:485` | 同阶段 4 验收（约 24 次调用） | 不一致 |
| §28 同一 Key 下两模型映射同一逻辑模型并分别调度 | 计划.md:1828 | 已完成 | `tests/routing_e2e.rs:534`；`tests/common/mod.rs:629` | — | — |
| §28 所有调度严格在分组内 | 计划.md:1829 | 已完成 | `tests/gateway_e2e.rs:516`；`tests/admin_api.rs:336`；`src/config/mod.rs:144,319,348` | — | — |
| §28 高优先级层有合格目标时低层永无流量 | 计划.md:1830 | 已完成 | `tests/routing_e2e.rs:54,80-84`；`src/routing/mod.rs:634`；`src/gateway/passthrough.rs:308` | — | — |
| §28 同优先级四维加权，分差小接近均分、分差大留探索 | 计划.md:1831 | 已完成 | `src/routing/score.rs:556,566`；`src/routing/mod.rs:607` | — | — |
| §28 归一化全部用比值，微小差异不被放大 | 计划.md:1832 | 已完成 | `src/routing/score.rs:419,436,449` | — | — |
| §28 倍率超宽限/超限无新请求，宽限内可用但降权告警 | 计划.md:1833 | 已完成 | `tests/routing_e2e.rs:663,705-723`；`src/routing/score.rs:501` | — | — |
| §28 探针系统性故障不会打死自己 | 计划.md:1834 | 已完成 | `tests/routing_e2e.rs:904`；`src/multiplier/refresh.rs:464` | — | — |
| §28 倍率变化不取消在途但阻止排队与后续 | 计划.md:1835 | 部分完成 | "阻止排队/后续" `src/gateway/passthrough.rs:601,1012` | "不取消在途"没有测试证据 | 缺失 |
| §28 慢请求不会触发并行复制调用 | 计划.md:1836 | 已完成 | `tests/routing_e2e.rs:189` | — | — |
| §28 SSE 首个语义块后绝不拼接另一上游 | 计划.md:1837 | 已完成 | `tests/routing_e2e.rs:488`；`tests/protocol_e2e.rs:699`；`src/gateway/stream.rs:519-545` | — | — |
| §28 /compact 与注入内容不会打断粘性 | 计划.md:1838 | 已完成 | `src/routing/sticky.rs:385,402` | — | — |
| §28 粘性长等待不阻塞无粘性 | 计划.md:1839 | 已完成 | `src/routing/queue.rs:231` | 只有单元层，没有同进程 e2e | — |
| §28 工具/图片/结构化/未知字段无静默丢失，thinking 降级仅故障切换且有标记 | 计划.md:1840 | 部分完成 | `tests/protocol_e2e.rs:302,469,506,738`；`src/protocol/anthropic.rs:1024` | 引用未实现，属该条之外的独立缺口 | 不一致 |
| §28 三协议能被官方 SDK 使用 | 计划.md:1841 | 部分完成 | `review/2026-09-15-真实上游验收.md:18-24`；`tests/sdk/acceptance.py` | count_tokens 因上游未实现返回 400；cancel/delete/items 未进 SDK 脚本 | 缺失 |
| §28 网关错误状态码使客户端重试行为符合预期 | 计划.md:1842 | 已完成 | `tests/release_harden.rs:206`；`src/gateway/error.rs:224` | 流内错误不带错误码（见 §18.2 那条） | 不一致 |
| §28 模型刷新失败或 0 模型不破坏配置或选择集 | 计划.md:1843 | 已完成 | `tests/discovery_selection.rs:274,850` | — | — |
| §28 重启后粘性与评分立即可用 | 计划.md:1844 | 已完成 | `tests/routing_e2e.rs:942,986-1003` | — | — |
| §28 成本页不跨模型加总且同时显示最便宜目标 | 计划.md:1845 | 已完成 | `tests/admin_cost.rs:92,174`；`web/src/pages/Cost.tsx:159,174`；`src/admin/resources.rs:2172` | — | — |
| §28 普通请求正文不落日志；Responses 状态加密保存并可清理 | 计划.md:1846 | 已完成 | `tests/gateway_e2e.rs:544`；`tests/protocol_e2e.rs:771`；`src/app/tasks.rs:330` | — | — |
| §28 Docker 与 Linux 原生版都能从空数据目录首次启动和真实调用 | 计划.md:1847 | 部分完成 | 空目录启动 `tests/common/mod.rs:472`；`dist/` 二进制；真实调用 `review/2026-09-15:18-26` | Docker 容器内首次启动 + 真实调用没有验收记录 | 缺失 |
| §28 热路径不等待 SQLite 日志写入，无无界队列/缓存 | 计划.md:1848 | 部分完成 | `src/app/recorder.rs:3,152`；`src/routing/queue.rs:33`；`src/routing/mod.rs:25` | sticky/perf/health 内存表无容量上限（见 §19.4 那条） | 缺失 |
| §29.1 落点1 触发条件：background + 原生优先 + allow_managed_background | 计划.md:1879 | 已完成 | `src/gateway/background.rs:511-530,594`；`src/domain/mod.rs:226`；`src/config/mod.rs:235` | — | — |
| §29.1 落点2 ID 分层 bg_akh_/resp_akh_，未知 ID → response_state_expired | 计划.md:1880 | 已完成 | `src/gateway/background.rs:40,45`；`src/gateway/responses.rs:24,404,500,545`；`tests/managed_background.rs:45` | — | — |
| §29.1 落点3 background_tasks 表，状态与执行者落 SQLite，不引入 Redis | 计划.md:1881 | 已完成 | `src/storage/schema.sql:284-304`；`Cargo.toml` 无 redis | — | — |
| §29.1 落点4 受控任务循环（有界并发、每任务独立超时） | 计划.md:1882 | 部分完成 | 每任务超时 `src/gateway/background.rs:255-256`；心跳 `:359`；`tests/managed_background_e2e.rs:361` | 没有有界并发（无信号量/名额），也没有"任务级名额"限制 | 缺失 |
| §29.1 落点5 真取消（句柄表 + 断连 + 可能已计费标注） | 计划.md:1883 | 已完成 | `src/gateway/background.rs:56,82,443`；`tests/managed_background.rs:57,183`；`tests/managed_background_e2e.rs:120` | — | — |
| §29.1 落点6 重启不骗人（重绑上游或标 interrupted） | 计划.md:1884 | 部分完成 | `src/gateway/background.rs:110 recover_stale`；`src/storage/store.rs:406`；`src/app/mod.rs:357`；`tests/managed_background.rs:104` | "有 upstream_id 且上游支持查询则重绑为代理查询"分支未实现，一律标 interrupted | 缺失 |
| §29.1 落点7 生命周期与配额：开始/结束各结算一次、任务级名额、倍率准入 | 计划.md:1885 | 未实现 | `src/gateway/background.rs` 全文件无 quota/settle/Admission 相关代码 | "任务开始与结束各结算一次"与"任务级名额"都没有实现 | 缺失 |
| §29.1 落点8 边界：进程挂即中断、不承诺多实例、store:false 不落输出 | 计划.md:1886 | 部分完成 | 进程挂 → interrupted 已实现；单实例无多实例代码 | `store:false` 未处理：`background.rs` 不读 `store` 字段，`sealed_output` 照存（`tests/managed_background.rs:229`） | 缺失 |
| §29.1 验收1 上游不支持 + 未开开关 → 400 unsupported_parameter | 计划.md:1890 | 未实现 | `tests/managed_background_e2e.rs:42` 只断言没拿到 `bg_akh_`，实际走普通转发返回 200 | 未开开关时不返回 400，第一期口径在代码中不存在 | 缺失 |
| §29.1 验收2 开了开关 → bg_akh_*，queued → in_progress → completed，输出完整 | 计划.md:1891 | 已完成 | `tests/managed_background_e2e.rs:68,304`；`src/gateway/background.rs:127` | "queued" 初态没有单独断言 | — |
| §29.1 验收3 取消 bg_akh_* 后上游连接确实断开，状态 cancelled | 计划.md:1892 | 已完成 | `tests/managed_background_e2e.rs:120,174,185` | — | — |
| §29.1 验收4 跑到一半 kill 重启后 interrupted 或跟上游走，绝不停在 in_progress | 计划.md:1893 | 部分完成 | `tests/managed_background.rs:104` | "跟上游走"分支无实现也无用例；测试模拟的是心跳过期而非真实 kill | 缺失 |
| §29.1 验收5 background=true 打原生上游走 resp_akh_ 代理 | 计划.md:1894 | 待确认 | `src/gateway/background.rs:529-532` 原生可用则 return None，随后走普通转发拿 `resp_akh_` | 没有测试断言"原生上游时 ID 为 `resp_akh_` 且走代理路径" | 待确认 |

### 4.16 其他文档（README、偏差审计、验收记录、部署文档）

| 规划条目 | 出处 | 当前状态 | 证据 | 缺口说明 | 严重程度 |
|---|---|---|---|---|---|
| README 已实现：Rust/SQLite/主密钥/首次管理员 | README.md:23 | 已完成 | `tests/admin_api.rs:51`；`src/security/mod.rs:250` | — | — |
| README：分组/Key/账号/逻辑模型/调度目标 | README.md:24 | 已完成 | `tests/admin_api.rs:207` | — | — |
| README：三入口 + 图片接口 | README.md:25-30 | 已完成 | `src/gateway/mod.rs:29-34`；`tests/images_e2e.rs:170,213`；`review/2026-09-15:20-21` | 与 `计划.md:83` §2.2"不做图片端点"直接冲突 | 不一致 |
| README：/v1/models 双形态 | README.md:31 | 已完成 | `tests/gateway_e2e.rs:425` | — | — |
| README：请求 ID/错误码/状态码映射/加密/脱敏 | README.md:32 | 已完成 | `src/gateway/error.rs:224`；`src/security/redact.rs:97` | — | — |
| README：严格阶梯 + "普通粘性绑定仍有绕过已恢复高层的缺陷" | README.md:33-35 | 文档过时 | 代码已修：`src/gateway/passthrough.rs:306-315`（`plan.first_layer()` 限定最高合格层）；缺陷已在 `2026-09-06_功能审查-Akhub-report.md:25,30-32` 标 fixed；回归探针 `review/2026-09-06/gateway_probes.rs:51` | README 第 35 行声称的缺陷已经不存在，与代码现状矛盾 | 不一致 |
| README：层内四维评分 | README.md:36-37 | 已完成 | `src/routing/score.rs:419-578`；`tests/admin_api.rs:780` | — | — |
| README：会话粘性（四级派生/体积×新鲜度/429 原地等待） | README.md:38-40 | 已完成 | `src/routing/sticky.rs:442,535`；`tests/routing_e2e.rs:435` | — | — |
| README：熔断与限流 | README.md:41-43 | 已完成 | `tests/routing_e2e.rs:534`；`src/routing/mod.rs:720` | — | — |
| README：故障切换边界 | README.md:44-45 | 已完成 | `tests/routing_e2e.rs:266,488,189`；`tests/protocol_e2e.rs:699` | — | — |
| README：自动倍率 | README.md:46-47 | 已完成 | `tests/routing_e2e.rs:663,757,831,904` | — | — |
| README：重启恢复 | README.md:48-49 | 已完成 | `tests/routing_e2e.rs:942`；`src/app/tasks.rs:18-26` | — | — |
| README：跨协议六方向 + 原生透传 | README.md:50-53 | 已完成 | `tests/protocol_e2e.rs:96,778`；`src/protocol/mod.rs:426` | — | — |
| README：能力降级白名单 + X-Akhub-Degraded | README.md:54-58 | 已完成 | `tests/protocol_e2e.rs:302,351,436,469` | — | — |
| README：端点证据 24 小时 | README.md:59-60 | 已完成 | `src/upstream/evidence.rs:73,84,99`；`tests/protocol_e2e.rs:566` | — | — |
| README：未知字段透传 | README.md:61-62 | 已完成 | `tests/protocol_e2e.rs:506` | — | — |
| README：嵌入式 React 管理后台 | README.md:63-65 | 已完成 | `src/admin/ui.rs:9,98`；`web/src/pages/*` | — | — |
| README：Docker 与 Linux 部署示例 | README.md:66 | 已完成 | `Dockerfile`；`docker-compose.yml`；`deploy/README.md` | — | — |
| README 仍待完成：Docker 构建、多架构镜像与 Linux 工件（当前只有原生 systemd 部署） | README.md:80 | 文档过时 | `dist/akhub-x86_64-unknown-linux-gnu` 与 `-aarch64-unknown-linux-gnu` 均已存在（2026-09-16 02:22）；`Dockerfile`、`docker-compose.yml`、`.github/workflows/release.yml:43-96`、`scripts/release.sh:31-46` | "当前只有原生 systemd 部署"与事实不符；镜像实际推送/验证虽仍缺，但构建脚本与 Dockerfile 已具备 | 不一致 |
| README 仍待完成：§26.9 完整性能验收 | README.md:81 | 已完成（判断成立） | README.md:81 与 `review/2026-09-15-真实上游验收.md:51` 一致；验收文档 `:55-78` 只覆盖部分场景 | 与该验收文档不矛盾；README 未提"已有部分数据"属表述不完整 | — |
| README 仍待完成：关闭信号时取消排队请求 | README.md:82 | 文档过时 | 已实现：`src/routing/queue.rs:130-152`、`src/gateway/passthrough.rs:565-570`、`src/app/mod.rs:239`；提交 0d329c7；测试 `src/routing/queue.rs:262,291` | README 最后一次修改早于修复 25 分钟，没有同步 | 不一致 |
| README：本项目以 MIT 许可发布 | README.md:226 | 文档过时（自相矛盾） | `LICENSE:1` 写明"Akhub 非商业署名许可"，`:11-12` 限定非商业；`NOTICES.md:3`"本项目自身源码不采用 MIT 许可"；`Cargo.toml:6 license-file="LICENSE"` | README:226 是错误陈述，与同文件第 5-7 行、LICENSE、NOTICES.md 都冲突 | 不一致 |
| 偏差审计 P1-1 请求记录缺 Token/首字/尝试明细 | review/2026-09-15-计划偏差审计.md:9 | 已完成 | `src/storage/schema.sql:170-173`、`:181-195 request_attempts`；`src/admin/resources.rs:2631-2649` | — | — |
| 偏差审计 P1-2 请求记录页没有筛选 | review/2026-09-15-计划偏差审计.md:24 | 已完成 | `src/admin/resources.rs:2555-2571`；`src/storage/store.rs:2002-2046`；`web/src/pages/Requests.tsx:204-294` | 前端缺"实际目标"控件（见 §6.6 那条） | 不一致 |
| 偏差审计 P1-3 成本页没有 token 用量 | review/2026-09-15-计划偏差审计.md:32 | 已完成 | `src/admin/resources.rs:2183`；`src/storage/store.rs:1221`；`tests/admin_cost.rs:729` | — | — |
| 偏差审计 P1-4 概览页缺运行指标与告警 | review/2026-09-15-计划偏差审计.md:41 | 已完成 | `src/admin/resources.rs:145-170`；`web/src/pages/Overview.tsx:114-154,204-294`；`tests/admin_settings.rs:419,549` | 目标数口径与 §12.2 不一致（见 §6.2 那条） | 不一致 |
| 偏差审计 P1-5 账号列表没有筛选 | review/2026-09-15-计划偏差审计.md:50 | 已完成 | `web/src/pages/Accounts.tsx:88-110,232-283` | — | — |
| 偏差审计 P2-6 分组"最长等待时间"需确认 | review/2026-09-15-计划偏差审计.md:56 | 已完成 | 已补 `max_wait_secs`：`src/domain/mod.rs:223`；`src/storage/schema.sql:30`；`src/storage/mod.rs:164-168`；`tests/routing_e2e.rs:152` | 当初的"需确认"已经用"补字段"解决 | — |
| 偏差审计 P2-7 图片接口与计划冲突 | review/2026-09-15-计划偏差审计.md:62 | 文档过时 | 代码与 README 都有，`计划.md:83` 仍未更新 | 审计当时建议改计划文本，计划文本至今未改 | 不一致 |
| 偏差审计 P2-8 后台响应粘性未做端到端验收 | review/2026-09-15-计划偏差审计.md:68 | 部分完成 | `tests/managed_background_e2e.rs` 覆盖创建/查询/取消；原生粘性 `src/gateway/passthrough.rs:1658-1704` | 托管执行时目标会重选（见 §15.3 那条）；无真实原生后台上游的端到端验收 | 缺失 |
| 偏差审计 P3 三条措辞不一致 | review/2026-09-15-计划偏差审计.md:74-76 | 部分完成 | 配置版本已写入记录 `src/storage/schema.sql:173`；概览已展示最近配置变化 `web/src/pages/Overview.tsx:251-294` | "复制后打开编辑页"仍未做（见 §6.4 那条） | 不一致 |
| 验收记录：官方 SDK 14/14 | review/2026-09-15-真实上游验收.md:18 | 已完成 | `tests/sdk/acceptance.py` | 脚本里没有 delete/cancel/input_items 用例 | 不一致 |
| 验收记录：真实生图与图生图 | review/2026-09-15-真实上游验收.md:20-21 | 部分完成 | `tests/sdk/acceptance.py:369-387` 只覆盖 generate | 验收文档声称 edits 通过，但脚本里没有对应用例，无法从脚本复现 | 不一致 |
| 验收记录：尚未覆盖——§26.9 完整性能验收 | review/2026-09-15-真实上游验收.md:51 | 已完成（判断成立） | `:55-78` 有部分数据；README:81 口径一致 | — | — |
| 验收记录：尚未覆盖——Docker 镜像与多架构工件 | review/2026-09-15-真实上游验收.md:52 | 文档过时 | `dist/` 双架构二进制 + `Dockerfile` + `release.yml` 已存在 | 表述需更新 | 不一致 |
| 验收记录：尚未覆盖——跨账号故障切换实测 | review/2026-09-15-真实上游验收.md:53 | 部分完成 | e2e 有跨目标切换：`tests/gateway_e2e.rs:323`、`tests/routing_e2e.rs:54` | 真实上游的多账号切换仍未有实测记录 | 缺失 |
| deploy/README：Restart=always | deploy/README.md:30 | 已完成 | `deploy/akhub.service:21` | — | — |
| deploy/README：TimeoutStopSec=200 大于宽限期 | deploy/README.md:31 | 已完成 | `deploy/akhub.service`；`src/main.rs:28` | — | — |
| deploy/README：ProtectSystem/StateDirectory | deploy/README.md:33 | 已完成 | `deploy/akhub.service` | — | — |
| deploy/README：数据库版本校验拒绝旧二进制 | deploy/README.md:55-56 | 已完成 | `src/storage/mod.rs:50 check_schema_version`；`tests/release_harden.rs:329` | — | — |
| deploy/README：备份只需 sqlite + master.key | deploy/README.md:44-47 | 已完成 | `src/security/mod.rs` | — | — |
| deploy/README：/health/ready 返回 200 | deploy/README.md:38 | 已完成 | `src/server.rs:59-70` | — | — |
| tests/sdk/README：OpenAI 覆盖表 | tests/sdk/README.md:34 | 部分完成 | `tests/sdk/acceptance.py:103-271` | 脚本里没有 delete/cancel/input_items | 不一致 |
| tests/sdk/README：Anthropic 覆盖表（含 count_tokens） | tests/sdk/README.md:35 | 已完成 | `tests/sdk/acceptance.py:278-365` | — | — |
| tests/sdk/README：Images 覆盖（images.generate） | tests/sdk/README.md:36 | 部分完成 | `tests/sdk/acceptance.py:369-387` | 不含 edits | 不一致 |
| NOTICES：本项目自身源码不采用 MIT | NOTICES.md:3 | 已完成 | 与 `LICENSE:1` 一致 | 与 `README.md:226` 冲突 | 不一致 |
| NOTICES：能力目录来源 LiteLLM 且只含能力 | NOTICES.md:43-45 | 已完成 | `assets/capabilities.json` 的 `source`/`note` 字段；`src/capability/mod.rs:237` | 没有生成脚本（见 §16.6 那条） | 不一致 |
| NOTICES：行为参考四个项目 | NOTICES.md:49-54 | 已完成 | `NOTICES.md:49-54` | — | — |
| LICENSE：非商业署名许可 | LICENSE:1 | 已完成 | `LICENSE:1-12`；`Cargo.toml:6 license-file` | 与 README:226 冲突 | 不一致 |
| LICENSE：署名要求 | LICENSE:19 | 已完成 | `LICENSE:14-19` | — | — |

### 4.17 文档未覆盖的已有功能（只记录，不计缺失）

以下是代码里存在、但 `计划.md` 与其余规划文档**完全没有提到**的功能。按任务要求单列，不算缺失。

| 已有功能 | 证据 | 说明 |
|---|---|---|
| 图片生成端点 `POST /v1/images/generations` | `src/gateway/mod.rs:33`、`:69-72`；`src/upstream/mod.rs:133-136,186`；`tests/images_e2e.rs` | 计划 §2.2（`计划.md:83`）明确写"不做"，属计划外的反向实现（既未覆盖也冲突） |
| 图片编辑端点 `POST /v1/images/edits`（multipart） | `src/gateway/mod.rs:34`、`:74-77`；`src/upstream/mod.rs:135,187`；`src/gateway/passthrough.rs:146` | 同上 |
| 网关托管后台任务全套 | `src/gateway/background.rs`（612 行）、`src/storage/schema.sql:284-304`、`tests/managed_background.rs`、`tests/managed_background_e2e.rs` | §29.1 标题明写"第二期设计草案"，代码已落地；属超前实现 |
| New API 站点级凭据复用（一个 Base URL 配一次） | `src/storage/schema.sql:275-280 new_api_sites`；`src/admin/mod.rs:242-246`；`src/multiplier/refresh.rs` | 计划只描述账号级凭据，没有"站点级复用"这一层 |
| 分组级 `allow_managed_background` 开关 | `src/domain/mod.rs:226`；`src/config/mod.rs:235`；`src/storage/mod.rs:154` 迁移 | §29.1 草案提到该开关名，但 §20.1 表结构没有这一列 |
| `request_attempts` 每次尝试明细表 | `src/storage/schema.sql:181-195`；`src/storage/mod.rs:22`（v2 迁移） | §20.1 表清单里没有它；是偏差审计建议后新增的 |
| 管理员改密码接口 `POST /admin/api/auth/password` | `src/admin/mod.rs:149`；`src/auth/session.rs:128 revoke_all`；`tests/admin_settings.rs:86` | §7.4 接口表与 §6.7 设置页清单都没有 |
| 账号可用模型聚合接口 `GET /admin/api/groups/{id}/available-models` | `src/admin/mod.rs:167-170`；`tests/admin_settings.rs:480` | §7.4 接口表未列 |
| 账号别名接口 `GET/PUT /admin/api/accounts/{id}/aliases` | `src/admin/mod.rs:216-219` | §7.4 未列 |
| New API 站点接口 `GET/POST/DELETE /admin/api/new-api-sites` | `src/admin/mod.rs:241-246` | §7.4 未列 |
| 校准记录列表接口 `GET /admin/api/accounts/{id}/calibrations` | `src/admin/mod.rs:193-196` | §7.4 只列了 `calibrate` |
| New API 分组查询接口 `GET /admin/api/accounts/{id}/multiplier-groups` | `src/admin/mod.rs:183-186` | §7.4 未列 |
| 请求趋势迷你图与分钟桶填充 | `web/src/components/TrendChart.tsx`；`src/admin/resources.rs:169-175`；`tests/admin_settings.rs:549` | §6.2 只写了"不提供自定义图表搭建器"，没有说趋势图 |
| 从请求记录深链接定位到目标/账号 | `web/src/pages/Requests.tsx:484-495`；`web/src/lib/store.ts` | 计划里没有这个交互 |
| 数据库 schema 版本迁移机制（v1→v3） | `src/storage/mod.rs:118-168 migrate`；`tests/release_harden.rs:350` | §27 阶段 6 提"升级检查"，但没有迁移阶梯设计 |

---

## 五、缺失清单（按严重程度分组，最严重在前）

### 5.1 阻断（规划里的主功能完全缺失，或已实现但主流程跑不通）

> 本次核对**没有发现主流程跑不通的功能**：三个入口、调度、粘性、故障切换、倍率保护、Responses 生命周期都能走通并有测试。以下两条是"规划明确承诺、代码里完全没有"的主功能级缺口。

| # | 缺口 | 出处 | 现状与证据 | 还差什么 |
|---|---|---|---|---|
| 1 | **`GET /admin/api/metrics` 完全不存在** | `计划.md:513` §7.4 | `src/admin/mod.rs:143-254` 无该路由；全仓 grep 零命中 | 需要实现"查询分钟聚合和目标性能"接口。当前目标性能只能从 `/admin/api/targets` 的 `score` 字段间接读到 |
| 2 | **`performance_buckets` 分钟聚合表与聚合任务不存在** | `计划.md:1468` §20.1、`计划.md:1519` §22 | `src/storage/schema.sql` 只有 19 张表，无此表；趋势图改为在 SQL 里按 `started_at / bucket_secs` 现算（`src/storage/store.rs:1036-1081`） | 需要建表 + 写一个"性能分钟桶聚合"后台任务。`src/app/tasks.rs:55-63` 目前只 spawn 4 类任务（§19.1 说 6 类、§22 列 7 类） |

### 5.2 缺失（功能存在但关键环节没做）

按"影响面 × 是否会被用户直接看到"排序。

**A. 数据与持久化**

| # | 缺口 | 出处 | 现状与证据 | 还差什么 |
|---|---|---|---|---|
| 3 | `runtime_target_state` 表未建，熔断/额度状态不持久化 | `计划.md:1461` | 状态全在内存 `src/health/mod.rs Registry` | 重启丢失全部冷却与额度状态。需要建表并在启动时恢复 |
| 4 | `retention_days=0` 时仍照常写 `request_records` | `计划.md:1593` §24.2 | 清理跳过 `src/app/tasks.rs:236-245`，但写入路径 `src/gateway/passthrough.rs:1068`、`src/gateway/settle.rs:171` 没有 retention 门 | 需要在写入侧判断保留天数；同时补"成本页只能显示当日数据"的降级行为（`§26.6` 项完全未实现） |
| 5 | 备份不含 `app_settings`，跨机恢复会使分组 Key 全部失效 | `计划.md:1567-1568` §23.5 | `src/security/backup.rs:33-45 BackupData` 无 settings 字段；`key_digest` 依赖本机主密钥 pepper | 需要把系统设置纳入备份；跨机恢复需给出提示或提供重新签发 Key 的路径 |
| 6 | `endpoint_evidence` / `capability_evidence` 未落表 | `计划.md:1458-1459` §20.1 | `src/upstream/evidence.rs:7-9`、`src/capability/mod.rs:114-116` 都明确说明不持久化 | 规划要求落表；当前重启后证据丢失，会重新试一次廉价失败。**注意**：代码注释给出了明确的取舍理由，属于"有意偏离"而非遗漏 |

**B. 调度与限额**

| # | 缺口 | 出处 | 现状与证据 | 还差什么 |
|---|---|---|---|---|
| 7 | 性能维（首字/速度）的归一化参照系不是整个分组 | `计划.md:611` §9.4 | `src/routing/score.rs:263-268` 的 `warm()` 只遍历 `candidates`（当次候选集）；倍率维用 `cheapest_in_group`（`src/routing/mod.rs:283-298`）正确按分组 | 计划承诺"每个目标只有一个分数、跨层可比"，当前性能三维的分数会随同组其他模型的目标变化。需要把参照系扩到整个分组的同类目标 |
| 8 | 评分缺"滞后防抖" | `计划.md:587` §9.3 | 全仓 grep 防抖/滞后/hyster 只命中 `计划.md` 自身 | 评分随每次采样立即变化。需要加滞回或最小变化阈值 |
| 9 | 评分信号缺"429/5xx/损坏响应比例"与"在途数占比" | `计划.md:583-584` §9.3 | `src/routing/score.rs` 只有 `success_rate`/`first_token_ms`/`total_ms`/`output_tps` | 需要按目标分别记录 429/5xx/损坏比例，并把在途数与最大并发占比接入评分 |
| 10 | "账号 + 模型"影响范围没有落点，429 只打目标级 | `计划.md:858` §12.1、`计划.md:888` §12.3 | `src/health/mod.rs:496-505 Outcome::RateLimited` 只操作 `self.target.circuit` | 同账号同模型下多个目标会各自冷却，与"429 优先影响账号 + 模型"不符。需要引入账号+模型维度的电路 |
| 11 | 倍率转 `multiplier_unknown` 时不主动清除该账号的粘性绑定 | `计划.md:818` §11.4 | 全仓只有一处 `sticky.clear`：`src/gateway/passthrough.rs:320`（同键下次请求时惰性清除） | 需要在倍率进入 unknown 时主动清除指向该账号的全部绑定 |
| 12 | 没有"每目标独立队列"结构 | `计划.md:973` §13.6 | `src/routing/queue.rs:33 GroupQueues` 是**分组级**容量信号量；逐目标等待靠 `src/gateway/passthrough.rs:458-590` 循环 + 每目标 Semaphore（`src/health/mod.rs:646-652`） | 行为等价，但目标队列长度**不可观测、不可限长**。要完全符合文档需要真正的目标级队列 |
| 13 | 等待期间配置改变不重新排候选 | `计划.md:1000` §13.6 | `src/gateway/passthrough.rs:571-587` 只重做准入与倍率终检；`group`/`plan` 是请求开始时的 Arc 快照 | 等待期间改优先级或分组不会重新 `plan`。需要唤醒后重新执行选择 |
| 14 | 批量刷新倍率无入口 | `计划.md:791` §11.3、`计划.md:1808` §27 阶段5 | `src/app/tasks.rs:31-49,95-104 RefreshHandle.force` 已存在，但没有 admin 路由也没有前端按钮 | 需要暴露 `POST /admin/api/multipliers/refresh-all` 类接口并在账号页加按钮 |
| 15 | 限制设为 0 时仍会无条件估算 Token | `计划.md:1285` §17.2 | `src/gateway/passthrough.rs:275 estimate_tokens` 在入口无条件调用 | 只是不参与限流，但仍有计算开销。文档要求"完全跳过" |
| 16 | "已知 Tokenizer"分支不存在 | `计划.md:1283` §17.2 | 全仓无 tiktoken/tokenizers 依赖 | 只有估算路径。要么明确把这条改成"第一期只做估算"，要么引入 tokenizer |

**C. 后台展示**

| # | 缺口 | 出处 | 现状与证据 | 还差什么 |
|---|---|---|---|---|
| 17 | 逻辑模型页缺"首字延迟 / 输出速度"列 | `计划.md:357` §6.5 | EWMA 在 `src/routing/score.rs:49-99`，但 `TargetDto`（`src/admin/resources.rs:1265-1284`）不回吐这两个值 | 后端 DTO 加字段，前端加列 |
| 18 | 逻辑模型页缺"暂停原因"列 | `计划.md:358` §6.5 | 全仓 grep `pause_reason` 零命中 | 需要结构化暂停原因（倍率超限/额度耗尽/Key 失效/冷却中），并在 DTO 与表格里展示 |
| 19 | 请求记录缺"粘性等待时长"独立字段 | `计划.md:372` §6.6、`计划.md:1580` §24.1 | `src/storage/schema.sql:166-168` 只有 `queued_ms`；粘性等待被累加进去（`src/gateway/passthrough.rs:563,951`） | 需要独立的列，同时记录"等待预算"与"缓存新鲜度系数"（§24.1 明确要求） |
| 20 | 请求记录缺"候选过滤原因"与"选中的层" | `计划.md:1578` §24.1 | 过滤细节只在内存 Telemetry/plan 里 | 需要落库 |
| 21 | 请求记录缺"倍率来源/资格判断结果/额度状态" | `计划.md:1582` §24.1 | 记录行只有 `effective_multiplier`；来源在 `multiplier_snapshots.source` | 需要在这条记录上补齐这三个字段 |
| 22 | 请求记录缺"输出速度" | `计划.md:1581` §24.1 | 只在内存 EWMA | 需要落库 |
| 23 | 请求记录缺缓存/思考 token 细分 | `计划.md:837` §11.6 | `src/storage/schema.sql:170-172` 只有 input/output | `canonical.rs:348-379 Usage` 里有 `cache_read`/`cache_write`/`reasoning`，需要落列 |
| 24 | 请求记录缺"切换原因"字段 | `计划.md:1579` §24.1 | 只能由 `outcome`+`error_code` 推断 | 需要独立列 |
| 25 | 请求记录页前端不显示 `config_version` | `计划.md:371` §6.6 | 后端已返回 `src/admin/resources.rs:2636`，`Requests.tsx:322-457` 无此列 | 前端加列即可 |
| 26 | 请求记录页缺"实际目标"筛选控件 | `计划.md:366` §6.6 | 后端支持 `target_id`/`account_id`（`src/admin/resources.rs:2567-2568`），前端 `Requests.tsx:24-31` 无该字段 | 前端加控件 |
| 27 | 请求记录表格没有"分组"列 | `计划.md:369` §6.6 | 后端返回 `group_id`（`src/admin/resources.rs:2615`），表格无此列 | 前端加列 |
| 28 | 概览目标数口径与 §12.2 不一致 | `计划.md:264` §6.2 | `src/admin/resources.rs:72-85` 用 `health::TargetStatus` 五态（`src/health/mod.rs:148-166`）；倍率过期/未知返回账号数组 | 需要：①与 §12.2 的 11 个状态对齐；②补 `degraded`；③按目标而非账号计数 |
| 29 | 概览/前端缺 `degraded`、`missing_model`、`deselected` 状态 | `计划.md:872,879,880` §12.2 | `src/health/mod.rs:148-154` 无这三个变体；`web/src/lib/types.ts:21-26` 只有 5 态 | 需要状态枚举补全，或明确把这三条降级为"由 enabled/存在性隐式表达并更新文档" |
| 30 | 分组行内没有"当前告警" | `计划.md:279` §6.3 | `GroupDto`（`src/admin/resources.rs:322-338`）无告警字段 | 需要按分组聚合告警 |
| 31 | 账号列表行内缺健康/额度徽标 | `计划.md:434-436` §6.9 | `AccountDto`（`src/admin/resources.rs:554-589`）无这两个字段 | 需要账号级健康与额度状态 |
| 32 | 分维得分缺"加权贡献" | `计划.md:355` §6.5 | `web/src/components/ui.tsx:418-440 ScoreMeter` 只画归一化得分 | 需要显示"得分 × 分组权重"的贡献值 |
| 33 | 成本页单目标缺说明文案 | `计划.md:409` §6.8 | `web/src/pages/Cost.tsx:151` 只有"单目标"徽标 | 需要补文档写的那句说明 |
| 34 | 成本页全局区不显示账号 token 绝对值 | `计划.md:419` §6.8 | `src/admin/resources.rs:2230` 已返回 tokens，`Cost.tsx:121-122` 只显示占比与次数 | 前端补即可 |
| 35 | 校准助手缺"统计区间"与"该模型用量" | `计划.md:426-427` §6.8 | `CalibrationDialog.tsx:57` 不传 `period_start`，后端固定最近 30 天（`src/admin/resources.rs:2273`）；结果只有请求次数 | 需要区间选择器 + 展示该账号该模型的 token 用量 |
| 36 | 设置页缺"适配器版本" | `计划.md:388` §6.7 | `src/admin/resources.rs:198-214` 无该字段 | 需要给协议适配层一个可展示的版本标识 |
| 37 | 首次启动不显示数据目录 | `计划.md:253` §6.1 | `src/app/mod.rs:385` 有 `data_dir`，但没有 API 回吐 | 需要在 setup/status 或 overview 里返回 |
| 38 | "允许运行时自动适配"前端无控件 | `计划.md:308` §6.4 | `web/src/lib/types.ts:64`、`api.ts:110` 有字段，`Accounts.tsx` 从不渲染 | 需要在账号编辑页加开关 |
| 39 | 账号编辑页缺"所属分组"控件；"上游类型"改了不生效也无提示 | `计划.md:300-305` §6.4 | `AccountPatch`（`src/admin/resources.rs:533-552`）无 `group_id` 也无 `upstream_type`；`Accounts.tsx:693-714` 仍提交这两个字段 | 需要：①前端编辑态明确禁掉上游类型（或后端支持改并给出迁移语义）；②停止提交被忽略的字段 |
| 40 | "你排除过"标记无法实现 | `计划.md:1185` §16.2 | `CatalogEntry`（`src/discovery/mod.rs:43-46`）只有 `selected`/`is_new`/`missing` | 需要区分"曾明确取消"与"从没出现过" |
| 41 | 自动同步缺常驻"已托管，共 N 个模型"指示 | `计划.md:1197` §16.2 | 只在对话框与开关提示里出现 | 需要账号列表级的常驻显示与计数接口 |
| 42 | 内置能力目录未接入路由决策 | `计划.md:1249-1251` §16.6 | `src/capability/mod.rs:49 builtin()` 只被 `src/admin/resources.rs:208` 显示用 | 计划承诺的证据优先级第 3、4 级实际不可达。需要把目录接入 `check_eligibility`/`endpoints::choices` |
| 43 | 能力目录没有生成脚本，revision 停在 2026-09-04 | `计划.md:1244` §16.6 | `scripts/` 只有 `release.sh` | 需要可复现的转换脚本与版本更新流程 |

**D. 协议与错误**

| # | 缺口 | 出处 | 现状与证据 | 还差什么 |
|---|---|---|---|---|
| 44 | 流内错误不带稳定错误码与请求 ID | `计划.md:1323,1325` §18.2 | `src/protocol/mod.rs:304-318 stream_error` 只有 message；`GatewayError::sse_event()`（`src/gateway/error.rs:178`）是死代码 | 需要把错误码与 request_id 注入 SSE 错误帧 |
| 45 | 引用（citation/annotations）完全未实现 | `计划.md:1047,1066` §14.4/§14.5 | grep `citation` 在 `src/` 零命中；`src/protocol/openai_responses.rs:1156` 硬编码 `"annotations": []` | 需要中间格式加引用内容块 + 各协议解析/发射 |
| 46 | 中间格式"未识别字段"只存字段名，不是"原始扩展区" | `计划.md:1053` §14.4 | `src/protocol/canonical.rs:231-232 unknown: Vec<String>` | 需要保存原值 |
| 47 | 端点选择缺"已确认支持 vs 能力未知"区分，且每目标最多 2 个端点 | `计划.md:1031-1037` §14.3 | `src/upstream/endpoints.rs:102-149`；`MAX_ENDPOINTS_PER_TARGET=2`（`:41`） | 5 级顺序实际只有 3 段。需要补区分逻辑，并说明截断是否有意 |
| 48 | Anthropic `signature_delta` 流式丢弃且不标记降级 | `计划.md:1064,1080` §14.5/§14.6 | `src/protocol/anthropic.rs:658-659` | 需要计入 degraded 标记 |
| 49 | 供应商原始扩展没有保留区 | `计划.md:1050` §14.4 | vendor 工具直接拒绝（`src/protocol/anthropic.rs:114`、`src/protocol/openai_responses.rs:108`） | 需要扩展区 |
| 50 | stop reason 不保留内部原始原因 | `计划.md:1082` §14.6 | grep `raw_stop` 零命中；`RequestRecord` 无对应字段 | 需要落库或在日志里保留 |
| 51 | usage 无字段时不放入允许的扩展区 | `计划.md:1083` §14.6 | `src/protocol/openai_responses.rs:792 emit_usage` 直接省略 | 需要写扩展区 |
| 52 | 未知参数在混合原因下返回 `no_eligible_target` 而非 `unsupported_parameter` | `计划.md:1084` §14.6 | `src/gateway/passthrough.rs:360-365`、`src/routing/mod.rs:400-411` | 只有全部目标都是 Unsupported 才映射 400 |
| 53 | `stop` 在 Responses 侧被当采样参数静默降级 | `计划.md:1099-1106` §14.8 | `src/protocol/openai_responses.rs:557-559` | 超出了 §14.8 白名单表，需要要么加入白名单表并标注，要么改成拒绝 |
| 54 | 托管后台任务执行时目标会重选 | `计划.md:1145` §15.3 | `src/gateway/background.rs:259-275 forward_no_managed` 重新走调度；登记字段不被读取 | "固定目标"只对原生路径成立。需要让托管执行复用登记的目标 |
| 55 | count_tokens 的"本地精确计算"档不存在 | `计划.md:1156` §15.5 | 全仓无 tokenizer | 需要引入 tokenizer，或更新文档承认这一档不做 |

**E. 安全与运维**

| # | 缺口 | 出处 | 现状与证据 | 还差什么 |
|---|---|---|---|---|
| 56 | HTTPS 下 Cookie 不设 `Secure` | `计划.md:1542` §23.2 | `src/admin/mod.rs:119-124` 明确注释永不设置 | 需要一个配置项或按 `X-Forwarded-Proto` 自动判断 |
| 57 | `/backup/export` 可导出明文上游 Key | `计划.md:1545` §23.2 | `src/security/backup.rs:39`、`src/storage/store.rs:1375-1386` | 与"不提供查看完整 Key 的接口"精神冲突。需要一个明确的授权/提示流程，或在文档里承认这是设计范围内的例外 |
| 58 | 分组 Key 鉴权不做常量时间比较 | `计划.md:1385` §19.2 | `src/config/mod.rs:63-69` HashMap 摘要查表；无 `subtle`/`ct_eq` | 需要常量时间比较 |
| 59 | 脱敏只有 4 处显式调用，无全局兜底 | `计划.md:1487` §20.2、`计划.md:1561` §23.4 | `src/security/redact.rs:16-52`；调用点 `src/multiplier/refresh.rs:334`、`src/admin/resources.rs:881,936`、`src/gateway/passthrough.rs:1786` | 需要 tracing layer 兜底 |
| 60 | 写接口没有 `config_version` 与 409 `config_conflict` | `计划.md:517` §7.4 | 所有写 DTO（`src/admin/resources.rs:298-320,509-552,1245-1262`）无该字段 | 两个浏览器同时编辑会静默互相覆盖。需要乐观锁 |
| 61 | 除 `/requests` 外所有列表接口不分页 | `计划.md:517` §7.4 | `list_groups` `src/admin/resources.rs:360-370` 等 7 个接口全量返回 | 需要统一分页 |
| 62 | 校验分散在各 handler，不在配置事务内 | `计划.md:1497-1505` §21 | `src/config/mod.rs:137-214 build()` 只做跨组/死引用过滤；`reload()` 在提交之后调用 | 需要在事务内读候选配置并统一校验（权重总和、Key 唯一性、引用完整性） |
| 63 | 模型列表接口不校验 Content-Type | `计划.md:1553` §23.3 | `src/upstream/mod.rs:226,242` 有大小与超时 | 加一个 Content-Type 校验 |
| 64 | 分组页缺 `GET`/`PATCH` 等 4 个接口细节偏差 | `计划.md:500,503,504,505,508,509,513` §7.4 | 见 §4.4 表格 | `accounts/{id}` 缺 GET、`logical-models/{id}` 缺 GET、`targets` 集合无 PATCH/DELETE、3 条路径名不符、1 条完全缺失 |
| 65 | 审计日志 `result` 恒为 "ok" | `计划.md:517` §7.4 | `src/admin/resources.rs:2803-2811 audit()` | 失败路径不写审计。需要记录失败结果 |
| 66 | sticky/perf/health 内存表无容量上限 | `计划.md:1438` §19.4 | `src/routing/sticky.rs:302`、`src/routing/score.rs:205`、`src/health/mod.rs:191` 只有 `retain` 按 live id 清理 | 需要显式容量上限 |
| 67 | 流式无显式背压 | `plan` §19.4:1439 | `src/gateway/stream.rs` 直传 | 只靠 TCP 反压。需要水位/有界缓冲 |
| 68 | 无 `catch_unwind` 隔离 | `计划.md:1440` §19.4 | 全仓零命中；靠 tokio 任务级隔离 | 需要 panic 中间件 |
| 69 | 磁盘满无任何降级分支 | `计划.md:1732` §26.8 | grep 无磁盘空间检查 | SQLite 写失败只报错，没有可控降级 |
| 70 | 队列在途只做间接取消 | `计划.md:968` §13.5 | `src/gateway/settle.rs:132-137` 只结算；靠生成器丢弃后 reqwest Response 析构 | 需要主动 abort |

### 5.3 待确认（规划本身或现状证据不足，无法判定）

| # | 条目 | 出处 | 为什么无法判定 | 需要什么信息才能判定 |
|---|---|---|---|---|
| 71 | "零目标自动清理"是否覆盖管理端删目标路径 | `计划.md:481` §7.3 | 清理函数 `src/discovery/mod.rs:454 cleanup_auto_model` 只在模型同步路径被调用（`:203,418`）；管理员在"目标"页删最后一个目标时是否触发清理，代码里找不到路径 | 需要产品口径：管理端删目标是否也要触发自动清理？还是只有"取消勾选"才清理？ |
| 72 | 在途期间倍率升高，请求是否真的继续完成 | `计划.md:1691` §26.4、`计划.md:829` §11.5 | 终检只在发送前执行（`src/gateway/passthrough.rs:601`），逻辑上已经满足，但没有"发往上游后改倍率仍完成"的测试 | 需要一个把倍率改高的用例来证实；目前只有口头承诺 |
| 73 | 原生上游时 `background=true` 是否走 `resp_akh_` 代理路径 | `计划.md:1894` §29.1 验收5 | `src/gateway/background.rs:529-532` 在原生可用时 return None，随后确实走普通转发拿 `resp_akh_`，但没有断言 | 需要一个"上游声明支持后台"的假上游用例，断言 ID 前缀为 `resp_akh_` |

### 5.4 不一致与文档过时（行为与文档不符，或文档未同步）

这些不是功能缺失，但会让读者按文档操作时对不上。

| # | 条目 | 两份说法 | 现状 |
|---|---|---|---|
| 74 | **图片端点的做与不做** | `计划.md:83` §2.2 写"不做图片生成…独立 API 端点" vs `README.md:30` 声称已实现 | 代码有：`src/gateway/mod.rs:33-34`、`tests/images_e2e.rs`（7 用例）。规划文本至今未改 |
| 75 | **README 的许可证陈述** | `README.md:5-7` 写"非商业许可" vs `README.md:226` 写"本项目以 MIT 许可发布" | `LICENSE:1`、`NOTICES.md:3` 都支持前者。**第 226 行是错误陈述** |
| 76 | README 声称的粘性缺陷已不存在 | `README.md:33-35` vs `src/gateway/passthrough.rs:306-315` | 缺陷已在提交 8dd3e08 修复并在 `2026-09-06_功能审查-Akhub-report.md:25` 标 fixed，README 未同步 |
| 77 | README 声称 Docker 工件待完成 | `README.md:80` vs `dist/` + `Dockerfile` + `release.yml` | 双架构二进制与 Docker 构建脚本都已存在 |
| 78 | README 声称关闭取消排队请求待完成 | `README.md:82` vs `src/routing/queue.rs:130-152` | 已实现（提交 0d329c7），README 最后一次修改早于修复 25 分钟 |
| 79 | `preferred_endpoint` 字段名 | `计划.md:1022` vs `src/domain/mod.rs:238 preferred_protocol` | 实现用的是协议名而非端点名 |
| 80 | 写接口路径名 3 处不符 | `计划.md:503,504,505` vs `src/admin/mod.rs:179,205,209` | `models/fetch`→`models/refresh`、`models/selection`(PUT)→`models/select`(POST)、`multiplier/refresh`→`refresh-multiplier` |
| 81 | §20.1 表名 5 处不符 + 2 张未建 | `计划.md:1453-1466` vs `src/storage/schema.sql` | 见 §4.12 对应行。实际 19 张表 |
| 82 | §19.3 模块目录 5 处不存在 | `计划.md:1415-1423` vs `src/` | `catalog/`、`cost/`、`state/`、`observability/`、`tests/fixtures/` 都不存在 |
| 83 | 加权均倍率口径 | `计划.md:843` "Σ(倍率×token)/Σ(token)" vs `src/admin/resources.rs:2141-2143,2167-2171` | 实现按请求次数加权 |
| 84 | 相对最贵节省口径 | `计划.md:844` "1 - 加权均/最贵" vs `src/admin/resources.rs:2172-2178` | 实现的是"1 - 最便宜/加权均"（可再省比例） |
| 85 | 性能历史保留期 | `计划.md:1591` "默认 30 天" vs `src/app/tasks.rs:26-27 SNAPSHOT_HORIZON=24h` | 元数据 30 天、性能快照 24 小时，两个口径并存 |
| 86 | 每目标最多尝试端点 2 个 | `计划.md:1031-1037` 5 级顺序 vs `src/upstream/endpoints.rs:41` | 截断与 5 级顺序的意图不一致 |
| 87 | §27 阶段 4 验收"10 次操作" | `计划.md:1802` vs `tests/discovery_selection.rs:485` | 测试实际发出约 24 次 HTTP 调用 |
| 88 | 六方向黄金"夹具" | `计划.md:1633` vs `tests/protocol_e2e.rs:33` | 是 Rust 内联 JSON，没有独立 fixture 文件 |
| 89 | SDK 验收覆盖 | `计划.md:1659` 要求 delete/cancel/input_items vs `tests/sdk/acceptance.py` | 脚本里没有这三个用例，只有查询 |
| 90 | 验收文档声称 edits 通过 | `review/2026-09-15-真实上游验收.md:21` vs `tests/sdk/acceptance.py:369-387` | 脚本只有 generate，没有 edits 用例，无法复现 |
| 91 | 后台任务数量 | `计划.md:1367` 说 6 类、`计划.md:1515-1521` 列 7 条 vs `src/app/tasks.rs:55-63` 实际 4 类 | 数量对不上（部分任务合并实现） |
| 92 | 生产配置与日志写未分离连接池 | `计划.md:1437` §19.4 vs `src/storage/mod.rs:36-47` | 共用一个 `SqlitePool` |
| 93 | 复制后是否打开编辑页 | `计划.md:296` vs `web/src/pages/Accounts.tsx:166-177` | 实现停留列表（偏差审计 P3 已记录，仍未做） |
| 94 | §29.1 第一期口径 | `计划.md:1890` 验收1 要求 400 vs `tests/managed_background_e2e.rs:42` 实际 200 | 未开开关时走普通转发，不返回 400 |

---

## 六、核对方法与复核指引

- **方法**：以 `计划.md` 为主轴逐节拆条，辅以 `README.md`、`review/` 下 4 份报告、`deploy/README.md`、`tests/sdk/README.md`、`NOTICES.md`、`LICENSE`。每条都到源码/前端/测试里找对应实现，证据一律取"文件路径 + 行号"或"唯一函数/测试名"。
- **复核方式**：本报告所有证据都可以直接用编辑器跳到对应行，或 `grep` 对应函数名。测试类证据（`tests/*.rs`、`tests/sdk/acceptance.py`）可直接 `cargo test` / `python3 tests/sdk/acceptance.py` 复跑。
- **本次未做的事**：没有修改任何代码、规划文档、依赖、构建配置或运行行为；没有新增 Git 提交；没有调用真实上游；没有跑 `cargo test`（只做静态核对，测试通过与否以仓库内既有记录为准）。
- **已知局限**：①性能验收数据来自 `review/2026-09-15-真实上游验收.md` 的既有记录，本次未复测；②Docker 镜像是否真的推到 GHCR 无法离线验证；③部分"部分完成"的判定依赖对文档意图的解读，已在每条"缺口说明"里写清依据，若口径不同可以直接按证据重新判定。
