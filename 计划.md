# Akhub 完整产品与技术方案

> 文档状态：需求已确认，可直接进入实施设计与开发拆分。
>
> 更新日期：2026-09-01（第二版修订，含 22 条设计评审决议）
>
> 产品定位：一个简单、可靠、高性能的 AI 协议网关与组内负载均衡工具，用来替代 AxonHub 和 LiteLLM 的中间网关用途。

## 零、第二版修订摘要

本版基于一次逐条设计评审，对第一版做了如下实质修改。带 ⚠️ 的是**推翻**第一版明确写法的条目。

| # | 决议 | 影响 |
|---|---|---|
| 1 | ⚠️ 严格阶梯：不同优先级严格顺序不可越，相同优先级为同一层 | §9 重写 |
| 2 | ⚠️ 砍掉"自适应组"实体，用"相同优先级"表达 | §9 |
| 3 | 比值归一化，零硬编码刻度 | §9.4 |
| 4 | 归一化参照系=整个分组，选择作用域=当前层 | §9.4 |
| 5 | ⚠️ 调度权重 40/25/20/15，分组级可调 | §9.4、§6.3 |
| 6 | ⚠️ 层内按分数高次幂加权随机，非赢家通吃 | §9.5 |
| 7 | ⚠️ 尝试预算按成本+时间计，不按次数；删除"最大尝试数"设置项 | §13.1、§3、§6.3 |
| 8 | ⚠️ 每调度目标独立队列 + 分组总容量上限 | §13.6 |
| 9 | ⚠️ 能力降级白名单，仅故障切换时生效 | §14.1、§9.1、§14.6 |
| 10 | 网关错误码到 HTTP 状态码的完整映射 | §18.3 新增 |
| 11 | ⚠️ 倍率=账号级 Key/分组倍率，不下沉到模型；新增校准系数 | §11 重写 |
| 12 | NewAPI 自动倍率探针改为 `/api/user/self/groups`，需两个额外凭据 | §11.2、§6.4 |
| 13 | ⚠️ 倍率刷新失败按风险余量宽限，新增 `multiplier_stale` 状态 | §11.4、§12.2 |
| 14 | 新增成本页与校准助手 | §6.8 新增 |
| 15 | ⚠️ 粘性键只哈希稳定前缀，不含用户消息 | §10.1 |
| 16 | ⚠️ 粘性等待预算=体积 8 层阶梯 × 缓存新鲜度 3 档 | §10.3 |
| 17 | ⚠️ 粘性绑定与性能快照持久化 | §20.1 |
| 18 | 客户端范围确认为全部三类协议，Responses 全套保留 | §2.1 |
| 19 | ⚠️ 阶段 1 改为三入口原生透传 | §27 重写 |
| 20 | ⚠️ 调度目标自动归并同名模型 + 账号级模型别名 | §16、§6.5 |
| 21 | 模型选择集持久化，自动同步为临时覆盖 | §16.2 |
| 22 | ⚠️ 成本口径按逻辑模型拆分，不跨模型加总 | §6.8、§11.6 |

## 一、直接结论

Akhub 使用 Rust 开发，第一期采用单实例加 SQLite，发布 Docker 镜像和 Linux 原生可执行文件。它只负责以下三件事：

1. 把不同下游协议无损转换到合适的上游协议。
2. 在同一分组内，按**严格的优先级阶梯**选层，在层内按综合评分选目标。
3. 在不重复浪费慢请求、不中途拼接流式响应的前提下完成故障切换。

Akhub 不做计费、账单、充值、用户体系、租户体系或复杂权限。倍率不是收费功能，只是上游成本参考和"不亏本"保护条件。

Akhub 提供**成本可见性**，但这与计费无关：计费是向下游收钱，成本可见性是你自己对账。

后台设置必须比 LiteLLM 简单。管理员不需要填写模型上下文长度、最大输出长度、是否支持图片、是否支持工具或是否支持思考等模型卡信息。这些信息由内置开源能力目录、协议适配规则和真实调用结果自动处理。

## 二、目标与非目标

### 2.1 核心目标

- 下游只使用逻辑模型名，不感知真实上游、真实模型名、账号、API Key 或倍率。
- 同一逻辑模型可以绑定多个"上游账号 + 具体模型"，并在组内稳定调度。
- **人工优先级是硬排序，所见即所得**：高优先级层只要还有合格目标，绝不使用低优先级层。自适应只在同层内部起作用。
- 同一会话尽量粘在同一上游账号，保留供应商缓存和会话状态。
- 上游故障时自动切换，但上游只是慢时不并行复制请求，避免产生双份费用。
- OpenAI、OpenAI-compatible 和 Anthropic 三类协议之间尽可能完整转换。
- 文本、图片输入、标准函数工具、结构化输出和流式事件不能被静默删除；思考内容仅在故障切换且目标不支持时允许降级，并显式标记。
- 自动获取 New API、Sub2API 的上游倍率；其他上游允许手动设置。
- 上游倍率未知时按风险余量给予宽限期；宽限期结束或倍率超过分组上限时阻止新请求。
- 账号刷新模型列表后自动归并同名模型为调度目标，不需要逐个手工绑定。
- 热路径不等待 SQLite 写入，保证 Rust 实现的性能优势真正落地。
- 重启后粘性绑定和性能数据可恢复，不存在长时间的盲调度期。

支持的下游客户端范围（已确认，全部为一期目标）：

- Claude Code 与 Anthropic 官方 SDK（`/v1/messages`，依赖 `count_tokens`）
- OpenAI Chat 类客户端（Cherry Studio、油猴脚本、自写程序等）
- Codex CLI 与 OpenAI Responses 客户端
- 第三方 agent 软件（zcode、dsh、hermes、openclaw 等）
- 自己的程序，或下游再套一层网关

### 2.2 第一期明确不做

- 不做任何下游计费、账单、充值、套餐、余额扣减或利润报表。**成本页只做自己的相对消耗对账，不产生任何下游账单。**
- 不做多租户、RBAC、多个管理员或普通用户后台。
- 不跨分组调度，不提供跨组兜底。兜底通过分组内的低优先级层表达。
- 不做 OpenAI Responses WebSocket。
- 不做语音、Embedding、视频等独立 API 端点。
- ~~不做图片生成~~ **已修订（2026-09-17）**：图片生成与编辑端点按管理员要求纳入第一期，
  见 §7.1 与 §14.9；仅原生转发到 OpenAI 兼容上游，不做跨协议转换与 provider 适配。
- 不做多实例集群、Redis 依赖或分布式一致性。
- 不做 Windows 发行版，Windows 留到后续版本。
- 不做运行时脚本插件或任意代码插件系统。
- 不要求用户维护模型上下文、多模态、工具、思考等能力字段。
- **不维护模型价格表。** 成本口径只在同一逻辑模型内部横向比较。

## 三、已确认的产品决策

| 主题 | 最终决策 |
|---|---|
| 开发语言 | Rust，主要原因是低开销、高并发和可控内存占用 |
| 运行方式 | 第一期单实例，SQLite；Docker 与 Linux 原生版同时发布 |
| 管理员 | 单管理员，首次启动必须设置密码 |
| 分组 | 调度硬边界，不允许跨组 |
| 下游 Key | 每个分组一个 Key，同时接受 `Authorization: Bearer` 和 `x-api-key` |
| 逻辑模型 | 分组内暴露逻辑模型名，隐藏具体上游模型 |
| 调度单元 | "账号/API Key + 具体上游模型"；同一个 Key 下的不同具体模型独立调度 |
| 账号复制 | 一键直接复制，所有配置独立，Key 和状态不共享 |
| **人工优先级** | **硬分层。不同优先级严格顺序不可越；相同优先级为同一层** |
| **自适应** | **只在层内决定选谁。想让几个账号之间自动调节，就给它们同一个优先级数字** |
| **调度权重** | **倍率 40 / 可靠性 25 / 首字延迟 20 / 输出速度 15，分组级可调，总和 100** |
| 倍率 | 账号级的 Key/分组倍率（如 x1、x0.5），不是模型价格。只用于成本排序和硬性保护 |
| **校准系数** | **账号级，手填，默认 1.0。`有效倍率 = 上游倍率 × 校准系数`** |
| 倍率保护 | 有效倍率必须小于或等于分组倍率，否则暂停新请求 |
| **倍率未知** | **按风险余量给宽限期（≤60% 给 60 分钟，60–90% 给 15 分钟，>90% 立即停）；半数账号同时失败判定为探针系统性故障** |
| 中途变更 | 已在途请求完成；新请求和后续重试使用最新倍率重新校验 |
| **尝试预算** | **不按次数计。约束为"每目标最多一次 + 请求总超时 + 硬上限 8 个目标"；廉价失败不计入** |
| **队列** | **每调度目标一个独立队列；分组只设总容量上限，默认 100** |
| 请求体 | 8 MB 内存缓冲，超过后使用临时文件；单请求默认上限 64 MB |
| **模型拉取** | **手动获取时弹出勾选列表，选择集持久保存；打开"自动同步"则全量托管** |
| **目标生成** | **同名模型自动归并为同一逻辑模型的候选目标；名字不一致用账号级模型别名解决** |
| 空模型列表 | 拉取报错、格式错误或得到 0 个模型时，不改变现有列表 |
| 测试按钮 | 发送一次真实的 `hi`；测试数据不参与自适应、粘性和正常重试统计 |
| 正文日志 | 普通请求不保存正文 |
| 元数据保留 | 默认 30 天，管理员可调；设为 0 时只保留实时聚合数据 |
| Responses 状态 | 仅为 `previous_response_id`、`conversation` 和 HTTP 生命周期保存必要加密状态 |
| **重启恢复** | **粘性绑定与性能 EWMA 快照持久化，重启后直接加载，不重新学习** |
| 配置生效 | 不可变版本快照原子切换；在途请求使用旧快照，新请求使用新快照 |

## 四、核心概念

### 4.1 分组

分组是完全独立的调用空间，包含：

- 一个下游 API Key。
- 一个分组倍率上限。
- 一组逻辑模型。
- 一组独立上游账号。
- 四个调度权重（倍率、可靠性、首字延迟、输出速度）。
- 队列总容量、等待时间和降级开关。

分组之间不共享账号、Key、粘性绑定、倍率状态、健康状态或性能数据。

**分组倍率上限是绝对红线，不是偏好。** 它表达"我绝对不接受超过这个价"。"我偏好用便宜的"由优先级表达。想要"全部挂掉时才用贵的官方直连"，做法是把它放在最低优先级层，同时把分组上限提到能容纳它的水平——严格阶梯保证日常永远不会碰到它。

### 4.2 上游账号

一个上游账号代表一套独立凭据和连接配置，包括：

- 上游类型、Base URL、API Key。
- 首选协议与"允许运行时自动适配"。
- 默认人工优先级。
- 倍率模式（自动 / 手动）与**校准系数**。
- 上游类型为 New API 且倍率模式为自动时，额外的**访问令牌**与**用户 ID**。
- **模型别名表**（上游真名 → 对外逻辑模型名）。
- **模型选择集**（本账号已纳入调度的模型集合）与"自动同步上游模型"开关。
- RPM、TPM、最大并发限制。

同一个真实 Key 如果要放入两个分组，必须复制成两个独立账号记录。复制后的记录最初可以拥有相同 Key，但修改、健康状态、倍率状态和调度状态完全独立。

### 4.3 上游模型

上游模型是某个账号实际返回或人工录入的模型，例如 `glm-5.3-bf16`。上游模型只属于一个账号。

上游模型是否参与调度，由该账号的**模型选择集**决定，见 §16.2。

### 4.4 逻辑模型

逻辑模型是下游看到的名称，例如 `glm-5.3`。

逻辑模型有两种来源：

- **自动创建**：勾选某个上游模型且同名逻辑模型不存在时自动创建，名称即上游模型名。当它的最后一个调度目标被移除时自动清理。
- **手工创建**：管理员显式创建并指定对外名称。零目标时保留记录，但从 `/v1/models` 移除。

### 4.5 调度目标与层

调度目标是以下三者的确定组合：

`分组 + 上游账号 + 具体上游模型`

例如同一个 Key 下的 `glm-5.3-bf16` 和 `glm-5.3-pf8` 是两个目标，必须分别记录优先级、模型错误、性能和能力证据。账号级额度、Key 失效和最大总并发仍由两个目标共享。

**层**是优先级数字相同的一批调度目标。层是自动形成的，不需要任何配置：

```
分组「主力」                     优先级
  ├─ 账号A / sonnet              100  ┐
  ├─ 账号B / sonnet              100  ┘── 第 1 层
  ├─ 账号C / sonnet               60  ─── 第 2 层
  ├─ 账号D / sonnet               30  ┐
  └─ 账号E / sonnet               30  ┘── 第 3 层
```

把账号 C 的优先级改成 100，它就并入第 1 层，与 A、B 一起参与自适应。这是表达"这几个账号之间允许自动调节"的唯一方式，系统中不存在独立的"自适应组"实体。

## 五、参考项目的取舍

### 5.1 New API

主要借鉴：

- 上游渠道与模型列表管理方式，特别是"渠道声明模型名 + 模型重定向"这一组合，它让管理员不必逐个手工创建逻辑模型（`model/ability.go`、`model/channel.go`）。
- 模型列表安全刷新和差异应用。
- 协议适配器的组织方式（`relay/channel/*/adaptor.go`）。
- 跨协议转换时对无法表达字段的**降级**处理。`service/convert.go` 的 `ClaudeToOpenAIRequest` 只处理 `text`/`image`/`tool_use`/`tool_result` 四类内容块，对 `thinking` 与 `redacted_thinking` 既不报错也不转换而是直接丢弃——这是它能让 Claude Code 打通非 Anthropic 上游的原因。Akhub 采纳这一行为口径，但补上它缺失的可观测性。
- 会话粘性、请求体缓存和渠道测试交互。
- Responses HTTP 接口的兼容处理。

**明确不采用**：其优先级机制。`model/channel_cache.go:139-142` 用 `sortedUniquePriorities[retry]` 把重试序号直接当作优先级层序号——第一次失败就整层放弃最高优先级，且层数超过重试上限后永远不可达，层内又是无视健康状态的加权随机。这正是"优先级功能无效"的根源。

同样不采用：计费、用户、套餐、充值、渠道类型堆叠和大量全局倍率配置。

### 5.2 Sub2API

主要借鉴：

- 账号、分组和独立复制思路。
- 真实请求驱动的状态学习。
- Failover 循环、半开恢复和会话粘性。
- `/v1/sub2api/billing` 的 Key 级倍率探针。
- 倍率保护在选号后、真正发请求前再次校验的做法。
- Responses 思考、工具和状态切换相关处理。

不采用：下游计费、用户额度、订阅、OAuth 账号体系和跨平台业务逻辑。

### 5.3 LiteLLM

主要借鉴：

- OpenAI Chat、OpenAI Responses、Anthropic Messages 的完整转换边界。
- 模型能力目录和参数支持规则。
- 流式事件状态机与错误映射。
- Responses 的创建、查询、删除、取消、输入项和压缩接口。
- 未知能力、供应商扩展和原生透传的处理原则。

不采用：复杂配置文件、虚拟 Key 管理、预算、团队、插件回调、企业功能和庞大的路由参数面板。也不采用其模型目录中的价格数据——Akhub 不维护价格表。

### 5.4 AxonHub

只借鉴首次使用和基础配置较直观的部分。明确不采用要求管理员填写上下文大小、输出长度、多模态、工具能力等模型元数据的设计。

### 5.5 许可证边界

- New API 是 AGPL-3.0，Sub2API 是 LGPL-3.0；只借鉴公开行为和架构思路，不直接复制其实现代码。
- AxonHub 主体为 Apache-2.0，但部分目录使用 LGPL-3.0；同样只做行为参考。
- LiteLLM 的非企业部分为 MIT；能力目录可以按许可引入，但必须保留来源、版本和许可声明。
- Akhub 的协议适配代码使用干净实现，并以官方协议样例和自建测试夹具验证。

## 六、后台页面与操作流程

后台采用一个嵌入 Rust 二进制的前端应用，不需要单独部署前端服务。桌面和手机都可使用，但主要面向桌面管理。

### 6.1 首次启动

首次打开后台时只进行以下操作：

1. 设置管理员密码。
2. 显示数据目录和主密钥状态。
3. 进入空的概览页。

不强迫用户在向导中创建分组或账号。

### 6.2 概览页

显示：

- 当前请求量、成功率、平均延迟和高位延迟。
- 在途请求数、排队请求数和队列超时数。
- 可用、降级、暂停、倍率过期和倍率未知的目标数量。
- 最近错误和最近配置变化。
- 倍率探针系统性故障告警（红色，见 §11.4）。

不提供自定义图表搭建器。

### 6.3 分组页

每个分组显示：

- 名称和下游 Key 前缀。
- 分组倍率上限。
- **调度权重四项**：倍率、可靠性、首字延迟、输出速度，总和必须为 100，默认 `40 / 25 / 20 / 15`。
- 队列总容量和最长等待时间。
- 故障切换时允许能力降级（默认开启）。
- 逻辑模型数量、可用目标数量和当前告警。

**不再有"最大尝试目标数"设置项**，尝试预算改为按成本和时间计算，见 §13.1。

Key 创建后只完整显示一次。之后只显示前缀，并提供重新生成按钮。重新生成后旧 Key 立即失效。

### 6.4 账号页

账号列表可按分组、状态和上游类型筛选。每行提供：

- 编辑。
- 一键复制。
- 测试连接。
- 获取模型。
- 手动刷新倍率。
- 启用或停用。

复制按钮不弹出复杂向导：直接创建一个停用状态的"名称 - 副本"，复制静态配置、模型选择集和模型别名，重新加密 Key，重置健康、粘性、性能和自动倍率状态，然后打开编辑页。修改分组后，原分组的逻辑模型绑定会被清除，避免跨组引用。

账号编辑页字段：

```
  名称            [                            ]
  所属分组         [ 主力                    v ]
  上游类型         [ NewAPI                  v ]
  Base URL        [                            ]
  API Key         [ sk-...                     ]

  首选协议         [ OpenAI Chat             v ]
  [✓] 允许运行时自动适配

  默认人工优先级    [ 50 ]      0-100，数值越大越优先

  倍率模式         (●) 自动    ( ) 手动
  ┌─ 自动所需凭据（仅 NewAPI 类型显示）──────────┐
  │ 访问令牌  [                                ] │
  │ 用户 ID   [                                ] │
  │ 在 NewAPI 个人设置页生成，与 API Key 不同    │
  └──────────────────────────────────────────────┘
  校准系数         [ 1.00 ]    见 §11.1 与 §6.8

  [ ] 自动同步上游模型     打开后全量托管，忽略模型选择集

  模型别名（可选）
    上游真名                    对外名
    claude-sonnet-4-5-20250929  claude-sonnet-4-5

  RPM [ 不限 ]   TPM [ 不限 ]   最大并发 [ 不限 ]
```

倍率模式选"手动"时，两个凭据框直接消失。Sub2API 类型不显示这两个框，它的 `/v1/sub2api/billing` 是 Key 级探针，一把 API Key 就够。

不会出现上下文长度、最大输出长度、多模态、工具或思考能力输入框。

### 6.5 逻辑模型页

逻辑模型和调度目标**默认自动生成**，不需要逐个手工创建：

1. 建账号，给一个默认优先级。
2. 点"获取模型"，勾选要用的模型（见 §16.2）。
3. 勾选的模型自动创建或加入对应逻辑模型，成为候选目标，优先级继承账号默认值。

只有以下情况需要手工操作：

- 覆盖某个目标的优先级（把它提到或压到另一层）。
- 排除某个自动生成的目标。
- 手工创建逻辑模型以使用自定义对外名。

目标行显示：

| 列 | 说明 |
|---|---|
| 目标 | 账号 / 上游模型名 |
| 优先级 | 有效优先级（账号默认值或目标覆盖值），同值即同层 |
| 有效倍率 | `上游倍率 × 校准系数` |
| **综合评分** | 四维加权总分，全分组可比，见 §9.4 |
| 分维得分 | 倍率 / 可靠性 / 首字 / 速度，各自的归一化得分与加权贡献 |
| 状态 | 见 §12.2 |
| 首字延迟 / 输出速度 | 当前 EWMA 值 |
| 暂停原因 | 有则显示 |

综合评分是**全分组统一参照系**下计算的，所以这一列跨层可比。这是一个诊断视图：你能一眼看出"我把评分最高的目标压在第 2 层了"，然后自己决定是否把它提上来。

分维得分必须展开可见——这是为了让分组权重调错时能自我诊断。任何"为什么它老选那个号"的疑问都应该在这张表里直接有答案。

### 6.6 请求记录页

支持按时间、请求 ID、分组、逻辑模型、实际目标、状态和错误码筛选。记录内容包括：

- 请求 ID、开始时间、协议和是否流式。
- 分组和逻辑模型。
- 每次尝试的目标、端点、耗时和失败原因，以及该次失败是否计入尝试预算。
- 首字延迟、总延迟、Token、切换次数和配置版本。
- 粘性键命中与否、粘性等待时长。
- **能力降级标记**（发生降级的请求标红，注明降级了哪些能力）。
- 最终网关错误码与返回的 HTTP 状态码。

默认不显示也不保存消息正文、图片、工具参数或思考内容。

### 6.7 设置页

只保留以下系统设置：

- 自动倍率刷新间隔。
- 自动模型同步间隔。
- 请求元数据保留天数。
- Responses 可重放状态保留天数。
- 请求总超时和请求体上限。
- 配置备份与恢复。
- 当前版本、模型目录版本和适配器版本。

高级算法参数不进入后台。调度权重属于分组级设置，见 §6.3。

### 6.8 成本页

成本页以**逻辑模型**为单位组织，不做跨模型加总。

```
claude-sonnet-4-5              本月 100 万 token · 1,240 次请求
  账号A    有效倍率 0.50    72% 流量
  账号B    有效倍率 0.80    28% 流量
  ────────────────────────────────────────
  加权均倍率   0.58
  最便宜目标   0.50     只用它会更省，但没有备份
  最贵目标     0.80
  相对最贵节省 27%

glm-4.6                        本月 500 万 token · 3,102 次请求
  账号A    有效倍率 0.50   100% 流量
  ────────────────────────────────────────
  只有一个目标，无调度空间
```

**为什么不跨模型加总**：倍率是折扣，不是价格。`倍率 × token` 只在同一个逻辑模型内部与真实花销成正比；跨模型相加会被模型组合严重扭曲。例如某账号本月 claude-sonnet 用 100 万 token、glm-4.6 用 500 万 token，倍率同为 0.5，按加总口径 glm 占 83%，但两者真实花销其实各占一半。

**为什么同时显示最便宜目标**：只显示"相对最贵节省 27%"是自我恭维。如果你本来就只用最便宜的账号 A（0.50），Akhub 让你用了 0.58，实际多花了 16%——换来的是不中断。这个交换真实存在，数字必须让你看得见。

全局区域只显示不失真的量：

- token 总用量、请求总数。
- 各账号的请求数与 token 占比。
- 不出现任何跨模型的"总成本"或"总节省"。

**校准助手**（在账号编辑页内）：

```
  选一个模型         [ claude-sonnet-4-5      v ]
  统计区间           [ 本月                   v ]
  本账号该模型用量     72 万 token
  站点后台该模型扣费   [          ] 元
                      → 校准系数自动算出：0.83
```

必须按单个模型对账，不能用账号的总用量。总用量对账的结果会随你这个月用了哪些模型而漂移，同一个账号连续两个月能算出两个不同的系数。

### 6.9 账号级性能与状态

不单独设页面。账号的健康、额度、倍率状态在账号列表行内以徽标显示，详细信息在逻辑模型页的目标行展开。

## 七、下游公开接口

### 7.1 第一期推理与模型接口

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/v1/chat/completions` | OpenAI Chat Completions，支持普通和 SSE |
| `POST` | `/v1/messages` | Anthropic Messages，支持普通和 SSE |
| `POST` | `/v1/messages/count_tokens` | Anthropic Token 计数，Claude Code 依赖 |
| `POST` | `/v1/responses` | OpenAI Responses，支持普通、SSE 和 HTTP 后台模式 |
| `GET` | `/v1/responses/{response_id}` | 查询已保存或后台响应 |
| `DELETE` | `/v1/responses/{response_id}` | 删除响应及其本地状态 |
| `POST` | `/v1/responses/{response_id}/cancel` | 取消后台响应 |
| `GET` | `/v1/responses/{response_id}/input_items` | 查询输入项 |
| `POST` | `/v1/responses/compact` | 压缩 Responses 上下文 |
| `POST` | `/v1/responses/input_tokens` | 计算 Responses 输入 Token |
| `POST` | `/v1/images/generations` | 图片生成，**仅原生转发到 OpenAI 兼容上游**（§14.9） |
| `POST` | `/v1/images/edits` | 图片编辑，multipart 原样透传，**仅原生转发**（§14.9） |
| `GET` | `/v1/models` | 返回当前分组配置的逻辑模型，**响应形状按鉴权头判定** |
| `GET` | `/v1/models/{model}` | 返回单个逻辑模型，同样双形状 |

不增加历史别名或兼容路径；第一期只维护表中的标准路径。

### 7.2 鉴权规则

- 所有 `/v1` 接口都使用分组 Key 鉴权。
- 同时支持 `Authorization: Bearer <key>` 和 `x-api-key: <key>`。
- 两个头同时存在且值不同，返回鉴权错误，不猜测使用哪一个。
- Key 唯一对应一个分组，所以模型、队列、倍率保护和调度自然被限制在该分组。

### 7.3 模型列表规则

`/v1/models` 的响应结构按鉴权头判定：

| 鉴权头 | 响应形状 |
|---|---|
| `x-api-key` | Anthropic 形状：`{data:[{type,id,display_name,created_at}], has_more, first_id, last_id}` |
| `Authorization: Bearer` | OpenAI 形状：`{object:"list", data:[{id,object,created,owned_by}]}` |

两者都存在且值相同时，按 `x-api-key` 判定为 Anthropic 客户端。

内容规则：

- 只返回该分组已启用、且**至少有一个调度目标**的逻辑模型。
- 有目标但目标全部不健康时，逻辑模型仍保留在列表中，避免客户端因为瞬时故障缓存错误的模型列表。
- **零目标**是另一回事：自动创建的逻辑模型会被清理；手工创建的保留记录但从列表移除。
- `owned_by` 固定为 `akhub`，`display_name` 使用逻辑模型名，不暴露上游名称。
- 请求未知逻辑模型时直接返回 `model_not_found`，不跨组搜索。

### 7.4 后台 REST API

后台页面只调用同一服务内的 `/admin/api`，不直接访问 SQLite。除首次设置、登录和健康接口外，所有接口都需要管理员会话。

| 方法 | 路径 | 作用 |
|---|---|---|
| `GET` | `/admin/api/setup/status` | 判断是否需要首次设置 |
| `POST` | `/admin/api/setup` | 创建首个管理员并初始化主密钥状态 |
| `POST` | `/admin/api/auth/login` | 管理员登录 |
| `POST` | `/admin/api/auth/logout` | 管理员退出 |
| `GET` | `/admin/api/overview` | 概览指标和告警 |
| `GET/POST` | `/admin/api/groups` | 查看和创建分组 |
| `GET/PATCH/DELETE` | `/admin/api/groups/{id}` | 修改或删除分组（含调度权重） |
| `POST` | `/admin/api/groups/{id}/regenerate-key` | 重新生成分组 Key |
| `GET/POST` | `/admin/api/accounts` | 查看和创建上游账号 |
| `PATCH/DELETE` | `/admin/api/accounts/{id}` | 修改或删除账号 |
| `POST` | `/admin/api/accounts/{id}/copy` | 独立复制账号 |
| `POST` | `/admin/api/accounts/{id}/test` | 发送一次真实 `hi` 测试连接 |
| `POST` | `/admin/api/accounts/{id}/models/refresh` | 拉取上游模型列表并返回勾选候选 |
| `POST` | `/admin/api/accounts/{id}/models/select` | 提交模型选择集（带 `force` 二次确认） |
| `POST` | `/admin/api/accounts/{id}/refresh-multiplier` | 手动刷新账号倍率 |
| `POST` | `/admin/api/accounts/{id}/calibrate` | 提交单模型对账数据，反算校准系数 |
| `GET/POST` | `/admin/api/logical-models` | 查看和创建逻辑模型 |
| `PATCH/DELETE` | `/admin/api/logical-models/{id}` | 修改或删除逻辑模型 |
| `GET/POST` | `/admin/api/targets` | 列出与创建目标 |
| `PATCH/DELETE` | `/admin/api/targets/{id}` | 修改或删除单个目标 |
| `GET` | `/admin/api/cost` | 成本页数据，按逻辑模型分组 |
| `GET/PATCH` | `/admin/api/settings` | 查看和修改全局设置 |
| `GET` | `/admin/api/requests` | 分页查询请求元数据 |
| `GET` | `/admin/api/metrics` | 查询分钟聚合和目标性能（`since`/`until`/`target_id`；数据源 `performance_buckets`） |
| `POST` | `/admin/api/backup/export` | 导出加密配置备份 |
| `POST` | `/admin/api/backup/import` | 校验并原子恢复配置备份 |
| `POST` | `/admin/api/auth/password` | 管理员改密码（校验旧密码，改完吊销全部会话） |
| `GET/POST/PATCH/DELETE` | `/admin/api/new-api-sites` | 站点级 NewAPI 凭据的增删查 |
| `GET/PUT` | `/admin/api/accounts/{id}/aliases` | 读取与覆盖账号模型别名表 |
| `GET/POST` | `/admin/api/accounts/{id}/models` | 读取模型目录与手动添加上游模型 |
| `POST` | `/admin/api/accounts/{id}/models/sync` | 对单个账号立即执行一轮自动同步 |
| `GET` | `/admin/api/accounts/{id}/calibrations` | 校准对账历史 |
| `GET` | `/admin/api/accounts/{id}/multiplier-groups` | 查询 NewAPI 站点上这把 Key 的可用分组 |
| `GET` | `/admin/api/groups/{id}/available-models` | 聚合该分组下所有账号的模型目录 |
| `POST` | `/admin/api/accounts/refresh-multipliers` | 批量刷新所有启用的自动倍率账号（逐账号收集结果） |

所有列表接口都分页并限制上限。四个主要列表（分组、账号、逻辑模型、调度目标）
统一返回 \`{data, total, limit, offset}\`，默认 200 条、上限 1000 条；被截断时
后台顶部明确提示"哪些列表没显示全"，不能让人以为配置里就只有这些。返回对象中的 Key 只有前缀和状态；完整 Key 只在创建或重新生成成功的响应中返回一次。

**乐观锁**：写接口用请求头 `x-akhub-config-version` 携带它读到的配置版本，
与服务端当前版本不一致时返回 `409` 且错误以 `config_conflict:` 开头，后台提示
用户重新加载后再保存，避免覆盖其他修改。用请求头而不是塞进每个 payload：
DELETE 没有正文，备份恢复之类的接口本来就有自己的字段，统一走头部才不会漏。
服务端在**每个**响应里回带当前版本（写成功之后版本已经前进）；客户端只在成功
响应上采纳它——冲突时若采纳，用户再点一次保存就会覆盖别人的修改，"提示重新
加载"就成了一句空话。不带头部表示脚本初始化这类明确意图，保持兼容。

刷新、测试和复制操作返回结构化结果，并在审计日志中留下操作者、对象和结果，不返回请求正文。

## 八、一次请求的完整处理流程

1. 生成或接收请求 ID。
2. 验证请求体大小和 JSON 基本格式，记录请求体字节数（后续粘性等待预算要用）。
3. 验证分组 Key，取得不可变配置快照。
4. 根据路径识别下游协议，解析逻辑模型和请求所需能力。
5. 构建可重放请求体：8 MB 内存，超过后落临时文件。
6. 找到该逻辑模型在本组内的所有调度目标，按优先级分层。
7. 执行硬性资格过滤（§9.1），从最高层开始找出第一个有合格目标的层。
8. 计算粘性键（§10.1）并查找绑定。命中且目标在合格集合内时优先使用。
9. 未命中粘性时，在当前层内按综合评分加权随机选择目标（§9.4、§9.5）。
10. 获取目标并发槽位：
    - 粘性命中且目标忙 → 进入该目标的队列，等待预算见 §10.3；预算耗尽则降级为无粘性请求重新选择。
    - 无粘性且当前层有任一目标空闲 → 直接使用，不排队。
    - 无粘性且当前层全满 → 进入"任意目标"队列。
11. 真正发出上游请求前，再读取最新倍率和动态状态进行终检。
12. 选择该账号最合适的原生端点或跨协议适配器；跨协议且存在不可无损表达的能力时，按 §14.8 判定是否降级。
13. 转发非流式响应，或通过协议状态机逐事件转发 SSE。
14. 根据结果更新真实流量样本、健康状态、额度状态和能力证据。
15. 异步写入请求元数据，释放槽位并唤醒对应队列。

SQLite 写入、历史聚合和后台刷新都不能阻塞第 12 至 13 步。

## 九、候选资格与调度算法

### 9.1 硬性资格过滤

以下任一条件不满足，目标不能参与本次选择：

- 不属于当前分组。
- 账号、逻辑模型或目标被管理员停用。
- 上游模型已从有效列表消失，或被移出账号的模型选择集。
- 自动倍率未知**且已超过宽限期**（`multiplier_unknown`）。
- 有效倍率高于分组倍率上限（`multiplier_exceeded`）。
- Key 已失效、账号被封禁或总额度已明确耗尽。
- 账号或模型正在冷却，且当前请求没有获得唯一半开试运行名额。
- 请求需要的能力已被明确证实为不支持，**且该能力不在降级白名单内**。
- RPM、TPM 或最大并发已达到硬限制（对粘性请求，"并发满"不构成不合格，见 §10.3）。

能力状态为"未知"不等同于不支持，但在层内评分时按保守中性分处理。

**注意**：第一版把"请求包含不能无损转换的供应商专有字段，且该目标没有匹配的原生端点"列为硬性不合格，本版取消该条。降级白名单内的能力不再导致不合格，只在层内评分时略微降权，见 §14.8。

### 9.2 人工优先级与分层

- 账号默认优先级为 `50`，范围 `0-100`，越大越优先。
- 调度目标可以覆盖账号默认值。
- **优先级数字相同的目标构成一层。**
- **层是硬边界：当前层只要还有一个合格目标，就绝不使用下一层。** 只有当前层全部不合格（熔断、倍率超限、额度耗尽、模型消失、被停用）时才下沉。
- 同一个 Key 只映射一个模型时，账号默认优先级直接决定该 Key 的排序。
- 同一个 Key 映射多个模型时，每个"Key + 模型"分别使用自己的有效优先级，可以分处不同层。

这条规则是"所见即所得"的全部含义：你设 `A=100 B=50`，B 永远拿不到流量，除非 A 真的不合格。系统中不存在任何能让自适应越过优先级的机制。

想让 A 和 B 之间自动调节，就把它们设成同一个数字。

### 9.3 自适应信号

只使用真实用户请求，测试按钮和后台任务不进入样本。按"逻辑模型 + 调度目标 + 协议 + 是否流式"分别维护：

- 成功率和连续错误。
- 首字或首个语义事件延迟。
- 非流式总延迟。
- 每秒输出 Token。
- 429、5xx 和损坏响应比例。
- 当前在途数与最大并发占比。
- 有效倍率。

统计采用 EWMA，即"越新的请求权重越高的移动平均"，并设置最小样本数和滞后防抖。EWMA 每 60 秒快照到 SQLite，重启后加载，见 §20.1。

### 9.4 层内综合评分

评分只决定**层内选谁**，不影响跨层顺序。

**归一化一律使用比值，不使用 min-max，不使用硬编码刻度。**

```
倍率得分     = 参照系内最低有效倍率 / 自己的有效倍率
可靠性得分   = 自己的成功率                        （天然 0-1）
首字延迟得分 = 参照系内最低首字延迟 / 自己的首字延迟
输出速度得分 = 自己的输出速度 / 参照系内最高输出速度
```

比值归一化没有放大问题。对比一下：

| 参照系内倍率 | min-max 得分 | 比值得分 |
|---|---|---|
| 0.50 / 0.50 / 0.51 | 1.0 / 1.0 / **0.00** | 1.0 / 1.0 / **0.98** |
| 0.05 / 0.08 | 1.0 / 0.00 | 1.0 / **0.63** |

min-max 会把 2% 的差距放大成满分到零分；同值时该维度又完全失效，让分配给它的权重形同虚设。比值归一化两个问题都没有。

**归一化的参照系是整个分组，选择的作用域是当前层。**

这样每个目标只有一个分数，跨层可比，后台可以直接列成一列做诊断。选择时仍严格限制在当前层内。

```
目标         优先级   有效倍率   首字    综合评分
账号A          100      0.05     1.2s      0.94
账号B          100      0.06     1.0s      0.91
账号C           60      0.05     0.8s      0.99    ← 分数最高但在第 2 层
账号D           30      0.08     3.0s      0.55

第 1 层有合格目标 → 只在 A / B 之间选，C 碰都不碰
```

**权重**（分组级可调，总和必须为 100）：

| 维度 | 默认权重 |
|---|---|
| 倍率 | 40 |
| 可靠性 | 25 |
| 首字延迟 | 20 |
| 输出速度 | 15 |

可靠性必须进评分，不能只靠熔断兜底。熔断按 §12.3 需要"60 秒内连续或高比例"才触发，一个稳定维持 85% 成功率的目标永远不会被熔断，但每 7 次请求就有一次要重试；如果失败发生在 SSE 首个语义块之后，按 §13.3 连重试都不允许，直接报错给客户端。

冷启动：目标真实样本不足 20 个时，性能三维使用保守中性分，倍率维正常参与。

### 9.5 层内流量分配

**按分数的高次幂加权随机，不是赢家通吃。**

权重 `∝ score^k`，k 为内置常数，不暴露到后台。效果：

```
A 0.96  B 0.84   →   74% / 26%
A 0.95  B 0.94   →   53% / 47%      差距小时几乎均分
A 0.95  B 0.55   →   96% /  4%      差距大时留保命口粮
```

留那 4% 是必要的：

- 备胎的 EWMA 永远是新的。主力挂掉那一秒，你知道备胎现在什么样，而不是三天前什么样。
- 备胎悄悄变好了能被发现。

**粘性命中的请求不参与抽签**，直接走已绑定目标。抽签只发生在新粘性键的首次选择上，而不同粘性键本来就不共享前缀缓存，分散过去零成本。

## 十、会话粘性

### 10.1 粘性键推导

按以下优先级自动识别，不要求用户配置规则：

1. Responses 的 `conversation`、`previous_response_id` 或网关响应 ID。
2. 显式会话头，例如 `session-id`、`conversation_id`、`x-session-id`、`x-session-affinity`。
3. `prompt_cache_key` 或协议中的稳定用户/会话字段。
4. **稳定前缀摘要**：

```
粘性键 = hash( system prompt + 工具定义 + 分组标识 + 逻辑模型名 )
```

**第 4 级不包含任何用户消息。** 这是与第一版最重要的区别。

理由是上游的 prompt cache 按前缀匹配，缓存亲和的正确单位是"前缀"而不是"会话"。把第一条用户消息塞进哈希会产生两个后果：

- **`/compact` 必然打断粘性。** agent 客户端压缩上下文后第一条消息被替换成摘要，哈希改变，可能换号——而这恰好发生在上下文最大、缓存最值钱的那一刻。
- **注入内容每轮都在变。** agent 客户端常在第一条消息注入当前时间、git 状态、目录快照。这些参与哈希就等于每轮重置粘性。

只哈希稳定前缀后，同一项目的多个会话共用一个粘性键、粘同一个账号、共享前缀缓存；换一个项目（system prompt 或工具定义不同）则自然换键。

只保存哈希，不把用于粘性的正文写入日志。

### 10.2 粘性行为

- 命中且目标仍合格时，直接使用，不参与层内抽签。
- 粘性不是永久锁死。目标出现下列情况时清除绑定并重新选择：熔断、倍率超限或未知、Key 失效、额度耗尽、模型消失、被管理员停用。
- **"忙"不属于上述情况。** 并发满或收到带 `Retry-After` 的 429 时按 §10.3 等待，不立即换号。
- 普通粘性使用 1 小时滑动过期，绑定持久化到 SQLite（§20.1）。
- Responses 状态链使用独立的持久化映射。
- 目标恢复后，新会话可重新选择；旧会话不会强制抢回已经稳定的新目标。

### 10.3 粘性等待预算

换一次号的代价是一次完整的前缀缓存重建。以 100K token 上下文为例：

| | 单价量级 | 成本 |
|---|---|---|
| 缓存命中（粘住） | 缓存读价 | ≈ $0.030 |
| 缓存未命中（换号） | 输入价 + 缓存写价 | ≈ $0.675 |

所以粘性请求的排队价值远高于普通请求，不能共用同一个等待上限。

**等待预算 = 体积档位 × 缓存新鲜度系数**

体积档位以**请求体字节数**为锚点。字节数在 §8 第 2 步已经知道，零额外开销；估算 token 反而需要 tokenizer 或另一个粗估。

| 请求体大小 | 基础等待预算 |
|---|---|
| < 8 KB | 0 秒（直接换号） |
| 8 – 24 KB | 5 秒 |
| 24 – 64 KB | 12 秒 |
| 64 – 160 KB | 25 秒 |
| 160 – 320 KB | 40 秒 |
| 320 – 640 KB | 60 秒 |
| 640 KB – 1.2 MB | 75 秒 |
| > 1.2 MB | 90 秒 |

缓存新鲜度系数以"距上次命中该目标的时间"为准。粘性命中不等于缓存命中——Anthropic 缓存默认 TTL 5 分钟（每次使用刷新），OpenAI 自动缓存也在 5–10 分钟量级。上次命中是 20 分钟前的话，缓存早没了，等待毫无价值。

| 距上次命中 | 系数 |
|---|---|
| < 4 分钟 | 1.0 |
| 4 – 6 分钟 | 0.5 |
| > 6 分钟 | 0.1 |

`last_used_at` 本来就存在粘性绑定表里，这个系数是免费的。

```
例：400 KB 请求，2 分钟前刚命中   → 60 × 1.0 = 等 60 秒
例：400 KB 请求，15 分钟前命中    → 60 × 0.1 = 等 6 秒就换
```

**429 的处理**：带 `Retry-After` 且其值 ≤ 剩余等待预算时，等待；否则立即换号。

等待预算耗尽后，请求降级为无粘性请求，重新走 §9.5 的层内选择，并把粘性重新绑定到新目标。这也让"高并发时同一前缀自然分散到几个号"成为免费行为，不需要额外机制。

## 十一、倍率获取与不亏本保护

### 11.1 倍率语义

**倍率是账号级的 Key/分组折扣（如 x1、x0.5），不是模型价格。** 它不下沉到具体模型。

```
有效倍率 = 上游倍率 × 校准系数
```

- **上游倍率**：自动探针获取或人工录入，反映站点给这把 Key 的折扣档。
- **校准系数**：账号级，手填，默认 1.0。用来编码"站 A 的 x1 大约相当于站 B 的 x0.7"这类跨站点基础价格差异。可由 §6.8 的校准助手反算。
- **分组倍率上限**：允许使用的最高有效倍率，是绝对红线。
- `有效倍率 <= 分组倍率上限` 才能发起新推理请求。
- 倍率比较使用定点整数，不使用浮点数直接比较，避免边界误差。
- 倍率只影响资格和层内评分，不计算下游费用。

**已知限制**：账号级的单个校准系数表达不了同一站点内跨模型的加价差异。若某站对 claude 加价 20%、对 glm 加价 50%，单个系数无法同时对上两者。这是为避免维护价格表而接受的取舍，见 §29。

### 11.2 倍率来源

每个账号只能选择一种来源：

**自动 - Sub2API**

调用 Key 级 `/v1/sub2api/billing`，严格校验对象类型、版本、计费范围、有效倍率、观察时间和峰值倍率字段。一把 API Key 即可，不需要额外凭据。

**自动 - New API**

探针为 `GET /api/user/self/groups`，需要两个 API Key 之外的凭据：

```
Authorization: <访问令牌>          在 NewAPI 个人设置页生成
New-Api-User:  <用户 ID>           必填请求头
```

**必须使用这个接口，不能使用第一版写的 `/api/pricing` 或 `/api/ratio_config`**：

| 接口 | 鉴权 | 能否拿到"我这把 Key 的分组倍率" |
|---|---|---|
| `/api/pricing` | `TryUserAuth()`，只读 cookie session | 否。匿名调用只返回公开分组的倍率表，不知道你属于哪个分组 |
| `/api/ratio_config` | 无鉴权，但站长可关闭，默认关 | 否。全站倍率表，同样不含"你是哪一档" |
| `/api/user/self/groups` | cookie 或访问令牌 + `New-Api-User` 头 | **是** |

New API 的 `sk-xxx` API Key 不被上述任一接口接受，所以自动倍率对 New API 类上游必须依赖额外凭据。用户不提供凭据时，倍率模式只能选手动。

**手动**

管理员输入倍率，状态始终视为已知，不受自动刷新失败影响，直到管理员修改或清空。

### 11.3 自动刷新

- 默认每 5 分钟刷新一次，可在设置页调整。
- 各账号加入随机抖动，避免同时请求上游。
- 提供账号级和批量手动刷新按钮。批量接口是 `POST /admin/api/accounts/refresh-multipliers`：
  只扫启用的自动倍率账号，逐账号收集结果，一个失败不影响其余，并把失败的账号名如实回显。
- Sub2API 若返回明确峰值时段，Akhub 按已校验的数据在本地计算当前有效倍率，不必等到下一轮刷新。
- 刷新失败后按 `30 秒、1 分钟、2 分钟、5 分钟、10 分钟、30 分钟` 退避重试，并加入抖动。

### 11.4 刷新失败：按风险余量宽限

第一版的"刷新失败一次即立即硬停"与"调用不中断"直接冲突，且逻辑上不自洽——刷新成功时每个请求用的都是最多 5 分钟前的旧倍率并被认为可接受，刷新失败后同一个值的信息量完全相同，不应突然变成绝对不可用。

更重要的是失败是**相关的**：网络抖动会让所有账号同时刷新失败，NewAPI 访问令牌过期会让推理用的 `sk-xxx` 完好无损的账号被硬停，站长关闭管理后台做维护时 `/v1/chat/completions` 一切正常也会被硬停。一次探针故障就能造成全网关不可用。

**新规则**：

刷新失败后目标进入 `multiplier_stale`，保留最后已知值，宽限期长度取决于风险余量：

| 最后已知有效倍率 / 分组上限 | 宽限期 |
|---|---|
| ≤ 60% | 60 分钟 |
| 60% – 90% | 15 分钟 |
| > 90% | 0，立即硬停 |

宽限期内：

- 目标仍可参与调度。
- 层内评分中降权。
- 目标行显示"倍率已过期 N 分钟"。
- 概览页黄色告警。

宽限期结束 → 进入 `multiplier_unknown`，硬停，清除指向该账号的普通粘性绑定，后台按退避计划继续刷新。恢复成功后自动解除，不需要手动启用。

**探针系统性故障保护**：

超过半数账号在同一轮刷新中失败时，判定为"探针侧系统性故障"而非"上游集体改价"，所有账号的宽限期统一延长到 60 分钟，概览页红色告警。这条规则的作用是防止 Akhub 因为自己这边的网络问题把自己打死。

### 11.5 倍率发生变化

- 刷新结果通过动态状态原子替换。
- 排队中或尚未真正发给上游的请求必须重新校验。
- 新有效倍率超过分组上限时，账号立即进入 `multiplier_exceeded` 硬停。
- 已经把请求发给上游的调用不取消，避免破坏用户结果和产生已经支付却拿不到结果的损失。
- 倍率恢复到允许范围后自动恢复，不需要手动启用。

### 11.6 成本数据的计算与口径

请求记录中保存：

- 该次请求的逻辑模型、实际目标、有效倍率。
- 输入、输出、缓存 token 细分。
- 当时该逻辑模型在本分组内**最贵合格目标的有效倍率**与**最便宜合格目标的有效倍率**（用于 §6.8 的反事实基准）。

成本聚合**只在同一逻辑模型内部进行**：

```
该模型加权均倍率 = Σ(有效倍率 × token) / Σ(token)
相对最贵节省     = 1 - 加权均倍率 / 最贵目标倍率
```

跨模型不做任何加总。理由见 §6.8。

### 11.7 能力边界

轮询无法在上游"刚改倍率但 Akhub 尚未刷新"的几分钟内提前知道变化。默认 5 分钟刷新用于缩短该窗口；需要更严格保护时，应缩短刷新间隔或给分组倍率上限预留安全余量。任何宣称能在上游未提供推送接口时做到零检测窗口都不真实。

## 十二、额度、健康与熔断状态

### 12.1 状态范围

| 现象 | 影响范围 |
|---|---|
| Key 失效、账号封禁、总额度耗尽、倍率过期或未知 | 整个账号 |
| 模型不存在、模型限流、模型能力不支持 | 账号 + 具体模型 |
| 单个协议端点 404、405 或明确不兼容 | 账号 + 协议端点 |
| 单次网络错误、上游 5xx | 当前尝试；达到阈值后影响目标 |
| 单纯变慢 | 降低层内评分。不熔断 |

**关于"慢"**：在严格阶梯下，层内评分下降只影响同层内部的流量分配，不会让目标下沉到下一层。这是有意的——你把某个账号设成最高优先级，可能就是知道它慢但想先用完它的余额。Akhub 不替你推翻这个决定。持续慢到需要放弃时，请自己调优先级或停用它；后台的综合评分列会让这件事一眼可见。

### 12.2 运行状态

| 状态 | 含义 |
|---|---|
| `active` | 正常参与调度 |
| `degraded` | 仍可调用，但层内评分降低 |
| `cooldown` | 暂时停止新请求 |
| `half_open` | 只允许一个真实用户请求试运行 |
| `multiplier_stale` | **倍率已过期但在宽限期内，可用，降权，后台告警** |
| `multiplier_unknown` | 倍率未知且宽限期已结束，硬暂停 |
| `multiplier_exceeded` | 有效倍率超过分组上限，硬暂停 |
| `quota_exhausted` | 额度耗尽，等待恢复时间或试运行 |
| `missing_model` | 上游模型列表已移除该模型 |
| `deselected` | 已被移出账号的模型选择集 |
| `disabled` | 管理员停用 |

后台的状态列、概览的目标计数、账号健康摘要共用**同一个**判定函数
（`effective_target_status`），取值顺序即严重程度：配置（停用）→ 倍率
（未知/超限/过期）→ 健康（Key 失效/额度/冷却/半开）→ 正常。三处口径不会漂移。

### 12.3 熔断规则

- 401/403 明确证明 Key 失效时立即暂停账号，修改凭据或手动测试成功后恢复。
- 明确额度不足且带恢复时间时，按恢复时间进入半开。
- 没有恢复时间时使用指数冷却，结束后只放行一个真实请求。
- 429 优先影响账号 + 模型，并尊重 `Retry-After`。对粘性请求，429 不直接触发换号，见 §10.3。
- 60 秒内连续或高比例出现连接错误、5xx 或损坏响应时进入冷却；孤立错误只触发本次切换。
- 半开请求成功后恢复正常状态；失败则延长冷却。
- 不发送后台 `hi` 来自动探测恢复。

## 十三、故障切换、超时与排队

### 13.1 尝试预算

**尝试预算按成本和时间计算，不按次数。** 第一版的"最多尝试 3 个不同目标"是错误的预算单位，因为不同失败的代价差几个数量级。

**廉价失败**——上游根本没有开始生成，零成本、耗时通常在秒级以内：

- DNS、TCP、TLS 或连接建立失败
- 401 / 403 鉴权失败
- 404 模型不存在
- 429 限流（未产出任何 token）
- 立即返回的 5xx

**廉价失败不计入任何上限**，只受下面的三条硬约束。

**昂贵失败**——已经产生成本：

- 已向下游发出第一个有语义的 SSE 块
- 已产生 usage

昂贵失败按 §13.3 本来就禁止重试。

**实际约束**：

1. 每个调度目标最多尝试一次。
2. 请求总超时（默认 10 分钟，设置页可调）。
3. 硬上限 8 个目标——纯粹的失控保护，正常场景不会触及。
4. 层内先耗尽，再降层。

```
场景：第 1 层 5 个目标，其中 3 个连接失败（各 0.5 秒）
  → 1.5 秒内跳过 3 个，第 4 个成功
  → 第 1 层承诺不被破坏，也没有因为"用完了 3 次"而失败
```

每次新尝试前重新执行倍率和动态状态终检。

**分组页不再有"最大尝试目标数"设置项。**

### 13.2 可以切换的情况

- DNS、TCP、TLS 或连接建立失败。
- 明确的 Key 鉴权失败。
- 明确的额度不足或账号封禁。
- 429 限流（对粘性请求先按 §10.3 判断是否等待）。
- 上游 5xx。
- 上游返回损坏、无法解析或违反所选协议的响应。
- 模型不存在。
- 明确的能力不支持，且其他目标可能支持。

### 13.3 不切换的情况

- 下游请求 JSON 非法。
- 下游模型名不存在。
- 上下文过长。
- 内容审核拒绝。
- 参数本身不合法。
- 请求已经发出后上游只是慢，没有明确失败。
- 已达到请求总超时。
- SSE 已向下游发送第一个有语义的内容、思考或工具增量。

"慢请求不切换"是为了避免两个上游同时生成、产生两次成本。慢请求完成后，其延迟会降低该目标的层内评分。

### 13.4 流式边界

- 在只收到 HTTP 头、空白、注释、ping 或协议开始标记时，仍可根据明确错误决定切换。
- 一旦发送文本、思考、工具参数、引用或其他有语义增量，禁止拼接第二个上游。
- **流中途的降级必须留痕**：解析阶段丢掉的东西（目前只有 Anthropic 的
  \`signature_delta\`——签名是 Anthropic 独有的生命周期元数据，跨协议无处安放）
  记入请求记录的降级列。响应头在流开始前就发出去了，中途才知道的降级补不上头，
  但记录页照样标红——这是"不静默丢失"的实际落点。
- 后续错误按下游协议发送终止错误事件并关闭流。
- 不伪造正常完成事件掩盖上游中断。

### 13.5 请求总超时

- 默认总超时 10 分钟，可在系统设置调整。
- 达到总超时后取消上游请求并返回超时，不再向第二个目标重放。
- 客户端主动断开时立即取消排队或在途上游请求，并释放资源。
- 上游可能已开始计费，因此取消只能尽量减少浪费，不能承诺供应商一定停止计费。

### 13.6 队列

**队列按调度目标划分，不是分组单队列。** 分组单队列会产生队头阻塞：

```
分组单队列（第一版）：[X, Y, Z] FIFO

X  粘在账号A，A 并发满 → 等 A 的槽位（预算 90 秒）
Y  新会话，无粘性 → 账号B 有空位，本可立刻走
Z  新会话，无粘性 → 账号B 也有空位

结果：Y 和 Z 被 X 卡住 90 秒，而 B 一直空着
```

**新规则**：

| 请求类型 | 行为 |
|---|---|
| 粘性命中，目标有空位 | 直接走 |
| 粘性命中，目标忙 | 进入**该目标自己的队列**，等待预算见 §10.3；耗尽则降级为无粘性请求重新选择并重绑粘性 |
| 无粘性，当前层任一目标有空位 | 立即使用，**根本不排队** |
| 无粘性，当前层全满 | 进入"任意目标"队列；任何一个目标释放槽位即唤醒 |

分组只负责一件事：**所有队列加起来的总容量上限**，默认 100，可修改或设为 0。超出返回 `queue_full`。

其余规则不变：

- 只有临时容量不足、目标冷却或可恢复限流才进入队列。
- `multiplier_unknown`、`multiplier_exceeded`、鉴权失败、未知模型和明确不支持能力绝不排队。
- 等待期间配置或倍率改变时重新选择目标。
- 客户端断开立即移出队列。
- 等待超时返回 `queue_timeout`，并给出 `Retry-After`。

## 十四、协议网关设计

### 14.1 总体原则

协议层采用"原生优先、完整中间格式、白名单外不可降级"的方式：

1. 下游与可用上游协议相同时，尽量原样透传，只改鉴权、Base URL 和模型名。
2. 上游有匹配下游协议的原生端点时，优先使用该端点。
3. 没有原生端点时才进入跨协议转换。
4. 未知字段在同协议路径原样保留。
5. 跨协议时，**降级白名单内的能力**允许在故障切换场景下有损降级并显式标记；**白名单外的能力**不能表达时返回错误。

第一版写的是"跨协议时任何不能可靠表达的字段都不能静默删除"，配合 §9.1 的硬性过滤，效果是带 thinking 的 Anthropic 请求在只有 OpenAI Chat 端点的账号上**连尝试都不会尝试**，直接 `no_eligible_target`。这让故障切换在跨协议场景下形同虚设。详见 §14.8。

### 14.2 上游协议设置

账号保存两个值：

- `preferred_protocol`：管理员选择的首选协议（OpenAI Chat、OpenAI Responses 或 Anthropic Messages）。
  实现里的字段名是协议而非端点，因为一个协议可能对应多个端点（§14.3）。
- `adaptive_protocol`：是否允许运行时选择更合适的原生端点，默认开启。
  数据库列没有默认值，由创建路径写入 `true`；关掉它就只用首选端点（§14.2）。

例如账号首选 `/v1/chat/completions`，但真实上游也支持 `/v1/messages`。收到 Anthropic 请求时，如果 `/v1/messages` 未被证实不支持，先走原生 Messages；失败明确证明端点不存在后，再考虑转换到 Chat。

不会通过后台模拟对话探测端点。端点能力来自配置、模型目录和真实用户请求。

### 14.3 端点选择顺序

对一个已选调度目标：

1. 与下游协议相同且已确认支持的端点。
2. 与下游协议相同但能力未知的端点。
3. 账号首选端点，前提是请求可无损转换。
4. 其他已确认支持、且转换保真度相同的端点。
5. 其余能无损表达的端点（能力未知）。需要降级白名单内能力才能使用的端点
   排在**所有**无损端点之后——**仅在故障切换时可用**。

**"已确认支持"的定义**：这个账号的这个端点曾经成功服务过一次请求（拿到成功
响应即记一条支持证据，24 小时过期）。它和"能力未知"的唯一区别就是这个。把
它显式记下来的意义在于第 4 档：两个都能无损表达这次请求的端点里，已经成功过
的那个先试，不必再拿一次失败去试错。

**档位之间的顺序是硬的，档内保持候选的自然顺序**（稳定排序）：推测端点列表
本身有一定道理（越靠前的越通用），不该被排序打乱。降级与否先于证据比较，
否则会出现"降级但已确认支持"插到"无损但未知"前面。

明确不支持的端点在限制记录过期或配置变化前不会重复尝试。两类证据一起被清空
（配置变化时），也会一起过期。

### 14.4 中间请求格式

内部统一请求至少保留：

- 逻辑模型与真实目标模型。
- `system`、`developer`、`user`、`assistant`、`tool` 等角色语义。
- 有顺序和索引的文本、图片、文件、拒绝、引用等内容块。
- 工具定义、工具选择、并行工具调用、调用 ID、参数增量和工具结果。
- JSON Schema 和结构化输出要求。
- 思考强度、思考摘要、加密思考块、签名和供应商原始扩展。
- stop、温度、top-p、最大输出等生成参数。
- usage 的输入、输出、缓存、思考等细分 Token。
- 未识别字段的原始扩展区。

中间格式不是"只保留公共最小字段"，而是能表达三个目标协议的并集。

### 14.5 中间流式事件

流式层使用独立事件状态机，至少包含：

- 响应开始和响应 ID。
- 内容项开始与结束。
- 文本增量。
- 思考或思考摘要增量。
- 工具调用开始、参数增量和结束。
- 引用、拒绝和其他内容项。
- usage 更新。
- 正常完成、未完成和错误。

适配器必须维护事件顺序、内容索引、工具调用 ID、stop reason 和最终 usage，不能用简单字符串替换 SSE。

### 14.6 关键转换规则

| 能力 | 转换原则 |
|---|---|
| 文本消息 | 保留角色、顺序和多内容块，不擅自合并不同语义角色 |
| 图片输入 | Base64 与原生 URL 只在目标协议支持时转换；不为转换主动下载外部 URL。**不可降级** |
| 标准函数工具 | 转换名称、描述、JSON Schema、调用 ID、参数和工具结果。**不可降级** |
| 供应商专有工具 | 只有目标协议能完整表达时才允许，否则选择原生目标或报错 |
| 思考 | 目标协议能表达时保留可见摘要、加密内容、签名和对应生命周期。**在降级白名单内**，见 §14.8 |
| 结构化输出 | 只有语义等价时转换；不使用普通提示词伪装严格 Schema。**不可降级** |
| stop reason | 映射到下游协议最接近且稳定的原因，同时保留内部原始原因供日志使用 |
| usage | 保留已知细分项；目标协议没有字段时放入允许的扩展区，不能伪造数字 |
| 未知参数 | 同协议透传；跨协议不能表达且不在白名单内时返回 `unsupported_parameter` |

### 14.7 请求头与响应头

- 下游鉴权头绝不转发给上游。
- 上游使用账号配置生成正确的 `Authorization`、`x-api-key`、`anthropic-version` 等头。
- `anthropic-beta`、`openai-beta` 等供应商协议头只在匹配供应商时按白名单转发。
- 禁止跨域跟随会携带 Key 的重定向；探针默认完全禁用重定向。
- 返回安全的请求 ID、限流和 `Retry-After` 信息，不返回完整上游 URL 或 Key。
- **发生能力降级时返回 `X-Akhub-Degraded` 头**，值为被降级的能力列表。

### 14.8 能力降级白名单

判断标准是：**丢掉这个能力，客户端还能不能正常工作？**

| 能力 | 丢了会怎样 | 是否可降级 |
|---|---|---|
| 工具定义 / 工具调用 / 工具结果 | agent 完全瘫痪 | **否** |
| 结构化输出 Schema | 客户端解析崩溃 | **否** |
| 图片 / 文件输入 | 模型看不到内容，答案错但不报错——静默错误最危险 | **否** |
| 角色语义 | 对话结构被破坏 | **否** |
| thinking / reasoning 块 | 模型正常回答，只是没有思考过程 | **是** |
| 协议独有采样参数（top_k 等） | 生成分布略变 | **是** |

**降级只在故障切换时生效，首次选择时不生效。**

- 首次选择：优先选能完整表达请求的目标。只能降级表达的目标仍然合格，但层内评分中降权。
- 故障切换：白名单内能力允许丢弃，请求继续；白名单外能力仍然报错。

发生降级时：

- 响应头 `X-Akhub-Degraded: thinking`
- 请求记录页该行标红，注明降级了哪些能力
- 不修改响应体内容去掩盖降级

这一口径与 New API 的实际行为一致（`service/convert.go` 直接丢弃 thinking 块），区别只在 Akhub 让它可见。New API 完全静默，你无从知道发生过降级。

### 14.9 图片端点（2026-09-17 纳入）

原 §2.2 写"不做图片生成"，按管理员要求改为纳入第一期。口径与 §29 的"后续方向"不同，
第一期就有，但要明确边界：

- **只做原生转发**：`POST /v1/images/generations` 与 `POST /v1/images/edits` 只在目标账号
  的首选端点支持图片接口时使用，不做跨协议转换、不做 provider 适配、不读 `images/` 之外的协议。
- **没有等价物就明确拒绝**：非 OpenAI 兼容上游返回 `unsupported_parameter`，不静默降级、
  不伪造图片结果。
- **edits 保留 multipart 字节**：`multipart/form-data` 正文原样透传，不做 JSON 重编码。
- **走完整调度链路**：鉴权、分组、优先级、熔断、倍率门、端点证据都与其他推理端点一致；
  不新增独立的鉴权或限额路径。
- **公开的模型能力字段仍然不要求用户填写**（§一）：图片能力由内置目录与真实调用结果判断。

## 十五、OpenAI Responses HTTP 全套能力

### 15.1 网关响应 ID

Akhub 对外生成自己的 `resp_akh_*` ID，并保存它到真实上游响应 ID、账号、模型和分组的映射。这样查询、取消和多轮状态不会暴露上游身份，也不能被另一个分组访问。

### 15.2 `previous_response_id` 与 `conversation`

这两类调用依赖历史内容。不同上游不能识别彼此的响应 ID，所以无损切换需要 Akhub 保存可重放状态。

规则如下：

- `store` 未设或为 `true` 时，加密保存输入项、输出项、工具项、思考协议项和路由映射。
- `store:false` 时不保存可重放正文，只保存完成当前原生生命周期所需的最小定位信息。
- 默认保留 30 天，管理员可调整。
- 保留天数设为 0 时，不保存可重放正文；原生上游仍可通过最小定位映射继续，但无法保证跨上游重建。
- 状态过期后收到引用，返回明确的 `response_state_expired`，不把缺失历史的请求当作新对话发送。
- 删除响应时同时删除本地加密正文、索引和上游映射；若上游支持删除，再转发删除。

### 15.3 后台响应

- 上游原生支持 `background:true` 时，Akhub 代理其创建、查询和取消，并保持目标粘性。
- 上游没有原生后台能力时，不把普通同步请求伪装成可靠后台任务。
- 只有适配器明确实现并通过后台生命周期测试，才允许网关托管后台执行。
- 后台请求一旦得到上游 `in_progress` ID，就固定到该目标；后续查询错误不会转移到另一个上游继续生成。
- 倍率在任务开始后变化时，已经接受的后台任务视为在途任务，允许完成；新后台任务受最新倍率限制。

### 15.4 `compact` 与 `input_tokens`

- `compact` 只使用上游原生压缩能力或经过等价性测试的适配器，不使用普通"帮我总结"对话冒充。
- `input_tokens` 优先调用上游原生计数；有确定本地 Tokenizer 时可本地计算。
- 无法准确计算时返回不支持，不返回估算值冒充精确值。

### 15.5 `/v1/messages/count_tokens`

Anthropic 侧的对应端点。规则与 `input_tokens` 相同：优先原生转发，其次本地精确计算，都不可行时返回不支持。

Claude Code 依赖此端点做上下文管理，第一期必须提供。

## 十六、模型发现与选择

### 16.1 拉取模型列表

账号创建后不自动发模拟对话。管理员点击"获取模型"时：

1. 按账号协议调用上游模型列表接口。
2. 限制响应体大小并校验 HTTP 状态、JSON 结构和模型 ID。
3. 去空、去重并标准化列表，但保留真实模型名称大小写。
4. 应用账号的模型别名表。
5. 弹出勾选对话框（§16.2）。

拉取报错、格式异常或返回 0 个有效模型时，保留原列表和选择集，只记录错误，不弹对话框。

### 16.2 模型选择集

**每个账号维护一个持久的模型选择集**——已纳入调度的模型集合。

自动同步开关关闭时（默认），点"获取模型"弹出：

```
  已选 8 / 共 31                        [只看新增]
  [✓] claude-sonnet-4-5                 已在用
  [✓] glm-4.6                           已在用
  [ ] gpt-3.5-turbo                     你排除过
  [ ] some-new-model                    新出现  ◀
  [ ] another-new                       新出现  ◀
```

- 已在选择集内的默认勾选。
- 曾被明确取消勾选的保持不勾。
- **新出现的默认不勾**，你只需要关注新的那几个。
- "只看新增"过滤器让常规维护变成几秒钟的事。

自动同步开关打开时：

- 全部模型加入调度，**忽略选择集**。
- 后台显示"已托管，共 N 个模型"。
- 按设置页的自动同步间隔（默认 30 分钟，带抖动）执行。

自动同步重新关闭时：

- **回到选择集**，那 8 个。选择集不会被自动同步期间的全量托管污染。

**多账号行为**：新账号的模型列表里，已存在同名逻辑模型的**默认勾选**（你已经表达过"我要这个模型"），该账号独有的新模型默认不勾。

### 16.3 目标自动生成

勾选一个模型后自动发生：

- 同名逻辑模型不存在 → 创建逻辑模型，并把该"账号 + 模型"加为它的调度目标。
- 同名逻辑模型已存在 → 直接把该"账号 + 模型"加为新的候选目标。
- 目标优先级继承账号的默认人工优先级。

取消勾选时：

- 对应调度目标从调度中移除，在途请求完成。
- **该模型最近 24 小时有流量时弹出警告**，显示调用次数，需要二次确认。
- 若这是某个**自动创建**的逻辑模型的最后一个目标，该逻辑模型被清理。
- 若是**手工创建**的逻辑模型的最后一个目标，逻辑模型保留但从 `/v1/models` 移除。

这套机制让 8 账号 × 20 模型的配置从约 170 次手工操作降到约 10 次：建 8 个账号各设一个默认优先级，各点一次"获取模型"，完成。

### 16.4 模型别名

账号级的"上游真名 → 对外名"映射表，解决跨站点模型命名不一致：

```
claude-sonnet-4-5-20250929   →   claude-sonnet-4-5
anthropic/claude-sonnet-4.5  →   claude-sonnet-4-5
```

别名在拉取模型列表后、勾选之前应用，所以对话框和选择集里出现的都是对外名。这让不同站点的同一个模型能正确归并到同一个逻辑模型下。

### 16.5 模型消失与手动模型

- 消失且未被选择的模型可从可选列表移除。
- 消失但已在选择集内的模型保留记录，标记 `missing_model` 并停止新请求。
- 模型重新出现时自动解除 `missing_model`。
- 模型列表接口不可用时允许手动输入上游模型名。手动模型不会被一次失败的自动拉取覆盖；真实请求可以逐步记录其能力。

### 16.6 开源能力目录

- 以 LiteLLM 的公开模型目录作为初始来源之一。
- 发布时把指定版本转换为 Akhub 自己的精简、版本化快照并随二进制发布。
- **只取能力信息，不取价格数据。** Akhub 不维护价格表。
- 不在运行中直接读取远程仓库，也不静默热替换协议规则。
- 目录只提供初始能力判断，不要求用户确认模型卡。

能力证据优先级固定为：

`明确的真实请求结果 > 上游接口返回 > Akhub 内置适配规则 > 开源目录`

**目录如何参与决策**（2026-09-17 落地）：目录按**上游真名**回答它知道的四项能力
（`function_calling` / `vision` / `response_schema` / `reasoning`），返回三态：

- `Some(true)`：明确支持，不影响调度；
- `Some(false)`：明确不支持；
- `None`：目录里没这个模型，或该项没写——**一律按未知处理**。把 `None` 当成
  "不支持"会让目录没收录的新模型全部被降权，比不接入更糟。

`Some(false)` **不是硬性不合格**，只让该候选在**同一层内**排到最后：
层内按偏好档分三段（目录无异议的无损目标 → 目录存疑的无损目标 → 需要降级的目标），
档间硬顺序、档内才抽签。放在"需要降级"之前是有意的：宁可试一个目录存疑但能无损
表达的目标，也不要直接丢掉 thinking 这类白名单内的能力——目录会出错，丢能力不会。

**已经有真实证据的能力不归目录管**：只要该账号该模型学到了明确结果（支持或不支持），
目录的意见直接作废，这就是上面优先级顺序的实际含义。

### 16.7 能力学习

能力状态分为：

- `supported`：真实成功或可靠上游声明。
- `unsupported`：上游明确返回该模型不支持该能力。
- `unknown`：没有足够证据。

规则：

- 只有明确"不支持"的错误才缓存为限制。
- 普通 400、5xx、超时和网络错误不能证明能力不支持。
- 限制默认 24 小时过期。
- 模型列表变化、账号协议变化、适配器版本变化时，相关限制立即失效。
- 首次选择时不通过删除图片、工具、思考或 Schema 来强行让请求成功；只有故障切换且能力在降级白名单内时才允许，见 §14.8。

## 十七、限流、并发和请求体

### 17.1 账号与模型限制

每个账号提供默认值，每个调度目标可以覆盖：

- RPM：每分钟请求数。
- TPM：每分钟 Token 数。
- 最大并发。

默认均为不限。账号级最大并发和额度状态由同一 Key 下的所有模型共享，模型覆盖值用于更细的限制。

### 17.2 Token 预留

- 已知 Tokenizer 时，根据输入和最大输出进行预留，完成后按真实 usage 归还差额。
- 不知道 Tokenizer 时使用保守估算，并在后台明确显示"估算限流"。
- TPM 未配置时完全跳过 Token 估算，避免无意义开销。

### 17.3 请求体存储

- 8 MB 以内保存在内存。
- 8-64 MB 写入数据目录下的专用临时目录。
- 请求结束、失败或客户端断开时删除临时文件。
- 启动时清理上次异常退出遗留且超过安全时间的临时文件。
- 超过 64 MB 返回 `413 request_too_large`，上限可在系统设置调整。

请求体字节数在缓冲时即记录，用于 §10.3 的粘性等待预算，零额外开销。

## 十八、错误格式

### 18.1 稳定网关错误码

第一期至少定义：

- `auth_invalid`
- `model_not_found`
- `no_eligible_target`
- `multiplier_unknown`
- `multiplier_exceeded`
- `queue_full`
- `queue_timeout`
- `request_too_large`
- `unsupported_parameter`
- `upstream_timeout`
- `upstream_exhausted`
- `response_state_expired`
- `rate_limited`
- `upstream_protocol_error`
- `internal_error`

### 18.2 协议对应格式

- OpenAI Chat 和 Responses 返回 OpenAI `error` 对象。
- Anthropic Messages 返回 Anthropic `type:error` 对象。
- 每个错误带稳定网关错误码和请求 ID。
- 上游原始错误只提取安全信息，不返回完整 Key、内部 URL、数据库 ID 或堆栈。
- SSE 已开始后使用该下游协议允许的错误事件，随后关闭连接。此时 HTTP 状态码
  早已发出，**稳定网关错误码与请求 ID 必须写进事件体**：OpenAI 形状放在
  `error.code` / `error.request_id`，Anthropic 形状放在 `error.code` 与顶层
  `request_id`，Responses 形状放在 `code` / `request_id`。不带的话客户端拿到
  的只是一句人话，既没法据此重试，也没法在报问题时给出可检索的标识。

### 18.3 HTTP 状态码映射

状态码直接决定客户端是否重试，**客户端重试是"调用不中断"的最后一道防线**。Claude Code 一类客户端收到 429/500/502/503/504 会自动退避重试，收到 4xx 则直接失败并报给用户。

**可重试**（附 `Retry-After`）：

| 错误码 | HTTP |
|---|---|
| `queue_full` | 429 |
| `queue_timeout` | 429 |
| `rate_limited` | 429 |
| `no_eligible_target` | 503 |
| `upstream_exhausted` | 503 |
| `upstream_timeout` | 504 |
| `upstream_protocol_error` | 502 |
| `internal_error` | 500 |

**不可重试**（快速失败）：

| 错误码 | HTTP |
|---|---|
| `multiplier_unknown` | 403 |
| `multiplier_exceeded` | 403 |
| `auth_invalid` | 401 |
| `model_not_found` | 404 |
| `unsupported_parameter` | 400 |
| `request_too_large` | 413 |
| `response_state_expired` | 404 |

`multiplier_*` 归入不可重试的理由：按 §11.4，能走到 `multiplier_unknown` 说明宽限期已经用完，问题至少持续了 15–60 分钟。客户端再重试也是白搭，不如马上把真实原因告诉你。这两个错误是"你的钱包在拦你"，不是上游故障。

## 十九、Rust 技术架构

### 19.1 总体形态

第一期是一个 Rust 进程：

- 同一 HTTP 服务承载 `/v1` 网关、`/admin/api` 管理接口和后台静态文件。
- SQLite 保存配置、加密状态、粘性绑定、性能快照和历史元数据。
- 内存保存配置快照、调度状态、粘性、并发计数、短期指标和队列。
- 后台任务负责倍率刷新、模型同步、数据清理、聚合刷盘、性能快照和健康恢复计时。

不引入微服务、消息队列或 Redis。

### 19.2 推荐依赖

| 用途 | 选择 |
|---|---|
| HTTP 服务 | `axum`、`tower`、`tower-http` |
| 异步运行时 | `tokio` |
| 上游客户端 | `reqwest`/`hyper`，使用 `rustls` |
| JSON | `serde`、`serde_json`，原生透传路径尽量使用 `RawValue` |
| SQLite | `sqlx` SQLite，WAL 模式 |
| 配置热切换 | `arc-swap` |
| 日志 | `tracing`、`tracing-subscriber` |
| 密钥内存保护 | `secrecy`、`zeroize` |
| 数据加密 | `chacha20poly1305`，使用 XChaCha20-Poly1305 |
| 密码哈希 | `argon2` |
| Key 摘要 | `hmac`、`sha2`；鉴权按定长常量时间比较（分组是个位数，逐字节比较的代价可忽略） |
| 并发控制 | `tokio::sync::Semaphore` |
| 临时文件 | `tempfile` |
| 标识符 | `ulid` 或 `uuid` |
| 定点小数 | `rust_decimal` 或自建定点整数类型 |

依赖在编码前仍需检查当前稳定版本和维护状态；不为了少量代码自行重写成熟的加密、HTTP 或限流基础能力。

### 19.3 代码模块

保持单个 Rust crate，按职责分模块，不提前拆成多个服务：

```text
src/
  main.rs
  lib.rs               模块清单
  server.rs            HTTP 装配、健康接口、优雅关闭（§24.3、§25.3）
  sync.rs              锁毒化恢复辅助
  app/                 启动、关闭、共享状态、后台任务、请求记录写入
  admin/               后台 API、会话与静态页（mod.rs / resources.rs / ui.rs）
  auth/                管理员会话与分组 Key 鉴权
  config/              配置事务、校验和 ArcSwap 快照
  domain/              分组、账号、模型、目标等领域类型 + 定点倍率
  gateway/             下游入口、请求生命周期、错误返回
    passthrough.rs     调度游走与转发主路径
    responses.rs       Responses 状态链与生命周期
    background.rs      网关托管后台任务（§29.1）
    stream.rs          流式状态机与结算触发
    settle.rs          统一结算（成功/失败/取消）
    translate.rs       协议发射与响应 ID 改写
    models.rs          /v1/models 双形状
    error.rs           错误码、状态码映射与错误体
  protocol/            openai_chat.rs / openai_responses.rs / anthropic.rs
                       canonical.rs（中间格式）/ degrade.rs（降级白名单）
                       sse.rs（帧解析）/ translate.rs（转换缓存）
  routing/             资格过滤、分层、层内评分、加权选择、粘性、队列
  upstream/            Client 池、URL、Header、端点选择与探针
  health/              熔断、冷却、半开、额度与准入限额
  discovery/           模型列表刷新、选择集、别名与目标归并
  capability/          内置能力目录与能力证据学习
  multiplier/          New API/Sub2API/手动倍率、宽限期、校准
  storage/             SQLite、批量写入、快照、聚合与清理
  security/            加密、脱敏、URL/SSRF 防护与配置备份
web/                   React + TypeScript 管理后台
tests/                 Rust 端到端测试与 SDK 验收脚本
```

**目录命名说明**（2026-09-17 校正）：规划早期版本里的 `catalog/`、`cost/`、`state/`、
`observability/` 四个目录没有单独建立，对应职责分别落在 `discovery/` + `capability/`、
`admin/resources.rs` + `storage/`、`gateway/responses.rs` + `gateway/background.rs`、
`app/recorder.rs` + `health/` 里。模块划分按职责而非按规划目录名，测试夹具内联在
`tests/protocol_e2e.rs` 等文件中，没有单独的 `tests/fixtures/` 目录。

适配器使用编译期 Rust trait，不做运行时脚本插件。

### 19.4 热路径性能原则

- 每个上游 Origin 复用长期 HTTP Client 和连接池，不按请求创建 Client。
- 配置读取只做一次原子指针读取。
- 动态计数使用原子值、分片锁或 Semaphore，避免全局大锁。
- 原生同协议请求只做浅解析和必要字段改写，尽量不重建整个 JSON。
- 跨协议转换才构建完整中间对象。
- 请求记录通过有界通道交给单独批量写入任务。
- **粘性绑定和性能快照的持久化由后台任务批量执行，热路径只写内存。**
- SQLite 开启 WAL、`busy_timeout`，配置写和日志写分离调度。
- 所有队列、缓存、状态表和通道都有容量上限。
- **SSE 帧缓冲也要有上限**：单帧超过 2 MB 仍未结束（上游一直发字节却不发空行）
  时丢弃缓冲并记下原因，由调用方按 §18.2 在流内报错——提交前发现就换目标，
  提交后发现就在流内报错。**不能**当作正常结束：那会让下游收到一个突然断掉的
  流，无从判断是正常收尾还是上游坏了。缓冲同时记录扫描游标，避免慢速滴水式
  输入让切帧退化成 O(n²)。内存表的上限是**兜底**：
  粘性绑定 10000 条（超过时按批淘汰最久未使用的），性能统计与动态状态各 5000 条
  （超出时淘汰一条并告警——正常路径本该由 retain 清理，走到这里说明清理漏了）。
  信号量、通道与分组队列的上限在各自模块里。
- 下游读取慢时通过背压控制上游读取，不无限缓存流式数据。
- 每个请求处理任务的 panic 必须被隔离，不能导致进程退出。实现用 tokio 的任务级
  隔离 + 一层 catch-panic 中间件：进程不受影响，客户端拿到的是**带稳定错误码的
  500**（\`internal_error\`）而不是一个没有任何解释的连接重置。

## 二十、配置与数据结构

### 20.1 主要数据表

> 表名以 `src/storage/schema.sql` 为准（2026-09-17 校正）。当前结构版本 **v6**，
> 迁移阶梯见 `src/storage/mod.rs` 的 `migrate()`。

| 表 | 作用 |
|---|---|
| `admin_users` | 单管理员密码哈希和安全状态 |
| `app_settings` | 全局设置与 `schema_version`（配置版本本身在内存快照里，见 §21） |
| `groups` | 分组、倍率上限、Key 摘要、**四个调度权重**、队列总容量、最长等待、降级与托管后台开关 |
| `upstream_accounts` | 独立账号、Base URL、协议、默认优先级、倍率模式、**校准系数**、NewAPI 用户 ID / 分组名、限制 |
| `upstream_secrets` | 加密 API Key 与 NewAPI 访问令牌（每条独立 nonce） |
| `new_api_sites` | **站点级 NewAPI 凭据**：一个 Base URL 配一次，该站点下的账号自动继承 |
| `account_models` | 账号模型列表（上游真名 → 对外名），含**选择集标记** `selected` 与消失标记 `missing` |
| `account_aliases` | **账号级"上游真名 → 对外名"映射** |
| `logical_models` | 分组对外逻辑模型，含来源（自动 / 手工） |
| `dispatch_targets` | 逻辑模型到"账号 + 具体模型"的绑定和优先级覆盖 |
| `multiplier_snapshots` | **按账号**的当前倍率、来源、状态、宽限期起点、观察和刷新时间 |
| `sticky_bindings` | **粘性前缀哈希 → 目标 + `last_used_at`** |
| `target_perf_snapshot` | **每个目标的 EWMA 当前值与快照时间** |
| `response_states` | 网关响应 ID 到上游资源的加密定位映射 + 可重放的加密输入输出项 |
| `background_tasks` | 网关托管后台任务的状态、心跳、上游 ID 与加密输出（§29.1） |
| `request_records` | 不含正文的请求元数据、降级标记、成本基准倍率与 §24.1 的调度诊断列 |
| `request_attempts` | **每次上游尝试的明细**：目标、端点、耗时、失败原因、是否计入预算 |
| `performance_buckets` | **分钟级目标性能聚合**，`GET /admin/api/metrics` 的数据源（§22） |
| `calibration_records` | **校准助手的对账记录** |
| `admin_audit_log` | 配置修改、复制、测试和备份恢复记录 |

**不落库的两类状态**（有意为之，不属于缺失）：

- **端点证据与能力证据**（`endpoint_evidence` / `capability_evidence`）：放在进程内存
  （`src/upstream/evidence.rs`、`src/capability/mod.rs`），24 小时 TTL，配置变更即清空。
  重启后第一个请求用一次廉价失败重新学会，代价比重建一张表和一条恢复路径更低。
- **运行态熔断与额度**（`runtime_target_state`）：冷却、额度恢复时间与半开名额都在
  `src/health/mod.rs` 的内存注册表里。重启后这些状态清零，等同于"刚重启所以一切都新鲜"，
  这是可接受的：它们本来就会随时间自然恢复。

**关于持久化**：第一版把"不逐请求写 SQLite"和"完全不持久化"混成了一件事。前者是正确的热路径要求，后者是不必要的代价：

- `sticky_bindings` 只在首次绑定和换号时写入，且现在是**前缀级粒度**（一个项目一行），数据量很小。
- `target_perf_snapshot` 由后台任务每 60 秒批量写一次，20 个目标就是 20 行/分钟，可忽略。
- 两者都不在热路径上。内存仍是唯一真相，写盘只是备份。

重启后：粘性直接恢复，前缀缓存不丢；评分从快照继续，不是从零开始；快照超过 24 小时则丢弃。

不这样做的代价是可算的：重启后所有粘性绑定清零，按 §9.5 的加权随机重新抽签，第 1 层有 3 个目标时落回原目标的概率是 1/3；5 个活跃项目一次重启期望丢失约 3.3 个前缀缓存。同时四个评分维度里只有倍率还有数据，其余三维全是中性分，层内基本是均匀随机，要跑够 20 个真实样本才恢复正常判断——个人量级的流量可能是一两个小时。

### 20.2 密钥存储

- 上游 API Key 与 NewAPI 访问令牌使用 XChaCha20-Poly1305 加密，每条记录使用独立随机 nonce。
- 下游分组 Key 是高熵随机值，只保存带服务端 pepper 的 HMAC 摘要和可显示前缀，不保存可恢复明文。
- 账号复制时在内存解密并用新 nonce 重新加密，不能让两条记录引用同一个密钥对象。
- 所有日志统一经过密钥与 Header 脱敏器。**兜底在输出层**：格式化器把事件先渲染
  成最终要写出去的文本，再过一遍脱敏，所以覆盖面是百分之百——不能只靠"每个
  调用点记得调一次"，新增的日志点很容易漏。脱敏本身是幂等的，调用点先脱一次
  也不会变成"脱敏的脱敏"。

### 20.3 倍率数值

数据库和资格判断使用放大后的整数，例如 `1.234567` 保存为 `1234567`。校准系数同样使用定点整数，`有效倍率` 的乘法在定点域内完成并向上取整（对成本保护取保守方向）。页面只负责格式化显示。

层内评分可使用浮点归一化，但硬性倍率门只能使用定点值。

## 二十一、配置原子切换

所有管理端写操作走同一个配置服务：

1. 开启 SQLite 写事务。
2. 应用修改。
3. 在事务内读取完整候选配置。
4. 校验分组、账号、模型绑定、Key 唯一性、倍率、调度权重总和、引用完整性。
5. 构建新的不可变 `RuntimeConfig` 并增加版本号。
6. 提交事务。
7. 使用 `ArcSwap` 一次替换当前配置。

在途请求持有旧 `Arc`，所以不会读到一半新一半旧的配置。新请求只拿新版本。

倍率、熔断、并发和额度属于动态安全状态，不等待配置版本；每次真正发请求前读取最新状态。这样即使请求持有旧配置，也不能绕过刚发生的倍率超限。

## 二十二、后台任务

第一期后台任务只有：

- 自动倍率刷新与宽限期计时。
- 开启自动同步的账号的模型列表刷新。
- 能力限制、冷却和粘性状态过期清理。
- Responses 状态、请求记录和临时文件清理。
- 请求记录批量写入和性能分钟桶聚合。
- **粘性绑定与性能 EWMA 快照落盘（每 60 秒）。**
- 后台 Responses 生命周期管理。

后台任务遵守以下规则：

- 有界并发和抖动，不能同时压向上游。
- 使用独立超时和响应体上限。
- 不发送模拟对话探测模型能力或账号恢复。
- 应用关闭时停止接收新任务并等待安全刷盘。

## 二十三、安全与隐私

### 23.1 主密钥

- 首次启动在数据目录生成 32 字节主密钥文件，权限设为仅当前用户可读写。
- 也允许通过 `AKHUB_MASTER_KEY` 环境变量提供固定主密钥。
- 环境变量存在时优先使用，不把它写入数据库或日志。
- 主密钥丢失后，数据库中的上游 Key 和加密 Responses 状态无法恢复。

### 23.2 管理后台

- 管理员密码使用 Argon2id。
- 登录 Cookie 使用 `HttpOnly`、`SameSite=Strict`。`Secure` 按请求判定：
  Akhub 自身不终止 TLS，所以看反向代理给的 `X-Forwarded-Proto`（取代理链最初
  那一跳）。硬加 `Secure` 会让 `127.0.0.1` 直连无法登录，完全不加又等于在
  HTTPS 部署下白丢一层保护。
- 所有写操作需要 CSRF 防护。
- 登录失败有速率限制和短暂锁定。
- 后台不提供查看完整上游 Key 的接口，只允许覆盖更新。需要把同一个 Key 用在多处时使用账号复制功能。

### 23.3 SSRF 与 Key 泄漏防护

- Base URL 保存前做严格 URL 校验，只允许 HTTP/HTTPS。
- 默认阻止环回、链路本地、云元数据和内网地址；确需访问局域网上游时由管理员显式开启该账号的内网访问。
- DNS 解析后再次校验目标地址，防止 DNS Rebinding。
- 带 Key 的请求不跨 Origin 跟随重定向。
- 倍率和模型探针限制响应体大小、超时和 Content-Type。

### 23.4 正文与状态

- 普通 Chat、Messages 和无状态 Responses 不落请求正文。
- 只有为 Responses 状态链完成查询、取消或跨上游重放所必需的内容才加密保存。
- 粘性键只保存哈希，不保存参与哈希的 system prompt 或工具定义原文。
- 请求记录页面不提供正文调试开关，避免管理员误开启长期敏感日志。
- 崩溃和错误日志不包含请求体、Key 或思考内容。

### 23.5 配置备份与恢复

- 导出时由管理员输入备份密码。
- 在内存生成仅包含配置的规范化数据，用 Argon2id 派生备份密钥，再整体使用 XChaCha20-Poly1305 加密。
- 备份包含分组、调度权重、账号、上游 Key、模型选择集、模型别名、逻辑模型映射和设置，不包含请求正文、Responses 历史、性能记录、粘性绑定或运行日志。
- 恢复时先完整解密、校验版本和引用，再用本机主密钥重新加密秘密，最后通过配置事务原子切换。
- 密码错误、文件损坏或出现 0 个有效分组时不修改现有配置。

## 二十四、日志、监控与保留

### 24.1 默认请求日志

记录：

- 请求 ID、配置版本、入口协议和逻辑模型。
- 候选过滤原因、选中的层、最终目标和端点。
- 每次尝试、切换原因、该次失败是否计入尝试预算、队列等待时间。
- 粘性键命中与否、粘性等待时长、等待预算与缓存新鲜度系数。
- 首字延迟、总延迟、输出速度和 Token。
- 倍率来源、有效倍率、资格判断结果和额度状态。
- 该逻辑模型当时最贵与最便宜合格目标的有效倍率。
- 能力降级标记。
- 标准化错误码、返回的 HTTP 状态码和安全的上游状态码。

不记录完整 Header、API Key、正文、工具参数、图片或思考。

### 24.2 数据保留

- 请求元数据和性能历史默认保留 30 天。
- 管理员可以调整保留天数。
- **设为 0 时不新增历史明细**：请求记录不写 SQLite，改由
  `src/app/live_stats.rs` 在内存里累加当天的实时汇总（请求量、成功率、延迟分位、
  队列超时、最近错误、小时趋势、按逻辑模型的用量与倍率样本）。概览页与成本页
  照常工作，响应里带 `retention_off: true`，界面明确标注"只覆盖当天、重启清零"。
  成本页在此设置下只能显示当日数据，选"本月"也一样。
- 内存汇总必须有界：延迟样本只留最近 4096 个、最近错误 20 条，其余是定长计数器，
  跨天滚动（小时桶保留，所以趋势图跨天仍然连续）。
- **只向前滚天**：跨过午夜仍在跑的流式请求结算时带的是昨天的开始时间，
  不能因此把今天已经累计的统计清空。
- 清理任务分批删除，避免长事务阻塞 SQLite；性能桶与请求明细同一保留期。

### 24.3 健康接口

- `/health/live`：进程事件循环正常。
- `/health/ready`：数据库、配置和主密钥已加载，可以接收请求。
- 健康接口不暴露账号、模型、倍率或版本细节。

## 二十五、部署方案

### 25.1 Docker

- 提供单镜像，内含 Rust 二进制、管理后台和能力目录。
- 使用非 root 用户运行。
- `/data` 作为唯一持久卷，保存 SQLite、主密钥和临时目录。
- 容器内默认监听 `0.0.0.0:8080`。
- 提供最小 `docker compose` 示例、健康检查和 `restart: unless-stopped`。

### 25.2 Linux 原生版

- 第一批发布 `x86_64` 和 `aarch64`。
- 单个二进制加一个数据目录即可运行。
- 提供 `systemd` 示例，默认监听 `127.0.0.1:8080`，`Restart=always`。
- 生产 HTTPS 推荐由 Caddy、Nginx 或现有入口代理终止，Akhub 第一期不自行管理证书。

### 25.3 优雅关闭

收到停止信号后：

1. 停止接收新请求。
2. 取消排队请求并返回可重试错误。
3. 给在途请求默认 **180 秒**完成时间。agent 客户端的长流式响应加上思考时间常常超过一分钟，第一版的 60 秒会截断它们。
4. 刷新日志、粘性绑定和性能快照写入队列。
5. 超时后取消剩余上游连接并退出。

## 二十六、测试策略

### 26.1 协议黄金测试

为三种协议建立固定输入、非流式输出和 SSE 事件夹具，覆盖：

- 纯文本和多轮消息。
- 图片输入。
- 单个和并行函数工具。
- 工具参数分片与工具结果。
- 思考、思考摘要、签名和加密块。
- JSON Schema 结构化输出。
- 拒绝、引用和不同 stop reason。
- usage 的缓存与思考 Token。
- 未知字段原生透传和跨协议拒绝。
- **降级白名单内能力的降级路径与标记正确性。**

每一种跨协议方向都必须有测试：

- Chat ↔ Responses
- Chat ↔ Messages
- Responses ↔ Messages

### 26.2 官方 SDK 兼容测试

使用官方 OpenAI 和 Anthropic SDK 作为黑盒客户端验证：

- 普通与流式创建。
- 工具调用循环。
- `previous_response_id`。
- 查询、删除、取消和输入项。
- `count_tokens`。
- `/v1/models` 双形状按鉴权头正确切换。
- 错误对象能被 SDK 正常解析，且 HTTP 状态码触发预期的重试行为。

### 26.3 路由与故障测试

必须覆盖：

- 同一 Key 下两个相似模型按具体模型独立调度。
- 组内调度且绝不跨组。
- **严格阶梯：高优先级层有合格目标时，低层永远拿不到流量。**
- **同优先级即同层，层内按评分加权分配，分差小时接近均分、分差大时留保命口粮。**
- **比值归一化：倍率 0.50/0.50/0.51 不会把第三个打死。**
- **归一化参照系为整个分组，同一目标的分数跨层可比。**
- **尝试预算：第 1 层 5 个目标 3 个连接失败时，第 4 个仍被尝试且成功。**
- **每目标独立队列：粘性请求长等待不阻塞可立即执行的无粘性请求。**
- 粘性命中、`/compact` 后不断链、故障清除和恢复。
- **粘性等待预算按体积和缓存新鲜度正确计算。**
- DNS、TLS、401、429、5xx、损坏响应的切换。
- 慢请求和总超时不产生第二份调用。
- 第一个语义 SSE 事件后禁止切换。
- 客户端断开、队列满和队列超时。

### 26.4 倍率保护测试

- 自动倍率成功、失败、空数据、格式错误和超时。
- **刷新失败按风险余量进入 `multiplier_stale` 并在宽限期后转 `multiplier_unknown`。**
- **半数账号同时失败触发系统性故障保护，宽限期统一延长。**
- 手动倍率不受自动刷新失败影响。
- 有效倍率等于分组上限允许调用，高于则拒绝。
- 请求排队或等待并发期间倍率升高，终检必须阻止发送。
- 在途期间倍率升高，请求继续完成。
- 倍率恢复后自动恢复调度。
- **校准系数参与有效倍率计算，定点乘法取保守方向。**
- **NewAPI 探针使用 `/api/user/self/groups` 并正确携带两个凭据；凭据缺失或过期时的行为。**
- Sub2API 峰值时段切换。

### 26.5 模型选择与目标生成测试

- 0 个模型不覆盖，错误 JSON 不覆盖。
- **选择集持久：二次拉取时已选保持选中，新增默认不勾。**
- **自动同步打开时全量托管、关闭后回到选择集且选择集未被污染。**
- **多账号同名模型自动归并为同一逻辑模型的候选目标。**
- **模型别名在归并前正确应用。**
- **取消勾选有流量的模型触发警告。**
- **自动创建的逻辑模型在零目标时被清理；手工创建的保留但从 `/v1/models` 移除。**
- 已选模型消失后保留记录并暂停，再次出现自动恢复。
- 手动模型不被错误刷新删除。

### 26.6 成本与校准测试

- **成本聚合严格按逻辑模型分组，不出现跨模型加总。**
- 加权均倍率、最贵目标、最便宜目标计算正确。
- **校准助手按单模型对账，反算结果不受模型组合影响。**
- 保留天数设为 0 时成本页的降级行为。

### 26.7 重启恢复测试

- **粘性绑定重启后恢复，同一前缀仍打到原目标。**
- **性能快照重启后加载，评分不从零开始。**
- 快照超过 24 小时被丢弃。
- 快照写入失败不影响热路径。

### 26.8 安全与稳定性测试

- Key 与 NewAPI 访问令牌不出现在日志、错误和备份元数据中。
- 不同分组不能读取彼此 Responses ID。
- SSRF、DNS Rebinding 和跨域重定向。
- 加密数据篡改检测。
- 临时文件在取消和崩溃恢复后清理。
- JSON 和 SSE 解析使用模糊测试。
- 单个请求任务 panic 不导致进程退出。
- SQLite 忙、磁盘满和写入通道满时，推理热路径可控降级。

### 26.9 性能验收

在固定硬件和固定请求体下分别测量原生透传与跨协议转换：

- 网关自身延迟，不把上游网络时间混入。
- 并发连接、SSE 长连接和内存上限。
- 64 KB、8 MB 和 64 MB 请求体。
- 日志开启、保留设为 0 和 SQLite 清理期间的差异。
- 粘性绑定与性能快照落盘期间的热路径抖动。
- HTTP 连接复用率和每请求分配量。

正式发布前再根据测试机器给出可复现的吞吐数字，不提前承诺脱离硬件条件的 RPS。

## 二十七、分阶段实施顺序

每一阶段结束都必须是可运行产品，不能长期保留两套实现或临时兼容层。

**阶段顺序按"最早能真实替换 axonhub"排列，不按协议复杂度排列。** 第一版把 Anthropic 入口和六方向跨协议转换捆在阶段 3，导致工程量最小的东西被工程量最大的东西拖了三个阶段——而 Claude Code 要到那时才第一次能跑起来。实际上同协议原生透传只是改鉴权头和模型名，三个入口一样简单；难的是跨协议转换，那个可以晚做。

### 阶段 1：三个入口能真实干活

- Rust 服务、SQLite、主密钥和首次管理员设置。
- 分组、下游 Key、账号、逻辑模型和单目标映射。
- **三个入口的原生透传（同协议上下游），普通与 SSE**：
  - `POST /v1/chat/completions`
  - `POST /v1/messages`
  - `POST /v1/messages/count_tokens`
  - `POST /v1/responses`
- `GET /v1/models` 与 `/v1/models/{model}`，按鉴权头双形状。
- 请求 ID、基础错误、HTTP 状态码映射、密钥加密和脱敏日志。
- 嵌入式最小后台。
- Docker 与 Linux 开发构建。

**验收**：Claude Code、Codex CLI 和一个 OpenAI Chat 客户端三者都能通过 Akhub 完成真实工作（单上游、单目标）。

Responses 的 `previous_response_id` 在单目标场景下天然正确，不需要状态链——这也是把它放进阶段 1 的可行性依据。

### 阶段 2：可靠组内路由

- 多目标、账号/模型调度单元。
- **严格优先级阶梯与分层。**
- **层内比值归一化评分、四维权重、加权随机分配。**
- **粘性：稳定前缀哈希键、体积×新鲜度等待预算、持久化绑定。**
- 并发、RPM/TPM、请求体可重放。
- **成本+时间尝试预算、每目标独立队列、流式切换边界、熔断与半开。**
- 手动倍率、校准系数与硬性倍率门。
- Sub2API、New API 自动倍率探针、宽限期与终检。
- **性能 EWMA 与快照持久化。**

**验收**：故障注入、倍率变化、宽限期、多模型同 Key 场景全部通过；严格阶梯与层内分配行为符合 §26.3。

### 阶段 3：协议完整性

- 完整中间格式。
- Chat、Responses、Messages 六个方向的转换。
- 工具、思考、图片输入、结构化输出和错误映射。
- **能力降级白名单与 `X-Akhub-Degraded` 标记。**
- 未知字段原生透传与跨协议明确拒绝。

**验收**：官方 SDK 和黄金夹具通过，白名单外能力无静默丢失，白名单内降级有明确标记。

### 阶段 4：Responses 状态与模型能力

- Responses 查询、删除、取消、输入项、压缩和 Token 计数。
- 加密状态链、网关响应 ID 和后台响应。
- **模型选择集、自动同步、模型别名与目标自动归并。**
- LiteLLM 开源能力目录导入（仅能力，不含价格）、证据优先级和过期失效。

**验收**：`previous_response_id` 在允许保存状态时可跨同组上游重建；`store:false` 行为符合边界说明；8 账号 × 20 模型的配置在 10 次操作内完成。

### 阶段 5：完整后台与成本视图

- 概览、账号状态、逻辑模型状态（含综合评分与分维得分）和请求记录页。
- **成本页与校准助手。**
- 账号一键独立复制、测试按钮和批量刷新。
- 配置备份恢复、保留清理和管理员审计。

**验收**：调度权重调错时能通过分维得分自我诊断；成本页数字口径经得起推敲。

### 阶段 6：发布硬化

- 全量协议、故障、安全、模糊和负载测试。
- 优雅关闭、磁盘异常、SQLite 恢复和升级检查。
- 多架构 Docker、Linux 二进制、部署文档和参考配置。
- 许可证与第三方声明审查。

**验收**：满足下一节的完成标准后发布 `v1.0.0`。

## 二十八、完成标准

只有同时满足以下条件，第一期才算完成：

- 管理员无需配置任何模型上下文、多模态、工具或思考字段。
- **8 个账号 × 20 个模型的初始配置在约 10 次操作内完成。**
- 同一 Key 下两个具体模型可以映射同一逻辑模型，并分别调度。
- 所有调度严格在分组内完成。
- **高优先级层只要还有合格目标，低优先级层永远拿不到流量；不存在任何让自适应越过优先级的路径。**
- **相同优先级的目标按四维加权评分分配流量，分差小时接近均分，分差大时保留探索流量。**
- **归一化全部使用比值，微小差异不被放大，同值时不产生虚假区分。**
- 自动倍率超过宽限期或超限时，没有任何新推理请求能发到该账号；宽限期内可用但降权并告警。
- **探针系统性故障不会导致 Akhub 把自己打死。**
- 倍率变化不会取消已在途请求，但会阻止排队请求和后续尝试。
- 慢请求不会触发并行复制调用。
- SSE 首个语义块之后绝不拼接另一个上游。
- **`/compact` 与注入内容不会打断会话粘性。**
- **粘性请求的长等待不阻塞可以立即执行的无粘性请求。**
- 工具、图片输入、结构化输出和未知字段没有静默丢失；thinking 的降级仅发生在故障切换且有明确标记。
- OpenAI Chat、Responses HTTP 全套、Anthropic Messages（含 `count_tokens`）能被官方 SDK 使用。
- **网关错误的 HTTP 状态码使客户端的重试行为符合预期。**
- 模型刷新失败或返回 0 个模型不会破坏现有配置或选择集。
- **重启后粘性与评分立即可用，不存在盲调度期。**
- **成本页不出现任何跨模型加总的失真数字，且同时显示最便宜目标作为诚实基准。**
- 普通请求正文不落日志；必要 Responses 状态加密保存并可清理。
- Docker 和 Linux 原生版都能从空数据目录完成首次启动和真实调用。
- 热路径不等待 SQLite 日志写入，没有无界队列或无界缓存。

## 二十九、已知边界与后续方向

- 单实例意味着进程或主机故障时服务会中断。Akhub 在你和上游之间成为一个新的单点：此前站 A 挂了可以手动换站 B，此后 Akhub 挂了则全部不可达。缓解手段是 `Restart=always` / `restart: unless-stopped` 加上 §20.1 的状态持久化，使重启代价压到数秒且无缓存损失。第一期不承诺控制面高可用。
- **账号级的单个校准系数表达不了同一站点内跨模型的加价差异。** 若某站对 claude 加价 20%、对 glm 加价 50%，一个系数无法同时对上两者。这是为了避免维护价格表而明确接受的取舍。需要更高精度时只能手工按主力模型校准，并接受其他模型的偏差。
- **成本口径只在同一逻辑模型内部有效。** Akhub 无法回答"本月总共花了多少钱"，因为那需要跨模型的价格权重。
- 上游未提供倍率推送时，只能通过定时刷新发现变化，存在刷新间隔窗口；宽限期机制进一步扩大了这个窗口，换来的是可用性。
- `store:false` 或状态保留设为 0 时，无法保证 Responses 跨上游恢复完整历史。
- 上游已收到请求后即使取消，也可能已经计费。客户端断开时立即取消只能减少浪费，不能消除。
- 供应商专有工具没有等价协议时只能走原生上游或明确拒绝。
- thinking 的降级在多轮工具调用中可能改变后续回合的历史结构。已知主流 agent 客户端会自适应处理，但这是一个未经穷尽验证的假设。
- 严格阶梯意味着一个"合格但很慢"的最高优先级目标会持续吃掉全部流量，Akhub 不会自动绕开它。这是"所见即所得"的代价；后台的综合评分列让这件事一眼可见，但处置权在你手里。
- 后续可以按独立阶段增加 Responses WebSocket、语音、Embedding、视频、Windows 和多实例，但不能提前把这些复杂度塞入第一期核心。（图片端点已在第一期以"仅原生转发"口径纳入，见 §14.9。）

### 29.1 网关托管的后台任务（第二期设计草案）

第一期的口径是"只代理原生后台，上游不支持就明确拒绝"。不自己托管的原因见下；这里把它写成可实施的设计，并明确**要做得比参考实现更诚实的三件事**。

**参考实现的做法与缺陷**（2026-09-17 查证）

LiteLLM 的 `polling_via_cache`：`background=true` 且开了开关、有 Redis 时立刻返回 `litellm_poll_*` ID，一个 `asyncio.create_task` 在进程内消费上游流并把状态写进 Redis（默认 TTL 3600 秒），客户端轮询取部分结果。三处缺陷：

1. **取消是假的**：`cancel_polling()` 只把缓存状态改成 `cancelled`，而 `asyncio.create_task()` 的返回值没有保存，拿不到 task handle、无法中断上游。用户以为停了，上游仍在跑并计费。
2. **重启即失联**：任务只活在事件循环、状态在 Redis，`response_polling/` 里没有任何恢复逻辑。重启后 `polling_id` 仍是 `in_progress`，却没有东西推进它。
3. **强依赖 Redis**，与单实例 + SQLite 的第一期形态不兼容。

New API 把 `background` 字段注释掉不实现；Sub2API 直接返回 `400 Unsupported parameter: background`。当前真实可用的上游都不支持后台，所以这件事的价值在于支持那些确实提供后台能力的站点，而不是补一个空转功能。

**Akhub 的落点**

- 触发条件：`background: true` 且目标上游原生支持时，默认只走原生；自托管仅在上游不支持且分组显式打开 `allow_managed_background` 时启用。
- ID 分层：`bg_akh_*` 是网关托管，`resp_akh_*` 是原生代理；查询/取消/删除先看前缀再决定找谁，不认识的 ID 一律 `response_state_expired`。
- 持久化：`background_tasks` 表（`id`、`group_id`、`logical_model`、`account_id`、`target_id`、`status`、`upstream_id`、`created_at`、`heartbeat_at`、`finished_at`、`error_code`、`sealed_output`），状态与执行者都落 SQLite，**不引入 Redis**。
- 执行：受控任务循环（有界并发、每任务独立超时）取任务，流式消费上游并把输出增量写回加密的 `sealed_output`。
- **真取消**：任务句柄存在内存表，取消时先断开上游连接（流式路径 drop `reqwest::Response` 即可），再置 `cancelled`；上游若已产生费用，记录里标明"取消时上游可能已计费"。
- **重启不骗人**：启动时扫描 `status in ('queued','running')`：有 `upstream_id` 且上游支持查询的重绑为代理查询；否则标记 `interrupted`（附原因），客户端拿到明确失败，绝不停留在 `in_progress`。
- 生命周期与配额：任务开始与结束各结算一次；长任务用"任务级名额"而非长期占并发槽；倍率变化不影响已开始的任务，新任务按最新倍率准入。
- 边界：进程挂掉任务即中断（靠 `interrupted` 说清楚）；不承诺多实例；`store:false` 时不落输出，只保留定位映射。

**验收标准**（第二期）

1. 上游不支持 + 分组未开开关 → 400 `unsupported_parameter`（第一期行为不变）。
2. 上游不支持 + 开了开关 → 返回 `bg_akh_*`，可查到 `queued → in_progress → completed`，输出完整。
3. 取消 `bg_akh_*` 后上游连接确实断开（挂起的假上游断言连接关闭），状态为 `cancelled`。
4. 任务跑到一半 kill 进程，重启后状态是 `interrupted` 或跟着上游走，**绝不停留在 in_progress**。
5. `background=true` 打到原生支持的上游时走 `resp_akh_*` 代理路径，与第一期一致。


参考来源：

- [AxonHub，检查版本 `4483c2e`](https://github.com/looplj/axonhub)
- [LiteLLM，检查版本 `4ba8517`](https://github.com/BerriAI/litellm)
- [New API，检查版本 `27ff6a8`](https://github.com/QuantumNous/new-api)
- [Sub2API，检查版本 `e98ef32`](https://github.com/Wei-Shaw/sub2api)
- [OpenAI Responses 接口文档](https://developers.openai.com/api/reference/resources/responses/methods/create)
- [OpenAI 会话状态文档](https://developers.openai.com/docs/guides/conversation-state)

本版评审中直接查证的 New API 代码位置：

- `model/channel_cache.go:139-142` — 重试序号即优先级层序号，"优先级无效"的根源
- `model/ability.go:81-88` — 同上，DB 路径
- `service/quota.go:64-79` — 计费公式含 ModelRatio / GroupRatio / CompletionRatio 三层
- `middleware/auth.go:140-149` — `TryUserAuth` 只读 cookie session，API Key 无效
- `controller/pricing.go:11-51` — `/api/pricing` 匿名调用拿不到自己的分组
- `router/api-router.go:69-79` — `/api/user/self/groups` 的鉴权要求
- `service/convert.go:150-198` — Claude → OpenAI 转换只处理四类内容块，thinking 被静默丢弃
- `setting/ratio_setting/model_ratio.go:704-721` — `FormatMatchingModelName` 是硬编码特例，不是通用规范化
