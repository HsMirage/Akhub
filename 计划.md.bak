# Akhub 完整产品与技术方案

> 文档状态：需求已确认，可直接进入实施设计与开发拆分。
>
> 更新日期：2026-09-01
>
> 产品定位：一个简单、可靠、高性能的 AI 协议网关与组内负载均衡工具，用来替代 AxonHub 和 LiteLLM 的中间网关用途。

## 一、直接结论

Akhub 使用 Rust 开发，第一期采用单实例加 SQLite，发布 Docker 镜像和 Linux 原生可执行文件。它只负责以下三件事：

1. 把不同下游协议无损转换到合适的上游协议。
2. 在同一分组内，根据人工优先级、可靠性、性能、倍率和当前负载选择上游。
3. 在不重复浪费慢请求、不中途拼接流式响应的前提下完成故障切换。

Akhub 不做计费、账单、充值、用户体系、租户体系或复杂权限。倍率不是收费功能，只是上游成本参考和“不亏本”保护条件。

后台设置必须比 LiteLLM 简单。管理员不需要填写模型上下文长度、最大输出长度、是否支持图片、是否支持工具或是否支持思考等模型卡信息。这些信息由内置开源能力目录、协议适配规则和真实调用结果自动处理。

## 二、目标与非目标

### 2.1 核心目标

- 下游只使用逻辑模型名，不感知真实上游、真实模型名、账号、API Key 或倍率。
- 同一逻辑模型可以绑定多个“上游账号 + 具体模型”，并在组内稳定调度。
- 人工优先级真实生效；候选差距小时人工优先，差距明显时自适应结果优先。
- 同一会话尽量粘在同一上游账号，保留供应商缓存和会话状态。
- 上游故障时自动切换，但上游只是慢时不并行复制请求，避免产生双份费用。
- OpenAI、OpenAI-compatible 和 Anthropic 三类协议之间尽可能完整转换。
- 文本、图片输入、标准函数工具、思考、结构化输出和流式事件不能被静默删除。
- 自动获取 New API、Sub2API 的上游倍率；其他上游允许手动设置。
- 上游倍率未知或超过分组倍率时立即阻止新请求，保证成本保护规则不会被绕过。
- 热路径不等待 SQLite 写入，保证 Rust 实现的性能优势真正落地。

### 2.2 第一期明确不做

- 不做任何下游计费、账单、充值、套餐、余额扣减或利润报表。
- 不做多租户、RBAC、多个管理员或普通用户后台。
- 不跨分组调度，不提供跨组兜底。
- 不做 OpenAI Responses WebSocket。
- 不做图片生成、语音、Embedding、视频等独立 API 端点。
- 不做多实例集群、Redis 依赖或分布式一致性。
- 不做 Windows 发行版，Windows 留到后续版本。
- 不做运行时脚本插件或任意代码插件系统。
- 不要求用户维护模型上下文、多模态、工具、思考等能力字段。

## 三、已确认的产品决策

| 主题 | 最终决策 |
|---|---|
| 开发语言 | Rust，主要原因是低开销、高并发和可控内存占用 |
| 运行方式 | 第一期单实例，SQLite；Docker 与 Linux 原生版同时发布 |
| 管理员 | 单管理员，首次启动必须设置密码 |
| 分组 | 调度硬边界，不允许跨组 |
| 下游 Key | 每个分组一个 Key，同时接受 `Authorization: Bearer` 和 `x-api-key` |
| 逻辑模型 | 分组内暴露逻辑模型名，隐藏具体上游模型 |
| 调度单元 | “账号/API Key + 具体上游模型”；同一个 Key 下的不同具体模型独立调度 |
| 账号复制 | 一键直接复制，所有配置独立，Key 和状态不共享 |
| 人工优先级 | 账号提供默认优先级，具体模型可单独覆盖 |
| 自适应 | 每组只有一个“启用自适应调度”开关，默认开启；不暴露复杂参数 |
| 倍率 | 只用于成本排序和硬性保护，不产生下游账单 |
| 倍率保护 | 上游有效倍率必须小于或等于分组倍率，否则暂停新请求 |
| 倍率未知 | 自动倍率刷新失败立即显示未知并暂停新请求；手动倍率不受自动刷新影响 |
| 中途变更 | 已在途请求完成；新请求和后续重试使用最新倍率重新校验 |
| 默认尝试数 | 最多尝试 3 个不同目标，分组可以覆盖 |
| 队列 | 每组默认 100 个请求、最长等待 30 秒，可修改或设为 0 |
| 请求体 | 8 MB 内存缓冲，超过后使用临时文件；单请求默认上限 64 MB |
| 模型拉取 | 默认手动；账号可开启“自动更新上游模型” |
| 空模型列表 | 拉取报错、格式错误或得到 0 个模型时，不改变现有列表 |
| 测试按钮 | 发送一次真实的 `hi`；测试数据不参与自适应、粘性和正常重试统计 |
| 正文日志 | 普通请求不保存正文 |
| 元数据保留 | 默认 30 天，管理员可调；设为 0 时只保留实时聚合数据 |
| Responses 状态 | 仅为 `previous_response_id`、`conversation` 和 HTTP 生命周期保存必要加密状态 |
| 配置生效 | 不可变版本快照原子切换；在途请求使用旧快照，新请求使用新快照 |

## 四、核心概念

### 4.1 分组

分组是完全独立的调用空间，包含：

- 一个下游 API Key。
- 一个分组倍率。
- 一组逻辑模型。
- 一组独立上游账号。
- 自适应开关、重试上限、队列容量和等待时间。

分组之间不共享账号、Key、粘性绑定、倍率状态、健康状态或性能数据。

### 4.2 上游账号

一个上游账号代表一套独立凭据和连接配置，包括上游类型、Base URL、API Key、首选协议、倍率来源、默认人工优先级、模型列表和限流设置。

同一个真实 Key 如果要放入两个分组，必须复制成两个独立账号记录。复制后的记录最初可以拥有相同 Key，但修改、健康状态、倍率状态和调度状态完全独立。

### 4.3 上游模型

上游模型是某个账号实际返回或人工录入的模型，例如 `glm-5.3-bf16`。上游模型只属于一个账号。

### 4.4 逻辑模型

逻辑模型是下游看到的名称，例如 `glm-5.3`。创建时可以直接选择某个上游模型，并用它的名称作为默认逻辑模型名，之后可修改。

### 4.5 调度目标

调度目标是以下三者的确定组合：

`分组 + 上游账号 + 具体上游模型`

例如同一个 Key 下的 `glm-5.3-bf16` 和 `glm-5.3-pf8` 是两个目标，必须分别记录优先级、模型错误、性能和能力证据。账号级额度、Key 失效和最大总并发仍由两个目标共享。

## 五、参考项目的取舍

### 5.1 New API

主要借鉴：

- 上游渠道与模型列表管理方式。
- 模型列表安全刷新和差异应用。
- 协议适配器的组织方式。
- 会话粘性、请求体缓存和渠道测试交互。
- Responses HTTP 接口的兼容处理。

不采用：计费、用户、套餐、充值、渠道类型堆叠和大量全局倍率配置。

### 5.2 Sub2API

主要借鉴：

- 账号、分组和独立复制思路。
- 真实请求驱动的状态学习。
- Failover 循环、半开恢复和会话粘性。
- `/v1/sub2api/billing` 的 Key 级倍率探针。
- 倍率保护在选号后、真正发请求前再次校验的做法。
- Responses 思考、工具和状态切换相关处理。

不采用：下游计费、用户额度、订阅、OAuth 账号体系和跨平台业务逻辑。

### 5.3 LiteLLM

主要借鉴：

- OpenAI Chat、OpenAI Responses、Anthropic Messages 的完整转换边界。
- 模型能力目录和参数支持规则。
- 流式事件状态机与错误映射。
- Responses 的创建、查询、删除、取消、输入项和压缩接口。
- 未知能力、供应商扩展和原生透传的处理原则。

不采用：复杂配置文件、虚拟 Key 管理、预算、团队、插件回调、企业功能和庞大的路由参数面板。

### 5.4 AxonHub

只借鉴首次使用和基础配置较直观的部分。明确不采用要求管理员填写上下文大小、输出长度、多模态、工具能力等模型元数据的设计。

### 5.5 许可证边界

- New API 是 AGPL-3.0，Sub2API 是 LGPL-3.0；只借鉴公开行为和架构思路，不直接复制其实现代码。
- AxonHub 主体为 Apache-2.0，但部分目录使用 LGPL-3.0；同样只做行为参考。
- LiteLLM 的非企业部分为 MIT；模型目录可以按许可引入，但必须保留来源、版本和许可声明。
- Akhub 的协议适配代码使用干净实现，并以官方协议样例和自建测试夹具验证。

## 六、后台页面与操作流程

后台采用一个嵌入 Rust 二进制的前端应用，不需要单独部署前端服务。桌面和手机都可使用，但主要面向桌面管理。

### 6.1 首次启动

首次打开后台时只进行以下操作：

1. 设置管理员密码。
2. 显示数据目录和主密钥状态。
3. 进入空的概览页。

不强迫用户在向导中创建分组或账号。

### 6.2 概览页

显示：

- 当前请求量、成功率、平均延迟和高位延迟。
- 在途请求数、排队请求数和队列超时数。
- 可用、降级、暂停和倍率未知的目标数量。
- 最近错误和最近配置变化。

不提供自定义图表搭建器。

### 6.3 分组页

每个分组显示：

- 名称和下游 Key 前缀。
- 分组倍率。
- 自适应开关。
- 最大尝试目标数，默认继承全局值 3。
- 队列容量和等待时间。
- 逻辑模型数量、可用目标数量和当前告警。

Key 创建后只完整显示一次。之后只显示前缀，并提供重新生成按钮。重新生成后旧 Key 立即失效。

### 6.4 账号页

账号列表可按分组、状态和上游类型筛选。每行提供：

- 编辑。
- 一键复制。
- 测试连接。
- 手动获取模型。
- 手动刷新倍率。
- 启用或停用。

复制按钮不弹出复杂向导：直接创建一个停用状态的“名称 - 副本”，复制静态配置和模型列表，重新加密 Key，重置健康、粘性、性能和自动倍率状态，然后打开编辑页。修改分组后，原分组的逻辑模型绑定会被清除，避免跨组引用。

账号编辑页只显示必要字段：

- 名称、所属分组、上游类型、Base URL、API Key。
- 首选协议和“允许运行时自动适配”。
- 默认人工优先级，范围 `0-100`，数值越大越优先。
- 倍率模式：自动或手动。
- 自动更新上游模型开关，默认关闭。
- RPM、TPM、最大并发，默认不限。

不会出现上下文长度、最大输出长度、多模态、工具或思考能力输入框。

### 6.5 逻辑模型页

操作流程：

1. 选择分组。
2. 从任一上游账号选择一个模型，或手动输入模型名。
3. 默认使用所选上游模型名作为逻辑模型名。
4. 继续添加其他“账号 + 模型”作为候选目标。
5. 必要时给某个目标覆盖账号默认优先级。

目标行显示当前有效优先级、倍率、健康状态、首字延迟、输出速度和暂停原因，但不要求用户配置这些数据。

### 6.6 请求记录页

支持按时间、请求 ID、分组、逻辑模型、实际目标、状态和错误码筛选。记录内容包括：

- 请求 ID、开始时间、协议和是否流式。
- 分组和逻辑模型。
- 每次尝试的目标、端点、耗时和失败原因。
- 首字延迟、总延迟、Token、切换次数和配置版本。
- 最终网关错误码。

默认不显示也不保存消息正文、图片、工具参数或思考内容。

### 6.7 设置页

只保留以下系统设置：

- 自动倍率刷新间隔。
- 自动模型刷新间隔。
- 请求元数据保留天数。
- Responses 可重放状态保留天数。
- 全局默认重试数、请求总超时和请求体上限。
- 配置备份与恢复。
- 当前版本、模型目录版本和适配器版本。

高级算法参数不进入后台。

## 七、下游公开接口

### 7.1 第一期推理与模型接口

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/v1/chat/completions` | OpenAI Chat Completions，支持普通和 SSE |
| `POST` | `/v1/messages` | Anthropic Messages，支持普通和 SSE |
| `POST` | `/v1/responses` | OpenAI Responses，支持普通、SSE 和 HTTP 后台模式 |
| `GET` | `/v1/responses/{response_id}` | 查询已保存或后台响应 |
| `DELETE` | `/v1/responses/{response_id}` | 删除响应及其本地状态 |
| `POST` | `/v1/responses/{response_id}/cancel` | 取消后台响应 |
| `GET` | `/v1/responses/{response_id}/input_items` | 查询输入项 |
| `POST` | `/v1/responses/compact` | 压缩 Responses 上下文 |
| `POST` | `/v1/responses/input_tokens` | 计算 Responses 输入 Token |
| `GET` | `/v1/models` | 返回当前分组配置的逻辑模型 |
| `GET` | `/v1/models/{model}` | 返回单个逻辑模型 |

不增加历史别名或兼容路径；第一期只维护表中的标准路径。

### 7.2 鉴权规则

- 所有 `/v1` 接口都使用分组 Key 鉴权。
- 同时支持 `Authorization: Bearer <key>` 和 `x-api-key: <key>`。
- 两个头同时存在且值不同，返回鉴权错误，不猜测使用哪一个。
- Key 唯一对应一个分组，所以模型、队列、倍率保护和调度自然被限制在该分组。

### 7.3 模型列表规则

- 只返回该分组已启用的逻辑模型。
- 临时没有健康目标时，逻辑模型仍保留在列表中，避免客户端因为瞬时故障缓存错误的模型列表。
- `owned_by` 固定为 `akhub`，不暴露上游名称。
- 请求未知逻辑模型时直接返回 `model_not_found`，不跨组搜索。

### 7.4 后台 REST API

后台页面只调用同一服务内的 `/admin/api`，不直接访问 SQLite。除首次设置、登录和健康接口外，所有接口都需要管理员会话。

| 方法 | 路径 | 作用 |
|---|---|---|
| `GET` | `/admin/api/setup/status` | 判断是否需要首次设置 |
| `POST` | `/admin/api/setup` | 创建首个管理员并初始化主密钥状态 |
| `POST` | `/admin/api/auth/login` | 管理员登录 |
| `POST` | `/admin/api/auth/logout` | 管理员退出 |
| `GET` | `/admin/api/overview` | 概览指标和告警 |
| `GET/POST` | `/admin/api/groups` | 查看和创建分组 |
| `GET/PATCH/DELETE` | `/admin/api/groups/{id}` | 修改或删除分组 |
| `POST` | `/admin/api/groups/{id}/regenerate-key` | 重新生成分组 Key |
| `GET/POST` | `/admin/api/accounts` | 查看和创建上游账号 |
| `GET/PATCH/DELETE` | `/admin/api/accounts/{id}` | 修改或删除账号 |
| `POST` | `/admin/api/accounts/{id}/copy` | 独立复制账号 |
| `POST` | `/admin/api/accounts/{id}/test` | 发送一次真实 `hi` 测试连接 |
| `POST` | `/admin/api/accounts/{id}/models/refresh` | 手动获取上游模型列表 |
| `POST` | `/admin/api/accounts/{id}/multiplier/refresh` | 手动刷新账号倍率 |
| `GET/POST` | `/admin/api/logical-models` | 查看和创建逻辑模型 |
| `GET/PATCH/DELETE` | `/admin/api/logical-models/{id}` | 修改或删除逻辑模型 |
| `GET/POST/PATCH/DELETE` | `/admin/api/targets` | 管理逻辑模型的账号 + 模型目标 |
| `GET/PATCH` | `/admin/api/settings` | 查看和修改全局设置 |
| `GET` | `/admin/api/requests` | 分页查询请求元数据 |
| `GET` | `/admin/api/metrics` | 查询分钟聚合和目标性能 |
| `POST` | `/admin/api/backup/export` | 导出加密配置备份 |
| `POST` | `/admin/api/backup/import` | 校验并原子恢复配置备份 |

所有列表接口都分页并限制上限。返回对象中的 Key 只有前缀和状态；完整 Key 只在创建或重新生成成功的响应中返回一次。写接口携带 `config_version`，版本不一致返回 `409 config_conflict`，后台提示用户重新加载后再保存，避免覆盖其他修改。刷新、测试和复制操作返回结构化结果，并在审计日志中留下操作者、对象和结果，不返回请求正文。

## 八、一次请求的完整处理流程

1. 生成或接收请求 ID。
2. 验证请求体大小和 JSON 基本格式。
3. 验证分组 Key，取得不可变配置快照。
4. 根据路径识别下游协议，解析逻辑模型和请求所需能力。
5. 构建可重放请求体：8 MB 内存，超过后落临时文件。
6. 找到该逻辑模型在本组内的所有调度目标。
7. 执行硬性资格过滤：启用状态、模型存在、能力、协议、倍率、额度、熔断和限流。
8. 尝试命中会话粘性目标；目标不合格时清除粘性并重新选择。
9. 使用人工优先级和自适应分数选择目标。
10. 获取目标并发槽位；没有容量时切换其他目标或进入分组队列。
11. 真正发出上游请求前，再读取最新倍率和动态状态进行终检。
12. 选择该账号最合适的原生端点或跨协议适配器。
13. 转发非流式响应，或通过协议状态机逐事件转发 SSE。
14. 根据结果更新真实流量样本、健康状态、额度状态和能力证据。
15. 异步写入请求元数据，释放槽位并唤醒队列。

SQLite 写入、历史聚合和后台刷新都不能阻塞第 12 至 13 步。

## 九、候选资格与调度算法

### 9.1 硬性资格过滤

以下任一条件不满足，目标不能参与本次选择：

- 不属于当前分组。
- 账号、逻辑模型或目标被管理员停用。
- 上游模型已从有效列表消失。
- 自动倍率未知。
- 有效上游倍率高于分组倍率。
- Key 已失效、账号被封禁或总额度已明确耗尽。
- 账号或模型正在冷却，且当前请求没有获得唯一半开试运行名额。
- 请求需要的能力已被明确证实为不支持。
- 请求包含不能无损转换的供应商专有字段，且该目标没有匹配的原生端点。
- RPM、TPM 或最大并发已达到硬限制。

能力状态为“未知”不等同于不支持，但排在已确认支持的目标之后。

### 9.2 人工优先级

- 账号默认优先级为 `50`，范围 `0-100`，越大越优先。
- 调度目标可以覆盖账号默认值。
- 同一个 Key 只映射一个模型时，账号默认优先级直接决定该 Key 的排序。
- 同一个 Key 映射多个模型时，每个“Key + 模型”分别使用自己的有效优先级。

### 9.3 自适应信号

只使用真实用户请求，测试按钮和后台任务不进入样本。按“逻辑模型 + 调度目标 + 协议 + 是否流式”分别维护：

- 成功率和连续错误。
- 首字或首个语义事件延迟。
- 非流式总延迟。
- 每秒输出 Token。
- 429、5xx 和损坏响应比例。
- 当前在途数与最大并发占比。
- 有效倍率以及距离分组倍率上限的余量。

统计采用 EWMA，即“越新的请求权重越高的移动平均”，并设置最小样本数和滞后防抖。

### 9.4 人工与自适应的合并规则

系统内部把自适应质量归一化到 `0-1`，默认组成如下：

- 可靠性：50%。
- 成本余量：20%。
- 首字延迟：15%。
- 输出速度：10%。
- 当前负载：5%。

这些比例不出现在后台设置中。

选择规则：

- 自适应分差不超过 10%：先看人工优先级，再看自适应分数。
- 自适应分差达到 30%：自适应分数优先，可越过较高的人工优先级。
- 分差处于 10%-30%：平滑混合两者，避免突然跳变。
- 新目标不足 20 个真实样本时使用保守中性分，并获得少量真实流量收集样本。
- 新赢家必须持续领先至少 30 秒才替换稳定赢家，硬故障和倍率状态变化除外。

这保证“差距小时人工优先，差距大时自适应优先”，同时不会让一个暂时的快请求造成频繁切换。

### 9.5 同分目标

同分目标使用带负载感知的加权随机选择，而不是固定选择列表第一个，避免所有并发请求集中到同一账号。

## 十、会话粘性

### 10.1 粘性键来源

按以下优先级自动识别，不要求用户配置规则：

1. Responses 的 `conversation`、`previous_response_id` 或网关响应 ID。
2. 显式会话头，例如 `session-id`、`conversation_id`、`x-session-id`、`x-session-affinity`。
3. `prompt_cache_key` 或协议中的稳定用户/会话字段。
4. 系统提示、工具定义和第一条用户消息的不可逆摘要，并混入分组 Key 标识和逻辑模型。

只保存哈希，不把用于粘性的正文写入日志。

### 10.2 粘性行为

- 命中且目标仍合格时，给予最高选择偏好，但仍受倍率、额度、熔断和限流硬门控制。
- 粘性不是永久锁死；目标故障、倍率未知或倍率超限时允许在同组切换。
- 普通粘性默认使用 1 小时滑动过期，仅保存在有界内存缓存中。
- Responses 状态链使用独立的持久化映射，不受普通粘性缓存重启丢失影响。
- 目标恢复后，新会话可重新选择；旧会话不会强制抢回已经稳定的新目标。

## 十一、倍率获取与不亏本保护

### 11.1 倍率语义

- 分组倍率是允许使用的最高上游倍率。
- 上游倍率是账号或具体目标当前真实生效的成本倍率；如果上游只按模型返回倍率，就按“账号 + 模型”目标保存。
- 倍率比较使用定点整数，不使用浮点数直接比较，避免边界误差。
- `上游倍率 <= 分组倍率` 才能发起新推理请求。
- 倍率只影响资格和调度，不计算下游费用。

### 11.2 倍率来源

每个账号只能选择一种来源：

- **自动 - Sub2API**：调用 Key 级 `/v1/sub2api/billing`，严格校验对象类型、版本、计费范围、有效倍率、观察时间和峰值倍率字段。
- **自动 - New API**：读取上游提供的 `/api/pricing`、`/api/ratio_config` 或等价倍率接口，解析模型/路由倍率。若接口同时给出 Key 所属上游分组或有效 Key 倍率，则绑定该上下文；New API 通常只暴露模型级价格时，转换成每个“账号 + 具体模型”目标的有效倍率。无法确认 Key 所属分组或适用倍率时不猜测、不使用旧值兜底，状态为未知；管理员可选择上游分组或切换手动倍率。多个可能值并存时取最高值做成本保护。
- **手动**：管理员输入倍率，状态始终视为已知，直到管理员修改或清空。

New API 的模型价格数据和 Key 的分组倍率是两个概念。Akhub 只把可确定的账号/分组有效倍率用于硬门；不能从接口唯一确定时不猜测，状态保持未知，管理员可选择上游分组或切换手动倍率。

### 11.3 自动刷新

- 默认每 5 分钟刷新一次，可在设置页调整。
- 各账号加入随机抖动，避免同时请求上游。
- 提供账号级和批量手动刷新按钮。
- Sub2API 若返回明确峰值时段，Akhub 按已校验的数据在本地计算当前有效倍率，不必等到下一轮刷新。
- 刷新失败后按 `30 秒、1 分钟、2 分钟、5 分钟、10 分钟、30 分钟` 退避重试，并加入抖动。

### 11.4 刷新失败

自动倍率刷新只要失败一次：

1. 当前倍率立即显示“未知”。
2. 账号停止接收新推理请求。
3. 已经在途的请求继续完成。
4. 清除指向该账号的普通粘性绑定。
5. 后台按退避计划继续刷新。
6. 成功后恢复此前人工优先级并重新参与调度。

绝不继续使用过期倍率作为兜底。

### 11.5 倍率发生变化

- 刷新结果通过动态状态原子替换。
- 排队中或尚未真正发给上游的请求必须重新校验。
- 新倍率超过分组倍率时，账号立即进入 `multiplier_exceeded` 暂停状态。
- 已经把请求发给上游的调用不取消，避免破坏用户结果和产生已经支付却拿不到结果的损失。
- 倍率恢复到允许范围后自动恢复，不需要手动启用。

### 11.6 能力边界

轮询无法在上游“刚改倍率但 Akhub 尚未刷新”的几分钟内提前知道变化。默认 5 分钟刷新用于缩短该窗口；需要更严格保护时，应缩短刷新间隔或给分组倍率预留安全余量。任何宣称能在上游未提供推送接口时做到零检测窗口都不真实。

## 十二、额度、健康与熔断状态

### 12.1 状态范围

| 现象 | 影响范围 |
|---|---|
| Key 失效、账号封禁、总额度耗尽、自动倍率未知 | 整个账号 |
| 模型不存在、模型限流、模型能力不支持 | 账号 + 具体模型 |
| 单个协议端点 404、405 或明确不兼容 | 账号 + 协议端点 |
| 单次网络错误、上游 5xx | 当前尝试；达到阈值后影响目标 |
| 单纯变慢 | 只降低后续自适应权重，不熔断 |

### 12.2 运行状态

目标运行状态包括：

- `active`：正常参与调度。
- `degraded`：仍可调用，但权重降低。
- `cooldown`：暂时停止新请求。
- `half_open`：只允许一个真实用户请求试运行。
- `multiplier_unknown`：自动倍率未知，硬暂停。
- `multiplier_exceeded`：倍率超限，硬暂停。
- `quota_exhausted`：额度耗尽，等待恢复时间或试运行。
- `missing_model`：上游模型列表已移除该模型。
- `disabled`：管理员停用。

### 12.3 熔断规则

- 401/403 明确证明 Key 失效时立即暂停账号，修改凭据或手动测试成功后恢复。
- 明确额度不足且带恢复时间时，按恢复时间进入半开。
- 没有恢复时间时使用指数冷却，结束后只放行一个真实请求。
- 429 优先影响账号 + 模型，并尊重 `Retry-After`。
- 60 秒内连续或高比例出现连接错误、5xx 或损坏响应时进入冷却；孤立错误只触发本次切换。
- 半开请求成功后恢复人工优先级；失败则延长冷却。
- 不发送后台 `hi` 来自动探测恢复。

## 十三、故障切换、超时与排队

### 13.1 尝试次数

- 默认最多尝试 3 个不同调度目标，每个目标最多一次。
- 分组可覆盖该值，包括设为 1。
- 同一目标因安全的端点不匹配而改走另一个端点，不算新的目标尝试；只有 404、405 或明确“端点不支持”才允许这种切换。
- 每次新尝试前重新执行倍率和动态状态终检。

### 13.2 可以切换的情况

- DNS、TCP、TLS 或连接建立失败。
- 明确的 Key 鉴权失败。
- 明确的额度不足或账号封禁。
- 429 限流。
- 上游 5xx。
- 上游返回损坏、无法解析或违反所选协议的响应。
- 模型不存在。
- 明确的能力不支持，且其他目标可能支持。

### 13.3 不切换的情况

- 下游请求 JSON 非法。
- 下游模型名不存在。
- 上下文过长。
- 内容审核拒绝。
- 参数本身不合法。
- 请求已经发出后上游只是慢，没有明确失败。
- 已达到请求总超时。
- SSE 已向下游发送第一个有语义的内容、思考或工具增量。

“慢请求不切换”是为了避免两个上游同时生成、产生两次成本。慢请求完成后，其延迟会降低该目标后续权重。

### 13.4 流式边界

- 在只收到 HTTP 头、空白、注释、ping 或协议开始标记时，仍可根据明确错误决定切换。
- 一旦发送文本、思考、工具参数、引用或其他有语义增量，禁止拼接第二个上游。
- 后续错误按下游协议发送终止错误事件并关闭流。
- 不伪造正常完成事件掩盖上游中断。

### 13.5 请求总超时

- 默认总超时 10 分钟，可在系统设置调整。
- 达到总超时后取消上游请求并返回超时，不再向第二个目标重放。
- 客户端主动断开时立即取消排队或在途上游请求，并释放资源。
- 上游可能已开始计费，因此取消只能尽量减少浪费，不能承诺供应商一定停止计费。

### 13.6 分组队列

- 默认容量 100，最长等待 30 秒；两个值均可按分组修改或设为 0。
- 只有临时容量不足、目标冷却或可恢复限流才进入队列。
- `multiplier_unknown`、`multiplier_exceeded`、鉴权失败、未知模型和明确不支持能力绝不排队。
- 等待期间配置或倍率改变时重新选择目标。
- 客户端断开立即移出队列。
- 队列满返回 `queue_full`，等待超时返回 `queue_timeout`，并给出 `Retry-After`。

## 十四、协议网关设计

### 14.1 总体原则

协议层采用“原生优先、完整中间格式、无法表达就报错”的方式：

1. 下游与可用上游协议相同时，尽量原样透传，只改鉴权、Base URL 和模型名。
2. 上游有匹配下游协议的原生端点时，优先使用该端点。
3. 没有原生端点时才进入跨协议转换。
4. 未知字段在同协议路径原样保留。
5. 跨协议时任何不能可靠表达的字段都不能静默删除。

### 14.2 上游协议设置

账号保存两个值：

- `preferred_endpoint`：管理员选择的首选端点，例如 OpenAI Chat、OpenAI Responses 或 Anthropic Messages。
- `adaptive_protocol`：是否允许运行时选择更合适的原生端点，默认开启。

例如账号首选 `/v1/chat/completions`，但真实上游也支持 `/v1/messages`。收到 Anthropic 请求时，如果 `/v1/messages` 未被证实不支持，先走原生 Messages；失败明确证明端点不存在后，再考虑转换到 Chat。

不会通过后台模拟对话探测端点。端点能力来自配置、模型目录和真实用户请求。

### 14.3 端点选择顺序

对一个已选调度目标：

1. 与下游协议相同且已确认支持的端点。
2. 与下游协议相同但能力未知的端点。
3. 账号首选端点，前提是请求可无损转换。
4. 其他已确认支持、且转换保真度相同的端点。

明确不支持的端点在限制记录过期或配置变化前不会重复尝试。

### 14.4 中间请求格式

内部统一请求至少保留：

- 逻辑模型与真实目标模型。
- `system`、`developer`、`user`、`assistant`、`tool` 等角色语义。
- 有顺序和索引的文本、图片、文件、拒绝、引用等内容块。
- 工具定义、工具选择、并行工具调用、调用 ID、参数增量和工具结果。
- JSON Schema 和结构化输出要求。
- 思考强度、思考摘要、加密思考块、签名和供应商原始扩展。
- stop、温度、top-p、最大输出等生成参数。
- usage 的输入、输出、缓存、思考等细分 Token。
- 未识别字段的原始扩展区。

中间格式不是“只保留公共最小字段”，而是能表达三个目标协议的并集。

### 14.5 中间流式事件

流式层使用独立事件状态机，至少包含：

- 响应开始和响应 ID。
- 内容项开始与结束。
- 文本增量。
- 思考或思考摘要增量。
- 工具调用开始、参数增量和结束。
- 引用、拒绝和其他内容项。
- usage 更新。
- 正常完成、未完成和错误。

适配器必须维护事件顺序、内容索引、工具调用 ID、stop reason 和最终 usage，不能用简单字符串替换 SSE。

### 14.6 关键转换规则

| 能力 | 转换原则 |
|---|---|
| 文本消息 | 保留角色、顺序和多内容块，不擅自合并不同语义角色 |
| 图片输入 | Base64 与原生 URL 只在目标协议支持时转换；不为转换主动下载外部 URL |
| 标准函数工具 | 转换名称、描述、JSON Schema、调用 ID、参数和工具结果 |
| 供应商专有工具 | 只有目标协议能完整表达时才允许，否则选择原生目标或报错 |
| 思考 | 保留可见摘要、加密内容、签名和对应生命周期；不能只留下正文 |
| 结构化输出 | 只有语义等价时转换；不使用普通提示词伪装严格 Schema |
| stop reason | 映射到下游协议最接近且稳定的原因，同时保留内部原始原因供日志使用 |
| usage | 保留已知细分项；目标协议没有字段时放入允许的扩展区，不能伪造数字 |
| 未知参数 | 同协议透传；跨协议不能表达时返回 `unsupported_parameter` |

### 14.7 请求头与响应头

- 下游鉴权头绝不转发给上游。
- 上游使用账号配置生成正确的 `Authorization`、`x-api-key`、`anthropic-version` 等头。
- `anthropic-beta`、`openai-beta` 等供应商协议头只在匹配供应商时按白名单转发。
- 禁止跨域跟随会携带 Key 的重定向；探针默认完全禁用重定向。
- 返回安全的请求 ID、限流和 `Retry-After` 信息，不返回完整上游 URL 或 Key。

## 十五、OpenAI Responses HTTP 全套能力

### 15.1 网关响应 ID

Akhub 对外生成自己的 `resp_akh_*` ID，并保存它到真实上游响应 ID、账号、模型和分组的映射。这样查询、取消和多轮状态不会暴露上游身份，也不能被另一个分组访问。

### 15.2 `previous_response_id` 与 `conversation`

这两类调用依赖历史内容。不同上游不能识别彼此的响应 ID，所以无损切换需要 Akhub 保存可重放状态。

规则如下：

- `store` 未设或为 `true` 时，加密保存输入项、输出项、工具项、思考协议项和路由映射。
- `store:false` 时不保存可重放正文，只保存完成当前原生生命周期所需的最小定位信息。
- 默认保留 30 天，管理员可调整。
- 保留天数设为 0 时，不保存可重放正文；原生上游仍可通过最小定位映射继续，但无法保证跨上游重建。
- 状态过期后收到引用，返回明确的 `response_state_expired`，不把缺失历史的请求当作新对话发送。
- 删除响应时同时删除本地加密正文、索引和上游映射；若上游支持删除，再转发删除。

### 15.3 后台响应

- 上游原生支持 `background:true` 时，Akhub 代理其创建、查询和取消，并保持目标粘性。
- 上游没有原生后台能力时，不把普通同步请求伪装成可靠后台任务。
- 只有适配器明确实现并通过后台生命周期测试，才允许网关托管后台执行。
- 后台请求一旦得到上游 `in_progress` ID，就固定到该目标；后续查询错误不会转移到另一个上游继续生成。
- 倍率在任务开始后变化时，已经接受的后台任务视为在途任务，允许完成；新后台任务受最新倍率限制。

### 15.4 `compact` 与 `input_tokens`

- `compact` 只使用上游原生压缩能力或经过等价性测试的适配器，不使用普通“帮我总结”对话冒充。
- `input_tokens` 优先调用上游原生计数；有确定本地 Tokenizer 时可本地计算。
- 无法准确计算时返回不支持，不返回估算值冒充精确值。

## 十六、模型发现与能力目录

### 16.1 手动获取模型

账号创建后不自动发模拟对话。管理员点击“获取模型”时：

1. 按账号协议调用上游模型列表接口。
2. 限制响应体大小并校验 HTTP 状态、JSON 结构和模型 ID。
3. 去空、去重并标准化列表，但保留真实模型名称大小写。
4. 在事务中计算差异并生成新配置快照。

### 16.2 自动更新模型

- 每个账号有“自动更新上游模型”开关，默认关闭。
- 开启后按全局间隔执行，默认 30 分钟并加入抖动。
- 拉取失败、格式异常或返回 0 个有效模型时，保留原列表和映射，只记录错误。
- 新模型加入可选列表，但不会自动暴露成逻辑模型。
- 消失且未被使用的模型可从可选列表移除。
- 消失但已绑定逻辑模型的模型保留记录，标记 `missing_model` 并停止新请求。
- 模型重新出现时自动解除 `missing_model`。

### 16.3 手动模型

模型列表接口不可用时允许手动输入上游模型名。手动模型不会被一次失败的自动列表覆盖；真实请求可以逐步记录其能力。

### 16.4 开源能力目录

- 以 LiteLLM 的公开模型目录作为初始来源之一。
- 发布时把指定版本转换为 Akhub 自己的精简、版本化快照并随二进制发布。
- 不在运行中直接读取远程仓库，也不静默热替换协议规则。
- 目录只提供初始能力判断，不要求用户确认模型卡。
- 上下文长度和价格等与本产品无关的数据不作为必填配置。

能力证据优先级固定为：

`明确的真实请求结果 > 上游接口返回 > Akhub 内置适配规则 > 开源目录`

### 16.5 能力学习

能力状态分为：

- `supported`：真实成功或可靠上游声明。
- `unsupported`：上游明确返回该模型不支持该能力。
- `unknown`：没有足够证据。

规则：

- 只有明确“不支持”的错误才缓存为限制。
- 普通 400、5xx、超时和网络错误不能证明能力不支持。
- 限制默认 24 小时过期。
- 模型列表变化、账号协议变化、适配器版本变化时，相关限制立即失效。
- 不会通过删除图片、工具、思考或 Schema 来强行让请求成功。

## 十七、限流、并发和请求体

### 17.1 账号与模型限制

每个账号提供默认值，每个调度目标可以覆盖：

- RPM：每分钟请求数。
- TPM：每分钟 Token 数。
- 最大并发。

默认均为不限。账号级最大并发和额度状态由同一 Key 下的所有模型共享，模型覆盖值用于更细的限制。

### 17.2 Token 预留

- 已知 Tokenizer 时，根据输入和最大输出进行预留，完成后按真实 usage 归还差额。
- 不知道 Tokenizer 时使用保守估算，并在后台明确显示“估算限流”。
- TPM 未配置时完全跳过 Token 估算，避免无意义开销。

### 17.3 请求体存储

- 8 MB 以内保存在内存。
- 8-64 MB 写入数据目录下的专用临时目录。
- 请求结束、失败或客户端断开时删除临时文件。
- 启动时清理上次异常退出遗留且超过安全时间的临时文件。
- 超过 64 MB 返回 `413 request_too_large`，上限可在系统设置调整。

## 十八、错误格式

### 18.1 稳定网关错误码

第一期至少定义：

- `auth_invalid`
- `model_not_found`
- `no_eligible_target`
- `multiplier_unknown`
- `multiplier_exceeded`
- `queue_full`
- `queue_timeout`
- `request_too_large`
- `unsupported_parameter`
- `upstream_timeout`
- `upstream_exhausted`
- `response_state_expired`
- `rate_limited`
- `upstream_protocol_error`
- `internal_error`

### 18.2 协议对应格式

- OpenAI Chat 和 Responses 返回 OpenAI `error` 对象。
- Anthropic Messages 返回 Anthropic `type:error` 对象。
- 每个错误带稳定网关错误码和请求 ID。
- 上游原始错误只提取安全信息，不返回完整 Key、内部 URL、数据库 ID 或堆栈。
- SSE 已开始后使用该下游协议允许的错误事件，随后关闭连接。

## 十九、Rust 技术架构

### 19.1 总体形态

第一期是一个 Rust 进程：

- 同一 HTTP 服务承载 `/v1` 网关、`/admin/api` 管理接口和后台静态文件。
- SQLite 保存配置、加密状态和历史元数据。
- 内存保存配置快照、调度状态、粘性、并发计数、短期指标和队列。
- 后台任务负责倍率刷新、模型刷新、数据清理、聚合刷盘和健康恢复计时。

不引入微服务、消息队列或 Redis。

### 19.2 推荐依赖

| 用途 | 选择 |
|---|---|
| HTTP 服务 | `axum`、`tower`、`tower-http` |
| 异步运行时 | `tokio` |
| 上游客户端 | `reqwest`/`hyper`，使用 `rustls` |
| JSON | `serde`、`serde_json`，原生透传路径尽量使用 `RawValue` |
| SQLite | `sqlx` SQLite，WAL 模式 |
| 配置热切换 | `arc-swap` |
| 日志 | `tracing`、`tracing-subscriber` |
| 密钥内存保护 | `secrecy`、`zeroize` |
| 数据加密 | `chacha20poly1305`，使用 XChaCha20-Poly1305 |
| 密码哈希 | `argon2` |
| Key 摘要 | `hmac`、`sha2`，常量时间比较 |
| 并发控制 | `tokio::sync::Semaphore` |
| 临时文件 | `tempfile` |
| 标识符 | `ulid` 或 `uuid` |

依赖在编码前仍需检查当前稳定版本和维护状态；不为了少量代码自行重写成熟的加密、HTTP 或限流基础能力。

### 19.3 代码模块

保持单个 Rust crate，按职责分模块，不提前拆成多个服务：

```text
src/
  main.rs
  app/                 启动、关闭、共享状态
  admin/               后台 API 与会话
  auth/                管理员和分组 Key 鉴权
  config/              配置事务、校验和 ArcSwap 快照
  domain/              分组、账号、模型、目标等领域类型
  gateway/             下游入口、请求生命周期、错误返回
  protocol/
    openai_chat/        Chat 请求、响应与 SSE
    openai_responses/   Responses HTTP 与 SSE
    anthropic/          Messages 请求、响应与 SSE
    canonical/          完整中间格式
  routing/             资格过滤、优先级、自适应、粘性、队列
  upstream/            Client 池、URL、Header 和探针
  health/              熔断、冷却、半开与额度状态
  catalog/             模型目录、能力证据、列表刷新
  multiplier/          New API/Sub2API/手动倍率
  state/               Responses 状态与后台任务
  storage/             SQLite、批量写入、清理
  observability/       请求记录、聚合和指标
  security/            加密、脱敏、SSRF 防护
web/                   React + TypeScript 管理后台
tests/fixtures/         协议黄金样例和故障场景
```

适配器使用编译期 Rust trait，不做运行时脚本插件。

### 19.4 热路径性能原则

- 每个上游 Origin 复用长期 HTTP Client 和连接池，不按请求创建 Client。
- 配置读取只做一次原子指针读取。
- 动态计数使用原子值、分片锁或 Semaphore，避免全局大锁。
- 原生同协议请求只做浅解析和必要字段改写，尽量不重建整个 JSON。
- 跨协议转换才构建完整中间对象。
- 请求记录通过有界通道交给单独批量写入任务。
- SQLite 开启 WAL、`busy_timeout`，配置写和日志写分离调度。
- 所有队列、缓存、状态表和通道都有容量上限。
- 下游读取慢时通过背压控制上游读取，不无限缓存流式数据。

## 二十、配置与数据结构

### 20.1 主要数据表

| 表 | 作用 |
|---|---|
| `admin_users` | 单管理员密码哈希和安全状态 |
| `app_settings` | 全局设置和当前配置版本 |
| `groups` | 分组、倍率、Key 摘要、重试和队列配置 |
| `upstream_accounts` | 独立账号、Base URL、协议、优先级、倍率模式和限制 |
| `upstream_secrets` | 加密 API Key 和自定义敏感 Header |
| `upstream_models` | 账号模型列表、来源、是否仍存在和最后发现时间 |
| `logical_models` | 分组对外逻辑模型 |
| `dispatch_targets` | 逻辑模型到“账号 + 具体模型”的绑定和优先级覆盖 |
| `endpoint_evidence` | 账号端点支持、不支持、证据和过期时间 |
| `capability_evidence` | 账号模型能力证据和失效条件 |
| `multiplier_snapshots` | 当前倍率、来源、状态、观察和刷新时间 |
| `runtime_target_state` | 冷却、额度恢复时间和必要的恢复状态 |
| `response_locators` | 网关响应 ID 到上游资源的加密定位映射 |
| `response_state` | 可重放的加密 Responses 输入与输出项 |
| `background_jobs` | HTTP 后台响应状态和取消信息 |
| `request_records` | 不含正文的请求元数据 |
| `performance_buckets` | 分钟级目标性能聚合 |
| `admin_audit_log` | 配置修改、复制、测试和备份恢复记录 |

普通粘性绑定和实时 EWMA 不逐请求写 SQLite，重启后重新学习；Responses 明确状态单独持久化。

### 20.2 密钥存储

- 上游 API Key 使用 XChaCha20-Poly1305 加密，每条记录使用独立随机 nonce。
- 下游分组 Key 是高熵随机值，只保存带服务端 pepper 的 HMAC 摘要和可显示前缀，不保存可恢复明文。
- 账号复制时在内存解密并用新 nonce 重新加密，不能让两条记录引用同一个密钥对象。
- 所有日志统一经过密钥与 Header 脱敏器。

### 20.3 倍率数值

数据库和资格判断使用放大后的整数，例如 `1.234567` 保存为 `1234567`。页面只负责格式化显示。性能排序可使用浮点归一化，但硬性倍率门只能使用定点值。

## 二十一、配置原子切换

所有管理端写操作走同一个配置服务：

1. 开启 SQLite 写事务。
2. 应用修改。
3. 在事务内读取完整候选配置。
4. 校验分组、账号、模型绑定、Key 唯一性、倍率和引用完整性。
5. 构建新的不可变 `RuntimeConfig` 并增加版本号。
6. 提交事务。
7. 使用 `ArcSwap` 一次替换当前配置。

在途请求持有旧 `Arc`，所以不会读到一半新一半旧的配置。新请求只拿新版本。

倍率、熔断、并发和额度属于动态安全状态，不等待配置版本；每次真正发请求前读取最新状态。这样即使请求持有旧配置，也不能绕过刚发生的倍率超限。

## 二十二、后台任务

第一期后台任务只有：

- 自动倍率刷新。
- 开启账号的模型列表自动刷新。
- 能力限制、冷却和粘性状态过期清理。
- Responses 状态、请求记录和临时文件清理。
- 请求记录批量写入和性能分钟桶聚合。
- 后台 Responses 生命周期管理。

后台任务遵守以下规则：

- 有界并发和抖动，不能同时压向上游。
- 使用独立超时和响应体上限。
- 不发送模拟对话探测模型能力或账号恢复。
- 应用关闭时停止接收新任务并等待安全刷盘。

## 二十三、安全与隐私

### 23.1 主密钥

- 首次启动在数据目录生成 32 字节主密钥文件，权限设为仅当前用户可读写。
- 也允许通过 `AKHUB_MASTER_KEY` 环境变量提供固定主密钥。
- 环境变量存在时优先使用，不把它写入数据库或日志。
- 主密钥丢失后，数据库中的上游 Key 和加密 Responses 状态无法恢复。

### 23.2 管理后台

- 管理员密码使用 Argon2id。
- 登录 Cookie 使用 `HttpOnly`、`SameSite`，在 HTTPS 下使用 `Secure`。
- 所有写操作需要 CSRF 防护。
- 登录失败有速率限制和短暂锁定。
- 后台不提供查看完整上游 Key 的接口，只允许覆盖更新。

### 23.3 SSRF 与 Key 泄漏防护

- Base URL 保存前做严格 URL 校验，只允许 HTTP/HTTPS。
- 默认阻止环回、链路本地、云元数据和内网地址；确需访问局域网上游时由管理员显式开启该账号的内网访问。
- DNS 解析后再次校验目标地址，防止 DNS Rebinding。
- 带 Key 的请求不跨 Origin 跟随重定向。
- 倍率和模型探针限制响应体大小、超时和 Content-Type。

### 23.4 正文与状态

- 普通 Chat、Messages 和无状态 Responses 不落请求正文。
- 只有为 Responses 状态链完成查询、取消或跨上游重放所必需的内容才加密保存。
- 请求记录页面不提供正文调试开关，避免管理员误开启长期敏感日志。
- 崩溃和错误日志不包含请求体、Key 或思考内容。

### 23.5 配置备份与恢复

- 导出时由管理员输入备份密码。
- 在内存生成仅包含配置的规范化数据，用 Argon2id 派生备份密钥，再整体使用 XChaCha20-Poly1305 加密。
- 备份包含分组、账号、上游 Key、模型映射和设置，不包含请求正文、Responses 历史、性能记录或运行日志。
- 恢复时先完整解密、校验版本和引用，再用本机主密钥重新加密秘密，最后通过配置事务原子切换。
- 密码错误、文件损坏或出现 0 个有效分组时不修改现有配置。

## 二十四、日志、监控与保留

### 24.1 默认请求日志

记录：

- 请求 ID、配置版本、入口协议和逻辑模型。
- 候选过滤原因、最终目标和端点。
- 每次尝试、切换原因和队列时间。
- 首字延迟、总延迟、输出速度和 Token。
- 倍率来源、资格判断结果和额度状态。
- 标准化错误码和安全的上游状态码。

不记录完整 Header、API Key、正文、工具参数、图片或思考。

### 24.2 数据保留

- 请求元数据和性能历史默认保留 30 天。
- 管理员可以调整保留天数。
- 设为 0 时不新增历史明细，只保留内存中的实时汇总和必要运行状态。
- 清理任务分批删除，避免长事务阻塞 SQLite。

### 24.3 健康接口

- `/health/live`：进程事件循环正常。
- `/health/ready`：数据库、配置和主密钥已加载，可以接收请求。
- 健康接口不暴露账号、模型、倍率或版本细节。

## 二十五、部署方案

### 25.1 Docker

- 提供单镜像，内含 Rust 二进制、管理后台和模型目录。
- 使用非 root 用户运行。
- `/data` 作为唯一持久卷，保存 SQLite、主密钥和临时目录。
- 容器内默认监听 `0.0.0.0:8080`。
- 提供最小 `docker compose` 示例和健康检查。

### 25.2 Linux 原生版

- 第一批发布 `x86_64` 和 `aarch64`。
- 单个二进制加一个数据目录即可运行。
- 提供 `systemd` 示例，默认监听 `127.0.0.1:8080`。
- 生产 HTTPS 推荐由 Caddy、Nginx 或现有入口代理终止，Akhub 第一期不自行管理证书。

### 25.3 优雅关闭

收到停止信号后：

1. 停止接收新请求。
2. 取消排队请求并返回可重试错误。
3. 给在途请求默认 60 秒完成时间。
4. 刷新日志和状态写入队列。
5. 超时后取消剩余上游连接并退出。

## 二十六、测试策略

### 26.1 协议黄金测试

为三种协议建立固定输入、非流式输出和 SSE 事件夹具，覆盖：

- 纯文本和多轮消息。
- 图片输入。
- 单个和并行函数工具。
- 工具参数分片与工具结果。
- 思考、思考摘要、签名和加密块。
- JSON Schema 结构化输出。
- 拒绝、引用和不同 stop reason。
- usage 的缓存与思考 Token。
- 未知字段原生透传和跨协议拒绝。

每一种跨协议方向都必须有测试：

- Chat ↔ Responses
- Chat ↔ Messages
- Responses ↔ Messages

### 26.2 官方 SDK 兼容测试

使用官方 OpenAI 和 Anthropic SDK 作为黑盒客户端验证：

- 普通与流式创建。
- 工具调用循环。
- `previous_response_id`。
- 查询、删除、取消和输入项。
- 错误对象能被 SDK 正常解析。

### 26.3 路由与故障测试

必须覆盖：

- 同一 Key 下两个相似模型按具体模型独立调度。
- 组内调度且绝不跨组。
- 人工优先在小性能差距时生效。
- 大性能差距时自适应可以越过人工优先。
- 粘性命中、故障清除和恢复。
- DNS、TLS、401、429、5xx、损坏响应的切换。
- 慢请求和总超时不产生第二份调用。
- 第一个语义 SSE 事件后禁止切换。
- 默认 3 个目标和分组覆盖。
- 队列满、队列超时和客户端断开。

### 26.4 倍率保护测试

- 自动倍率成功、失败、空数据、格式错误和超时。
- 刷新失败立即未知并暂停。
- 手动倍率不受自动刷新失败影响。
- 倍率等于分组倍率允许调用。
- 倍率高于分组倍率拒绝新调用。
- 请求排队或等待并发期间倍率升高，终检必须阻止发送。
- 在途期间倍率升高，请求继续完成。
- 倍率恢复后自动恢复原人工优先级。
- New API 多分组取最高值或人工选择上游分组。
- Sub2API 峰值时段切换。

### 26.5 模型刷新测试

- 0 个模型不覆盖。
- 错误 JSON 不覆盖。
- 真正新增和删除能原子应用。
- 已映射模型消失后保留记录并暂停。
- 模型再次出现自动恢复。
- 手动模型不被错误刷新删除。

### 26.6 安全与稳定性测试

- Key 不出现在日志、错误和备份元数据中。
- 不同分组不能读取彼此 Responses ID。
- SSRF、DNS Rebinding 和跨域重定向。
- 加密数据篡改检测。
- 临时文件在取消和崩溃恢复后清理。
- JSON 和 SSE 解析使用模糊测试。
- SQLite 忙、磁盘满和写入通道满时，推理热路径可控降级。

### 26.7 性能验收

在固定硬件和固定请求体下分别测量原生透传与跨协议转换：

- 网关自身延迟，不把上游网络时间混入。
- 并发连接、SSE 长连接和内存上限。
- 64 KB、8 MB 和 64 MB 请求体。
- 日志开启、保留设为 0 和 SQLite 清理期间的差异。
- HTTP 连接复用率和每请求分配量。

正式发布前再根据测试机器给出可复现的吞吐数字，不提前承诺脱离硬件条件的 RPS。

## 二十七、分阶段实施顺序

每一阶段结束都必须是可运行产品，不能长期保留两套实现或临时兼容层。

### 阶段 1：最小端到端网关

- Rust 服务、SQLite、主密钥和首次管理员设置。
- 分组、下游 Key、账号、逻辑模型和单目标映射。
- OpenAI Chat 原生非流式与 SSE 透传。
- `/v1/models` 与单模型查询。
- 请求 ID、基础错误、密钥加密和脱敏日志。
- 嵌入式最小后台。
- Docker 与 Linux 开发构建。

验收：从后台创建分组和账号后，可以用分组 Key 完成一次真实流式调用。

### 阶段 2：可靠组内路由

- 多目标、账号/模型调度单元和人工优先级。
- 粘性、并发、RPM/TPM、请求体可重放。
- 默认 3 目标故障切换、流式切换边界、熔断、半开和队列。
- 手动倍率与硬性倍率门。
- Sub2API、New API 自动倍率探针和终检。

验收：故障注入、倍率变化和多模型同 Key 场景全部通过。

### 阶段 3：协议完整性

- 完整中间格式。
- Anthropic Messages 入口和适配器。
- OpenAI Responses 创建与 SSE。
- Chat、Responses、Messages 六个方向的转换。
- 工具、思考、图片输入、结构化输出和错误映射。
- 未知字段原生透传与跨协议明确拒绝。

验收：官方 SDK 和黄金夹具通过，关键能力无静默丢失。

### 阶段 4：Responses 状态与模型能力

- Responses 查询、删除、取消、输入项、压缩和 Token 计数。
- 加密状态链、网关响应 ID 和后台响应。
- 手动/自动模型拉取和安全差异应用。
- LiteLLM 开源能力目录导入、证据优先级和过期失效。

验收：`previous_response_id` 在允许保存状态时可跨同组上游重建；`store:false` 行为符合边界说明。

### 阶段 5：自适应与完整后台

- EWMA、最小样本、滞后防抖和冷启动流量。
- 概览、账号状态、逻辑模型状态和请求记录页。
- 账号一键独立复制、测试按钮和批量刷新。
- 配置备份恢复、保留清理和管理员审计。

验收：后台只暴露已确认的简单设置，自适应无需用户理解算法参数。

### 阶段 6：发布硬化

- 全量协议、故障、安全、模糊和负载测试。
- 优雅关闭、磁盘异常、SQLite 恢复和升级检查。
- 多架构 Docker、Linux 二进制、部署文档和参考配置。
- 许可证与第三方声明审查。

验收：满足下一节的完成标准后发布 `v1.0.0`。

## 二十八、完成标准

只有同时满足以下条件，第一期才算完成：

- 管理员无需配置任何模型上下文、多模态、工具或思考字段。
- 同一 Key 下两个具体模型可以映射同一逻辑模型，并分别调度。
- 所有调度严格在分组内完成。
- 人工优先级和自适应规则按已确认关系生效。
- 自动倍率未知或超限时，没有任何新推理请求能发到该账号。
- 倍率变化不会取消已在途请求，但会阻止排队请求和后续尝试。
- 慢请求不会触发并行复制调用。
- SSE 首个语义块之后绝不拼接另一个上游。
- 工具、思考、图片输入、结构化输出和未知字段没有静默丢失。
- OpenAI Chat、Responses HTTP 全套和 Anthropic Messages 能被官方 SDK 使用。
- 模型刷新失败或返回 0 个模型不会破坏现有配置。
- 普通请求正文不落日志；必要 Responses 状态加密保存并可清理。
- Docker 和 Linux 原生版都能从空数据目录完成首次启动和真实调用。
- 热路径不等待 SQLite 日志写入，没有无界队列或无界缓存。

## 二十九、已知边界与后续方向

- 单实例意味着进程或主机故障时服务会中断；第一期不承诺控制面高可用。
- 上游未提供倍率推送时，只能通过定时刷新发现变化，存在刷新间隔窗口。
- `store:false` 或状态保留设为 0 时，无法保证 Responses 跨上游恢复完整历史。
- 上游已收到请求后即使取消，也可能已经计费。
- 供应商专有工具没有等价协议时只能走原生上游或明确拒绝。
- 后续可以按独立阶段增加 Responses WebSocket、图片、语音、Embedding、视频、Windows 和多实例，但不能提前把这些复杂度塞入第一期核心。

参考来源：

- [AxonHub，检查版本 `4483c2e`](https://github.com/looplj/axonhub)
- [LiteLLM，检查版本 `4ba8517`](https://github.com/BerriAI/litellm)
- [New API，检查版本 `27ff6a8`](https://github.com/QuantumNous/new-api)
- [Sub2API，检查版本 `e98ef32`](https://github.com/Wei-Shaw/sub2api)
- [OpenAI Responses 接口文档](https://developers.openai.com/api/reference/resources/responses/methods/create)
- [OpenAI 会话状态文档](https://developers.openai.com/docs/guides/conversation-state)
