{
  "name": "akhub-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "test:key-paste": "tsc src/lib/key-paste.ts src/lib/key-paste.check.ts --module esnext --target es2022 --moduleResolution bundler --outDir node_modules/.cache/key-paste-check --skipLibCheck && node node_modules/.cache/key-paste-check/key-paste.check.js",
    "test": "npm run typecheck && npm run test:key-paste"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.1.0",
    "typescript": "^5.9.0",
    "vite": "^8.0.0"
  }
}
